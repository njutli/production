# JuiceFS 方案性能调优演进报告

> 环境：3 节点 Ceph（6 OSD，EC 4+2，SSD）/ JuiceFS v1.3.1 / TiKV 元数据 / 1Gbps 网络
> 达标标准：单项带宽 ≥ 59 MB/s（千兆网卡半速）
> 数据来源：全量标准化 fio 测试，每组配置双轮取最高值，原始日志可追溯

---

## 一、调优工作总览

| 阶段 | 关键动作 | 核心成果 |
|------|---------|---------|
| **阶段 0：初始状态** | RGW + 4M block，千兆网络 | 完成集群部署，基本功能实现；顺序写仅 18.6 MB/s，随机读写仅 4.6 MB/s |
| **阶段 1：基础设施修复** | 排查发现 node1/node2 网卡仅 100Mb/s；三节点网络升级千兆 + node1 重装纳管 | 后端裸能力提升约 10 倍 |
| **阶段 2：存储架构优化** | 部署双 RGW + HAProxy 负载均衡；去 RGW 直连 RADOS；block-size 4M→256K | 顺序写 +189%，随机读写 +235%~276% |
| **阶段 3：客户端参数调优** | 暖态缓存（cache=100G）；--max-readahead 0；--max-uploads 150 | 随机读 +254%，顺序写 +23% |
| **阶段 4：深度诊断与排除** | 白盒实验排查瓶颈方向；穷举排除无效方向 | 残余瓶颈怀疑方向收敛，仍在分析 |

---

## 二、关键调优突破

### 突破 1：网络基础设施修复（阶段 1）

调优起步时，通过自底向上隔离发现：裸盘 SSD 随机写可达 420 MB/s，但 `rados bench` 后端仅 9.87 MB/s、延迟 17.6 秒——典型的网络拥塞特征。定位到 ceph-node1/node2 网卡仅协商到 100Mb/s（约 12.5 MB/s），是整套系统的头号物理瓶颈。

修复后后端裸能力从 ~10 MB/s 提升到 ~108 MB/s，端到端顺序吞吐从 ~8 MB/s 提升到 ~80 MB/s。

### 突破 2：去 RGW 直连 RADOS（阶段 2）

通过 128G 同口径对照实验，去掉 RGW（S3 网关）HTTP 协议层、改用 JuiceFS librados 直连 Ceph：

- 顺序写 18.6 → 53.8 MB/s（**+189%**）
- 随机读写 4.6 → 15.6 MB/s（**+239%**）

### 突破 3：block-size 4M → 256K（阶段 2，核心突破）

通过白盒实验 + `juicefs stats` 采集 + fio bs-sweep + block-size-sweep 精确定位根因：**JuiceFS 默认 4MB block 对 256K 随机读造成 ~16× 读放大**——客户端拉了一堆用不上的数据，object 层 ~100 MB/s 全浪费，FUSE 有效读仅 ~5 MB/s。

将 block-size 改为 256K 后，经多次重测互证：

- 纯随机读提升约 3.7 倍
- 顺序读写不降反略升
- 存储空间零代价（EC 开销不变）、内存反而更低

### 突破 4：暖态缓存 + 客户端参数调优（阶段 3）

在 256K block 基础上叠加客户端调优：

- **暖态缓存（cache=100G）**：随机读 33.6 → 105 MB/s（+213%），随机读写 +34%
- **--max-uploads 150**：顺序写 +23%、顺序读 +14%、布局写 +39%
- **--max-readahead 0**：写类全面受益（顺序写 +33%、随机写 +8%），但顺序读和随机读写受损，需按负载选配

---

## 三、深度诊断工作（阶段 4）

为定位调优后残余瓶颈，执行了大量诊断实验，形成完整证据链：

### 3.1 瓶颈分层定位

| 诊断层 | 路径 | 放大倍数 | 初步判断 |
|--------|------|---------|------|
| L1 裸 librados（rados bench 256K rand） | 绕过 JuiceFS/FUSE | **1.04×** | EC 取片传输开销仅 4%，后端可能非放大主要来源 |
| L2 JuiceFS FUSE（端到端） | 全链路 | **~2.5×** | 放大可能主要在 JuiceFS 内部 |

### 3.2 strace 系统调用级追踪

通过 `strace` 追踪 JuiceFS 进程的 Ceph socket 读取行为：

- 每个 fio 256K 读取触发 **139 次 socket read() 调用 ≈ 4 OSD × 35**
- 数量上接近 EC 4+2 四分片，初步排除 slice 碎片化假设
- 怀疑残余放大与 Ceph messenger 协议帧开销有关，仍在进一步确认

### 3.3 穷举排除的无效方向

| 方向 | 排除依据 |
|------|---------|
| 磁盘介质（HDD→SSD） | 裸盘实测 4K 随机读 92,500 IOPS，确认为 SSD |
| WAL/DB 换更快介质 | 内存盘测试仅 +4.8%，无效 |
| 全内存盘集群 | 纯 RAM 替换 SSD 后吞吐不变（106.6 vs 106.4） |
| BlueStore 引擎参数 | 三组参数全部无效 |
| EC→副本（size=3） | 实测反而慢 16%（写放大 3× > EC 1.5×） |
| 磁盘调度器/IO 合并 | 所有指标纹丝不动 |
| RGW HTTP 层 | 去掉后随机读仅 +22%，非根因 |
| JuiceFS 并发/缓冲/预读参数 | buffer-size 反降，prefetch 无变化 |
| 元数据（TiKV） | 延迟仅 1ms，非瓶颈 |
| FUSE 内核节流 | waiting≈0 全程 50 采样点 |
| FUSE splice/max_read/max_pages | 系统 kernel 5.15 / fusermount 3.10.5 不支持 |
| EC 2+1 / MTU jumbo / overwrites | L1=1.04× 已证 EC/网络非放大源 |
| JuiceFS v1.4 升级 | 单客户端反而 -3.3% |
| CephFS 内核态对照 | 读 3.6× 但写触发 EC RMW 反降 |
| 多客户端聚合 | 有效但验收线同步抬高，治标不治本 |

---

## 四、调优演进对比

### 4.1 冷态（不使用 JuiceFS 缓存）

以原始状态为基准，列出各指标增减百分比

| 测试 | 配置 | 顺序读 | 顺序写 | 多线程读 | 多线程写 | 随机读 | 随机写 | 随机读写 R | 随机读写 W |
|------|------|--------|--------|----------|----------|--------|--------|------------|------------|
| RGW（原始状态） | bs=4M / bs=256K / cache=0 / mu=默认 / S3 via haproxy | 78.8 | 18.6 | 109 | 20.7 | 30.6 | 32.7 | 4.6 | 4.6 |
| 直连（一轮调优） | bs=4M / bs=256K / cache=0 / mu=默认 / Ceph RADOS | 79.9 (+1.4%) | 53.8 (+189%) | 107 (-1.8%) | 39.7 (+92%) | 32.4 (+5.9%) | 33.3 (+1.8%) | 15.6 (+239%) | 15.4 (+235%) |
| 冷态全量（二轮调优） | bs=256K / bs=256K / cache=0 / mu=默认 | 80.4 (+2.0%) 已达标 | 50.8 (+173%) 达标86%差14% | 110 (+0.9%) 已达标 | 41.5 (+100%) 达标70%差30% | 33.6 (+9.8%) 达标57%差43% | 53.5 (+64%) 达标91%差9% | 17.3 (+276%) 达标29%差71% | 17.0 (+270%) 达标29%差71% |

---

### 4.2 暖态（使用 JuiceFS 缓存）

以冷态全量（二轮调优）为基准，列出各指标增减百分比

| 测试 | 配置 | 顺序读 | 顺序写 | 多线程读 | 多线程写 | 随机读 | 随机写 | 随机读写 R | 随机读写 W |
|------|------|--------|--------|----------|----------|--------|--------|------------|------------|
| 冷态全量（二轮调优） | bs=256K / bs=256K / cache=0 / mu=默认 | 80.4 | 50.8 | 110 | 41.5 | 33.6 | 53.5 | 17.3 | 17.0 |
| 暖态全量（二轮调优带缓存） | bs=256K / bs=256K / cache=100G / mu=默认 | 80.6 (+0.2%) | 48.7 (-4.1%) | 109 (-0.9%) | 40.6 (-2.2%) | 105 (+213%) | 51.1 (-4.5%) | 23.2 (+34%) | 22.6 (+33%) |
| 暖态 ra=0 | bs=256K / bs=256K / cache=100G / mu=默认 / ra=0 | 48.8 (-39.3%) | 67.3 (+32.5%) | 99.6 (-9.5%) | 49.1 (+18.3%) | 155 (+361%) | 57.9 (+8.2%) | 8.8 (-49.1%) | 8.6 (-49.4%) |
| 暖态 mu=150 **（当前最优）** | bs=256K / bs=256K / cache=100G / mu=150 | 91.5 (+13.8%) 已达标 | 62.4 (+22.8%) 已达标 | 109 (-0.9%) 已达标 | 42.6 (+2.7%) 达标72%差28% | 119 (+254%) 已达标 | 56.0 (+4.7%) 达标95%差5% | 18.9 (+9.2%) 达标32%差68% | 18.7 (+10%) 达标32%差68% |

---

## 五、当前状态总结

### 5.1 已达标指标（≥59 MB/s）

| 指标 | 当前最优值 | 配置 | 较原始状态提升 |
|------|----------|------|--------------|
| 顺序读 | 91.5 MB/s | 暖态 mu=150 | +16% |
| 顺序写 | 62.4 MB/s | 暖态 mu=150 | **+235%**（18.6→62.4） |
| 多线程读 | 109 MB/s | 各配置均达标 | — |
| 随机读 | 119 MB/s | 暖态 mu=150 | **+289%**（30.6→119） |
| 随机写 | 56.0 MB/s | 暖态 mu=150 | **+71%**（32.7→56.0），达标95% |

### 5.2 未达标指标

| 指标 | 当前最优值 | 距目标 | 怀疑方向 |
|------|----------|--------|---------|
| 多线程写 | 42.6 MB/s | 差28% | EC 写放大 + 1Gbps 网络瓶颈 + page cache 碎片化，仍在分析 |
| 随机读写 R | 18.9 MB/s | 差68% | 读写争抢 + 协议开销，所有调参手段无效或有害，正在深度诊断 |
| 随机读写 W | 18.7 MB/s | 差68% | 同上 |

### 5.3 残余瓶颈分析（进行中）

已通过 strace 系统调用级追踪 + L1 裸 RADOS 对照实验形成初步怀疑方向，仍在进一步分析测试中：

- **EC 4+2 协议开销**：每个 256K 读取需从 4 个 OSD 各取分片，strace 观测到 139 次 socket read 调用，怀疑是残余放大的主要来源
- **读写争抢（randrw）**：混合读写场景下读被写阻塞的机制尚不明确，正在通过 pprof / strace / OSD perf dump 等手段深入诊断

---
> 以下为草稿内容，非正式报告章节，待后续调优推进后整合。

## 【草稿】冷态基线及相关配置参数

> 以下冷态全量测试数据是后续瓶颈定位和调优工作的基准口径。
> 所有配置对比、瓶颈分析均以冷态数据为准，暖态/writeback 数据仅用于评估用户体验带宽。

### 6.1 冷态基线配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `--storage` | ceph | 直连 RADOS，绕过 RGW |
| `--block-size` | 256K | 对齐 fio bs=256K，消除读放大 |
| `--cache-size` | 0 | 关闭客户端缓存，确保数据走网络到 Ceph |
| `--max-uploads` | 20（默认） | 不额外调参 |
| `--max-readahead` | 默认 | 不额外调参 |
| `--writeback` | 关闭 | 不启用写缓存 |
| fio 顺序 bs | 256K | 与 block-size 对齐 |
| fio 随机 bs | 256K | 与 block-size 对齐 |
| fio 随机 numjobs | 128 | 128 并发 |
| fio 随机 iodepth | 128 | libaio 深度 128 |
| fio `--direct` | 1 | 绕过内核 page cache |
| fio `--time_based --runtime` | 60s | 时间基准测试 |
| 工作集 | 128G（128jobs × 1G） | 超出缓存容量，确保冷态 |

### 6.2 冷态全量基线数据（双轮取最高值）

| 顺序读 | 顺序写 | 多线程读 | 多线程写 | 布局写 | 随机读 | 随机写 | 随机读写 R | 随机读写 W |
|--------|--------|----------|----------|--------|--------|--------|------------|------------|
| 80.4 | 50.8 | 110 | 41.5 | 33.3 | 33.6 | 53.5 | 17.3 | 17.0 |

> 单位 MB/s。fio bs=256K，block-size=256K，cache=0，无 writeback，mu=默认。
> 达标标准：≥59 MB/s。冷态下达标项：顺序读、多线程读。其余均未达标。

### 6.3 为什么以冷态为调优基准

1. **消除缓存效应**：cache=0 确保每次读取都走网络到 Ceph，反映后端真实性能
2. **消除 staging 污染**：无 writeback，写操作直接穿透到后端，读测试不会被 staging→cache 硬链接污染
3. **可复现**：冷态测试每轮独立（destroy + reformat + drop_caches），结果稳定可对比
4. **反映集群真实能力**：暖态/writeback 数据反映的是客户端缓存能力，不是集群吞吐

---

## 【草稿】writeback + mu=150 + cache=100G 测试数据及讨论

> 以下数据来自 `results/full-bs256k-warm-writeback-mu150-20260629-131422/`。
> 配置：`--cache-size 102400 --cache-dir /data/jfsCache --max-uploads 150 --writeback`

### 7.1 测试结果

| 指标 | 冷态基线 | 暖态 mu=150（无 writeback） | 暖态 writeback+mu150 |
|------|---------|---------------------------|---------------------|
| 顺序读 | 80.4 | 89.9 | **1193** |
| 顺序写 | 50.8 | 62.4 | **346** |
| 多线程读 | 110 | 109 | **12800** |
| 多线程写 | 41.5 | 41.7 | **431** |
| 布局写 | 33.3 | 42.5 | **417** |
| 随机读 r1 | 33.6 | 38.3 | **197** |
| 随机读 r3 | 33.6 | 102 | **864** |
| 随机写 | 53.5 | 42.8 | **688** |
| 随机读写 R | 17.3 | 18.9 | **260** |
| 随机读写 W | 17.0 | 18.7 | **261** |

> 单位 MB/s。writeback 列的高带宽来自本地 SSD，NIC 流量极低（10~3000 KB/s），数据几乎未经过网络到 Ceph。

### 7.2 数据解读

#### writeback 的工作原理

`--writeback` 开启后，写操作先将 block 写入本地 staging 磁盘（`rawstaging/` 目录），`Finish()` 立即返回，不等后端上传完成。后台由 `max-uploads`（150）个 uploader goroutine 异步将 staging 文件上传到 Ceph。同时，staging 文件通过 `os.Link()` 硬链接到 cache 目录，使等待上传的数据同时成为读缓存。

#### 这些数据不代表集群真实吞吐能力

1. **写带宽是本地 SSD 速度，不是集群带宽**：seqwrite=346、randwrite=688 均为本地磁盘写入速度，后台上传仍受 1Gbps 网络 + EC 4+2 写放大限制，实际 drain 速率约 50~60 MB/s
2. **读数据被 staging→cache 硬链接污染**：layout 写入的 128G block 全部硬链接到 cache 目录，后续读测试命中本地缓存，NIC_RX 几乎为零，测的是本地 SSD 读带宽而非 Ceph 读性能
3. **长时间大压力写入会被打回原形**：若持续写入速率超过后台 drain 速率（~60 MB/s），staging 会不断堆积直到磁盘满，前端最终被回压到后端真实速度

#### 但在突发型负载场景下有可观收益

对于 AI 训练 checkpoint 等突发写入场景（写一批数据 → 计算一段时间 → 再写一批），writeback 的收益链条为：

1. **突发写秒级完成**：如 50GB checkpoint 写本地 SSD ~300 MB/s，约 3 分钟完成（无 writeback 需 ~14 分钟）
2. **计算间隙后台 drain**：训练继续跑 GPU 的 30 分钟里，150 个 uploader goroutine 以 ~60 MB/s 速度清空 staging（50GB 约 14 分钟），下一个 checkpoint 到来前 staging 已清空
3. **checkpoint 读取命中本地缓存**：恢复训练时读 checkpoint，数据仍在 cache 目录（staging 硬链接），不走网络

关键前提：**平均写入速率需低于后台 drain 速率（~60 MB/s）**。AI 训练 checkpoint 场景（大块数据间歇写入）通常满足此条件；持续日志/流式写入场景则不适用。

#### 风险评估

| 风险场景 | 影响 | 评估 |
|---------|------|------|
| 进程 crash | staging 文件在本地磁盘，下次 mount 时 `scanStaging()` 自动恢复上传 | 安全 |
| 正常 umount | `waitWritebackComplete()` 阻塞等待 staging 清空 | 安全 |
| 硬掉电 | staging 文件 `closeFile()` 未显式 fsync，page cache 可能未刷盘 | 数据中心环境（UPS）下风险极低 |
| staging 磁盘满 | 回退到直接上传，前端降至后端真实速度 | 需确保本地盘空间充足（建议 ≥150GB） |

### 7.3 生产配置建议

```
juicefs mount -d \
  --storage ceph --bucket ceph://juicefs-data \
  --access-key ceph --secret-key client.juicefs \
  --block-size 256K \
  --cache-size 102400 --cache-dir /data/jfsCache \
  --max-uploads 150 \
  --writeback \
  tikv://192.168.11.12:2379/juicefs-prod /mnt/juicefs
```

> 后续瓶颈定位和调优工作继续以冷态全量测试为基准。writeback 配置仅用于生产部署，不参与调优分析。
