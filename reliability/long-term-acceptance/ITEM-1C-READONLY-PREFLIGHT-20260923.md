# 157 LT-002 只读预检与容量决策

2026-09-23，在8线程主挂载纠偏后，将经过本地离线检查的LT运行库及LT-001～004用例复制到157独占目录 `/home/sunrise/reliability-lt-20260923/`；本地/远端七份脚本SHA256相同。远端只运行 `LT-002 ... plan 20260923-091500 randrw256k`，没有 `prepare/start`，没有创建卷内数据或执行fio。

只读 `plan` **PASS**：卷、二进制、FUSE 256K、实际8个worker、Ceph健康及脚本环境门通过。计划负载为128 jobs×QD128、256K randrw、每job 1GiB固定数据集（总128GiB）、默认2小时、60秒采样。当前Ceph pool `MAX AVAIL≈26TiB`，六OSD BlueStore DB空闲约37.9～38.1GiB/OSD。

但该 `plan` 的默认保护线仅为每OSD DB剩余4GiB，不能据此批准2小时满压。05-3b曾有一个256K控制格使pool暂增约562.5GiB、最小DB空闲下降约1.62GiB；64GiB固定逻辑资产也曾保留4.83TiB历史slice。将单格增长简单线性外推并非可靠预测，却足以说明2小时满压可能耗尽40GiB/OSD DB预算，pool总空闲也不能代替DB门。现有脚本还没有明确的全局写入速率或总写入量限制。

因此下一步改为**先加最小限量参数和高DB停止线，执行10～15分钟独占canary**，量出写入量、对象/DB增长和停写回收，再决定能否跑2小时。限量参数已加入测试脚本（每job独立的prepare/verify/正式读写速率上限，默认为0即不限制），本地`test-long-term.sh` Gate 0已通过；远端仍需同步新版、校验哈希并重新运行只读`plan`。此项只用于安全和容量估算，不进入性能或长稳验收。24/72小时阶段还必须先冻结真实业务持续/峰值负载与SLO；当前缺这些输入，不得把任意满压fio称为生产长稳。

后续进展：新版运行库已同步至157，SHA256为`2c59d0f548da855483b2f5914da34f96ba34ea92a06ca7b3103259a1f4ac6cac`；以RUN_ID `20260923-091500`运行只读`plan`通过。该计划冻结10分钟、两段各300秒、每job读/写各2MiB/s、准备写2MiB/s、校验读2MiB/s、每OSD DB free≥32GiB、Ceph pool max avail≥20TiB。首次`prepare`在`mkdir`结果目录时因`/mnt/jfs-cache`根目录权限失败，**未启动fio**；卷内仅留下三个层级的空目录。确认全为空后，已用三条精确`rmdir`清除，未触及上层数据目录。结果父目录`/mnt/jfs-cache/reliability-lt-results`不存在，已就精确创建命令申请用户批准；批准前不重试。

**目标纠正：**用户随后确认长稳测试应在192.168.11～14进行。前述157 sudo目录创建申请随即撤销，命令从未执行；157原挂载已按单独授权恢复，详见`ITEM-1E-157-ORIGINAL-MOUNT-RESTORE-20260923.md`。本报告只作历史审计，不得继续作为157测试放行依据。
