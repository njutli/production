# 192 长稳环境旧 RUN 数据清理结果

时间：2026-09-23（北京）。操作节点仅 `192.168.11.12`，卷 UUID `3e54dcc4-991e-425a-8e76-16c9c1fb6836`，Ceph 池 `juicefs-data`。本项只清理已完成的旧 LT-002 数据文件；**不是新长稳测试，也不能用于证明原生自动回收通过**。

## 执行范围与安全核对

- 本地 `20260921-175359/LT-002` 的 `manifest.sha256` 逐项校验通过；`20260922-144100` 的归档包 gzip、路径安全性和内含 1100 项 manifest 逐项校验通过。
- 远端精确脚本 SHA256 为 `f773e113ad064a1bd933a6c7dc3e1643a9ed9810ea2cee579a0f473912fec659`，与本地一致。两次 `plan` 均确认各 128 个 `lt.0.0`～`lt.127.0` 文件、各 1 GiB，卷 UUID、挂载源、`COMPLETE` 状态、结果归档、无活动 fio 均通过。
- 仅对下列两个精确目录逐文件执行普通用户 `rm --`，随后 `rmdir` 其空的三级目录；未使用递归删除、sudo、JuiceFS GC、Ceph 写命令或 OSD 操作：
  - `/mnt/juicefs/reliability-lt-data/20260921-175359/LT-002/randrw256k/`
  - `/mnt/juicefs/reliability-lt-data/20260922-144100/LT-002/randrw256k/`
- 两次均返回 `EXACT_DELETE_PASS`，两个 RUN 数据目录均已不存在；对应 `/data/reliability-lt-results/<RUN_ID>/LT-002/manifest.sha256` 均保留。另三个旧 RUN 的空目录及卷内其他内容未动；157/150～152 未操作。

## 容量与健康证据

| 采样点 | `juicefs-data max_avail` (B) | `stored` (B) | 对象数 |
|---|---:|---:|---:|
| 清理前 | 537,945,309,184 | 668,525,993,388 | 2,593,040 |
| 删除第一组后即时 | 544,317,243,392 | 661,894,405,548 | 2,567,495 |
| 删除第一组约 200 秒 | 609,804,615,680 | 596,664,065,452 | 2,315,699 |
| 删除第二组后约 180 秒 | **656,347,430,912** | **550,488,776,108** | **2,137,991** |

最终快照约 **611.3 GiB** 可用，较清理前 **增加 110.3 GiB**；pool stored 减少约 109.9 GiB、对象数减少 455,049。末点仍在缓慢回收，故这是已观察到的增量，不是最终稳定值。清理后的健康核对为 `HEALTH_OK`、6 OSD up、161 PG `active+clean`。

与 0.4 TiB（409.6 GiB）停止线的距离暂为约 **201.7 GiB**。历史同负载 2 小时曾消耗约 107.1 GiB；从容量预算看，新一轮 **2 小时安全筛查有条件可进入独立 preflight**，但须在开跑当时复测池可用量、健康、OSD DB、后台排空和脚本版本，不得凭本次快照直接启动。现有回收量不能担保 24/72 小时长测，也不能替代原生空间收敛判定。

本项到此暂停；未启动新的 fio 或长稳控制器。
