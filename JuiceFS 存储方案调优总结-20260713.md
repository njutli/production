# JuiceFS 存储方案调优总结

> 编写日期：2026-07-13
> 性质：内部工作总结（非对外汇报），力求全面覆盖，单点不求极细，细节以 `report/JuiceFS 方案性能调优演进报告.md` 及 `doc/perf-analysis/` 各文档为准。
> 环境：3 节点 Ceph（6 OSD，EC 4+2，PERC H730 挂 SSD）/ JuiceFS v1.3.1（patched）/ TiKV 元数据 / 1Gbps 网络
> 达标口径：单项带宽 ≥ 59 MB/s（千兆网卡半速），256K block，单客户端，randrw 为准

---

## 一、背景与方案选型

**方案设计与对比：store.md**

### 1.1 需求与硬性约束

存储方案的选型不是自由发挥，而是被一组硬性规格约束框死的，这决定了后续几乎所有技术取舍：

| 约束 | 具体要求 | 来源 |
|------|---------|------|
| 冗余方式 | **EC 4+2**（纠删码 4 数据 + 2 校验） | `03-env-change-2026-06.md` |
| 空间利用率 | **≥ 60%**（EC 4+2 = 4/6 ≈ 66.7%，满足） | 同上 |
| 访问语义 | **多客户端共享 POSIX** 文件语义 | `10_3`、`08-next-steps-comparison.md` |
| 网络 | **千兆**（1Gbps，单网卡线速 ~118 MB/s） | 演进报告 |
| 验收口径 | 单项 **≥ 59 MB/s**（千兆半速），256K block，单客户端，randrw 为准 | 用户拍板 |
| EC 部署 | OSD 数量 ≥ k+m（6 OSD 正好承载 4+2，failure-domain=osd，容忍任意 2 OSD 故障） | `ceph-troubleshooting.md` |

这几条里，**"EC 4+2 + 利用率 ≥60%"** 和 **"多客户端共享 POSIX"** 是两把直接砍掉大量候选方案的刀。

### 1.2 为什么选 JuiceFS + Ceph(EC 4+2) + TiKV

方案选型经过横向对比，被约束逐一排除后收敛到当前架构：

| 候选方案 | 结论 | 原因 |
|---------|------|------|
| **3 副本（size=3）** | ❌ 排除 | 利用率仅 33% < 60% 违规；且 rados bench 实测反而慢 16%（EC 102 vs 副本 86），写放大 3× > EC 1.5× |
| **Ceph RBD（块设备）** | ❌ 排除 | 块设备单客户端独占，不满足多客户端共享 POSIX |
| **CephFS（内核态）** | ⚠️ 仅备选 | 无 FUSE 税，随机读快 3.6×；但就地改写触发 EC overwrite 的 RMW（读改写），写反而更慢；JuiceFS 整块写新对象绕过 RMW，写更优 |
| **MinIO / 换对象网关** | ❌ 排除 | 不改变落盘 IO 模型（瓶颈非网关 CPU，仅 3~8%）；且丢失 POSIX 多客户端共享语义 |
| **Redis 元数据引擎** | ❌ 不换 | TiKV 元数据延迟仅 ~1ms，非瓶颈 |
| **EC 4+2 + JuiceFS + TiKV** | ✅ 选定 | EC 4+2 唯一同时满足利用率与容错；JuiceFS 提供 POSIX 多端共享，整块写绕过 EC RMW；TiKV 元数据非瓶颈 |

**JuiceFS 的一个天然架构优势**：它把文件切成固定 block（对象），整块写为新的 RADOS 对象 = EC 整条带写（full-stripe write），天然避开了 EC 就地改写的 RMW 惩罚（这正是 CephFS 写慢的根源），代价是写放大。

### 1.3 架构演进

```
         JuiceFS Client (FUSE mount, 单客户端)
      ┌──────────────┴──────────────┐
  元数据 (TiKV)                  数据 (Ceph EC 4+2)
  单节点单副本                    6 OSD, failure-domain=osd
  192.168.11.12                   .11 / .13 / .14
```

数据面链路本身经历过一次演进：
- **早期**：JuiceFS → HAProxy LB → 多 RGW(S3 网关) → RADOS。当初上 RGW 是为兼容标准 S3 协议接入。
- **现在**：JuiceFS → **librados 直连 RADOS**（`--storage ceph`，池 `juicefs-data`）。确认 RGW 非瓶颈、且业务无强制 S3 需求后去掉 S3 网关层，顺序写 +189%、随机读写 +239%。

---

## 二、整体调优思路

两条主线并行：

1. **主动调优（自顶向下）**：先摸清每个测试项的 IO 模型（顺序/随机、读/写、并发度、块大小）和基线数据，结合 JuiceFS/Ceph 官方配置说明与以往经验，选出理论上对该 IO 模型有帮助的参数逐个验证。
2. **瓶颈驱动（自底向上）**：对未达标项，沿整个 IO 栈（裸盘 → 网络 → Ceph 后端 → librados → JuiceFS/FUSE → fio）逐层隔离，用数据定位瓶颈落在哪一层，再针对性地解决。

贯穿始终的方法论（吃过亏后固化）：**先量化上限，再定目标，最后调优**。而不是盲目套通用调优清单——早期就是因为把 PD 时间戳 / TiKV block-cache 当主因、连调三轮系统参数无效，才回到"自底向上逐层隔离"的正道。

---

## 三、配置参数分析（主动调优）

> 本节收录基于 IO 模型和经验**主动尝试**的参数：含义、为什么调、效果。瓶颈驱动改的参数（block-size、max-readahead、patch）放第四节，不重复。

### 3.1 `--max-uploads`（20 → 150）

- **含义**：客户端到后端的并发上传 goroutine（连接）数。是 mount 参数（format 期不支持）。
- **为什么调**：验收用 128 并发 fio job，默认只有 20 个 upload slot，128 个 job 争抢 20 个槽位导致写请求严重排队。这是从"并发写 IO 模型 + 默认值偏小"直接推出的调整。
- **效果（真盘冷态 20→150）**：顺序写 +12.2%、多线程写 +5.3%、布局写 +17.4%、随机写 +4.1%、随机读写 R/W +12%；暖态 randrw +30%。读操作不走 upload pipeline，randread 不受影响（符合预期）。
- **注意**：早期短测曾误报"无效"，是缓冲暂态占比大 + 并发不够高；patched 高并发全量测试推翻了该误判。
- **落地**：纳入生产基线。

### 3.2 `--cache-size`（0 冷态 / 100G 暖态）

- **含义**：客户端本地读缓存大小（MB）。
- **为什么调**：cache=0 是为了让数据真正走网络打到 Ceph，反映后端真实能力（同时强制关掉 prefetch），是所有冷态达标测试的基线前提；cache=100G 用于评估暖态缓存命中场景的上限。
- **效果（暖态 cache=100G）**：随机读 33.6 → 105（+213%）、随机读写 +34%。但暖态 randread 逐轮爬升到 r5=146（>124 即超千兆，是缓存命中假象），因此**暖态数据仅作定性参考，不作达标依据**。
- **落地**：验收基线用 cache=0；生产按场景可选 100G。

### 3.3 `--buffer-size`（300M → 试 1G/2G）

- **含义**：读写缓冲，影响并发。
- **为什么调**：想通过加大缓冲提升随机读并发。
- **效果**：**对随机读无效甚至反降**（buffer 2048 时 randread 反跌到 3.2 MB/s；300→600→2048 sweep 零效果）；但对**随机写**有效（`--max-uploads 300 --buffer-size 1024` 随机写达 76.9，buffer 2048 时 randwrite +54~57%）。
- **落地**：未纳入生产基线（对目标项随机读写增益不稳定）。

### 3.4 `--writeback`（关 → 可选开）

- **含义**：写先落本地 staging 盘、立即返回，异步刷后端；staging 硬链接兼作读缓存。
- **为什么调**：评估对写延迟敏感的生产场景（如 AI checkpoint 突发写）的上限。
- **效果（本地缓存性能，非后端真值）**：顺序写 346、随机写 688、多线程写 431、随机读写 268/270、顺序读 1193 MB/s，NIC 流量全程 <20 MB/s，证实走本地路径。前提是平均写速 < 后台 drain 速率（~60 MB/s）。
- **落地**：不参与后端能力评估；生产按延迟需求可选。

### 3.5 `--prefetch`（默认 1，试 0/16 → 不调）

- **含义**：随机读时预取相邻 block 的对象数。
- **为什么调 / 结论**：多组对照全部零差异（256K rados 直连 31.3 vs 31.1 等）；单独关掉还略降（77.7→75.9）。**证伪，保持默认**。

### 3.6 Ceph / BlueStore 侧尝试（多数无效）

| 参数 | 尝试 | 结论 |
|------|------|------|
| `bluestore_max_blob_size_ssd` | 64K→512K/1M | 写 -2.6~-5.2%，无效 |
| `bluestore_min_alloc_size` | 调大对齐 block | 仅建议，需重建 OSD，未落地 |
| BlueStore 引擎参数（三组） | sweep | 全部无效 |
| EC → 副本 size=3 | 对照 | 反而慢 16%，且违规 |
| 磁盘调度器 / IO 合并 | 切换 | 指标纹丝不动 |

> `bluestore_prefer_deferred_size` 因涉及"SSD 被 RAID 卡误判为 HDD"这一发现，归入第四节瓶颈分析。

### 3.7 RGW / HAProxy / TiKV 侧

| 项 | 调整 | 效果 |
|----|------|------|
| HAProxy `balance` | roundrobin → **source** | 多 RGW 下 roundrobin 使 PUT 与紧随 GET 落到不同 RGW，写后读 404（9/15 失败）；改 source 按源 IP 固定后 15/15 成功。（注：`deploy-lb.sh` 脚本仍写 roundrobin，此为运行时手改） |
| 多 RGW + LB | 单 RGW → 双/三 RGW + HAProxy | 单→双 RGW 端到端 80→88.9（+9~10%）；三 RGW 无额外收益（撞后端上限） |
| TiKV `max-replicas` | 3 → **1** | 单 TiKV 节点，3 副本无意义且 PD 持续报 replica-mismatch；设 1 后集群 healthy |

### 3.8 测试口径参数（fio）

标准随机项验收口径：`--bs=256k --ioengine=libaio --iodepth=128 --numjobs=128 --direct=1 --time_based`，工作集 128G（128 job × 1G，超缓存确保冷态），runtime 60s（阶段 5 达标测拉长到 180s）。

**关键口径校准（阶段 5，血泪教训）**：必须开 `--write_bw_log/--read_bw_log --log_avg_msec=1000` 记录逐秒瞬时带宽，截掉开头缓冲暂态后取**稳态中位数**——因为 fio 全程平均被客户端写缓冲拉高 7~8%，曾报 120~127 MB/s（超千兆物理上限 ~118）纯属假象。此后所有 fio 必须开 bw_log。

---

## 四、瓶颈分析（瓶颈驱动）

> 本节记录逐层定位到的瓶颈：靠什么数据、怎么分析、如何解决。

### 4.1 网络瓶颈：node1/node2 网卡只协商到 100Mb/s

- **怎么发现**：`rados bench` 直压 Ceph EC 后端（绕过 JuiceFS/TiKV/PD/FUSE）只有 9.87 MB/s、延迟 15~18 秒；而裸盘随机写能到 420 MB/s。绕过一切上层后端仍只有 ~10 MB/s，说明瓶颈 100% 在 Ceph 数据路径。
- **定位**：`ethtool ... | grep speed` 查协商速率——ceph-node1/node2 网卡只有 **Speed: 100Mb/s（≈12.5 MB/s）**，node3/tikv=1000Mb/s；rados 带宽抖到 0、延迟十几秒是典型 100Mb 网卡拥塞/丢包特征。（网络定位实测证据 = rados 9.87 + ethtool 协商速率；iperf3 仅在计划清单里，未实际执行。）
- **出处**：`doc/perf-analysis/01-measured-data.md`（ethtool 输出、rados 9.87）。
- **解决**：三节点网络升级千兆 + node1 重装纳管。
- **效果**：后端裸能力 ~10 → ~108 MB/s（约 10×），端到端顺序 ~8 → ~80 MB/s。这是整套系统的头号物理瓶颈，不修它任何软件调优都无意义。

### 4.2 4M block 对 256K 随机读的 ~16× 读放大

- **怎么发现**：白盒实验 + `juicefs stats` 采集 + fio bs-sweep + block-size-sweep。观察到 object/s3 层跑满 ~100 MB/s，但 FUSE 有效读仅 ~3~5 MB/s；fio bs-sweep 在同一 4M-block 卷上 bs=256k→5.0 MB/s（放大 16×）、bs=4M→40.9 MB/s（请求==block 零放大）。
- **分析**：JuiceFS 默认 4M block，fio 只读 256K，客户端每次都拉整个 4M block → ~16× 读放大，带宽全浪费在没用的数据上。
- **解决**：`--block-size` 4M → **256K**（单位 KiB，config 里 `BlockSize=256`；注意别误设成 4096）。
- **效果**：纯随机读 12.3 → 45.8 MB/s（3.7×）；顺序读写不降反略升；存储零膨胀、内存反而更低。**核心突破之一**。
- **出处**：`doc/perf-analysis/08_2-abc-bottleneck-localization.md`（object~100/FUSE~5、fio bs-sweep 表）。

### 4.3 v1.3.1 loadRange bug 造成 2× 读放大

- **怎么发现**：改 256K 后随机读仍有残余放大。分层定位（L1 裸 librados 放大仅 1.04×，t=128 有效读 112.7/NIC RX 117.6 → 排除 EC/后端；L2 JuiceFS 端到端 ~2.5×）把放大锁定在 JuiceFS 内部；`strace -f -e trace=read` 追踪到每个 256K 读被打散成大量细碎 socket read（实测每 256K 读 ~295 次、粒度 5~6KB；按 4 OSD×帧数折算约 139 帧/读，两个口径都指向"半块被完整拉取"）。
- **分析（源码级）**：v1.3.1 独有 bug，`cached_store.go:153` 条件 `len(p) <= blockSize/4`（即 64K）错误地阻止了 128K FUSE 半块读取，导致每次随机读都触发完整 256K block 读取 → 2× 读放大。
- **解决**：应用官方修复 commit `eaf3d21f`（已在 main，未 backport 到 release-1.3），得到 patched 版本 `1.3.1+2025-12-02.e0032b2a`。
- **效果**：随机读后端放大 **2.17× → 1.11×**、BW 51.8→98.3 MiB/s、每 op 127K→67K（≈EC data chunk 64K）。**核心突破之一**。生产落地需升级到含该 commit 的 v1.4.x 或向 1.3 backport。
- **出处**：`doc/perf-analysis/10_3-l1-rados-and-cache-compare-results.md`（L1 1.04×）、`results/loadrange-patch-verify-20260701/`（2.17→1.11、源码分析）、`results/strace-rerun-20260629/README.md`（socket read 计数）。

### 4.4 默认预读对随机读的 2.02× 投机放大（`--max-readahead 0`）

这是**结论反转最曲折**的一项，值得记下教训：

- **真盘阶段（阶段 4）**：ra sweep（ra=0/1/2/4/8）显示 randread r1 均为 51~53 MiB/s，无差异，结论"ra 对 randread 无影响，无需设 ra=0"。
- **全内存盘阶段（阶段 5）修正**：去掉后端磁盘瓶颈后暴露真相——默认预读（2 MiB）对随机读造成 **2.02× 投机预取放大**（object GET 跑到 111 MB/s，但 fio 有效仅 55.7），一半带宽浪费在预读上。关掉预读后有效吞吐翻倍。
- **为什么真盘看不出**：真盘后端磁盘本身是瓶颈（~52 MB/s），预读浪费的带宽和有效带宽被同一堵墙限住，所以看不出差异。内存盘去掉这堵墙，放大才显形。
- **解决**：`--max-readahead 0`。
- **效果（全内存盘 A→B 单变量）**：randread 55.7 → **112.2**（+103%）、randrw R/W 48.6 → **76.1**（+57%）。对应 object GET=111 但 A 组 fio 有效仅 55.0（放大 2.02×），B 组 GET≈fio（放大 ~1.0×）。
- **副作用**：顺序单流读 -33%（103→69.1，预读对顺序读有益）；写类不受影响。
- **生产建议**：**按负载选配**——随机读/混合负载为主必须 `--max-readahead 0`；顺序单流读为主保留默认预读。
- **出处**：`results/memdisk-fullretest-128g-v3-20260711/report.md`（A/B 达标表）、`.../amplification.md`（A-randread GET=111/Fio=55.0/放大 2.02×）。

### 4.5 写侧瓶颈：底层磁盘写路径（root cause = node2 RAID 写异常）

写侧是整个调优里数据取证最完整的一环，**不是靠"rados 比 JuiceFS 快"这种差值现象猜出来的，而是靠一边压测一边抓三节点 IO 延时、发现 node2 延时异常高逐步锁定的**。分四步：

- **第一步·现象**：rados 直打 EC 池 256K 裸能力均值 ~52.7，而客户端网卡 TX 仅 49 MB/s（=44% 千兆，墙不在网络），mu/buffer 参数怎么扫都无法坐实达标——说明瓶颈在后端写路径本身，不在客户端并发参数。

- **第二步·抓 IO 延时锁定 node2（关键定位手段）**：跑 `rados bench -p juicefs-data write -b 256K -t 64` 的同时，三节点并行采磁盘 IO 延时——node1 有 sysstat 用 `iostat -x 1`，node2/node3 无 sysstat 用 `/proc/diskstats` 1s 采样脚本算 w_await/%util；同时每 OSD 测前/测后各抓一份 `ceph tell osd.X perf dump`，用 delta 拆单个写 op 的分段延迟。数据一摊开，**node2 延时明显异常高**：
  - iostat/diskstats：**node2 sdb w_await = 73.2ms、%util=100%**，而 node1/node3 仅 **0.06ms、%util≈50%**（差三个数量级）。
  - OSD perf 分段延迟：`state_aio_wait_lat` **osd.2=55.8ms / osd.3=55.2ms（node2）** vs osd.0/1=11.6ms（node1）vs osd.4/5=7.3ms（node3），node2 是 node3 的 5~8 倍；`subop_w_latency` osd.2=63.6ms / osd.3=92.8ms vs 其余 1.4~18.3ms。
  - 排除项同批采到：`txc_throttle_lat`=0.014ms（非 throttle 墙）、`kv_sync_lat`=2.9ms（WAL fsync 非大头）；`hdparm` 三节点读速一致（390/406/405 MB/s，物理盘健康）——**说明是 node2 的写路径异常，不是盘坏、不是网络、不是 throttle**。

- **第三步·查 node2 RAID 根因**：目标查 PERC H730 的 VD Cache Policy / BBU 状态。`perccli/storcli64/MegaCli` 三节点均装不上、下载失败，退而用 `smartctl + SCSI Mode Page 0x08 WCE + /sys/block/sdb/queue/write_cache`——OS/SCSI 层三节点一致（同型号、同磨损、WCE=0），差异只能在 **RAID 控制器固件层（因工具不可用无法直查）**；尝试 `echo "write back" > .../cache_type` 三节点均 `Invalid argument`。因此"node2 RAID WriteBack 未生效（疑 BBU 降级回退 WriteThrough）+ WAL/DB/DATA 竞争"这一根因是**由上面的 IO 延时取证 + 排除法 + 下一步内存盘反证共同坐实的，perccli 直查未做成**。

- **第四步·内存盘反证**：把 osd.2 的 WAL+DB 迁到 node2 本地 tmpfs 后 rados 复测，**node2 w_await 73ms→0.06ms、osd.2 aio_wait 55.8ms→0.101ms、rados 51.9→58.97（+13.6%）**；进一步 6 OSD 全 tmpfs 后 256K 写稳态 **112 MB/s、客户端 TX 117 MB/s=99% 千兆**，写墙彻底转移到网卡。**至此坐实写侧根因在底层磁盘写路径（node2 尤甚），非 JuiceFS 软件层**。

- **解决方向**：软件层已到位；真盘写侧差距留待生产环境针对 node2 RAID（WriteBack/BBU）/磁盘写路径另调。
- **出处**：`results/iops-wall-locate-20260708/`（report.md + segment-latency.md + 各节点 iostat/perf 原始逐秒）、`results/node2-raid-forensics-20260709/`、`results/node2-raid-fix-memdisk-ceiling-20260709/`。

### 4.6 SSD 被 RAID 卡误判为 HDD（`bluestore_prefer_deferred_size`）

- **怎么发现**：Ceph 因 PERC H730 不透传介质，把 SSD 误判为 HDD，生效 hdd 值 65536（64K），使 256K 写的 64K 分片全走 deferred 双写。
- **分析/尝试**：rados 直打改回 SSD 行为（=0）单次曾 +23%（72.3 vs 58.6），但**干净态 3 轮均值重测仅 +0.9%（SSD 57.9 vs HDD 57.4），+23% 不可复现**（疑 60s 短测缓冲暂态 r1 红利 + crush class 改动引入 PG 重映射假象；deferred 生效已由 OSD `issued_deferred_writes` 计数 delta 证明：HDD +2026/OSD、SSD ≈0）。配套的 `bluestore_throttle_cost_per_io`（670000→4000）同样干净态仅 +0.9%，证明 IO 不卡在 throttle 层。
- **结论**：非可靠净收益；写侧真实瓶颈仍在写路径本身（见 4.5）。
- **出处**：`results/clean-deferred-retest-20260707/report.md`（3 轮矩阵 + deferred delta）；+23% 原始出处 `results/cold-baseline-recheck-20260706/`。

### 4.7 BlueFS stall（`DB_DEVICE_STALLED_READ_ALERT`）——可管理现象非硬瓶颈

- **怎么发现**：高强度写期间出现 stall 告警,一度怀疑需独立 NVMe 做 DB/WAL。
- **分析**：GLM 干净基线复核证实——干净态（`compact_queue_len=0`）下 layout 128G + 64G 多 job 写全程零 stall（G1/G2/G3 三组 health-timeline 全 OK）；触发需"单流持续写 + OSD 已有 compaction 积压"两条件同时满足（污染态对照组 5/6 OSD stall、带宽掉到 44.3）。
- **解决**：`compact` 命令可秒级清除积压（restart OSD 无效）；deferred=0 可把 stall 延迟 5×。
- **结论**：独立 NVMe 从"生产必须"降为"可选优化"（仅在无法控制写入模式且 compaction 长期跟不上时才必要）。
- **出处**：`results/stall-repro-memdisk-20260706/report.md`、`doc/perf-analysis/11_1-step2-stall-compaction-branch.md`。

### 4.8 穷举排除的无效方向（负结果也是结论）

沿 IO 栈逐层验证后确认**不是**瓶颈的方向，避免了在错误方向上浪费精力：

| 方向 | 排除依据 |
|------|---------|
| 磁盘介质（HDD?） | 裸盘 4K 随机读 92,500 IOPS，确认为 SSD |
| WAL/DB 换更快介质 | 内存盘仅 +4.8% |
| 全内存盘集群（早期） | 纯 RAM 替换 SSD 吞吐不变（106.6 vs 106.4） |
| EC → 副本 | 反而慢 16%（写放大 3× > 1.5×） |
| RGW HTTP 层 | 去掉后随机读仅 +22%，非根因 |
| 元数据 TiKV | 延迟仅 1ms |
| FUSE 内核节流 | waiting ≈ 0 全程 50 采样点 |
| FUSE splice/max_read/max_pages | kernel 5.15 / fusermount 3.10.5 不支持 |
| EC 取片 / messenger 帧开销 | L1 放大 1.04% 已证 EC/网络非放大源；EC vs 副本 randread NIC RX 一致 |
| Redis 元数据 / `--max-downloads` | TiKV 非瓶颈；`--max-downloads` 在 v1.3.1 不存在 |

### 4.9 瓶颈分层定位方法（IO 栈）

统一用自底向上逐层隔离，每层与硬件能力对比，只在明显低于上限处调优：

1. **裸盘**：`fio --direct=1` 在 OSD 同类盘 → 确认介质能力（92500 IOPS，SSD）
2. **网络**：`ethtool` 协商速率 + `rados bench` 直压 → 抓出 100Mb/s 网卡（iperf3 仅在计划里，实测未用）
3. **后端**：`rados bench` 直压数据池，绕过 JuiceFS/TiKV → L1 放大 1.04%
4. **单 OSD/节点 IO 延时**：压测同时三节点并采 `iostat -x` / `/proc/diskstats`（w_await、%util）+ 各 OSD `ceph tell osd.X perf dump` delta（分段延迟）→ 抓出 node2 w_await 73ms、aio_wait 55ms 异常（写侧根因定位的关键手段）
5. **librados/JuiceFS**：`juicefs stats` + `strace` socket read 计数 → 定位 loadRange bug
6. **端到端**：fio（去 writeback，开 bw_log）→ 对比后端看 JuiceFS 层损耗

---

## 五、调优过程中遇到的问题

> 本节记录踩过的坑：现象、影响、如何解决。多数是"数据可信度"问题——这套调优最大的敌人是被假象带偏方向。

### 5.1 方向被带偏：把元数据当主因

- **现象**：初版调优指南把 PD 时间戳延迟、TiKV block-cache 当瓶颈主因，连调三轮系统/TiKV/Ceph 参数全无效。
- **影响**：白白浪费三轮调优。
- **解决**：改用 `rados bench` 绕过一切上层直压后端，一测就发现瓶颈在 Ceph 数据路径的网络层，与元数据无关。教训 → 固化"自底向上逐层隔离，先量化上限再调优"的方法论。

### 5.2 客户端缓存假象："调优有效"其实没打到后端

- **现象**：早期开 `--writeback --cache-size` 后官方基准跳到读 1367 / 写 372 MB/s，看似调优大成功。
- **影响**：差点把本地缓存性能当成后端真实能力上报。
- **解决**：识别出这是写只落本地、读命中本地缓存;用户真实负载（`--direct=1` 随机 128 并发）下缓存失效，回落到 ~10 MB/s 后端真值。验收测试一律 cache=0 + direct=1，评估后端能力时去 writeback。

### 5.3 fio 全程平均虚高：报出超千兆的假带宽

- **现象**：全内存盘写类 fio 全程平均报 120~127 MB/s，超过千兆物理上限 ~118。
- **影响**：全程平均被客户端写缓冲暂态（前 1~17s 近线速）拉高 7~8%，若直接采用会得出物理上不可能的达标结论。
- **解决**：改用 `--write_bw_log --log_avg_msec=1000` 逐秒瞬时带宽，截掉缓冲暂态取稳态中位数（~111-112 才是真值）。固化：**>124 MB/s（超千兆）必是缓存/缓冲假象；达标值 = fio 瞬时带宽稳态段中位数**。

### 5.4 ra 结论在真盘/内存盘间反转

- **现象**：真盘上 ra sweep 结论"ra 对 randread 无影响"，内存盘上却 +103%。
- **影响**：若只信真盘结论，生产不会加 `--max-readahead 0`，随机读永远达不到软件层上限。
- **解决**：厘清是"真盘后端磁盘瓶颈掩盖了预读放大"——两个环境结论都对，但适用范围不同。修正演进报告里"ra 对 randread 无影响"的旧结论，改为按负载选配。这也提醒：**同一参数的结论强依赖测试环境的瓶颈位置**。

### 5.5 全内存盘重测的三次返工（数据可信度红线）

阶段 5 的达标测试连续返工三版，全部因数据污染/口径问题：

- **v1 作废**：fio 未开 bw_log 只有全程平均；写类平均被缓冲暂态拉高（randwrite 报 127>千兆）；网卡只存汇总值且 RX(89) < object get(110) 物理矛盾。
- **v2 作废**：过程严重污染——3 次 attempt + 池删重建 3 次 + block-size 一度误设 4096；A 组数据来自不同 attempt 拼接；写类全在高压脏池/nearfull 降级；被迫降到 60s。
- **v3 干净重测通过**：一次连续、block-size=256 config 确认、TrashDays=0、全 180s、randrw 聚合 128 个 job 分文件按时间戳求和。核心结论坐实（randread A55.7/B112.2、randrw A48.6/B76.1）。
- **教训固化**：每格开测前净态三确认（清池对象 + compact 到 `compact_queue_len=0` + iostat idle）；组间 A/B 切换清残留；randrw 的 bw_log 必须聚合 128 job 分读/写再取中位数；object put/get 只算放大不当达标值。

### 5.6 gc --delete 语义误判为 bug

- **现象**：randwrite 覆盖写产生的 orphan slice，gc 报 skip，一度怀疑 gc bug。
- **影响**：担心池被 orphan 撑爆、gc 失效。
- **解决**：厘清 skip 是**正常语义**——slice 在文件仍存在时正确 skip，删文件后 gc 才生效（compact 只是慢）。另注意 gc 必须在 mounted 态做，umount 后 gc 会 skip 全部导致池假满。

### 5.7 高并发 fio 偶发起不来

- **现象**：128 并发 fio 偶发起不来。
- **分析**：非系统卡死、非 fio bug、非压力过大，根因 = 前一个 fio 被 SIGKILL 后的残留污染。
- **解决**：杀 fio 后等 `pgrep -x fio` 为空再起下一轮。

### 5.8 HAProxy 写后读 404

- **现象**：经 LB 写后立即读，9/15 失败返回 404。
- **分析**：`balance roundrobin` 把 PUT 与紧随的 GET 轮询到不同 RGW，跨 RGW 写后读一致性延迟。
- **解决**：改 `balance source` 按源 IP 固定到同一 RGW，15/15 成功。

### 5.9 内存盘部署的工程坑

- **现象**：tmpfs 不支持 O_DIRECT，OSD 起不来。
- **解决**：经 loop device + LVM 中转;WAL/DB 独立 tmpfs(4G) + DATA 独立 tmpfs(45G)，`bluefs_single_shared_device=0` + `dedicated_db=1`；渐进试写坐实 128G layout 安全承载（最高 OSD 73.5% < 85%）。另注意 node3 用 docker 不是 podman。

---

## 六、当前结论与后续

### 6.1 达标现状

- **全内存盘（软件层上限参照）**：B 组（patched + `--max-readahead 0` + mu=150 + cache=0）**8/8 全项达标**——顺序写 111.5、多线程写 111.8、顺序读 69.1、多线程读 111.6、随机写 111.1、随机读 112.2、随机读写 76.1、布局 112.1。证明 JuiceFS 软件层在千兆网络上能实现 59 MB/s 全项达标。
- **真盘冷态**：顺序读/多线程读达标；顺序写(57.0)/随机写(55.7)接近达标；随机读(52.0)差 12%；多线程写/随机读写仍有差距。**未达标根因已明确 = 磁盘/RAID 层写路径 + 预读放大，非 JuiceFS 软件层**。

### 6.2 生产落地要点

1. **挂载参数**：`--block-size 256`（KiB）+ `--max-uploads 150` + `--max-readahead 0`（随机/混合负载）+ `--cache-size 0`（冷态基线）或 100G（暖态）。
2. **patch 落地**：升级到含 `eaf3d21f` 的 v1.4.x，或向 release-1.3 backport。
3. **真盘复测**：内存盘是软件层上限参照，非交付基线；生产真盘性能若低于此属介质差异，需在生产环境针对 RAID(node2 WriteBack)/磁盘写路径另调。
4. **writeback**：对写延迟敏感场景可选启用（本地缓存异步刷盘）。

### 6.3 待办

- 主线：node2 RAID/真盘生产落地。
- seqread 单流不满速诊断（A=103，网卡 RX 仅 93% 未撞墙、放大 ~1.0× 却不满速，疑单流 psync 延迟 / FUSE+meta 开销）。
