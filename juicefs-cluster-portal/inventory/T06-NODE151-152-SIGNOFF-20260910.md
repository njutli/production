# T06 节点151/152采集器签收

> 时间：2026-09-10  
> 裁决：`T06_NODE151_152_PASS`  
> 顺序：151完整验收通过后才执行152。

## 151

- Node Exporter PID为`1312465`，重启次数为0，内存约9.1 MiB。
- NVMe oneshot成功生成4个控制器样本，timer为active且disabled。
- PD PID为`1911337`，TiKV PID为`2237198`；业务挂载正常。
- 从152抓取`151:9100`返回HTTP 200，耗时约16.6 ms。

## 152

- Node Exporter PID为`1189539`，重启次数为0，内存约8.7 MiB。
- NVMe oneshot成功生成4个控制器样本，timer为active且disabled。
- PD PID为`1589960`，TiKV PID为`2088516`；业务挂载正常。
- 本机抓取`152:9100`返回HTTP 200，耗时约17.5 ms。
- 原有Portal、Grafana和Prometheus均保持active，没有被重启或改配。

## 聚合检查

- 从152抓取150、151、152三个Node Exporter端点均返回HTTP 200，耗时均约16～17 ms。
- 三节点Node Exporter和NVMe timer均保持disabled，没有设置开机自启。
- 三节点均继续运行PD/TiKV，业务挂载未变。

## 执行偏差

- 152启动前曾误发一次包含不存在unit名称的`systemctl start`命令；systemd返回失败，未匹配、加载或启动任何unit。
- 随后执行用户已批准的正确命令，并通过服务、资源、业务指纹和不存在误启动unit的只读复核。
