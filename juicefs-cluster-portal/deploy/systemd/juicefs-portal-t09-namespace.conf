[Service]
# Portal serves SQLite snapshots only and must never traverse the writable
# JuiceFS control mount used by the fixed-command summary collector.
InaccessiblePaths=/var/lib/juicefs-portal/namespace-mount
