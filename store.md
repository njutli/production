# 方案1：nfs+raid+iscsi+dm

## 功能
- 单节点所有磁盘组dm-linear：充分使用节点所有存储能力，单节点上所有盘作为一个整体导出，当单节点宕机时，对于上层结构而言，只有一个设备异常，便于数据可靠性保证；
- 单节点将本地dm-linear通过iscsi导出：不同主机通过网络将本地存储能力导出；
- 服务节点整合所有网络导出设备，组raid5：数据可靠性保证，当单节点宕机时，raid设备数据仍可用；
- 服务节点格式化raid设备并挂载，通过nfs服务导出：提供文件存储能力
  - （服务节点可以有多个，但同时启用的只有一个）

## 性能
- 需实测，虽然数据栈浅，但没有客户端本地缓存管理

## 空间使用率
- (N - 1) / N，3块盘以上即可满足要求

## 优点
- 充分利用内核能力，数据栈浅，架构简单；

## 缺点
- 没有单独的客户端缓存管理，客户端数量增加后单个服务节点可能成为性能瓶颈，性能不一定能达标；
- 扩展性差，磁盘拓扑确定后无法在业务运行时加盘；
- 网络抖动可能导致raid重建；
- 客户端使用原生nfs，服务节点宕机后无法自动切换备用服务节点（同时只能有一个服务节点使用各个通过iscsi导出的设备，且客户端没有实现切换服务节点的逻辑）

## 举例
假如现在有3台机器，每个机器上两个盘：
```
node1: disk1 disk2
node2: disk3 disk4
node3: disk5 disk6
```
- node1上disk1+disk2组成dm-linear，通过iscsi导出sda，node2/3同理，导出sdb/sdc；
- 服务节点（node1/2/3或者其他的一个节点），使用sda/sdb/sdc组raid5，格式化本地文件系统并挂载，同nfsd导出供客户端使用。

---

# 方案2：nfs-ganesha+drbd+linstor+pacemaker

## 功能
- 单节点所有磁盘由LINSTOR自动纳管并创建DRBD资源：底层块设备通过DRBD协议在节点间进行同步复制，保证数据强一致性，同时实现基于多节点的数据多副本；
- LINSTOR作为控制平面统一管理存储拓扑：自动感知节点与磁盘状态，按需分配卷并配置DRBD复制链路；
- Pacemaker+Corosync构建集群HA框架：实时监控NFS-Ganesha服务与VIP状态，主节点宕机时秒级将VIP与服务漂移至备节点，客户端无感知自动重连；
- NFS-Ganesha以用户态提供文件服务：通过多实例提供文件存储服务以及故障快速恢复（相比较NFSD而言）；
  - （服务节点采用Active-Passive模式，同一时刻仅一个节点承载IO，备节点保持DRBD数据实时同步待命）

## 性能
- 需实测，相比较底层采用对象存储的方案而言，没有对象与文件转换的开销，但DRBD会引入数据同步开销；
- NFS-Ganesha为用户态实现，支持多线程与异步IO，相比内核nfsd在高并发小文件场景下CPU利用率更优；
- 缺少客户端侧独立缓存层，模型加载性能依赖NFS客户端自身page cache，大规模训练时需配合客户端挂载参数调优；

## 空间使用率
- DRBD只支持多副本，不支持EC，双副本模式下只有50%

## 优点
- 基于Linux内核成熟组件，无外部对象存储依赖，数据路径短；
- 故障切换全自动；
- LINSTOR提供声明式存储编排，加盘/换盘/扩容均可在线执行，无需中断业务或重建RAID；
- DRBD支持Bitmap增量同步，节点短暂离线后仅需同步差异块，避免全盘重建风暴；

## 缺点
- Active-Passive架构限制了横向扩展能力；
- DRBD同步写对网络质量敏感，跨机房或高延迟链路会导致写入性能显著下降；
- Pacemaker+Corosync配置复杂度较高；
- VIP漂移+NFS会话恢复时间秒级，可能导致客户端文件访问hung住影响业务运行；
- 同一时刻仅一个节点承载IO，备节点保持DRBD数据实时同步待命，存在资源浪费；

## 举例
假如现在有3台机器，每个机器上两个盘：
```
node1: disk1 disk2
node2: disk3 disk4
node3: disk5 disk6
```
- LINSTOR在三节点上自动创建DRBD资源池，disk1+disk2在node1上组成ZFS RAID-Z1（或LVM thin pool），并通过DRBD同步复制到node2/node3；
- Pacemaker管理VIP（如192.168.1.100）与NFS-Ganesha服务，初始在node1上启动Primary DRBD资源并挂载ZFS，通过NFS-Ganesha导出共享目录；
- 当node1宕机，Pacemaker检测到故障后，在node2上将DRBD提升为Primary、挂载ZFS、启动NFS-Ganesha并绑定VIP，客户端NFS自动重连至新地址继续IO；
- node1恢复后，DRBD自动以增量方式同步缺失数据，重新加入集群作为Standby节点待命。

---

# 方案3：nfs+JuiceFS+Ceph

## 功能
- 底层仍使用对象存储，利用对象存储保障可扩展性和数据可靠性；
- 通过Redis Sentinel等方式实现类似wekafs的多IP挂载，单节点宕机后自动平滑切换的功能；

## 性能
- 相比较CephFS，JuiceFS实现了客户端本地的缓存管理，没有追求强一致性的开销（完全可以满足没有共享写场景的功能需求）；
- AI场景应用案例与性能调优案例较多，可参考已有案例对当前业务场景进行调优；

## 空间使用率
- 对象存储使用纠删码方案，配置 EC 4+2 即达成 66.7% 可用容量
