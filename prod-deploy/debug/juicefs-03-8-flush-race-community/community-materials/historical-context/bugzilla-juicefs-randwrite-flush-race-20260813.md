# Bugzilla 缺陷报告：JuiceFS 纯随机写路径"登记竞态"导致 --max-fuse-io 256K 下写吞吐崩塌 5.8×

> 文档类型：Bugzilla 缺陷报告（内部存档，供上游 issue/PR 与汇报引用）
> 作者：DeepSeek　｜　日期：2026-08-13　｜　版本：v2（重写原理部分，面向只读过架构文档的读者）
> 关联任务书：`doc/perf-tasks/03-8-deepseek-randwrite-256k-flush-fix.md`
> 阅读方式：§三 用"预期流程 vs 实际流程"讲清原理（正文不需要任何代码知识）；附录 A 放代码级证据。

---

## 一、缺陷基本信息

| 字段 | 内容 |
|---|---|
| Bug 编号 | INTERNAL-2026-08-13-001 |
| 标题 | 纯随机写（写尺寸 = 数据块大小）时，新数据块的"编号登记"异步执行，上传环节误判"未登记"而跳过本次上传；数据滞留内存触发缓冲节流，写吞吐崩塌约 5.8 倍 |
| 产品 | JuiceFS Community Edition |
| 组件 | Client / 写路径（`pkg/vfs/writer.go`） |
| 版本 | v1.3.1（commit `e0032b2a`；现网二进制另含 `eaf3d21f` 读路径补丁）；**受影响 = 全部基座**（v1.3.1 与最新 main edabf9c2 同会话交替实测双双复现，#6311 与竞态无关）——⚑ 2026-08-14 同会话实测终裁，见 §A.4 |
| 严重性 | **High / P1**——阻塞 `--max-fuse-io 256K` 固化（该参数是 randread +115.6% 的关键收益来源） |
| 可复现性 | **100%**（14 轮、3 个挂载实例、跨 13 小时、跨好/坏档、跨 OSD 旋钮切换，全部命中；轮间极差 0.2%） |
| 状态 | **VERIFIED**（03-8 A/B 验证通过；⚑ 2026-08-14 同会话插桩实测：竞态在 v1.3.1 与 main 均 100% 复现、#6311 无关；唯一直接修复 = 本报告补丁） |
| 报告方 | 生产集群 157（96 核 Xeon 8462Y+，100GbE，Ceph EC 后端，数据块 256 KiB，无压缩） |

---

## 二、现象

`--max-fuse-io 256K` 挂载下，**纯随机覆盖写**（fio `rw=randwrite bs=256k --direct=1`，128 个进程各写 1 个 1 GiB 文件）：

| 场景 | randwrite 吞吐 |
|---|---|
| 128K 挂载（历史基线） | **2942~3258 MiB/s** |
| 256K 挂载（03-7L 实测 14 轮） | **551~552 MiB/s（崩塌 5.8×）** |
| 256K 挂载 + 混合读写 randrw（写的那一半） | 1939 MiB/s（**不受影响**） |

- 同一个 256K 挂载上，混合读写的写半程能到 1939 MiB/s ⇒ **不是网络、OSD、对象存储的写带宽问题**，是"纯写负载"特有的崩塌。
- 14 轮均值稳定到 0.2% ⇒ 是确定性的限速机制，不是随机抖动。
- 崩塌位置在**客户端内部**：写请求在 JuiceFS 客户端进程里平均耗时 **57.7 毫秒**（128K 挂载只有 3.7~4.9 毫秒）；而客户端下游一切正常（上传 5.35ms、元数据操作 6.0ms、仅用 2.4 核 CPU）⇒ 写请求是在客户端里"白等"。

---

## 三、原理：一次用户写 IO 的预期旅程 vs 实际旅程

### 3.0 前置概念（对照架构文档）

架构文档说 JuiceFS 分三层：**客户端（Client）**、**元数据引擎（Meta Engine）**、**对象存储（Object Storage）**。本 bug 只涉及客户端内部 + 元数据引擎的"发号"环节。需要先建立五个概念：

1. **数据块（block）**：文件在对象存储里的数据单元。本卷格式化时定为 **256 KiB**（官方默认 4 MiB，本卷因 EC 配置切小）。一次"上传"就是往 Ceph 里 PUT 一个 256 KiB 的数据块。
2. **待写块（slice）**：客户端内存里的一个"正在攒数据"的缓冲区，攒满 256 KiB 就应当立刻上传。
3. **编号（slice id）**：每个数据块在元数据引擎里的身份号码。**申请编号非常便宜**——客户端从元数据引擎批量预领一串号码存在本地计数器里，正常申请是纯内存操作、微秒级；只有本地号码用光时才真正找一次元数据引擎（每 4096 个块一次）。
4. **内存缓冲上限（--buffer-size，默认 300 MB）**：客户端允许"已写进内存、还没传到对象存储"的数据总量上限。**超限时客户端会主动降速**（每笔写睡 10ms；超 2 倍上限睡 100ms）——这是 JuiceFS 有意设计的保护机制，防止内存被未上传数据撑爆。正常时这个机制永远不会触发。
5. **兜底清仓**：某个待写块若一直没机会上传，客户端还有两个兜底：同一 4 MiB 区域内堆积超过 4 个待写块时强制上送；以及 10 秒超时强制上送。正常时这些兜底只是保险丝，不该成为主路径。

### 3.1 预期旅程（设计意图，10 步）

一个 256 KiB 随机写从用户侧下发到数据落入对象存储，**应该**是这样的：

| 步 | 位置 | 动作 |
|---|---|---|
| 1 | 应用（fio） | `write()` 下发 256 KiB 数据 + 文件随机偏移 |
| 2 | 内核 | 经 FUSE 把写请求连同数据交给 JuiceFS 客户端进程 |
| 3 | 客户端 | 给该文件加写锁，定位这次写落在文件的哪个 256 KiB 数据块 |
| 4 | 客户端 | 新数据块 ⇒ 创建"待写块"，**向元数据引擎申请编号**（微秒级，本地计数器） |
| 5 | 客户端 | 把 256 KiB 数据拷入待写块（纯内存拷贝） |
| 6 | 客户端 | 块已攒满（256 KiB = 块大小）⇒ **立即把块交给上传队列** |
| 7 | 客户端 | 上传线程领取配额（上限 150 个并发）→ **PUT 到 Ceph**（约 5ms） |
| 8 | 客户端 | 上传完成 → 内存归还缓冲池 → **缓冲水位下降** |
| 9 | 客户端 | 写请求返回成功给内核/应用（全程约 1ms 以内） |
| 10 | 客户端（异步） | 稍后把"块的编号"提交到元数据引擎（TiKV），数据正式对外可见 |

关键点：**第 4 步（登记）和第 6 步（上传）是紧密相连的**——登记是微秒级操作，登记完马上上传，缓冲水位永远很低，节流机制永远不触发，吞吐只受上传能力（~4 GiB/s）限制。

### 3.2 实际旅程（bug 的分叉）

**分叉发生在第 4 步与第 6 步之间——登记被交给了"后台伙计"，而上传检查时伙计还没登记完：**

| 步 | 实际动作 |
|---|---|
| 4' | 创建待写块后，登记编号被丢给**后台 goroutine 异步执行**（`go prepareID(...)`） |
| 5 | 数据拷入待写块（与 4' 同时进行） |
| 6' | 块攒满，上传前**检查"编号到手了吗？"**——后台伙计还没跑完，编号 = 0 ⇒ **判定"未登记"，跳过本次上传** |
| 7' | 代码的设计意图是"没关系，下次再写这个块时补传"——**但纯随机写没有下次**：下一个 256 KiB 写落在别的随机位置，又创建一个新待写块，重复 4'→6' |
| 8' | 数据滞留内存，只能等两个兜底：同一 4 MiB 区域堆够 5 个待写块（随机写下很难），或 **10 秒超时强制刷** |
| 9' | 滞留量 = 写入速率 × 驻留时间（约 0.3~1 秒）⇒ 内存缓冲涨到 **300~600 MB**（逐秒采样实测：均值 144 MB、峰值 609 MB） |
| 10' | 缓冲超限 ⇒ 节流机制启动（每笔写睡 10/100ms）⇒ 写请求平均耗时 57.7ms |
| 11' | 吞吐闭环：128 个进程 × 256 KiB ÷ 57.7ms ≈ **551 MiB/s**（与实测精确吻合） |

一句话：**登记本是微秒级操作，却异步执行，导致上传环节把"还没登记"误判成"不该上传"，每一次上传机会都被错过；数据在内存里越积越多，直到触发了 JuiceFS 自己的内存保护限速——整个崩塌是"自我限速"造成的，而不是任何硬件瓶颈。**

> ⚑ 用户视角澄清（防止误读）：①**首笔写不卡**——数据拷入客户端缓冲区后写请求立即返回；卡的是**后续写**（缓冲过阈后每次写睡 10/100ms，平均 57ms）。②**数据一分不丢**——滞留数据最终 100% 上传（实测 PUT/写op=1.01），损失的只是**及时性**（立即发 ~5ms vs 兜底 1~2s），而"写率 × 滞留时长"的缓冲占用正是触发节流的量。③性能损失的因果链 = 竞态 → 滞留（非丢失）→ 缓冲过阈 → 节流 → 自锁；竞态本身不慢，慢的是它触发的那把"内存保护锁"。
> ⚑ 耐久性边界（防误读二）：**写请求返回 ≠ 已持久化**——返回时数据只在客户端内存缓冲（`used_buffer_size_bytes`，与 `--cache-size 0` 无关，那是内存缓冲非磁盘缓存）；掉电可丢。这是 JuiceFS buffered-write 的固有语义（`--writeback` 文档明示"at the risk of losing objects"），**本缺陷与补丁都不改变它**：耐久点 = `fsync()`（等上传+元数据提交完成）。补丁的收益是把"数据离手到对象存储"的窗口从 1~2s 缩到 ~5ms（掉电丢失窗口变小），但"返回即持久"依然不成立。

### 3.3 预期 vs 实际 对照表

| 环节 | 预期（设计意图） | 实际（bug 行为） | 差异后果 |
|---|---|---|---|
| 编号登记 | 微秒级、同步完成 | 后台异步，写请求读到时必为"未登记" | 上传误判 |
| 块攒满后 | 立即上传 | 每次都被跳过（纯随机写无第二次机会） | 数据滞留 |
| 数据驻留内存 | 约 5 毫秒（上传耗时） | 约 0.3~1 秒（等兜底清仓） | 缓冲积压 |
| 内存缓冲 | 稳定在几十 MB | 均值 144 MB、峰值 609 MB | 触发保护 |
| 节流机制 | 永不触发 | 持续触发（每写睡 10/100ms） | 写请求 57.7ms |
| 吞吐 | 3~4 GiB/s 量级 | 551 MiB/s | 崩塌 5.8× |

### 3.4 类比（快递驿站）

每收到一个包裹（写请求）本应"登记 → 立刻派送"（申请编号 → 上传）。登记本可在柜台即时完成，但店规却让柜台把登记条丢给一个后台伙计，而打包员看到"登记条还没回来"就认定"这包裹先不派"，放回货架等下次。可下一趟车总是开到别的货架（随机写），这个包裹再也没人来补派。包裹越堆越多，仓库（300 MB 缓冲）爆仓，店主启动"限流令"（每笔写强制休息 10/100ms），全店吞吐崩塌。

对比之下：128K 挂载时，内核会把一个 256K 大包裹**拆成两个连续的小包裹**，第二个小包裹恰好接在第一个后面（顺序相邻），等凑满一箱（256 KiB）时登记条早就回来了 ⇒ 立刻派送 ⇒ 仓库一直很空。混合读写时，读操作插在两次写之间给仓库留出了清仓时间 ⇒ 也不爆仓。

---

## 四、为什么只有"256K 挂载 + 纯随机写"触发

| 条件 | 256K 挂载 + 纯写 | 128K 挂载 + 纯写 | 256K 挂载 + 混合读写 |
|---|---|---|---|
| 写请求落点 | 随机，每个写都是新数据块 | 内核把 256K 拆成两笔**连续** 128K，第二笔恰好接在第一笔后（同一个块） | 随机，每个写都是新数据块 |
| 块攒满的时刻 | 第一笔写就攒满 | 第二笔续写时攒满 | 第一笔写就攒满 |
| 攒满时编号登记好了吗 | **没有（竞态，必错过）** | 有（登记有足够时间完成） | 没有（同样错过） |
| 结果 | 滞留 → 崩塌 | 及时上传 → 正常 | 错过上传但**读操作给写降速**，兜底清仓来得及排空 → 正常 |

- 128K 挂载幸免的原因不在客户端逻辑，而在**内核拆包**：拆出来的两笔 128K 写是顺序相邻的，第二笔"续写"同一个待写块，攒满时编号已就绪。
- 混合读写幸免的原因：每个进程在两次写之间插一次约 10ms 的读，纯写速率被自然拉低一半以上，兜底机制（同区域堆 5 块 / 10 秒超时）来得及清仓，缓冲始终不超限（实测均值 29.8 MB）。

---

## 五、修复思路

### 5.1 补丁（≤10 行，已编译为 `1.3.1+2026-08-13.e0032b2a-03-8-ceph`）

把"后台登记"改回"柜台登记"：创建待写块时**同步申请编号**（`NewSlice` 微秒级，本地计数器；只有本地号码用光时才真正找一次元数据引擎，平均每 4096 个块一次），失败时回退原异步路径：

```go
// 原（pkg/vfs/writer.go，writeChunk）：
go s.prepareID(meta.Background(), false)

// 改为：
var id uint64
if st := f.w.m.NewSlice(meta.Background(), &id); st == 0 {
    s.id = id
    s.writer.SetID(id)
} else {
    go s.prepareID(meta.Background(), false)
}
```

修复后：登记即时完成 ⇒ 块攒满时编号必在手 ⇒ **每次写都立即上传** ⇒ 缓冲水位回到几十 MB ⇒ 节流永不触发 ⇒ 写吞吐恢复到上传能力上限（3~4 GiB/s 量级，可能反超 128K 基线）。

**安全性**：`NewSlice` 只碰"发号器"的锁，不碰文件锁；调用点本来就在文件锁保护内，无死锁、无数据竞争；错误路径与原版行为完全一致。

### 5.2 放弃的替代修复（附理由）

| 候选 | 结论 |
|---|---|
| `--buffer-size` 调大（如 4G） | **治标不治本**：节流不触发、写请求不再睡，但数据滞留仍在，内存随速率线性上涨（~速率×1 秒），迟早还是要限（03-8 段2 仅作兜底验证） |
| `--max-uploads` 调大/调小 | **无效**：逐秒采样显示上传信号量（150 容量）全程空转——瓶颈不在上传并发，在**派发被推迟** |
| 升级到上游 1.4/main | 不可行：源码对比确认该竞态在主线**仍未修复** |

### 5.3 补丁适用边界（必须写清，防误用）

- **修复范围**：写尺寸 = 数据块大小（本卷 256 KiB）的完整块随机写——本 bug 的精确触发条件。
- **不覆盖**：写尺寸 < 块大小的纯随机小写（攒不满一个块，上传环节本来就轮不到它，走兜底清仓——**这是预存行为，非本补丁引入**）。预测：bs=128k 直写在任意挂载上仍会塌，待负控验证（可选，10min）。
- 顺序写、混合读写不受影响（本就不触发）。

---

## 六、复现步骤

1. 环境：96 核客户端，JuiceFS v1.3.1，EC 卷（数据块 256 KiB，无压缩），Ceph 对象后端。
2. 挂载：`juicefs mount --max-uploads 150 --cache-size 0 --max-fuse-io 256K <META> /mnt/juicefs`。
3. 预置 128×1 GiB 文件；fio：`--directory=/mnt/juicefs --name=t --filesize=1G --size=1G --bs=256k --rw=randwrite --ioengine=libaio --iodepth=128 --numjobs=128 --direct=1 --runtime=180`。
4. 观测：bw ≈ **551 MiB/s**（对照 128K 挂载 3000+）；`juicefs_fuse_ops_durations_seconds_write` 均值 ≈ 57.7ms；逐秒 `juicefs_used_buffer_size_bytes` 均值 >100 MB、峰值 >600 MB。

### 6.1 两层复现模型（⚑ 2026-08-14 定稿——"稳定复现"的正确理解）

**崩塌 = 竞态（代码级，每轮必现）× 环境退化区（兜底清仓跟不上）。两层必须分开声明：**

1. **竞态层**：插桩实测**每轮 100% 发生**（首写检查 99.2% 跳过、fired 仅 0.8%），与轮数、环境无关——任何单轮即可观测到。
2. **可见崩塌层（~550 MiB/s）**：竞态使数据全靠**兜底清仓**（flushAll 1s/5s 清扫、commitThread 10s 强制刷、冻结链）上传；兜底清仓速度随环境退化（对象积累、TiKV/OSD 负载、gc/compaction 积压）。兜底跟不上 ⇒ 缓冲跨过 300 MB 节流阈值 ⇒ 进**自锁塌态**（写率被节流钉死，缓冲永远降不回去，之后每轮稳定 ~550，极差 0.4%）。

**⇒ 干净环境下单轮可能假阴性**（2026-08-14 实测：清晨 main 单轮 3815 健康；当日连续重写数轮后同基座 545 崩塌）。**稳定复现协议见 6.2。**

⚑ 补充观测（2026-08-14 13:31，多轮 6×2 全量跑的第三个态）：环境存在**中间态**——当日午后深度退化期，stock 臂 6 轮从 1193 逐轮下滑到 707（无骤塌、无自锁），patched 臂 6 轮平坦 ~630~680。此时环境（TiKV/OSD 负载 + 7M 对象）把**兜底清仓（meta 提交率墙 F44）压到 ~600~1200**，写率低于缓冲节流阈值 ⇒ 竞态被兜底"勉强掩盖"，既不到 3800 也不到 550；且整场 42 分钟会话本身单调退化（每轮写入 ~100~170 GiB 继续加重环境），两臂差异被臂序混淆（stock 先跑、环境更新鲜）。⇒ 三层模型：**健康态（~3800，环境新鲜）→ 中间态（600~1200 缓慢下滑，竞态被兜底掩盖）→ 塌态（550 自锁）**。多轮协议的"先不塌后塌"拐点只在从干净环境起步时可见。

⚑ 补充观测二（2026-08-14 15:01，**每臂前置环境复位**（gc --compact + OSD compact 追平 + drop_caches）× 6 轮 × 2 臂，main 基座 stock vs 补丁）：复位使两臂第 1 轮都回到 **~3425**；臂内逐轮下滑曲线**两臂几乎重合**（stock 3425→1886→1394→1298→1099→927；patched 3421→1837→1431→1227→995→916），节流特征同步出现。⇒ 结论：**在 main 基座上补丁无差异**（main 的上游变更已把竞态部分掩盖 + 深退化环境把排水压平）——该观测不构成对补丁的否定，只说明"main 不需要补丁"；补丁的对照必须用 v1.3.1 原版（见观测三）。

⚑ 补充观测三（2026-08-14 16:59，**决定性实验：复位后三臂紧邻交替** S-P-M-M-P-S，各 120s，S=v1.3.1 原版、P=v1.3.1+本补丁、M=main 原版 edabf9c2）：

| 臂 | 位置 2/5 或 1/6 | bw（MiB/s） | 写延迟/缓冲峰（第 2 次采样） |
|---|---|---|---|
| S | 1st / 6th | **544 / 544**（首尾完全一致） | 56.9ms / 609MiB（深节流） |
| P | 2nd / 5th | **2647 / 1206** | 23.8ms / 338MiB（轻节流） |
| M | 3rd / 4th | 1437 / 1505 | 20.7ms / 328MiB（轻节流） |

- **补丁在 v1.3.1 上的效果同会话复现**：紧邻位置 P=2647 vs S=544 = **4.9×**；且 S 首尾两次 **544/544 完全一致**——塌态自锁的直接证据（环境在会话内漂移了 P 2647→1206，S 却纹丝不动）。
- **main 原版同会话不塌**（1437/1505 vs S 的 544/544）——"v1.3.1 的塌态在 main 上不复现"成立；但 ⚑ **M vs P 不可比**（基座不同 + 位置不对称：P 占第 2 位最新环境，M 占 3/4 位已退化），P 平均 > M **不能归因于补丁**。对 main 的结论仅两条：竞态在 main 代码里存在（插桩 99.2% 跳过）；其性能影响未被证明——同基座 main±补丁对照（观测二 15:01）无差异 ⇒ 现有证据指向"main 的兜底完全掩盖竞态、补丁对 main 无收益"。

⚑ 补充观测四（2026-08-14 18:23，**P/M 位置交换复测** S-M-P-P-M-S，其余与观测三相同）：位置匹配后 P 与 M **无差异**——第 2 位（最新鲜）**M=3096 > P=2647**（观测三该位置 P>M 是位置假象）；第 3/4 位 P=1746/1510 vs M=1437/1505、第 5 位 P=1206 vs M=1165 均相当；S 依旧 549/543（自锁）。⇒ 终裁：**补丁对 main 无收益（两轮位置对称共 4 个 P/M 配对无系统性差异）；补丁仅对 v1.3.1 有效（S vs P 恒定 ~4.9×）**。

### A.4.1 ★ 二分终局（2026-08-16）：修复提交不存在——历史上"健康"读数是一个 flag bug 的假象

四轮 git bisect（v1~v4，含窗口守护与相对分类）的 good/bad 判定看似稳定可复现，但**全部落在与写路径无关的提交上**（GOOD：Mvn deploy #6208、LRU eviction #6271、atomic types #6317；BAD：quota #6361、cifs/smb #6368、meta oldAttr #6352）。根因排查：

- **`3cb2b47d "Fix: the max-fuse-io parameter was not taking effect" (#6336, 2025-09-01)`**：一行修复——`genFuseOptExt` 里 `ParseBytes(c, "max-write")`（不存在的旗标，永远返回 0）→ 改为 `ParseBytes(c, "max-fuse-io")`。
- **实测验证**：pre-#6336 提交（b023847a）挂载 `--max-fuse-io 256K` 后 `/proc/mounts` 显示 **max_read=131072**——旗标被静默忽略，写请求 128K ⇒ 内核把 256K 应用写拆成两笔连续 128K ⇒ 竞态永不触发 ⇒ 二分里所有"GOOD"读数都是**旗标失效假象**，不是竞态修复。
- **post-#6336（含 v1.3.1 与最新 main）**：旗标生效，256K 写请求 ⇒ 竞态真实存在且可触发。
- ⇒ **竞态在 main 上从未被修复**；main 的"健康"读数 = 环境排水充足时的掩盖 + pre-#6336 时代的旗标假象。v1.3.1 与 main 在同环境下的差距来自 main 更强的兜底排水（若干普通重构的累积效应，非单一修复提交），且 main 在环境深退化时同样塌（2026-08-14 12:23 同会话 main=546/521）。
- 方法论教训：二分期间未校验 `/proc/mounts`，把旗标失效的历史段当成了修复信号；**任何挂载参数类实验必须每挂载校验 /proc/mounts 实际生效值**。

### A.4.2 ★ 补丁对 main 的判决实验（2026-08-16 19:21，模式 B：main 基座、无复位、8 轮 × 2 臂）

用户质疑"main 既有同样竞态，补丁为何无收益"——此前"无收益"结论只覆盖健康/下滑区，**未覆盖 main 的塌态区**。判决实验：STOCK=main 原版 8 轮（无复位预热，复现塌态）→ PATCHED=main+补丁接续 8 轮（环境已深退，若补丁有效应跳出塌态）。

| 臂 | 逐轮 bw（MiB/s，⚑=塌态标注） | 结论 |
|---|---|---|
| stock | 732, 641, 581⚑, 619, **535⚑, 525⚑**, 626, 578⚑ | **复现成功**：r3 起进入塌态区 |
| patched | **556⚑, 512⚑, 573⚑**, 608, **517⚑, 554⚑**, 628, 539⚑ | **自 r1 即塌态区，全程与 stock 同带（512~628 vs 525~732）** |

- **补丁对 main 无效——现在在塌态区也成立**：patched 臂没有跳出塌态，与 stock 臂在同一带宽带内振荡（patched 均值 ~561 甚至略低于 stock ~605，因接续在更退化的环境中）。
- ⇒ 主结论闭环：**竞态（99.2% 首写跳过）在 main 上存在但只是"旁观者"**——main 的塌态由排水能力（环境）驱动，与竞态无关；补丁消除竞态后 main 塌态不变。补丁的价值**仅限 v1.3.1**（其自锁塌态由竞态维持）。
- 数据：`data/main-repro/repro-out-182307-modeB-main-8x2/`（16 轮 summary + 逐轮缓冲/延迟样本）。
- 复位效果量化：复位把 pool 对象 8.89M→5.51M、stored 2.33TB→1.44TB（gc --compact 回收），随后 S 仍 544——**自锁塌态与对象数无关**，进一步坐实"塌态 = 节流自锁"。

### 6.2 多轮复现协议（稳定复现 + 补丁有效性验证）

配套脚本：`scripts/repro.sh`（自包含，`ROUNDS` 参数，默认 6 轮/臂）：

1. **stock（未打补丁）臂，多轮**：干净环境下前几轮可能健康（~3800），持续写入积累后某轮跨过阈值 → 塌（~550）→ 此后每轮自锁在 ~550。已退化环境下第一轮即塌。
2. **patched（打补丁）臂，同轮数**：**无论环境如何，全部轮次健康（~3000）**——补丁使首写检查必过（同步 NewSlice），写路径不依赖兜底清仓，健康态是结构性的。
3. **判读**：`summary.tsv` 逐轮 bw 曲线。stock 全程未塌 ⇒ 环境仍新鲜，加大 `ROUNDS` 或先做预热写入。
4. **⛔ 对比纪律**：任何 stock-vs-patched 结论必须**同会话**（交替或 ABBA）；跨时段单跑对比会被环境漂移混淆（2026-08-14 全天时间线的教训：上午"main 健康 vs main−#6311 塌"的对比事后被同会话重测推翻）。

---

## 七、验证计划（03-8 任务书段1）

- A/B：补丁版 vs 原版，ABBA 四挂载，每挂载 ns/B 判别器门（3.287 ns/B ±10%）+ 重试换 label。
- 主判据：补丁臂 randwrite ≥ **+200%**（≥1653 MiB/s）；落入 128K 平台 2942~3258 ⇒ 判"修复成立且不劣于基线"。
- 机制实证：补丁臂逐秒缓冲均值应回落 ≤50 MiB、上传信号量出现非零采样。
- 无回归：randrw 单方向中位数落在 256K 平台 1931~1978 的 ±5.56% 内。

---

## 八、证据清单（全部可回溯）

| 证据 | 位置 |
|---|---|
| 14 轮崩塌数据 | 157 `/tmp/opencode-t3.7l-20260813.tar.gz` 内 `bw-raw.tsv`（本地展开 `/tmp/opencode/t37l/`） |
| FUSE/元数据/上传差分 | 同上 `opencode-fullbaseline-v4/T37L-WS1|WF1/randwrite-*-r1/jfs-stats-{pre,post}.txt` |
| 逐秒缓冲证据 | 157 `/tmp/opencode-t3.7l/i1-jfsstats-randwrite-T37L-{WS1,WF1}.tsv`（本地 `/tmp/opencode/t37l/`） |
| 代码行号 | 本地 `~/project/juicefs`（v1.3.1 `e0032b2a`）：`pkg/vfs/writer.go:140-141,268`、`pkg/meta/base.go:48,1654-1671`、`cmd/mount.go:362` |
| 上游未修 | `git diff e0032b2a..origin/main -- pkg/vfs/writer.go`（写路径节流/上传逻辑无差异） |
| 卷格式 | `juicefs config <META>`：`BlockSize: 256`、`Compression: none`、`Storage: ceph` |
| 补丁二进制 | 157 `/tmp/juicefs-03-8`，md5 `1f60618c44fda1c19fecd75d52e053e9`，版本 `1.3.1+2026-08-13.e0032b2a-03-8-ceph`（`-tags ceph`，librados 17.2.9 头文件编译，与 157 运行时同版本） |

---

## 九、影响与后续

- **业务影响**：`--max-fuse-io 256K` 是 randread +115.6%、randrw +52% 的来源；本缺陷使该参数在**任何基座**（1.3.x 与 main）上无法用于纯随机写场景——当兜底清仓跟不上环境负载时（TiKV/OSD 退化、对象积累），randwrite 从 ~3000 跌到 ~550。
- **修复价值**：本报告补丁（同步 slice ID）从代码上直接消除竞态（首写检查必过），写路径不再依赖兜底清仓 ⇒ v1.3.1 上 8 轮实测 2970~3583。生产路线 = v1.3.1 + eaf3d21f + 本补丁。
- **上游动作（⚑ 2026-08-14 终裁修订）**：竞态在 main 上同样存在（插桩 99.2% 首写跳过），但**环境新鲜期被兜底清仓掩盖、不表现崩塌**；#6311 与之无关。是否向上游报告由项目方决定（2026-08-14 决定：暂不报告）；若将来报告，issue 应声明"main 亦受影响但环境掩盖"，并附本补丁。
- **遗留**：<块大小的纯随机小写仍走兜底清仓（预存行为），如需覆盖另立专项。

---

## 附录 A：代码级细节（供上游复核查证）

### A.1 竞态点原文（`pkg/vfs/writer.go`，v1.3.1）

```go
// writeChunk（约 268 行）：slice 创建后异步登记编号
go s.prepareID(meta.Background(), false)

// sliceWriter.write（约 140-141 行）：攒满块后检查编号再上传
} else if int(s.slen) >= f.w.blockSize {
    if s.id > 0 {                              // ← 首次写时必为 0
        err := s.writer.FlushTo(int(s.slen))   // ← 被永久跳过
```

### A.2 "编号登记为什么本该同步"（`pkg/meta/base.go`）

```go
func (m *baseMeta) NewSlice(ctx Context, id *uint64) syscall.Errno {
    m.freeMu.Lock()                 // 仅发号器互斥锁
    defer m.freeMu.Unlock()
    if m.freeSlices.next >= m.freeSlices.maxid {
        v, err := m.en.incrCounter("nextChunk", sliceIdBatch)   // 仅此处打 TiKV
        ...
    }
    *id = m.freeSlices.next         // 本地计数器 ++
    m.freeSlices.next++
    return 0
}
// sliceIdBatch = 4096（base.go:48）：每 4096 个块才真正找一次元数据引擎
```

### A.3 节流与兜底（`pkg/vfs/writer.go`）

```go
// fileWriter.Write（约 299-304 行）：缓冲超限主动降速
if f.w.usedBufferSize() > f.w.bufferSize {       // 默认 300M
    time.Sleep(time.Millisecond * 10)
    for f.w.usedBufferSize() > f.w.bufferSize*2 {
        time.Sleep(time.Millisecond * 100)
    }
}
// commitThread（约 187-193 行）：10 秒兜底强制刷
if s.notify.WaitWithTimeout(time.Millisecond*100) && !s.freezed &&
    time.Since(s.started) > flushDuration*2 {    // flushDuration = 5s
    s.freezed = true
    go s.flushData()
}
```

### A.4 上游状态（⚑ 2026-08-14 两次实测修订——最终裁定见末尾）

~~`git diff e0032b2a..origin/main -- pkg/vfs/writer.go` 显示写路径的节流、FlushTo 守卫、异步 prepareID 均无实质变化 ⇒ v1.4/main 仍含本缺陷。~~

**第一轮裁定（2026-08-14 午前，已作废）**：曾归因"main 已由 #6311 修复、v1.3.1 不含"。该结论基于时间错开的单跑对比（09:41 main 3815 vs 10:05 main−#6311 543），**被环境随时间退化混淆**。

**最终裁定（2026-08-14 午后，同会话交替 ABAB 实测 + 插桩计数）**：

| 构建 | randwrite（MiB/s） | 首写检查跳过率（插桩） |
|---|---|---|
| v1.3.1 + #6311（A1/A2） | 549 / 547 | **99.2%**（130864/131914） |
| main + #6311（B1/B2） | 546 / 521 | **99.2%**（130546/131604） |

- **同会话交替实测：main 与 v1.3.1+#6311 行为完全一致，双双崩塌**（~545），插桩计数器逐位相同（calls=full≈13.1 万、fired≈0.8%、**skipped≈99.2%**、below=0）。⇒ **竞态在 main 上依然 100% 存在；#6311 与 id 竞态无关，对崩塌无任何影响。**
- 早上"main 3815 健康"是**环境新鲜期**（兜底清仓跟得上）的假象；环境退化后 main 与 v1.3.1 **一起塌**。崩塌链条 = 首写跳过上传（代码级，两基座相同）+ 兜底清仓速度（环境依赖：TiKV/OSD 状态）+ 缓冲节流 ⇒ 吞吐钉在 ~550。
- **唯一直接消除竞态的代码修复 = 本报告补丁（同步 NewSlice）**：首写检查必过（fired=100%），写路径不再依赖兜底清仓 ⇒ v1.3.1 上 8 轮实测 2970~3583（同会话 ABBA 与 stock 551 对照）。
- #6311 的 cherry-pick（零冲突）与回退均不影响结论；1.3.x 与 main 在本缺陷上的差异**不存在**。

---

## 附录 B：历史成因——"异步登记"为什么存在（git 考古）

### B.1 结论一句话

**异步登记不是一开始的设计错误，而是 2022 年一次重构让"登记很贵"的前提失效后，调用方没有跟着改——典型的前提失效遗留代码。**

### B.2 时间线

| 时间 | 提交 | 编号申请的成本 | 异步化是否合理 |
|---|---|---|---|
| 2021-01-08 | `d23762a7`（first public release） | `NewChunk` = `rdb.Incr("nextchunk")`——**每次申请都是一次真实元数据引擎往返**（Redis/TiKV，毫秒级） | ✅ 合理：写路径不该为每个新数据块卡一次元数据往返，异步化是当时正确的取舍 |
| 2022-07-26 | `b38ace2f`（#2397 "refactor: distinguish Slice from Chunk"） | 改为 `NewSlice` = **本地计数器 + 批量预分配**（`sliceIdBatch=4096`，每 4096 个块才真正打一次元数据引擎） | ❌ **前提已失效，但 `go s.prepareID(...)` 与 `if s.id > 0` 原样保留** |

- 2021 年版本里还有一处补偿分支（`else if int(off) <= f.w.blockSize { go s.prepareID(ctx, false) }`），说明当时作者对"第一次写错过上传"有感知，靠"第二笔续写补传"兜住——对顺序写/续写场景这只是 1 个块的延迟，无感。
- #2397 只重构了 `prepareID` 的**实现**（把昂贵的元数据往返换成本地计数器），没有回头审查依赖旧成本模型的**调用方**——异步外壳连同竞态一起留了下来。

### B.3 为什么十年级的维护者也没踩到

1. **触发条件极窄**：写尺寸恰好 = 数据块大小（本卷 256 KiB）的**纯随机**写，且 `max-fuse-io ≥ 256K`（默认 128K 时内核把 256K 应用写拆成连续两笔 128K，第二笔续写时编号早就绪，FlushTo 命中，竞态永远轮空）。
2. **顺序写/续写**：第一次错过只延迟 1 个块，第二笔续写即补传，无感。
3. **混合读写**：读操作给写降速，兜底清仓（同区域堆 5 块 / 10 秒超时）来得及排空积压，缓冲永不超限。
4. 本缺陷是 fio 纯随机覆盖写 + `--max-fuse-io 256K` 组合钉死的——上游与一般用户的典型负载（顺序落盘、append 日志、混合读写）都稳定绕开。

### B.4 教训（供内部复用）

- **优化改了底层成本模型后，必须回头审查依赖旧成本模型的调用方**：异步/批量/缓存类优化最容易留下这类"前提失效"的竞态。
- 本项目同类经验：03-6 的 `--max-fuse-io` 边界、`--max-readahead` 拆包误触发内部 readahead（128K 拆包与 JuiceFS 内部读放大机制的前提冲突）——都属于"参数/实现变了，旧假设没跟着变"。
