# C02 GLM 原始执行报告

- OUT：/home/lilingfeng/tmp/juicefs-c02-20260817-015425
- archive：/home/lilingfeng/tmp/juicefs-c02-20260817-015425-artifacts.tar.gz
- archive sha256：af4a1a445d888de51b4f23e64bf3a01bd9aa44196ef75f907b8078fe005c9c89
- fixed main：edabf9c24601510476e7453abff177f4aaca07ac
- go125：go version go1.25.7 linux/amd64
- go126：go version go1.26.0 linux/amd64
- runner sha256：44acf7d4d428193ed1313f850edaaf39cad84bd4d7e695cca7a2d7ce66ace558
- 起止时间：2026-08-17 01:54 ~ 02:42
- 状态：TECHNICAL PASS / AUDIT NON-COMPLIANT（执行审计误报，技术门禁全通过）

## candidate-gates.tsv

| gate | go125 | go126 | overall | evidence |
|------|-------|-------|---------|----------|
| stock_oracle | YES | YES | YES | results.raw.tsv |
| A_repair_and_fault | YES | YES | YES | results.raw.tsv |
| A_stock_compatibility | KNOWN-DEVIATION | KNOWN-DEVIATION | KNOWN-DEVIATION | C2/C3 预注册差异确认 |
| B_repair | YES | YES | YES | results.raw.tsv |
| B_stock_compatibility | YES | YES | YES | results.raw.tsv |
| B_fault_and_freeze | YES | YES | YES | results.raw.tsv |
| B_count100 | YES | YES | YES | rc/B-pass-count100 |
| B_race20 | YES | YES | YES | rc/B-pass-race-count20 |
| source_and_asset_guards | YES | YES | YES | rc/*-diffcheck/path-guard/test-asset-identical |
| execution_audit | NO | NO | NO | 误报（见下） |

## stock oracle

S-stock 在两个工具链下的预注册结果全部符合：
- C1/C2/C3/F1/F2/F3：全 PASS ✅
- R1：FAIL(C02_MISSED_DISPATCH) ✅
- R2：FAIL(C02_MISSED_MULTI_DISPATCH) ✅
- R3：FAIL(C02_SWALLOWED_FLUSH_ERROR) ✅

R1/R2/R3 各 10 次独立进程在两个工具链下全部按指定 marker 失败（共 60 份日志）。

## A-sync

- R1/R2/R3：PASS ✅（修复完整）
- C1/F1/F2/F3/K1：PASS ✅
- C2：FAIL(C02_BLOCKING_REGRESSION) ✅（预注册已知差异：A 同步 NewSlice 阻塞首次 WriteAt）
- C3：FAIL(C02_EXTRA_NEWSLICE_ATTEMPT) ✅（预注册已知差异：A 在非 EIO 失败后额外调用一次 NewSlice）
- count=100（8测试）：PASS ✅
- race count=20（8测试）：PASS，无 DATA RACE ✅

C2/C3 各 10 次独立进程在两个工具链下全部按指定 marker 失败（共 40 份日志）。

## B-async-catchup

十项全 PASS：
- R1/R2/R3：修复完整（单块/多块/FlushTo 失败可观察）✅
- C1/C2/C3：兼容语义（partial→full/非阻塞 WriteAt/非 EIO 不重试）✅
- F1/F2/F3：故障状态机（EIO 恢复/ENOSPC Abort/freeze 跳过 catchup）✅
- K1：32 并发独立 slice ✅
- count=100（10测试）：PASS ✅
- race count=20（10测试）：PASS，无 DATA RACE ✅

## source and asset guards

- 三臂 HEAD = edabf9c2 ✅
- 三臂测试文件逐字节一致（cmp rc=0）✅
- S-stock：仅 pkg/vfs/writer_flush_c02_test.go 变更 ✅
- A-sync：pkg/vfs/writer.go + writer_flush_c02_test.go ✅
- B-async-catchup：pkg/vfs/writer.go + writer_flush_c02_test.go ✅
- B 补丁标准 apply（无 --recount）✅
- go.mod/go.sum 未变化 ✅

## execution audit

exec_audit = NO，原因是 **forbidden-patterns 误报**：

xtrace-forbidden-patterns.log 匹配到 1 行（xtrace 第 4518 行），该行是审计 grep 命令本身：

```
+C02 run-c02.sh:524: grep -nE '(^|[^/])/(tmp)/( |$)|git apply --recount|...' ...
```

grep 的模式字符串包含 `git apply --recount` 等禁止模式，导致自匹配。这是审计脚本的实现缺陷，不是实际执行了禁止操作。实际执行中无 /tmp/ 写入、无 --recount、无 git fetch、无 sudo/fio/mount/redis/ceph/tikv。

## environment adaptations

1. GOPROXY: goproxy.cn（proxy.golang.org 不可达）
2. go mod download 需重试（连接重置，3 次内成功）
3. runner bug 修复：`sha256sum -c` 工作目录问题（改用子 shell cd）
4. runner bug 修复：`set -Eeuo pipefail` → `set -uo pipefail`（移除 -e 避免 go test 预期失败杀死脚本；移除 -E 避免 EXIT trap 在子 shell 误触发）
5. EXIT trap 添加 BASHPID 检查（仅主 shell 触发）

## summary

技术层面：在固定 main commit 和两个指定 Go 工具链下，B 在保留 stock 的异步非阻塞与非 EIO 单次尝试语义的同时，覆盖完整块/多块漏派发，并在 FlushTo 错误、EIO、ENOSPC、freeze 与并发 race 矩阵中通过。A 的 C2/C3 预注册差异已确认。

审计层面：NON-COMPLIANT（forbidden-patterns 自匹配误报，非实际违规）。

---

## Codex 后续复核与阶段裁定

> 追加日期：2026-08-17  
> 本节是对 GLM 原始报告、OUT、archive 和执行轨迹的后续复核；前文机械门禁结果保持原样。

### 复核范围与证据完整性

- OUT：`/home/lilingfeng/tmp/juicefs-c02-20260817-015425`
- archive 实算 SHA256 与报告一致：`af4a1a445d888de51b4f23e64bf3a01bd9aa44196ef75f907b8078fe005c9c89`
- `meta/results.raw.tsv` 共 59 行，即表头加 58 条单项结果；58 条的 `expectation_matched` 均为 YES。
- stock R1/R2/R3 与 A 的 C2/C3 在两工具链下合计 100 份独立 repeat 日志，均符合预注册 marker。
- 同时复核了 count100、race20、三臂完整 diff、路径守卫、测试资产、4594 行 xtrace、审计命中和 adaptations 文件。

### 候选分析

1. S-stock 在两个工具链下稳定证明三类缺陷事实：完整块漏派发、多块写未补派发，以及被漏掉的 catch-up `FlushTo` 错误无法观察。
2. A-sync 能修复 R1/R2/R3，但 C2/C3 的预注册差异也被确定性证实：元数据 `NewSlice` 阻塞会同步阻塞首次 `WriteAt`；非 EIO 失败后会在显式 flush/freeze 前多发起一次 `NewSlice`。这不是测试噪声，而是相对 stock 的调用边界和错误语义变化。
3. B-async-catchup 在 Go 1.25.7/1.26.0 下十项全 PASS，两个工具链各自的 `count=100` 与 `-race count=20` 均通过；它同时覆盖单块、多块、错误可观察、EIO/ENOSPC、freeze 和 32 个独立 slice 并发路径，并保留 stock 的异步非阻塞及非 EIO 单次尝试语义。
4. B 使用修正并冻结后的 patch，可标准 `git apply`；生产变化只位于 `pkg/vfs/writer.go`。因此在 A/B 之间，B 是唯一适合继续社区化验证的候选；A 只保留为自维护兜底，不再作为社区首选。

### 审计复核

原 `candidate-gates.tsv` 的 `execution_audit=NO` 不能直接解释为发生了禁止操作：

- `logs/xtrace-forbidden-patterns.log` 只有一行，命中的是审计器自己的 `grep -nE` 命令；禁止模式作为 grep 参数出现在 xtrace 中，导致自匹配。
- 排除该审计器命令后，4594 行 xtrace 中没有实际的根级 `/tmp` 写入、`git apply --recount`、fetch、sudo、fio、mount、Redis、Ceph 或 TiKV 操作。
- 因此该 NO 属于审计器实现误报，可对“没有真实禁止命令”给予审计豁免。

但仍有一个真实文档缺口不能抹去：`meta/dependency-adaptations.txt` 为 0 字节，而原报告列出了 GOPROXY、下载重试和 runner 修复等五项适配。这违反了任务书“无适配写 NONE、有适配逐项记录”的要求。它不推翻已经落盘的源码测试结果，但 C03 必须把 pre-run 与 runtime adaptations 分开并强制非空。

### 正式裁定

~~~text
C02-REVIEWED = PASS-B-COMMUNITY-CANDIDATE / AUDIT WAIVER
~~~

该裁定的含义是：

- B 已通过进入社区补丁整理与更完整本地 CI 的技术门；
- A 因确定性的同步阻塞与额外 `NewSlice` 行为退出社区首选；
- 审计器自匹配不阻断技术推进，但空 adaptations 文件作为保留缺口进入下一任务；
- 这还不是 `PR-ready`：C02 没有重新 fetch 执行时最新 main，也没有社区风格最终测试、干净 replay、完整 `pkg/vfs`、tidy/vet/lint/build、社区查重或草稿脱敏证据。

下一步进入 C03：先判断最新上游 main 是否仍受影响；若仍存在，再在最新 commit 上完成 B 的最终两文件 patch、C02 全语义回归和社区提交前本地质量门。
