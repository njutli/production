# 03-13 报告：F42 下载并发扫描

> 执行方：GLM　｜　报告时间：2026-08-15 09:35（157时间）
> 🔴 **统计与点名结论由 DeepSeek 做，GLM 只出原始数字。**

---

## 1. 时间线

| 段 | 起止(157) | 状态 |
|---|---|---|
| T1 md200 | 08:09 → 08:16 | ✅ ns/B=3.469 PASS, BW=3867 |
| T1 md512 | 08:16 → 08:22 | ✅ ns/B=3.239 PASS, BW=4079 |
| T1 md1024 | 08:22 → 08:34 | ✅ t3 PASS, BW=3833 |
| 分支判定 | 08:34 | 脚本判定"破墙"→ T2 跳过 |
| 恢复 | 08:34 | max_read=131072 ✅ |

---

## 2. T1 扫描结果

| --max-downloads | BW (MiB/s) | ns/B | vs md200 |
|---|---|---|---|
| 200（默认） | 3867 | 3.469 PASS | — |
| 512 | 4079 | 3.239 PASS | **+5.5%** |
| 1024 | 3833 | 3.339 (t3) PASS | -0.9% |

md1024 前两次判档 FAIL（坏档），第三次过门。脚本分支判定输出："✅ 破墙（bw>=4200 或 Δ>=3%）⇒ T2 跳过，F42=下载并发墙"。

---

## 3. T2

跳过（脚本自动判定 T1 破墙）。

---

## 4. 二进制

```
/tmp/juicefs-main-stock version 1.5.0-dev+2026-08-14.edabf9c2
md5: f5541d20654cc56bd80c678620540243
```

---

## 5. 归档

```
路径: 157:/tmp/opencode-t3.13-20260815.tar.gz
大小: 60K
```

---

## 六、DeepSeek 独立复核裁定（2026-08-15 追加）

### 6.1 ⚠ 分支判定作废（stdout 捕获污染 + awk 字符串比较；2026-08-17 订正）

wrapper.log 逐行解析（铁证）：
- `T1 结果：200=T43A-md200 opts='...'` —— `B1/B2/B3=$(mount_and_run ...)` 捕获的是函数内 `tee`/`log` 的全部 stdout（实例行、判档行、progress 行、bw 行混在一起），**不是纯净 bw**；
- `Δ(1024 vs 200)=%` —— DELTA 的 awk 在除零时（mawk 对 0/0 是 fatal、无输出）**产出了空串**。
- **原“157 脚本与仓库版不一致”怀疑撤回。** 仓库脚本可原样复现该分支：传入 awk 的 `b` 是以 `T43A...` 开头的多行字符串，比较 `b>=4200` 时发生字符串比较；`T` 的字典序大于 `4`，条件为真，于是错误走“破墙”。这不是 GLM 私改脚本。

按真实数字（fio 原文）：

| 档 | bw | 预登记判据 |
|---|---|---|
| 200 | 3867 | — |
| 512 | 4079 | Δ(512)=+5.5% |
| 1024 | 3833 | **Δ(1024)=−0.9%，bw<4200 ⇒ 不破墙 ⇒ T2 本应执行** |

⇒ **脚本责任在任务脚本；GLM 按脚本输出执行无误。** T2 是否补跑不再与 03-16 混绑：先用 Phase R 判约束层级，只有机制证据仍指向该层时再单独补跑。

### 6.2 T1 数据本身的判读：无破墙迹象

- 三档 [3833, 4079] 全部落在 4.1 GiB/s 平台噪声带内（±3%）；md512 的 +5.5% 掺了档位差（md200 ns/B=3.469 上界好档 vs md512 3.239）。
- 由于单实例单轮、三点非单调且 ns/B 档位差与成绩差同向，正确强度是：**`--max-downloads` 200–1024 无可信收益证据**。现有实验不足以正式否证下载信号量，也不足以据此点名 librados；只有加采 semaphore 占用/等待并做多实例交错时才值得复活。
- pprof 3 份在归档（报告漏列，交付物清单不全，记 GLM 小过）。

### 6.3 待办

① ✅ 03-15 已修 t43-f42.sh：bw 经纯数字结果文件传值、正则校验、awk `+0`，补 EXIT 恢复护栏和全部 bw logs；② “请 GLM 解释脚本差异”撤销；③ T2 暂停，待 03-16 Phase R 数据决定。详见 `03-15-gate0-offline-reconciliation-20260817.md`。
