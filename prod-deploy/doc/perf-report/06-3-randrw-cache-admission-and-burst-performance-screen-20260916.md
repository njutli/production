# 06-3：randrw新写块缓存与180秒突发收益筛选

> 状态：`SIX_CELL_RUN_COMPLETE / NUMERIC_VALID / CAUSAL_RESOLUTION_INSUFFICIENT / RAW_LOCAL_ARCHIVED_AND_RECOMPUTED`。
> 六格负载已执行；正式效应不可判，不能据本轮选择生产配置。§一至§七保留准备过程追溯，最新结论见§八。

## 一、本次问题和范围

固定256K、128 jobs、QD128、128×1GiB既有文件、180秒randrw，筛选：

| 配置 | 读缓存额度 | cache-large-write | writeback |
|---|---:|---|---|
| C | 0 | 关 | 关 |
| S | 96GiB | 开 | 关 |
| W | 96GiB | 开 | 开 |

六格顺序`C1/S1/W1/W2/S2/C2`。S/C回答缓存配置包收益，W/S回答该基座上WB增量。
96GiB不是客户端总内存或staging容量上限。宿主页缓存供给允许计入整机缓存收益，
但不能把命中高、磁盘读近零的结果称为NVMe介质读性能。

主值是完整timed-I/O字节除实际完成时长，R/W分别报告；前段加速、后段回压和完成尾部均保留。
预热与事后排空不计入主值，分别记录。末窗下降不直接否定有限突发收益。

## 二、06-1历史证据按新目标复算

原始来源：`/mnt/c/SunRise/test/06-1/20260915-204941/phase-a-raw/06-1-raw.tar.gz`，
SHA256：`ce8ec6c203d4bae4d8bf39cea38d1af64d7e67fe2a9001d6fa8b2af47383357c`。
成员为`opencode-06-1-20260915-204941/cells/<格>/formal/fio.json`。

计算式：`T=max(read.runtime,write.runtime)/1000`；`BW=io_bytes/T/2^20`。
每格JSON仅一个group汇总，不能再乘128；`job_runtime`是累计job时长，不能作为分母。

| 格 | 配置 | 实际时长(s) | READ MiB/s | WRITE MiB/s |
|---|---|---:|---:|---:|
| C1 | 无缓存 | 180.024 | 1804.72 | 1808.10 |
| T1 | 96GiB+WB | 180.430 | 2157.69 | 2161.70 |
| T2 | 96GiB+WB | 180.124 | 2080.46 | 2083.52 |
| C2 | 无缓存 | 180.341 | 1676.88 | 1680.62 |
| C均值 | 无缓存 | — | 1740.80 | 1744.36 |
| T均值 | 96GiB+WB | — | 2119.07 | 2122.61 |

处理组均值/对照组均值减1：读**+21.73%**，写**+21.68%**。
这是计入后段下降后的全程观测差，不只是开头峰值；但不是本轮预注册结果，也不是生产收益承诺。

历史T1/T2的Dirty起点约44.9/75.2GiB、峰值112.4/118.5GiB，暂存峰值约71.08/75.30GiB，
原报告排空均为47秒。起点不对称；C2/C1读带宽下降7.08%，T2/T1下降3.58%。
均衡轮序不能保证去除非线性漂移和状态残留，所以不能写成“两次独立确认”。
06-1原`RESOLUTION_INSUFFICIENT / NO_CANDIDATE`裁决保持不变。

历史资源值源自[06-1报告](06-1-randrw-cache-writeback-configuration-screen-20260915.md)；
本次分析器另外从raw输出完整时段诊断，二者窗口不同的量不得混称。
稀疏方向日志/记录缺口只允许输出明确标注的估计或未知，不补零、不剔除低速段、不缩放对齐JSON。

## 三、审计意见处理

- 补充非WB缓存准入的`sync := s != nil`条件和WB默认`BlockSize+1`阈值。
- 补充“完整fio时段”与有方七项脚本randrw三轮的口径一致性；资源/缓存/确认语义仍未对齐。
- 将2700MiB/s列为挑战目标，不作为缓存是否有效的唯一标准；不删除竞品较高首轮。
- 介质归因必须核对命中字节、设备读和内存，但不要求制造真实磁盘读才承认整机缓存收益。
- 不采纳Opus的G-3措辞修改：冻结源码`cached_store.go:601`确实执行`c.Writeback = false`。

## 四、离线实现和签收

**当前最终Gate PASS**：`RUN_ID=20260916-161945`，开跑候选根：
`/mnt/c/SunRise/test/06-3/20260916-161945/`，Gate证据在`offline/`。
完整性清单、脚本副本与SHA、实际自测结果、生成job、未批准合同模板、历史复算均已持久化。
离线准备不授权连接环境或运行六格矩阵。

已实现脚本：

- `scripts/FULLBASELINE/debug/t06-3-randrw-cache-driver.sh`：三臂参数、预热后恢复、精确生命周期。
- `scripts/FULLBASELINE/debug/t06-3-randrw-analyze.py`：全程端点、过程诊断、历史复算及六格筛选。
- `scripts/FULLBASELINE/debug/t06-3-gate0-offline.sh`：无环境fixture、历史独立核算、哈希与持久化。

历史06-1脚本和scrub组件作为冻结依赖复用，不热改。禁止继承自动GC/主动compact入口。

| 签收项目 | 结果及边界 |
|---|---|
| 语法/依赖 | `bash -n/-u -n`、Python AST和冻结依赖SHA通过；本机未安装shellcheck，未声称通过该工具 |
| 分析器 | 35项自测通过，含完整六格合成raw、实际180秒门、单group、慢尾、稀疏日志、错误及暂存上限反例 |
| 驱动mock | C/S/W参数、Dirty中途超门重置30秒计时、三零排空、超时保留、拒绝未排空卸载/删除及空间上限边界通过 |
| 暂存检查 | 仅扫描`<cell>/<卷UUID>/rawstaging`；不遍历整个读缓存；符号链接拒绝、并发删除不误判零的逻辑已复核 |
| 组件接口 | 生成fio job与分析合同逐字段校验通过；P95字段、C/S未注册stage指标的NA语义已对齐 |
| 历史独立复算 | 4格8个R/W值与第二份直接读取JSON的计算完全一致；归档SHA匹配，未复制/展开旧19MB包 |
| 防误执行 | PATH拦截器无触发；缺ACK返回42，旧`phase-a`入口拒绝；没有SSH、sudo、fio、挂载操作 |

代码审核与持久Gate证据由主审和独立只读复核方交叉核对。
离线检查**没有**执行真实fio安全停机、现场健康/容量门、真实挂载/卸载或成功缓存删除；
不能据此声称环境准入已通过。还须由只读inventory确认实际命令/指标和时延、可用资源及批准范围。

历史日志的常规积分估计与JSON差值范围为`-0.7093%～-0.2120%`；小于5%不代表每个分窗精确。
因方向日志稀疏，C1/C2仅第2、3窗，T1仅第2窗满足完整覆盖；T2没有完整可签窗，其他标为UNKNOWN。
代理积分保留供诊断，不补零、不重标定，不影响有完整JSON与时长证据的全程主值。
单独出现`stageFull`/同步PUT回落只登记压力警示，不抹去全程主值；实际错误、空间越线或排空失败仍为硬门。

先前Gate `20260916-155536`、`20260916-155931`及`20260916-161032`已被脚本修订取代，
**不是开跑版本**；只以`20260916-161945/offline`的脚本清单为准。旧离线证据保留修订追溯，
无远端副本需要清理。157只读清点保留在`20260916-161032/inventory/`，属于不同来源证据。

## 五、原执行计划（已由§八完成，留作追溯）

1. 只读inventory已完成；正式负载前仍需重核时间敏感的空间、进程、Ceph与并发负载。
2. 用生成的未批准模板形成精确计划：96GiB读缓存之外另列最坏暂存、业务/文件系统预留和
   停负载余量；明确sudo仅限获批scrub控制及恢复，冻结合同和脚本SHA后申请一次主线授权。
3. 获批后按`C1/S1/W1/W2/S2/C2`连续执行；成功格无需逐格求批，安全门失败停止保留并汇报。
   负载和预热合计24分钟，含恢复及证据审核估计2--3小时，按任务书3小时主线预算控制。

不准备未触发的分支脚本，不启动06-2b，不因离线复算约22%观测增幅而提前宣称新配置有效。

## 六、现场清点前的容量复核及暂停点（历史记录，2026-09-16）

本轮已用既有SSH入口**只读确认**157可连、`/mnt/juicefs/test_dir`和`/mnt/jfs-cache/04tmp3`存在。
进一步的远端清点**未执行**：自动安全审查两次拒绝将主机ID、卷/设备UUID、进程身份与Ceph状态
写到本地持久化目录，理由是本轮未明确批准这些具体字段及目的地。拒绝后未换路径或拆命令绕过。
因此旧RUN身份和资源只能作候选，不能填`APPROVED`合同或运行`phase`。

只读引用的[06-1既有清点](/mnt/c/SunRise/test/06-1/20260915-200000/gate0b/gate.tsv)
记录当时缓存NVMe可用`834.413 GiB`、宿主`MemAvailable≈906 GiB`、6/6 OSD和97/97 PG clean，
且`/mnt/jfs-cache/04tmp3`与`/mnt/beegfs-meta`在同一文件系统；这些值**不是今天的现场值**。
历史06-1 T1首45秒应用写`3130.84 MiB/s≈3.06 GiB/s`，高于旧清点采用的`2.5 GiB/s`上界。
若原驱动按`3.5 GiB/s × 210s`、期间完全不上传来预留暂存，单暂存就要`735 GiB`，再加
96GiB读缓存、系统和业务保留，会超过旧盘的可用容量，不能据此诚实填写批准合同。

驱动已改为可执行的硬门：每格冻结起始可用空间，`read_cache_bytes+worst_backlog_bytes`作为
**总占用上限**实时核对；越线即停fio并保留挂载，未满180秒则该格无效。绝对空闲和内存下限
仍并行生效。分析器独立根据`start-resources.tsv`与`safety.tsv`检查该上限；新增反例及
`20260916-161032/offline`当时整包Gate通过；随后因指标契约修复被取代。
它在资源不足时牺牲该格结果，不牺牲共享盘安全余量。

供现场复核的**待批准预算候选**（仅用历史证据推算，不是执行许可）：

| 项 | 候选值 | 依据 |
|---|---:|---|
| 读缓存 / 暂存占用上限 | 96 / 256 GiB | 06-1暂存观测峰约75 GiB；上限超出历史峰值3倍有余，越线停止 |
| 文件系统 / 同盘业务预留 | 88 / 64 GiB | 88GiB约为旧盘总量10%；业务共盘必须另留64GiB |
| 停止余量 | 72 GiB | 按3.5GiB/s与最多9秒检查＋10秒停负载估算，需至少66.5GiB |
| 每格最低起始可用 | 576 GiB | 上述五项之和；旧834.413GiB只说明历史可行 |
| 轮内绝对可用下限 | 224 GiB | 88＋64＋72；高于旧盘20%空闲约176GiB |
| 最低MemAvailable / fio最长墙钟 | 384 GiB / 210秒 | 保障共置业务，超时停止；均需现场复核 |

这份预算约束物理空间，但不能保证六格都跑满180秒；如果达到上限，应报告实际积压峰与停机
时刻，重新制定容量/速率策略，不得无证据放宽门槛。批准前还须核实指标名、业务进程启动指纹、
 卷UUID/128文件、缓存设备身份及Ceph健康，并审核scrub助手可能执行的精确
`sudo ceph osd set/unset noscrub,nodeep-scrub`操作和状态恢复。

## 七、已获准的157只读inventory及离线修复（2026-09-16）

本节取代§六“未获授权清点”的旧现场状态，但保留§六作决策追溯。用户仅批准采集157
主机/卷/设备UUID、进程PID及启动指纹、Ceph健康、128文件资产hash，并保存到
`/mnt/c/SunRise/test/06-3/20260916-161032/inventory/`；本轮未进行sudo、fio、挂载或集群写入。
权威原始输出为`host-157.stdout`及`host-157.stderr`，采集脚本副本亦在同目录。

| 项 | 只读结果 | 准入含义 |
|---|---|---|
| 目标与工具 | `oneasia-c1-cpu-node10`，machine-id=`4d7dca71dfd042a4aa37266eb5a2b258`；fio 3.28；JuiceFS MD5=`24fae0852051c80ca571cb2f20275d46` | 与候选脚本要求相符；开跑前重核 |
| 卷与资产 | `juicefs-prod` UUID=`e1b69ea9-0e3d-427d-bea9-8765928afa66`，BlockSize=256K；128个1GiB文件、128个不同inode、SHA256=`655faef11c4cf378097db497ac66fe5dbe5dddd725653619a762d18f69c08a44` | 不重新layout；原资产是本轮覆盖写对象 |
| 缓存介质 | `/dev/nvme1n1` ext4 UUID=`1b691709-e347-452c-96c4-5f37052bc203`，挂载`/mnt/jfs-cache`；候选目录为空、属主1002:1002、模式0700 | 与`/mnt/beegfs-meta`共用一块设备；必须保留同盘业务余量 |
| 资源 | 缓存盘可用895944495104 bytes≈834.4GiB；MemAvailable=960587684KiB≈916GiB | 超过候选起始空间576GiB及内存384GiB门，但属于采样时值 |
| 后端 | Ceph FSID=`f8137e5a-8af2-11f1-aa1c-4df480fc234d`、HEALTH_OK、6/6 OSD、97/97 PG active+clean，未预设noscrub | 当前干净；全局scrub开关尚未改变 |
| 已有客户端 | `/mnt/juicefs`两个JuiceFS进程及另两个portal进程的PID/start-ticks已冻结在原始清点；本轮目标挂载/结果目录均不存在 | 这些已有进程不得被测试脚本终止；PID重用须用启动指纹判定 |

从157现有两个JuiceFS指标端口只读查看指标名，`object_request_uploading`、三项
`staging_*`和`staging_block_errors`均注册，**没有JuiceFS pending指标**。旧脚本把
`pending`当必有指标，会让预热恢复/排空在正式运行中必定失败。已修为合同显式
`metrics.pending=UNREGISTERED`、原始TSV写`NA`，恢复与排空只要求已注册指标和私有
rawstaging归零；分析器拒绝把该`NA`冒充实测零。TiKV的pending-compaction不是这个指标。
驱动与分析器自测通过，修订版完整离线Gate在
`/mnt/c/SunRise/test/06-3/20260916-161945/offline/`通过：`remote_calls=0`、
`formal_load=NOT_AUTHORIZED`。该Gate取代§四、§六提及的旧脚本版本。

**当时的暂停点（后来已解除）**：正式执行尚未授权；如批准，须在157开跑前重核可用空间、缓存父目录为空、
Ceph 6/6/97 clean、受保护进程指纹及无并发基准测试；然后冻结经填充的`APPROVED`合同与脚本SHA。
主线仅覆盖既有128文件，顺序`C1/S1/W1/W2/S2/C2`；每格60秒读预热、180秒randrw，
共约24分钟纯fio，加恢复/排空/验证预计2–3小时。可能的环境写入限于本RUN私有结果、
私有挂载及缓存子目录、fio覆盖上述128文件；全局变更只有获准后临时设置/恢复Ceph
`noscrub`及`nodeep-scrub`。失败时停负载，排空未证实则保留挂载，不能强行卸载或删除。
它不会触碰有方环境，也不停止已有`/mnt/juicefs`或portal进程。因缓存盘与
`/mnt/beegfs-meta`共盘，**必须确认无其他业务/测试依赖该盘的空闲带宽并接受约2–3小时的
资源竞争窗口**，仅凭空闲容量不能推断对业务无影响。

## 八、正式六格执行与审计结论（2026-09-16）

用户批准§七所列正式负载及临时scrub控制后，在157执行
`RUN_ID=20260916-161945`。远端权威原始目录：
`/tmp/production/opencode-06-3-20260916-161945/`；冻结脚本、批准合同、
`launcher.exit=0`及分析器输出在
`/tmp/production/opencode-06-3-prep-20260916-161945/`。远端原始归档
`raw.tar.gz` SHA256=`6e6ee71fb57d78847522d2c80cf0a6f834f2eae863a1c065ea6da43788f9699f`。
第一次复制尝试因缺少对**此具体敏感载荷及目标路径**的明确跨主机保存授权被安全审查拒绝；
拒绝后没有改用其他路径或间接方式复制。用户随后明确批准从该远端路径复制到
`/mnt/c/SunRise/test/06-3/20260916-161945/raw.tar.gz`，传输完成且本地SHA256与远端一致。
本地复算和证据状态见§8.4。

### 8.1 六格全程结果

以下由每格fio单group的读/写字节除以两方向较长实际runtime独立重算，与157分析器一致；
所有fio error=0。`T`含真实完成拖尾，不固定除180秒。每格预热后恢复、排空、远端读回、
卸载和私有缓存清理均PASS；`PHASE_PASS`、Ceph scrub状态恢复及`launcher.exit=0`已核对。

| 格 | 配置 | 实际T (s) | 读 MiB/s | 写 MiB/s | 起点TiKV pending-compaction合计 GiB |
|---|---|---:|---:|---:|---:|
| C1 | 无缓存 | 194.031 | 638.77 | 639.79 | 0.00 |
| S1 | 96GiB + CLW，无WB | 189.939 | 616.22 | 617.81 | 21.07 |
| W1 | S + WB | 200.017 | 513.68 | 516.39 | 19.34 |
| W2 | S + WB | 195.778 | 516.07 | 517.71 | 14.18 |
| S2 | 96GiB + CLW，无WB | 189.131 | 526.47 | 528.22 | 34.70 |
| C2 | 无缓存 | 194.509 | 488.15 | 488.61 | 26.65 |

分析器认为六格**fio数字与身份/资源/健康证据有效**（`RUN_VALIDITY=VALID`、`errors=[]`），
但C1→C2读/写分别下降`23.58%/23.63%`，S1→S2下降`14.57%/14.50%`。
按预注册自测噪声底，S/C的读/写材料线约`47.16%/47.26%`，W/S约`29.13%/29.00%`。
S/C两次配对读分别为`-3.53%/+7.85%`，写为`-3.44%/+8.11%`；W/S两次配对读
`-16.64%/-1.98%`，写`-16.42%/-1.99%`。比值的方向及幅度均不足以越过材料线。
机械筛选为三项比较全部`SCREEN_STOP_NO_CLEAR_REPEATABLE_BENEFIT`，**不是缓存或WB本身被证伪**。
六格均未达到历史有方`2700 MiB/s`挑战数值，但不能把不同后端状态下的绝对带宽直接归因于缓存配置。

### 8.2 关键混杂与可解释范围

- 06-1同一`juicefs-data` pool起点约`1,995,639`对象、`523.1 GB` stored；本RUN C1前
  已约`5,257,621`对象、`1,378.4 GB` stored，约为旧起点`2.63`倍。版本/命令差之外，
  后端长期状态显著不同，故本RUN约`0.5–0.6 GiB/s`不能与06-1约`1.7–2.1 GiB/s`
  直接相减解释为参数退化。
- C1前三节点TiKV pending-compaction均为0；C1结束后升至约`21.07 GiB`，后续格开跑时
  分别为`21.07/19.34/14.18/34.70/26.65 GiB`，未回到同一后端起点。
  pool对象数在格间也并非单调（W2后约605万，S2前约589万），说明后台状态持续变化。
  这些观测与本RUN的大幅位置漂移一致，但**不单独证明**compaction是唯一原因。
  任务书要求状态不可比时停止做因果宣称；本RUN只保留真实点值和安全/生命周期证据。
- fio方向日志虽然各有128份，但缺口多、与JSON积分差超过5%；分析器将各45秒窗标为
  `UNKNOWN`且诊断`REVIEW`。不能用这些日志宣称“哪一窗下降”或还原瞬时速率；
  它不改变fio JSON完整字节和实际完成时长的主值有效性。
- W1/W2的rawstaging指标峰仅约`12.8/16.0 MB`，排空均约2秒，远小于06-1
  约71–75GiB和47秒；这说明**本RUN没有复现06-1的高强度缓存积压场景**，
  不足以反证有限突发缓存收益。

### 8.3 环境收口与后续

测试后无本RUN的FUSE挂载或缓存子目录；`/mnt/juicefs`和Weka `/mnt/data04`挂载仍在。
这里仅确认服务与挂载未中断，**没有测量Weka业务时延或吞吐，不能声称业务性能完全未受影响**。
scrub助手的状态文件确认其拥有的`noscrub`/`nodeep-scrub`均已撤销；Ceph为
`HEALTH_OK`、6/6 OSD、97/97 PG active+clean。测试后几分钟三节点TiKV
pending-compaction重新回到0。没有执行drop_caches、主动GC/compact或改动有方环境。

本轮结论：**六格安全闭环，性能数值可用，但缓存配置效应因起点不等价和高漂移而不可判；
不升级任何生产候选，也不立即追加四格条件分支。** 若仍需回答06-3主问题，先明确一种
对后端状态可执行且可重复的归一方案，再决定是否重测；不能只挑本轮较好的格。
原始证据已在获准后持久归档并完成本地复算，见§8.4。

### 8.4 本地原始归档与独立复算（授权后完成）

权威持久目录：`/mnt/c/SunRise/test/06-3/20260916-161945/`。其中：

| 文件 | SHA256 | 作用 |
|---|---|---|
| `raw.tar.gz` | `6e6ee71fb57d78847522d2c80cf0a6f834f2eae863a1c065ea6da43788f9699f` | 157私有原始目录，6044条目，24MB压缩包；本地/远端哈希一致 |
| `analysis.json` | `ed84d46a7ede552a477be6deb973341b2a88245c78ee9ece720b8d544d71db1d` | 本地冻结分析器从归档解包后重算；`RUN_VALIDITY=VALID`、`errors=[]` |
| `analysis.tsv` | `e5c475bfc45fa80df01a5845fcdf03fce85933f74d945bd8078ba9f8ab763633` | 六格主值；与157分析器生成的表逐字节相同 |

归档成员全部位于唯一`opencode-06-3-20260916-161945/`前缀下，无越界路径、符号链接或
特殊文件。本地重新运行冻结分析器的主值、比较效应、材料线和结论与157一致。两端完整
`analysis.json`字节哈希不同，仅因日志诊断中对大量浮点区间求和的迭代顺序导致约`1e-12`
量级舍入；所有格的`primary`、`lifecycle`、`mechanism`、有效性与全部`comparisons`
逐字段哈希一致，`analysis.tsv`逐字节一致。日志本来就标`REVIEW/UNKNOWN`，这点差异
不改变任何签收值或因果裁决。157原始目录及归档暂予保留，远端清理须另按生命周期规范处理。
