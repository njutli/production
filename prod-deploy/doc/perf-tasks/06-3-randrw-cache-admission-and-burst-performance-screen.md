# 06-3：randrw新写块缓存与180秒突发收益筛选

> 日期：2026-09-16；面向 GPT 负责设计/审核、执行方交原始证据。
>
> 历史状态：`SIX_CELL_RUN_COMPLETE / EFFECT_INCONCLUSIVE / RAW_LOCAL_ARCHIVED`。旧六轮已结束，裁决与证据不变。
> **2026-09-21 §七已完成：`CACHE-BURST-21P7 = BATCH_COMPLETE / SCREEN_STOP_NO_CLEAR_REPEATABLE_BENEFIT / PAUSED_INCONCLUSIVE`。**
> **后续分工**：已完成的是§七首批，不是收益来源归因。06待补工作按[06计划§十一](../perf-analysis/06-randrw-cache-and-write-path-tuning-plan.md)执行规划：先离线补证及恢复评估，具备可比条件并另获授权后才复验。buffer等新优化转入[07计划](../perf-analysis/07-randrw-cache-admission-and-write-path-optimization-plan.md)，暂不开展；本次修订不构成任何新负载或恢复授权。
> 本周主线一：确认原96GiB缓存＋writeback约21.7%全程观察增幅能否复现、收益来自哪些条件；不是原样重跑六轮。
> 旧Gate `/mnt/c/SunRise/test/06-3/20260916-161945/offline/`及旧inventory只覆盖历史RUN，不构成本次准备已完成或环境执行授权。
>
> 承接：06-1、04-tmp2j/04-tmp2i、05-1b/05-2及06-2冻结源码；不依赖06-2b性能结果。
>
> 规范：`TASK-BOOK-AUTHORING-GUIDE.md`、`TEST-DATA-LIFECYCLE-POLICY.md`，
> `skills/SYSTEM-SAFETY-SKILL.md`、`skills/EVIDENCE-INTEGRITY-SKILL.md`。
>
> 用户目标：允许缓存吸收有限时长突发，**完整测试期间平均R/W可重复提高即有价值**；
> 不要求后段平台不下降，排空时间单列，不强制摊入前台带宽。

```text
04-tmp2j  纯读缓存曲线已完成，不能当作所有缓存策略已测完
06-1      完整时段复算已有约21.7%观察增幅，仍未确认因果
  ↓
06-3      旧六轮因漂移不可判；你在这里：§七最小复现与来源确认
          ├─ 起点不可比：短诊断/另批精确恢复，不盲目重跑
          ├─ 原组合有收益：保留候选，优先拆writeback增量
          └─ 未复现且差异未解释：保留未解决项，不无限补样
06-2b     修正版已完成正确性与性能筛选，无交付候选
06-4      direct=0筛选已结束，不替代本任务
07        buffer/新缓存策略/新源码方向仅作规划，暂不开展
```

一句话：**用现有交付二进制筛选可复现的180秒缓存加速配置，分别报告前台收益、恢复代价和距有方历史数值目标的差距。**

## 〇、最小决策与预算

以下§〇～§六保留旧六轮合同供追溯；新增批次的矩阵、预算和完成条件以§七为准，禁止直接调用旧`phase`代跑。

```text
EVIDENCE_LEVEL=L0_OFFLINE -> L1_SCREEN
SCREEN_SOURCE=06-1归档全程复算；冻结v1.4.1源码CacheLargeWrite/WB分支；04-tmp2j
MINIMUM_DECISION_SET=既有证据重算+六格 C1,S1,W1,W2,S2,C2
SCREEN_CONTINUE=重复R/W同向且超过本RUN材料线；资源/错误/恢复闭环；进入下一决策不自动交付
SCREEN_STOP=主问题已回答/无清晰收益/状态不可比；不得因后段下降或排空非零直接否决
FORMAL_MATRIX=本任务不自动升级L2；最终候选的部署范围/长期验证另行审批
ESTIMATED_WALL_CLOCK=离线准备1--2h；六格负载含预热24min，连恢复/归档约2--3h
MAX_PREP_BUDGET=2h；优先复用，不写通用矩阵平台
MAX_EXECUTION_BUDGET=主线3h；至多一个获批四格分支增加1--2h；安全恢复不受预算截断
STOP_AFTER_ANSWER=无分支触发则主线复核后结束；有分支也最多新增四格
EVIDENCE_ROOT=/mnt/c/SunRise/test/06-3/<RUN_ID>
REMOTE_RESULT_ROOT=/tmp/production/opencode-06-3-<RUN_ID>
EVIDENCE_RETENTION=SCREEN
REMOTE_CLEANUP=AFTER_REVIEW
LOCAL_COMPACTION=AFTER_STAGE_CLOSE
ENVIRONMENT_ASSET_CLEANUP=仅本RUN私有挂载与缓存子目录；先排空后卸载，精确计划单独授权
```

## 一、背景与唯一主问题

**主问题**：在固定256K randrw与固定缓存预算下，能否获得优于同轮无缓存基线、可重复的完整测试平均R/W？
同时选择S/W中值得后续使用的配置；“超过有方”先作为历史数值目标，不伪装成新的同场竞品结论。

冻结源码 `06-2/20260916-091446/build/juicefs-v1.4.1-b-catchup-source.tar.gz` 说明：

- `cached_store.go:368--370`：无writeback时，默认只缓存小于BlockSize的写块。
  B256卷完整256K写默认不入缓存；`--cache-large-write`可把新写完整块也提交缓存队列。
  入缓存发生在PUT路径中，但不把原有Flush的远端上传完成条件改成本地stage确认。
  前提是`:357`的`sync := s != nil`：非WB上传在`:467`传入`s`，该缓存准入分支才生效；
  WB后台上传在`:448`传入`nil`，不走同一分支。
- `disk_cache.go:783--813`：writeback把stage硬链接到raw，上传后保留作读缓存；
  不是复制两份块。覆盖写使用新object key，不是在旧缓存文件原地修改。
  `cached_store.go:424/607--608`要求块长严格小于writeback阈值；默认阈值为`BlockSize+1`，
  因此B256卷的完整256K块可进入stage，不等于本负载已把WB关掉。
- 因此06-1未验证“无WB + cache-large-write”。其WB条件下无需再靠该flag缓存stage块，
  不等于关闭WB后该flag也无意义。
- 全程平均提高但末窗下降，可以是有效的有限突发收益；须报告数据确认/排空风险和资源需求，
  不能外推成长期持久化带宽。

不再默认扫描容量、U450/U600、upload-delay或内核dirty参数，不改fio IO模型或源码。

## 二、共同测量合同

### 2.1 完整时段主值与过程旁证

按用户新目标，本任务**显式覆盖**GUIDE §二.13及证据skill中“中间稳态窗口为唯一主值、summary仅旁证”的旧默认：
有方七项脚本randrw项也是完整fio时段汇总；采用此口径同时减少历史对比的窗口差异，
但不等于客户端资源、缓存状态与数据确认语义已经对齐。

```text
T = max(fio.json单个group汇总的read.runtime, write.runtime) / 1000
B_read  = read.io_bytes  / T / 2^20
B_write = write.io_bytes / T / 2^20
```

这里runtime=180是计划负载时长，T是实际完成时长；主值包括前段加速、后段回压和完成拖尾。
不能固定除180、取最高窗口、相加R/W验收，或把group汇总再乘128；也不能拿可能为累计job时长
的顶层 `job_runtime` 作墙钟分母。Gate须核实当前fio JSON
确为单group完整汇总；模式变化必须先修分析合同，不临场猜字段。
启动/60秒预热/事后排空不计入T，分别报告。实际I/O起点由完成时刻减实际runtime核对，不能用fork时刻。

并行保存全部128份bw日志，做重叠加权积分及 `[0,180)` 四个45秒窗，180秒以后另列完成尾部。
还输出旧 `[15,175)` 口径仅供解释历史差异；不与全程值混用。
逐秒没有完成不等于采集坏了：区分真实停顿、job结束与日志缺失，不删除低速秒，不将未知秒填零。
日志积分与JSON字节差逐方向报告；绝对差>5%须定位。若仅日志尾部/稀疏记录不能还原，
可保留身份/JSON/起止均闭合的全程值，但相应分窗和机制不得签收；若连全程字节或时长也不可信则该格无效。
5%是触发核查线，不是事后把日志乘系数对齐的许可。

**收益分层**：全程前台均值是主端点；CV、W4/W1、尾窗、峰值Dirty/staging、排空时间是适用范围。
若补充“含排空有效写带宽”，另列 `写字节/(T+排空时间)`，不能替换主端点。
真实I/O完成拖尾也不得因超过原06-2的窄runtime门而自动删样；记录其实际代价。

### 2.2 固定负载与主线六格

沿用06-1原job及seed：既有B256卷128×1GiB `rw_test`，50/50、256K、libaio、128 jobs×QD128、
direct=1、time_based/runtime=180、fallocate=none、allow_file_create=0、randrepeat=1、randseed=20260915。
不layout、不新卷、不缩热集；所有格固定60秒纯读预热，预热不是主性能窗口。
交付件预期MD5 `24fae0852051c80ca571cb2f20275d46`，公共FUSE256K/buffer300/U150/downloads200。

| 配置 | 读缓存预算 | cache-large-write | writeback | 回答什么 |
|---|---:|---|---|---|
| C | 0 | 关 | 关 | 当前无缓存对照 |
| S | 96GiB | 开 | 关 | “读缓存+主动缓存新写块”整包是否有收益 |
| W | 96GiB | 开 | 开 | 相对S，只开启WB是否进一步提高突发平均带宽 |

顺序固定 `C1 → S1 → W1 → W2 → S2 → C2`，每臂平均轮序3.5。
S/W采用同一已批准物理缓存文件系统、free-space-ratio=0.20、upload-delay=0，
stage并发及淘汰策略均保持默认。WB成功stage路径中CLW不再额外作用，仍保留同一flag以免偷偷改其他参数。

**因果范围**：S/C是配置包收益，不归因成cache-large-write单项收益；W/S才是该基座下WB的增量。
不为历史已有纯读缓存曲线默认再加一臂。若确需隔离CLW，走下节受限分支。
96GiB是固定读缓存预算，不是保证100%命中，也不是整个客户端缓存/内存/staging的总预算。

### 2.3 最多一个条件分支（主线完成后审核选择，不能全部扫）

| 分支 | 必须已有的触发证据 | 两臂四格ABBA，其他变量不变 |
|---|---|---|
| Q：CLW单项归因 | S有清晰配置包收益，且需要知道CLW是否必要 | 96GiB纯读缓存CLW关 vs 同配置CLW开；WB均关 |
| P：减少GET回填写盘 | W不优于S；对象GET及blockcache写入/本地写延迟显示值得核对回填成本 | W原配置 vs W仅加cache-partial-only |
| M：内存读缓存诊断 | S未提高、缓存drops/本地写压明显，无法区分缓存准入效果与NVMe成本 | S原配置 vs 同96GiB/CLW开/WB关，cache-dir=memory |

选择必须在新增负载前记录依据，主线已足够回答则全跳过；同时符合多个触发时仍只选一个，
其余记待办，不自动扩批；06-4已独立用于缓冲I/O，不是本表分支。P可能降低命中率，不预设净收益；它不禁止WB stage硬链接入raw。
M需独立核准RAM预算与OOM/业务余量，不用tmpfs/loop，不开启WB，不把内存缓存说成NVMe介质性能。
memory会禁用WB、不支持LRU；cache-size约束缓存数据，不是进程RSS硬上限。
冻结源码`cached_store.go:601`确有`c.Writeback = false`，不是仅告警；仍须显式关闭WB，
不能把M分支当成“内存writeback”测试。
LRU、max-stage-write调整和多目录并行不进入本轮，避免扩大变量。

## 三、起点恢复、安全与最小证据

1. **先冻结实际计划**：只读核实UUID/META、128文件名/inode/大小、二进制、fio版本、Ceph
   FSID/health、缓存路径设备/业务用途/空间、内存余量。默认缓存候选仅
   `/mnt/jfs-cache/04tmp3/<本RUN子目录>`，历史许可不是本轮自动授权。
2. **缓存起点对称**：S/W每格独立空目录，同一60秒预热。预热本身也会回填写出Dirty，
   所以恢复门必须位于“预热之后、正式fio之前”，不能只检查挂载前。
   已注册的staging指标、uploading及私有rawstaging归零；此版JuiceFS未注册pending指标，
   在原始记录中标`NA`，不得伪写为实测0；TiKV pending-compaction是另一指标。
   宿主Dirty不超过安静期P95+8GiB、Writeback不超过P95+1GiB，
   持续30秒；保存缓存占用/命中计数和从预热结束到开跑的时长。基线由首次负载前120秒安静观测冻结，
   不用宿主全局Dirty=0作不现实门；无法区分业务脏页或15分钟仍不满足则停止并评审。
3. **不要把缓存吸收清理掉**：上述约束只控制起点，不要求正式窗Dirty/staging不增长，
   不要求本地设备必须发生大量读。页缓存供给是允许的整机缓存资源，但须说明资源来源。
   报告须并列同窗blockcache命中字节、缓存设备读字节和宿主内存；命中高而设备读近零时，
   改述为“本地缓存路径主要由宿主页缓存供给”，不得声称验证了NVMe介质读性能或容量曲线。
   这限制介质归因，不取消真实的整机缓存带宽收益；设备统计与命中字节不强制逐字节相等。
   不drop_caches、不全局sync、不改内核dirty比例，不以cache=0删除其他业务缓存。
4. **后端状态**：每格记录对象数、TiKV延迟/compaction、OSD compact三指标及健康。
   默认被动恢复，不轮间relayout，不自动共享卷GC/主动OSD compact。若不能维持可比起点，
   暂停并单独审批一次精确恢复计划；不把GC后对象数相同等同于TiKV历史状态相同。
5. **空间/业务保护**：stage不计入96GiB预算，须在开跑计划中单列**批准的最大暂存占用**、读缓存、
   文件系统和业务预留、监控间隔及停负载安全余量。每格记录起始可用空间，轮内每次监测若
   可用空间下降超过“读缓存+批准暂存上限”或触及绝对可用空间/内存下限，即停该格并保留挂载；
   达上限而未满180秒只登记安全停止，不能当有效性能结果。这个动态上限不是自然负载所需容量的预测，
   不得依赖历史PUT率保证排空。停止余量需覆盖有界健康查询和停fio反应期间的可能写入。
   scrub仅在本phase内经授权暂停，保存原flags并精确恢复；禁止与05-3/06-2b等负载同时运行。
6. **结束顺序**：保持挂载上传→连续三次验证已注册staging/uploading与私有rawstaging为零
   （未注册pending保留`NA`）→优雅卸载→无缓存独立
   私有挂载抽样读回→卸载→精确清理本RUN缓存。记录排空秒数和可再次突发的恢复间隔；
   抽样可读不证明fio随机数据内容正确，不能称为完整一致性校验。

核心raw：fio JSON/全文/128日志/实际起止/rc、挂载身份与配置、完整命令、健康、df/内存、
rawstaging终态和恢复审计。机制采样复用06-1：hit/miss字节、cache writes/drops/evicts、
GET/PUT次数和字节、uploading、stage errors、iostat、Dirty/Writeback、NIC/CPU。
使用既有约1秒轻量采集；不递归每秒du、不默认trace/逐请求日志；指标名从实际prom核对。
机制采集缺失限制归因，不等于完整fio字节一定无效；容量、错误、身份等安全核心门不可降级。

## 四、判定与竞品目标

分别计算S/C、W/C和W/S的两组位置配对效应：同序号相除减1，逐方向列出，不把不同phase拼接。
每项比较的 `ε` 取两臂各自第二次/第一次变化绝对值的较大者，`M=max(5%,2ε)`。
两次READ/WRITE均为正且不小于M才记对应 `SCREEN_CONTINUE`；不足则记方向不明或分辨率不足，
不宣称等价/普遍无效。ε≥5%须保留细效应分辨率不足标签；明显大于2ε仍可作为升级筛选信号，
但六格L1本身不签生产普适收益或精确置信区间。CV/末窗/排空非零不作否决条件。

必须区分：**证据有效、180秒突发有收益、优于另一缓存配置、达到历史数值目标、可生产交付**，
这五项不能用一个PASS代替。若仅首段好看而全程未过线，不能称本目标完成；若全程过线且后段衰减，
应如实登记“有限突发收益”，不能再用非稳态理由抹去。

有方七项脚本randrw项三轮、256K历史R/W均值约2338/2339MiB/s，最高观测2663/2665MiB/s；本轮以两方向各
`2700 MiB/s`为挑战目标，不作为缓存是否有效的唯一判据；全部轮次照报，不挑低的竞品锚。
三轮不足以把竞品首轮认定为离群值，更不能据此证明该轮内不稳定。另列05阶段sweep约1768--1873MiB/s
及其漂移/窗口差异；不能与当前全程数混成同场效应。超过2700只叫“超过历史观测数值”，
严格竞品胜出仍需有方环境可用后对齐时长、缓存、资源及确认语义。1M/4M的约14--15kMiB/s
目标不在本轮，不以256K过线宣称所有BS胜出。

### 4.1 已有06-1数据的回顾性线索（不是本轮结果）

按单group的完整字节/实际runtime独立复算：

| 格 | 配置 | 实际时长(s) | READ MiB/s | WRITE MiB/s |
|---|---|---:|---:|---:|
| C1 | 无缓存 | 180.024 | 1804.72 | 1808.10 |
| T1 | 96GiB+WB | 180.430 | 2157.69 | 2161.70 |
| T2 | 96GiB+WB | 180.124 | 2080.46 | 2083.52 |
| C2 | 无缓存 | 180.341 | 1676.88 | 1680.62 |

处理组均值/对照组均值减1为READ **+21.73%**、WRITE **+21.68%**。这是全程观测收益，
不只是开头峰值；但T1/T2的Dirty起点约44.9/75.2GiB，C2/C1与T2/T1读带宽分别下降7.08%/3.58%。
均衡轮序只有在共同线性漂移等条件成立时才有抵消作用，不能清除非线性/状态残留；不能写成两次独立确认。
新任务以预热后恢复门减少这项混杂，旧06-1裁决及原始证据保持不变。

## 五、执行步骤与复用

- [x] **步骤0（本轮离线）**：通读规范、`TESTING-GUIDE.md` §1.3/2.2/3、
  `test-commands-reference.md` §8/9、`baseline-reproduction-skill.md` §2/3及长跑监控规范；
  确认本任务是有限突发，不照搬稳态否决或主动GC/compact模板。
- [x] **离线A：先用已有数据回答能回答的问题**。校验06-1归档SHA
  `ce8ec6c203d4bae4d8bf39cea38d1af64d7e67fe2a9001d6fa8b2af47383357c`，
  四格全程复算，列字节、实际时长、R/W、四窗、脏页/暂存峰值及排空。
  只在06-3记录新目标下的回顾性读法，不修改06-1原裁决、不把它追认为新合同预注册结果。
- [x] **离线B：复用与Gate 0**。复用 `t06-1-randrw-cache-driver.sh` 的采集、固定job、
  挂载身份、排空/读回和scrub组件；把原“T臂=cache+WB”的硬绑定拆为配置表。
  分析器复用重采样、增加全程字节端点，不热改历史冻结脚本、不重写通用平台。
  Gate只覆盖新增配置分支、group统计/长尾/稀疏日志、预热后恢复、排空与精确清理、明文凭据扫描。
- [x] **停点1**：离线通过后只读inventory，提交精确负载/缓存/空间/内存/sudo写全集、
  scrub与失败恢复计划；用户授权后六格连续运行，不逐格回传求批。
- [x] **停点2**：本地独立复算后结束主线；无清晰、可重复收益，不触发§二.3条件分支。
- [ ] **末步**：合规自查、恢复所拥有的环境状态、一次性证据签收与生命周期收口，报告已有结果即止。

### 5.1 已实现入口与开跑边界

脚本均在 `scripts/FULLBASELINE/debug/`，历史06-1脚本原样保留作为SHA冻结依赖：

| 文件 | 用途 |
|---|---|
| `t06-3-randrw-cache-driver.sh` | `plan`、`--self-test`均离线；`phase`才是经单独批准的六格执行入口 |
| `t06-3-randrw-analyze.py` | `self-test`；`replay-archive`复算历史；`analyze --root`审核新六格raw |
| `t06-3-gate0-offline.sh` | 拦截在线命令，校验生成的job与分析合同、自测、历史独立重算并冻结脚本 |

离线Gate命令（必须使用新的RUN_ID，不能覆盖已签收证据）：

```bash
bash prod-deploy/scripts/FULLBASELINE/debug/t06-3-gate0-offline.sh \
  <RUN_ID> /mnt/c/SunRise/test/06-3/<RUN_ID>/offline
```

生成的 `plan/gate0/contract-template.NOT-APPROVED.json` **不是环境许可**。
现场inventory后须填入机器/卷/文件/缓存设备身份、共置业务PID、实际指标名、容量和内存保护线，
审阅后才冻结批准合同及脚本SHA，并提供本RUN专属执行、缓存路径、scrub ACK。
主线只接受 `phase`；旧 `phase-a`、自动GC及条件分支均不可调用。

此入口仅指历史六轮实现；§七必须先做最小适配和离线Gate，不能借旧许可运行新矩阵。

容量预算不借用历史PUT速度：单列读缓存96GiB、**必须主动执行的暂存占用上限**、
文件系统/业务预留及停止余量；不足则先评审，不能自动缩小预留或加长负载。
安全检查含有界健康查询，df/内存采样实际间隔约1--9秒（不是严格1Hz）；停止余量须覆盖
最坏查询间隔和精确停止fio的时间。普通机制采集仍复用原约1秒采样。
只读inventory还需确认这些查询能在约定超时内完成；离线Gate不证明现场时延或权限已满足。

## 六、交付、通用注意事项与红线

报告：`doc/perf-report/06-3-randrw-cache-admission-and-burst-performance-screen-<日期>.md`。
一张表列C/S/W完整R/W、相对值、末窗、命中率、峰值staging/Dirty、排空/恢复时长和竞品目标差距；
再用一张累计字节或带宽曲线解释“收益发生在哪段”，不把所有诊断各写一份报告。
公共身份/脚本一份，cell仅增量raw；独立复算、commands、incidents和manifest进入唯一持久化根。
按生命周期规范审核后精确清远端副本；结果同步results-table与阶段计划。

通用注意事项引用GUIDE §二.1--23，保留固定资产、MiB/s/RW分报、身份、实际起点、完整日志、
健康/容量、预热对称、同轮对照及漂移、非性能门分离、脚本冻结、独立复核、scrub所有权、
增量持久化和精确清理。本任务统计覆盖项只限§二.1；不引入旧式挂载判档挑样或跨日因果相减。

红线：保护157 Weka/K8s及其他业务；不动既有 `/mnt/juicefs`、md0、网卡/内核，不重启；
不format/layout/destroy共享卷，不改Ceph/PG/TiKV配置，不全局drop_caches。
sudo写逐条确认，不懒/强卸载、不模式kill、不宽域删除；失败保留证据和未排空挂载，优先精确恢复
自己拥有的scrub变化。现场修复仅允许不改变量的工程bug，须离线回归并留incident；性能开跑后
改配置/统计规则必须终止该阶段并重新审批，不补格覆盖旧失败。本文件不授权执行测试。

## 七、本周主线一：21.7%观察收益复现与来源确认（2026-09-21）

**唯一主问题：原96GiB缓存＋writeback组合能否重复提高完整randrw平均带宽，哪些条件足以解释其收益或失效？**
跟踪项`CACHE-BURST-21P7`不得因旧六轮收口、06-2b结束或06-4提高带宽而关闭。本节替代旧六轮的执行入口，不改历史结论。

```text
EVIDENCE_LEVEL=L0_OFFLINE -> L1_SCREEN
MINIMUM_DECISION_SET=起点核对 + A1/B1/B2/A2四轮；复现后另批一次WB关/开四轮
SCREEN_CONTINUE=非性能门通过；两次R/W配对同向且达到§四本批材料线；保留工程候选，不自动交付
SCREEN_STOP=安全/证据失败、起点不可比、方向或分辨率不足；不追加好看样本
FORMAL_MATRIX=需要精确归因或交付时另批L2，不在本次预建
MAX_PREP_BUDGET=2h；复用驱动/分析器，只改必要入口，不搭新框架
MAX_EXECUTION_BUDGET=首批2h；每个后续归因批次另批1--2h；安全排空不因预算中断
ESTIMATED_WALL_CLOCK=首批四轮含预热16min纯负载，连恢复/审核目标2h，主动恢复异常另报
STOP_AFTER_ANSWER=够判断候选与下一步即停，不默认容量全扫描或七项回归
EVIDENCE_ROOT=/mnt/c/SunRise/test/06-3/<新RUN_ID>/
REMOTE_RESULT_ROOT=/tmp/production/opencode-06-3-<新RUN_ID>
EVIDENCE_RETENTION=SCREEN
REMOTE_CLEANUP=AFTER_REVIEW
LOCAL_COMPACTION=AFTER_STAGE_CLOSE
ENVIRONMENT_ASSET_CLEANUP=仅本批私有挂载/缓存，排空后按精确批准清单恢复；固定资产保留
```

### 7.1 离线差异核对：不重复下载、不改旧证据

复用已有原始命令、fio和采样，形成一张配置/状态差异表，并补齐06-3报告遗漏的缓存准入丢弃、淘汰、实际设备读写及主值统计说明。

| 历史证据 | 本次必须保留的边界 |
|---|---|
| 04-tmp2h | 已测128GiB总空间×75%普通缓存预算、接近96GiB；但原矩阵受采样器干扰，不能作为可靠负效应 |
| 04-tmp2i | 128GiB loop总空间、普通缓存预算约实际可用空间25%，WB组合约−16.88%；不是所有预算/路径的否定 |
| 06-1 | 原生NVMe/ext4目录、96GiB预算、WB开、CLW关；全程读+21.73%/写+21.68%，起始Dirty不对称 |
| 06-3旧六轮 | 96GiB并引入CLW，无缓存首尾约−23.6%；不能跨RUN相减得配置因果 |

核对二进制、完整挂载、job/seed、预热时长、GC/compact策略、总可用空间及Dirty/后端起点，缺失记未知。
`cache-size`是普通块缓存管理目标，不是独立只读分区，也不限制全部staging或宿主内存。
准入丢弃/淘汰是已采现象；“容量必然无效”“38ms队列必然小于49ms设备等待”不作为已证根因。

### 7.2 最小基线诊断与恢复决定

1. 步骤0仍须通读§六所引规范。先核对历史同二进制/负载证据，不为已确认的身份重做构建；新现场重新核实身份、业务并发及固定资产。
2. 按§三记录安静期、Dirty/Writeback、pool对象/用量、TiKV延迟/待压缩量和OSD状态。用量差不等于已确认垃圾，待压缩量归零也不等于历史状态相同。
3. 已满足可比起点时直接进入获准四轮，A1/A2兼作无缓存对照；**不默认另跑6～8轮漂移矩阵**。若无法判断，仅可在开跑计划中另列至多两次60秒同配置无缓存诊断，记为诊断，不能补入正式配对。
4. 被动恢复每次最多15分钟；积压/持续退化不能解决则停，提交一次精确恢复方案。共享卷GC、JuiceFS compact和主动OSD compact须分别明确作用范围、业务影响、前后证据及授权；不自动执行旧`gc --compact --delete`。
5. 恢复策略在新批次前冻结、各臂对称，恢复前后不得混成参数配对。不要求回到某个历史最高带宽；若仍显著低于历史，先限定本次状态及未解释差异，不把低基线上的相对值当成历史收益已复现。

### 7.3 首批四轮：复现原组合

顺序`A1 → B1 → B2 → A2`，A/B各自占位均值2.5。这里只抵消线性轮序偏差，不证明状态完全相同。

| 项目 | 固定合同 |
|---|---|
| 交付件/资产 | MD5 `24fae0852051c80ca571cb2f20275d46`，既有B256卷128×1GiB `rw_test`；不新建卷、不layout |
| fio | 沿用原256K、50/50、libaio、128 jobs×QD128、direct=1、180秒、seed=20260915及完整job |
| 公共挂载 | FUSE256K、buffer300、U150、downloads200；其余与06-1命令核对冻结 |
| A：对照 | cache-size=0、WB关、CLW关 |
| B：原组合 | cache-size=98304MiB、WB开、CLW关、free-space-ratio=0.20、upload-delay=0 |
| 缓存/预热 | 获准原生NVMe/ext4私有空目录，无128GiB loop总容量限制；每轮同60秒randread预热，再过§三起点门 |
| 主值 | 完整完成字节/实际读写较长runtime，R/W分报；保留慢尾，排空单列 |

不得以旧W臂（CLW开）替代B，不暗中扩大buffer或加入新参数。§二.1、§三安全/证据及§四计算继续生效。
最小证据包括原始fio/每job日志、hit/miss、cache drops/evicts、GET/PUT、staging、df、Dirty、设备时延/队列及TiKV状态；不新增重型profile。
正常WB允许无读冲突时先在本地暂存、再后台上传；但当前`vfs.Read`会先对整个inode执行
`writer.Flush`，其返回条件包含相关slice的数据上传及元数据提交，开启WB也不能让该读绕过这一步。
不要求上传槽达到150才承认WB有效，也不要求PUT带宽必须提高。若要解释“读等变短”还是“写吸收变快”，须有对应等待证据，不能仅因50/50下R/W一起提高就分配贡献。

### 7.4 复现后优先拆WB增量，新优化转07

每次最多批准一个单变量四轮ABBA；复用上批起点方法，但必须有本批对照，不拼历史数据作效应量。

| 对照 | 触发与解释边界 |
|---|---|
| 同96GiB、CLW关，WB关/开 | 原组合有正向信号后优先做，区分纯读缓存与WB组合的增量；结合首批回答收益来源，不将不同批次百分比相加 |
| 原较小预算/96GiB | 仅在需要解释04/06差异时；WB、原生路径、总空间等固定，预算取历史实际值而非名义乘法 |
| 总空间约束或loop/原生路径 | 仅有容量/回落/路径证据时分别设计，不同时改两项；需要设备或配额操作时另批，不预建脚本 |
| buffer300/2048 | **已转07-1，暂不开展**；不属于本任务闭环必测，不与历史原组合复验混做 |

旧Q/P/M及新CLW/容量/淘汰优化分支转07暂存，不因旧六轮结束自动开跑。上表预算/路径比较仅在确需解释历史21.7%差异时留在06，不展开新容量曲线。新参数收益标“新配置”，不能替代原21.7%来源确认。
先保留可重复组合候选，不要求解释全部内部机制；但确认单项因果或生产交付不能只靠L1四轮，须明确后续证据等级。

### 7.5 交付与关闭条件

- 已在现有`t06-3-*`组件中增加独立`burst-self-test/burst-plan/burst-phase`入口，旧六轮入口保留；四轮固定为`A1/B1/B2/A2`，B为96GiB＋WB且CLW关。离线Gate `RUN_ID=20260921-111637`通过，证据在`/mnt/c/SunRise/test/06-3/20260921-111637/offline/`；driver/analyzer/Gate SHA256分别为`060976ba…`/`d0a57ea1…`/`2c7abfe0…`。首次`111513`仅因Gate的`runpy`变量快照断言失败，未调用在线命令，目录保留审计。
- 157只读inventory通过：128文件/卷/二进制身份未变，无并发基准或目标路径残留；Ceph HEALTH_OK、6/6 OSD、97/97 PG clean，三节点TiKV pending-compaction=0，空间/内存超过冻结门。原始证据、提案/批准合同及精确执行计划位于`/mnt/c/SunRise/test/06-3/20260921-111637/{inventory,plan}/`。
- 计划审核后仅设开跑前安全审批与整批裁决两个停点；正式合同已批准并按整批连续执行。`RUN_ID=20260921-111637`的A1/B1/B2/A2全部生命周期PASS，scrub flags精确恢复，无任务挂载或缓存目录残留。
- A1→A2读写下降`34.57%/34.45%`，B1→B2仅变化`+0.41%/+0.49%`；两次B/A配对一负一正，均值比为读`-3.02%`、写`-2.89%`，裁决`SCREEN_STOP_NO_CLEAR_REPEATABLE_BENEFIT`。详见报告§九。
- 首批没有形成可重复正向信号，故§7.4的WB关/开第二批不触发；不追加样本，不登记生产候选。跟踪项保留为`PAUSED_INCONCLUSIVE`，不是“缓存已证无效”。
- 结果追加到本任务后续报告（同一报告集中记录，不为每小步建文档），同步阶段计划同一跟踪ID及results-table；旧raw不覆盖，新增结果使用独立RUN。
- `REPRODUCED_ATTRIBUTION_OPEN`：重复获得正向组合信号，保留候选但来源仍待必要拆分；不等于生产已固化。
- `PAUSED_INCONCLUSIVE`：漂移、资源限制、未复现但历史差异未解释；停止新增负载、保留未知，不判历史收益不存在。
- `RESOLVED`：已有足够证据说明可用配置的主要来源/适用条件，或说明历史增幅为何不能固化；写清证据等级，不能用L1冒充精确效应量。
- 若继续投入不值得，由用户明确同意挂起或限范围收口；**旧六轮结束、预算到期、06-2b/06-4完成都不能自动关闭此项**。
- 末步复核规范、安全排空和所拥有状态恢复；只持久化一次权威证据并按精确清单收尾，不为无收益结果追加无关测试。
