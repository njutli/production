# 03 · JuiceFS 参数调优执行计划

> 建立：2026-08-06　｜　**本版重构：2026-08-11**（并入 03-4 全部实测结论；剥离任务级叙事；作废内容归拢至附录 A）
> **Gate 0 对账：2026-08-17**（收敛 F42/F46 命名，关闭脚本 B4，冻结 03-16 Phase R）
> 上一版（641 行，含 03-1/03-2 复核过程叙事）见 git `eab18a3`，不再维护。
>
> 上阶段计划：`doc/perf-analysis/02-backend-raw-cap-and-juicefs-tuning-plan.md`（§十一 交接章）
> 基线交付：`report/基线测试报告-JuiceFS-E6-20260806.md`
> 方法沉淀：`skills/{FULLBASELINE,TUNING,SYSTEM-SAFETY}-SKILL.md`
> 原始数据：`results/stability-raw/`（含 `MANIFEST.md`）

---

## 〇、文档角色与编号约定

| 文档 | 职责 | 更新时机 |
|---|---|---|
| **本计划书** | **全部调优计划的唯一归属地**：分线、旋钮盘点、判据体系、排期、红线、待拍板 | 结论变化时原地更新 |
| **任务书** `doc/perf-tasks/03-N-*.md` | **一个**调优项或**一个**具体任务的执行描述（前置、步骤、回报格式、红线） | 开工前写，执行中不改 |
| **报告** `doc/perf-report/03-N-*-<date>.md` | 该任务的结果与结论 | 任务结束后写 |
| **周报** `report/周报-*.md` | 一周工作总结（对外） | **仅周末编写/修改** |

**两套编号，不要混**：

- **`T1.x / T2.x / T3 / T4`** = 调优实验编号，定义在本计划书（§六-§九）。这是"要做什么"。
- **`03-N`** = 任务书序号，一本任务书承载一个 `T` 编号或一个具体任务。这是"哪一次去做"。

对应关系见 §十一.1 台账。已发生的巧合：`03-1`~`03-4` 四本任务书承载的都是**前置测量**而非调优本身，这不代表 03 阶段是准备阶段——调优本体就是 §六-§九。

---

## 一、分线依据：按"切换代价"而非"改什么"

02 计划的 A/B/C/D 是按**改什么**分类（后端 / JuiceFS / OS / 方法论）。02-2 稳定性攻坚证明，真正决定能不能测的是**切换这个旋钮要付出多少噪声代价**：

| 切换方式 | 噪声代价 | 可用分辨力 | 结论 |
|---|---|---|---|
| `ceph config set`（在线，不 remount 不重启） | 仅剩轮间噪声 | **1.5-7.6%（逐项）** | ✅ 本阶段主力 = **T1** |
| 需 remount（JuiceFS 挂载参数） | 重抽档位，跨实例极差 **29.9%** | 需多实例分布比较 | ⚠ 成本 ×4 = **T2** |
| 需重启 OSD | CV 0.27%→**4.5%**，组合 9.5-13.0% | 效应 3-7% 被吞 | ⛔ 不做（§十） |
| 需重铺 layout / format / EC profile | 换 epoch，绝对值不可比；曾致 PG 落点 100% 改变、漂移 16-37% | σ_layout 未知 | ⛔ 先测 σ_layout = **T4** |

02 计划条目全部映射进来，不丢项 —— 见 §十二。

---

## 二、公理层事实（本阶段不再重验）

### 2.1 结构事实

| # | 事实 | 出处 |
|---|---|---|
| F1 | **性能档位属于 JuiceFS 挂载实例**：同实例轮间 ≤1.1%，跨实例极差 **29.9%**（1334-1891）。remount 不是波动源，是**抽签动作** | `02-2h-e6-v4-…md` §13.5 |
| F2 | 档位机制**未定**（RSS/IRQ 饥饿假设被 r=+0.01 否证），已主动放弃归因，改走探针验签 | 同上 §十四 |
| F3 | 档位**多档非二元**：≈1870 / 1790 / 1750 / 1714 / 1334，历史另有 1698 / 1834 | 同上 §13.5 |
| F4 | 高档带 `[1830,1930]` 内极紧凑：档内 2.2%，03-1 六实例 **1.3%** | §13.x / 03-1 |
| F5 | **读写解耦**：randread 跨 1222 GiB 写入极差仅 2.0%，与轮前对象数、累计写入量均不相关 | R4/R5 |
| F6 | **写侧受对象数支配**：r(randrw, 轮前 objects) = **−0.807**；安全上限 ≤3.11M；拐点 ∈ (3.78M, 4.40M]；`compact_秒 ≈ 1.95e-4 × 回收对象数` | R5 |
| F7 | **重启 OSD 本身是大波动源**：不重启 0.27% / 只重灌数据 3.9% / 只重启 OSD **4.5%** / 组合 ~10% | `02-2h-lc-locate-…md:78-81` |
| F8 | 重启 OSD 不改 CRUSH 映射，但超 `mon_osd_down_out_interval`（600s）会被标 out → 真重映射 + backfill | 本阶段核查 |
| F9 | **首轮效应 + 首运行效应**真实存在（首轮跑在干净池上系统性偏高）；"randwrite 单调下行"已被推翻 | W2 |
| F10 | **randrw 双口径**：`FULLBASELINE_V4.sh` 输出读侧（1215），`r5chk.py` 输出读写合计（2430）。同一性能，报数必须标口径 | W2 |
| F11 | **ITEMS 子集的绝对值 ≠ 全量运行的绝对值** ⇒ 每个战役自带同 `ITEMS` 基线臂，**禁止与全量签收值 1215/2860 直接比** | 本阶段核查 |
| F12 | 六类"降噪"手段全部无效或有害（缩缓存 / 内存盘 / 单进程 / 拉长 runtime 等）；单进程"稳但瞎"：CV 0.6% 但把 1.72× 真实差异测成 1.02× | 周报 §三 |
| **F22** | **JuiceFS 当前挂载无本地盘缓存**：`--max-uploads 150 --cache-size 0`（`/proc/1631722/cmdline` 实查）⇒ 观测到的 hit_rate 46-50% 不来自本地盘，磁盘紧张不影响性能数据有效性 | 03-4 复核 |
| **F28** | **旋钮的"runtime flag"≠"改了就生效"，必须查代码路径**（2026-08-11 桌面核查，见 §5.5）：`handle_conf_change` 跟踪列表 + 逐 op 直读 `cct->_conf` 才是真生效；**`rocksdb_cache_size` 带 `runtime` flag 但 block cache 只在 `_open_db` 建一次 ⇒ 改了不重启无效** ⇒ K2 作废 | `BlueStore.cc:5905/8262`、`RocksDBStore.cc:554` |
| **F30** | **`rocksdb_cache_size` 在 mon 里是比例阀且真生效**：`cache_kv_ratio = rocksdb_cache_size / mon_memory_target`（`OSDMonitor.cc:1061`），余量对半分给两个 OSDMap 缓存；我们集群 512 MiB / 2 GiB = **0.25**，`inc`/`full` 各 768 MiB。**但 mon 不在 JuiceFS 数据路径上**（数据走 librados→OSD，mon 只在 map 变化时被查；quorum age 13d、堆用量 363 MiB ≪ 2 GiB）⇒ 对七项基准效应**结构性为 0**。详见 §5.6 | §5.6 |
| **F29** | **OSD 侧 KV(RocksDB) 缓存实际是 ~9 GiB，不是 512 MiB**：`bluestore_cache_size=30 GiB`（显式设置，非 0 ⇒ 覆盖 `_ssd` 的 3 GiB）× `bluestore_cache_kv_ratio=0.30`。运行态旁证：`bluestore_cache_meta` 0.60 GiB + `cache_onode` 0.62 GiB = **1.22 GiB**，落在 30 GiB×`meta_ratio` 0.05 = 1.5 GiB 预算内；若走 3 GiB 默认则预算仅 0.15 GiB（不可能） ⇒ **01-5「512 MiB KV cache 太小」的前提读的是死配置** | 桌面核查 + `ceph tell osd.0 perf dump` |

### 2.2 噪声与漂移事实（判据分母的来源）

| # | 事实 | 出处 |
|---|---|---|
| **F13** | **读侧同实例漂移底噪（n=10，锁 pid，夜间 11h46m）三项性质不同**：`randread` **1.30%** 无 lag 依赖 = 纯噪声；`seqread` **3.89%** 无 lag 依赖 = 大噪声；`mseqread` lag1 **0.50%** 但随 lag 单调增长到 **1.69%(lag8)**，Spearman(运行序,值)=**+0.939** ⇒ 系统性单调上行漂移 | `03-2-…md` §四/§五 |
| **F15** | **写侧同实例漂移底噪已测定（n=10 run，38h17min，同一 pid=1631722）**：见 §三.2 完整表。关键结论 —— `randwrite` lag1 **2.13%** 最干净；`randrw` **2.78%**；`mseqwrite` **3.78%**；**`seqwrite` lag1 7.25% / 38h 极差 11.79% ⇒ 移出判据集** | 03-4；`bw-raw.tsv` 601 行经 opencode 独立重算 |
| **F16** | **38h 尺度存在与旋钮无关的单调退化**：`seqwrite` ρ(run序)=**−0.758**、1529→1370 = **−10.4%**；`randwrite` ρ=−0.624；`randread` ρ=−0.467；`mseqwrite` 无趋势(+0.152)。**回归掉 randread 后 seqwrite 残差极差反而升到 12.89%**（降幅 −9.3%）⇒ 非外部负载共因，是独立机制。周六/周日均值差：seqwrite **+5.88%** | 03-4 |
| **F17** | **A/B 紧邻交错可抵消 ~87% 的时间漂移**：03-3 日间 4.5h 漂移 1.72%（Spearman −0.771），交错后残差 0.233%。生效三判据：6 个 median `B A B A B A` 完全交织、Mann-Whitney U=7/9 不显著、两项符号相反 | 03-3 |
| **F18** | **randread 是协变量，不是看门狗**：日间 4.5h 跌 1.72%，最低点 1830.7 距高档带下界 1830 仅 **0.04%** 余量 ⇒ `±2%` / 区间式在线看门狗在数十小时尺度必然误报。实例同一性须用 **pid + starttime_ticks** 硬判据 | 03-3 |

> ⚑ **F16 的处置 = 声明超出作业尺度，不研究**。调优战役单次 ≤14h 且强制交错（F17），不进入 38h 区域。理由：追机理需要多实例 × 多昼夜对照，成本远超收益，而交错已把它压掉 87%。**纪律**：任何单战役跨度 >14h 必须拆批，且不得跨周末边界比较绝对值。

### 2.3 对象数与池行为事实（写侧战役的物理约束）

| # | 事实 | 出处 |
|---|---|---|
| **F19** | **池内对象大小恒为 256 KiB（精确，非平均）**：`objects × 262144 ≡ stored`，在 `pool-trace.tsv` **20,054/20,054 行**逐行成立 ⇒ 对象口径与字节口径是**同一个测量**，不存在"哪个更可信" | 03-4 |
| **F20** | **换算系数是分项的，没有全局常数**：顺序写（seqwrite/mseqwrite）**f=1.000**，即每 256 KiB 恰好 1 个新对象、零覆写（`io=268GiB` vs 对象 +1,098,336，比值 1.000）；随机覆写（randrw/randwrite）**f=0.876**，即 292 KiB/新对象。把 292 套到顺序写上**低估 25%** | 03-4 / R5 |
| **F21** | **单轮生成量实测**（`io=` ÷ 256 KiB，10 run 均值）：`randrw` 214.8 GiB→**0.88M**；`seqwrite` 252.8 GiB→**1.04M**；`randwrite` 518.9 GiB→**2.13M**（gross）；`mseqwrite` **846.9 GiB→3.47M**。mseqwrite 是唯一的量级异类 | 03-4 |
| **F23** | 🔴 **`obj_gate()` 是轮边界闸门，不是运行时看门狗** ⇒ `OBJ_MAX=10M` 红线**实测被击穿且全程无人察觉**：池峰值 **10,691,356**（106.9% 红线，2.55 TiB）@ 08-09 17:54:59，窗口内 38 个采样点 >10M、累计 9.5min。峰值成因：mseqwrite r4 结束 7.0M + 单轮 gross 3.47M ≈ 10.5M，全在轮内 | 03-4 |
| **F24** | 峰值 10.69M / 2.55 TiB 下集群仍 **HEALTH_OK**，`max_avail` 最低 23.99 TiB（与 03-1 的 17.03M / 15.30% 安全一致）⇒ 击穿是**机制失效**，不是安全事件 | 03-4 |
| **F25** | 🔴 **不存在时间驱动的后台回收**：池在 3,453,658 上静置 **8 小时零变化**（23:10→07:14，2568 行采样全同，HEALTH_OK）⇒ **唯一有效回收手段是显式 `juicefs gc --compact`**，"等待自愈"不可依赖。**F14 因此降级**，见附录 A | 03-4 |
| **F26** | **`gc --compact` 第二遍完全无效**：同一轮 p1/p2 的 `scanned` 与 `pending_delete_bytes` **逐字节相同**（如 4,440,410 / 36,104,568,832 两遍一致）⇒ `OBJ_GC_PASSES=2` 是纯浪费，应改 1 | 03-4 §5 |
| **F27** | `gc --compact` 清不掉约 9.2% 的顺序写残留（101,120 对象 = 24.7 GiB），机制为结构性待删切片（探针 C：第二遍 `deleted 0 pending slices`）；randrw 五轮地板恒为基线+1，无欠账 ⇒ **每轮起点对齐度是分项的** | 03-4 A2 |

### 2.4 ⚑ 跨层瓶颈事实（2026-08-11 新增，F31-F38；来源：03-5 副产物 + 172 轮归档数据重算）

> 本节全部来自**已采但从未挖掘**的数据（`jfs-stats-pre/post.txt`、`nic.txt`、`perf dump`），零额外机器时间。
> 提取器：`scripts/FULLBASELINE/analyze/latency-budget.py`。

| # | 事实 | 出处 |
|---|---|---|
| **F31** | 🔴 **FUSE 请求被拆包**：平均 FUSE 读请求尺寸 = **130,908~131,025 B（=128 KiB）**，而全部测试项 `bs=256k` ⇒ **每个客户端 IO 拆成 2 个 FUSE 请求**。七项全中。三处独立吻合：`.stats` 实测、`/proc/mounts` 的 `max_read=131072`、`juicefs mount --help` 的 `--max-fuse-io` 默认 `128K` | 172 轮归档 |
| **F32** | 🔴 **并发被压缩约 100×**：fio 要求 16384 并发，对象层实际在飞 GET 仅 **randread 164 / randrw 132**（Little 定律反推）。GET 延迟 **9.1-9.7 ms**，而单流 seqread 仅 **966 µs** ⇒ **延迟几乎全是排队，不是服务时间** | 172 轮归档 |
| **F33** | 🔴 **"JuiceFS 单进程 CPU 封顶 ~6 核"作废**：实测 randwrite **13.73 核**、randrw **10.09 核**、mseqread 6.07 核、randread 4.90 核。01-3 的 595%≈6.0 核是**该负载的平衡点**，不是进程上限。⇒ 03 计划 §四原"FUSE 单进程封顶为架构固有"的立论依据松动，见附录 A | 172 轮归档 |
| **F34** | 🔴 **randwrite 元数据层崩溃**：meta 平均延迟 **149,365 µs = 149 ms**，而 randrw 在几乎相同的 meta op 数（2.15M vs 1.96M）下仅 **1121 µs — 差 133×**；反推在飞 meta op ≈ **1625**。TiKV 位于 `10.20.1.150-152` = **10GbE 管理网**（与 SSH、外部租户共享）。空闲 RTT 仅 0.061 ms ⇒ 大概率是 TiKV 服务端而非网络拥塞，须在负载下复测 | 172 轮归档 |
| **F35** | **七项放大率全测出（写侧 TX 首次）**：RX 放大 seqread 1.02｜mseqread 1.02｜randread **2.07**｜randrw **2.33**；TX 放大 seqwrite **1.00**｜mseqwrite **1.07**｜randwrite **1.34**｜randrw 1.27。⇒ **写侧网卡放大几乎为零，网卡不是写侧障碍**；randwrite 的 1.34 与 RX 349 MiB/s 可能混入背景 compaction，需隔离 | 172 轮归档 |
| **F36** | **放大率是档位免疫指标**：跨 8 个挂载实例 CV 仅 **0.04-0.94%**（seqread 0.24%／mseqread 0.04%／randread 0.81%／randrw 0.94%），而吞吐跨实例 median 极差 **29.9%** ⇒ **少量挂载即可判定放大率类效应，吞吐类必须走完整 T2 协议** | 172 轮归档 |
| **F37** | **设备层余量 ≥3.7×**：K1 B 臂关闭 BlueStore 缓冲读后，单 OSD 落盘读取 **19→1073 MB/s（mseqread，56.6×）**、**498→979 MB/s（randread，1.96×）**，吞吐变化 **+0.22%/+0.27%**。1073 MB/s/OSD 是 01-5 所称"%util=100.4% 饱和值 290 MB/s"的 **3.70×** ⇒ `%util` 对 NVMe 无意义，01-5 该结论撤回（附录 A） | 03-5 |
| **F38** | **EC 4+2 对客户端 RX 字节的贡献为 0**：同一 EC 池上 seqread/mseqread 放大率 = **1.02**。若 EC 取片放大客户端字节，顺序读也应为 2.5× ⇒ randread 的 2.07× **全部是客户端 readahead 浪费**。EC 分片流量走 OSD 间 cluster 网，不经客户端。⇒ E6 §677 与 `10_A_4` §6.2 的两处归因均撤回（附录 A） | 03-5 + 172 轮归档 |

### 2.4b ⚑ 跨层瓶颈事实（续，F39-F45；2026-08-14 汇总，来源：03-6/03-7L/03-8/03-9/03-10/03-11）

| # | 事实 | 出处 |
|---|---|---|
| **F39** | 🔴 **fio `iodepth` 在 FUSE 上完全无效，有效并发 ≡ numjobs**（实测 randread 125.7/128、mseqread 14.5/16）⇒ 所有并发推导一律用 numjobs，不用 iodepth×numjobs | 03-6 |
| **F40** | **吞吐闭环公式**：吞吐 = numjobs × 请求尺寸 ÷ FUSE 写/读延迟。读写两侧均闭环（128K randwrite 128×0.25MiB÷(2×5.35ms)=2991≈3045；256K 塌态 128×0.25÷56.7ms=564≈551；randread 128×0.25÷7.68ms=4167≈4073） | 03-6/03-7L |
| **F41** | 🔴 **≥93% 读延迟不在 Ceph**：OSD `op_r_latency` 含排队 0.48ms、TiKV RTT 0.07ms，而客户端 GET 7.5ms ⇒ 读延迟在客户端内部排队 | 03-7L |
| **F42** | 🔴 **单挂载随机读在 ~4.1 GiB/s 饱和**：并发 ×8.8、延迟 ×9.0、吞吐不动（3815~4094 平台，两实例 j64→j128 仅 +0.9%/+1.8%），仅用 ~5 核/96。03-15 对 6 份 goroutine dump 离线复算：j64→j128 吞吐仅 +1.8%，但 `fileReader.waitForIO` 中位 61→127、`dataReader.Read` 138→262、`rados_read+rados_stat` 75→127，证明新增并发转成客户端读链路等待/在飞请求。⛔ debug=2 dump 看不到 librados 内部队列，**不得提前点名 objecter/messenger 的具体队列**；历史 N4=5013 又反证 4.1 不是无条件整机硬顶。约束层级须由 03-16 N1/N2 划界 | 03-9/03-11/03-15 |
| **F42b** ⚑ | ~~**F42 的饱和点 = 64 并发流**~~ ⇒ **2026-08-20 由 03-17d 订正为「64 流已进入收益递减区，不是饱和点」**（详见 §2.4e F60/F61 与附录 A A30）：在 `ms_async_op_threads=8` 下 numjobs 64→128 仍有 **+10.4%**（4997.3→5516.7，拉丁方设计，臂效应 F=80.2 显著）。原"j64→j128 崩跌 87~92%"的观测本身有效，但那是 **msgr=3 的 worker CPU 饱和态**下的特例，不是并发本身的饱和点。以下原文保留备溯源：**曲线凸形无硬断点**：两独立实例 5 档 ×3 轮（32 点）复现——j8→16→32 每步 +819~955 陡升，**j32→64 为最后大步（+450/+571），j64→128 崩跌 87~92%（仅 +36/+72 = +0.9%/+1.8%）**；每 job 效率 198→30 MiB/s（掉 6.3~6.7×，平滑无拐点，j×4 ≈ 效率减半）⇒ H1（低并发即饱和）拒绝、H3（低并发更高效）成立。F40 逐档闭环（延迟 1.16→3.88→7.68ms 近线性 = 单队列前端堆积）。两实例曲线平行偏移 7%（档位差），拐点位置完全一致 | 03-9 §3 + 03-11 §3（opencode 已从 s1v3-bw.tsv 逐点复算） |
| **F43** | **同夜同臂跨实例极差仅 1.0~1.9%**；F36 记的"29.9%"是**跨周漂移**，不是实例噪声 | 03-7L |
| **F44** | 🔴 **写侧 meta 提交率墙**：每个 256K 写 = 1 个新 slice = 1 次 meta 提交（meta/写op≈0.91~0.99）⇒ 写吞吐 = 客户端在飞 meta 队列（~0.5~2.2K，随并发自适应）÷ TiKV 每 op 延迟。白天态：延迟 120~160ms ⇒ **~12K 提交/s ⇒ ~3000 MiB/s**（四会话：03-7L WF 臂 11~12K/s、03-8 P 臂 10.5~12.7K/s、03-10 P1/P2 10.4~10.9K/s、03-11 段A 三点闭环）；退化态：延迟 85→315ms ⇒ 6~7K/s ⇒ 1500~1800（03-11 段A 夜）。PUT 管线全程空闲（在飞 PUT ~5/150）⇒ 上传不是墙。⚑ 状态：**已点名（客户端侧）**；TiKV 服务端归属待证（段D 指标抓取）。⚑ F44 还是**环境态开关**：兜底清仓速度 = meta 提交速度 ⇒ 它决定健康/中间/塌三态切换（见 F45） | 03-7L/03-8/03-10/03-11 |
| **F45** | ⚑ **环境三层态模型 + 复现纪律**：**健康态**（~3800，环境新鲜，竞态被兜底掩盖）→ **中间态**（600~1200 缓慢下滑，兜底勉强跟上，写率低于节流阈值）→ **塌态**（~550 自锁：缓冲>300MB ⇒ 节流 ⇒ 写率降 ⇒ 冻结率随写率 ⇒ 缓冲降不回）。竞态（INTERNAL-2026-08-13-001，FlushTo 登记竞态）本身每轮必现（插桩 99.2% 首写跳过），但可见崩塌依赖环境态。⛔ **对比纪律：任何跨基座/跨旋钮结论必须同会话（ABBA 或交替），跨时段单跑对比会被环境漂移混淆**（2026-08-14 全天教训：上午 main 3815 vs main−#6311 543 的"差异"被同会话重测推翻；#6311 与竞态无关） | 03-8/03-11 + bugzilla §6.1 |

### 2.4c ⚑ 跨层瓶颈事实（续，F46-F49；2026-08-15 新增，来源：03-12/03-13/03-14 + opencode 独立复算）

| # | 事实 | 出处 |
|---|---|---|
| **F46** | 🔴🔴 **单挂载内存在读写共享容量约束；层级与绝对容量尚未划界**。03-14 同挂载并发 randread(`read_test.*.0`) + randwrite(`storage_test.*.0`) 使用互斥数据集；fio 汇总因写侧跑 219s/读侧 180s 而失配，逐秒真并发窗口 0–180s 为读 2949 / 写 1203 / 合计 4152，分项强反相关。缓冲平稳后的写占比 10%–50% 分档中，读写配比大幅变化而合计约 **4038–4198**，支持共享约束；但写占比 >50% 的 28 秒可到 **4573–4715**，故**不是固定 4.1 GiB/s 字节硬顶**。原“buffer 峰 8MiB”错误：真实峰 **590.4MiB@t2**，8MiB 是结束后地板。且池对象 **7.05M→7.66M**，写侧处于中间态，绝对容量不得签收。历史旧基座 N4=5013 对整机硬顶构成反证。⇒ N1/N2 前统一命名为“单挂载读写共享约束”；randrw 单挂载当前仍距每向 6250 约 3.31×，但架构落锤待 Phase R/Phase X | 03-14/03-15 Gate 0 |
| **F47** | ⚑ **单流顺序项在 256K 下白赚 +13~20%（未测格首测）**：seqread 1450/1465（vs 128K 平台 [1234,1324]，**+13.0%**）、seqwrite 1626/1735（vs [1356,1462]，**+20.0%**）。机制：4M 应用 IO 在 256K 下拆成 16 个 FUSE 请求而非 32 个，请求数腰斩。**多流顺序项（mseqread/mseqwrite）不变** ⇒ 与 F31/03-6"顺序读零收益"（多流口径）不冲突。⛔ 两项门槛过宽（7.78%/14.50%，已移出判据集）⇒ **只记不判，且不改变 seqread 的架构限制判定** | 03-10（opencode 已复算全 14 项，与报数逐个精确一致） |
| **F48** | **`--max-downloads` {200,512,1024} 无可信收益证据**：3867/4079/3833 非单调，且每档仅单实例单轮；md512 的 +5.5% 与更好 ns/B 档位（3.239 vs md200 3.469）同向，档位差足以解释。原“已破墙”由脚本 stdout/字符串比较缺陷伪造，已撤销；现有实验强度也不足以写“正式否证”。仅在能采 semaphore 占用/等待并做同会话多实例交错时复活，优先级 P1 | 03-13/03-15 |
| **F49** | ⚑ **F44 的 TiKV 服务端归属仍不可判**：三节点 `block_cache_size_bytes` 恒定 4.3GB/节点，而退化时元数据工作集约 42GB，thrashing 只是候选；缺服务端逐 op 延迟/命中率，服务端慢与客户端在飞盖顶仍不可分。⛔ 拿到精确 TiKV 指标前不得声称 block cache 是根因。对象残留只定为**强性能状态协变量 + 实验有效性闸门**：6.02M 与 gc 后 2.36M 的性能差很强，但时间窗、GC/删除、cache/LSM/MVCC、remount 等混杂尚未切开，不能写“对象数本身是主因” | 03-12/03-15 |

**⚑ 由 F31-F38 得出的达标可行性表**（目标 = 有效数据带宽 6250 MiB/s = 100GbE 全双工各向 12500 的一半）：

| 项 | 当前有效 | 占目标 | 需倍数 | 达标时 RX | 达标时 TX | 物理可行 |
|---|---|---|---|---|---|---|
| mseqwrite | 4796 | 77% | 1.30× | 0.1% | 53% | ✅ |
| mseqread | 4161 | 67% | 1.50× | 51% | 0.2% | ✅ |
| randwrite | 2982 | 48% | 2.10× | 6% | 67% | ✅ |
| ~~**randrw（合计，重点项）**~~ ~~2420~~ ~~39%~~ ~~2.58×~~ ~~58%~~ ~~32%~~ | | | | | | |
| **randrw 读向（重点项）** ⚑ | **1210** | **19.4%** | **5.17×** | **115.5%** | — | ⛔ |
| **randrw 写向（重点项）** ⚑ | **1210** | **19.4%** | **5.17×** | — | **63%** | ✅ |

> ⚑ **2026-08-12 口径订正（用户拍板"选 A"）**：原表 randrw 一行用的是**两向合计**（2420 = 读 1210 + 写 1210），
> 与其余六项的**单向**口径不是一个量纲，被并排放在同一张表里比较。
> 违反 `TASK-BOOK-AUTHORING-GUIDE.md` §二.1 第 6 条："**randrw 的 R/W 分开报，不合计**（读=入向、写=出向，全双工独立，各自对验收线）"。
>
> **订正后的三个后果（都很重要）：**
> 1. **randrw 调优前只有 19.4%，是全部七项里最差的**（原表显示"中游 39%"），主攻优先级的排序依据随之改变。
> 2. **按 A 口径，randrw 在调优前其实是【不可达】的**：达标需 RX = 6250 × 2.31（旧放大）= 14438 MiB/s = **网卡的 115.5%**。
>    原表标 ✅ 是因为用了合计口径（3125 × 2.33 = 58%）。
> 3. **是 03-6 的 `--max-fuse-io 256K` 把它变成可达的**：RX 放大 2.31 → 1.25 后，达标需 RX = 7813 = **62.5%**，TX = 7625 = **61%**，两向都够。
>    ⇒ randread 与 randrw **两个重点项的"不可达"判定都由 03-6 解除**。
| seqwrite | 1405 | 23% | 4.45× | 0.1% | 50% | ✅ |
| seqread | 1285 | 21% | 4.86× | 51% | 0.2% | ⛔ 见下 |
| randread | 1768 | 28% | 3.54× | **103.5%** | 0.5% | ⛔ 放大所致 |

- **randread 是唯一被网卡物理卡死的项**，卡死它的正是 F38 的 2.07× readahead 浪费。降到 1.02 后 RX 仅 51% ⇒ 可行。
- **seqread 是架构限制（已证）**：单流 `psync iodepth=1`，吞吐 = 256 KiB ÷ 单请求延迟。达 6250 需延迟 **41 µs**，实测串行延迟 **1.46 ms**（ra0 口径，= 1254÷171 反推队列深度 7.33 ≈ readahead 2 MiB÷256 KiB = 8，模型自洽）。**41 µs 低于一次网络往返的物理下限（librados 跨网 RTT ≥200 µs）** ⇒ 与任何调优无关，除非口径给并发。用户已接受此项为架构限制，重点转多并发。

### 2.4d ⚑ 达标可行性表（2026-08-15 按**补丁版 256K 生产候选基座**重算）

> 上表（§2.4 末）是 **128K 出厂基座**下的可行性判断，已被 03-6/03-8/03-10 取代但保留以备溯源。
> 本表数据源 = `03-10` 全七项基线（2 挂载 ×7 项 ×2 轮 = 28 点，opencode 已从 fio 原文逐点复算，与报数**逐个精确一致**）；**"256K+补丁"列 = 该 4 点均值**（口径统一，见 A28/A29）；"跨会话 range" 列含 03-6/03-8/03-9/03-11/03-13 观测。
> **128K 基线列 = E6 已签收基线**（`report/基线测试报告-JuiceFS-E6-20260806.md`：randread 1880 / mseqread 4160 / randrw 1215 读侧 / seqread 1290 / seqwrite 1400 / mseqwrite 4800 / randwrite ≈2860；原始数据 `results/stability-raw/week3-20260803-0806/`，3 实例 ×5 轮 = 15 点，opencode 已重算：1885/4161/1218/1286/1405/4823/3009，与签收值一致）。
> 基座：`JuiceFS v1.3.1 + loadRange eaf3d21f + 03-8 FlushTo 竞态补丁`，`--max-fuse-io 256K --max-uploads 150 --cache-size 0`。
> ⚑ **2026-08-22 更新（03-17f 定案，见 §2.4e 收口结论第 6 条）：交付基座在上述挂载参数之外，追加客户端私有 `CEPH_CONF` 内 `[client] ms_async_op_threads = 8`**（规则：≥OSD数×1.33）。作用域仅高并发读（randread +37%~+45%，CV 20%→0.8%），写侧中性，成本 +1.24~1.42 核；⛔ 不改集群 `ceph.conf`、不执行 `ceph config set`（B6）。⚑ 本表"256K+补丁"列仍为 03-10（`1f60618c`、msgr=3）数值，**尚未按 msgr=8 重算** —— 新数值见 03-17f 报告 §4.2，其中 randread 由 3831 升至 **5544（占目标 88.7%）**，randwrite 由 3018 降至 **2707（−10.3%）—— ⚑ 该缺口已由 F68 定因为 meta `Write` 延迟漂移（+35.6%），与二进制无关（B-catchup 写侧实为 +5.8%），~~F67 的二进制归因已撤回~~**。

| 项 | 128K 基线 | **256K+补丁** | 跨会话 range (好档) | 占目标 | 需倍数 | 卡在哪面墙 | 物理可行 |
|---|---:|---:|---|---:|---:|---|---|
| mseqwrite | 4800 | **4699** | 4539~4828 | **75.2%** | 1.33× | 未点名（最接近目标） | ✅ |
| mseqread | 4160 | **3983** | 3946~3999 | 63.7% | 1.57× | **F42 单挂载读链路饱和** | ✅ |
| **randread**（重点） | 1880 | **3831** ⚑ | 3803~4094 (n=11) | **61.3%** | 1.63× | **F42 单挂载读链路饱和** | ✅ |
| randwrite | 2860 | **3018** ⚑ | 2941~3583 | 48.3% | 2.07× | **F44 meta 提交率** | ✅ |
| **randrw 读向**（重点） | 1215 | **1890** ⚑ | 1854~1961 | **30.2%** | 3.31× | **F46 单挂载读写共享（层级待划界）** | ⚠ 见下 |
| **randrw 写向**（重点） | 1215 | **1891** ⚑ | 1855~1961 | **30.3%** | 3.31× | **F46 + F44（层级/健康态待划界）** | ⚠ 见下 |
| seqwrite | 1400 | **1681** | 1583~1747 | 26.9% | 3.72× | ⛔ 判据门槛 14.5%，不作调优对象 | ✅ |
| seqread | 1290 | **1457** | 1431~1477 | 23.3% | 4.29× | ⛔ **架构限制已签收，停止投入** | ⛔ |

**三条从本表读出的结论：**

> ⚑ **2026-08-15 订正（A28，口径统一；⚠ 含一次本方误判的撤回）**：randrw 两向原登记 **1923 / 1923（占目标 30.8%，需 3.25×，128K 基线 1210）**。
>
> 🔴 **先撤回本方的一个错误判断**：opencode 曾断言"1923 系由 +51.4% 反推（1270×1.514=1922.8）"。**该断言错误，已撤销** —— 1923 实为**跨会话 9 个好档观测的中位数**（1854, 1859, 1888, 1916, **1923**, 1938, 1959, 1960, 1961，中位已复算 = 1923），推导可复现，与百分比反推的数值巧合而已。**再次违反 R15（未复现即指认根因）**，本次是对自己的旧数据下的误判。
>
> ✅ **真正需要改的是口径混用**：该 9 点集合里 **1959/1960 来自 03-8 的 stock 版实例（B2）**，1938 来自 03-14 的另一负载配置 —— 它们**不属于"补丁版 256K 候选基座"**，混入后抬高了中位；且同表其他行用的是 03-10 四点均值，两种口径混列而未标注。⇒ 统一改为**纯候选基座的 03-10 四点均值**：读 1961/1888/1854/1859 ⇒ **1890.5**，写 1961/1889/1855/1859 ⇒ **1891.0**，占目标 **30.2%/30.3%**，需 **3.31×**。128K 基线改用 **E6 已签收值 1215（读侧口径）**，效应 **+55.6%**（同期受控好档对照 03-7L WS1÷WF2 = 1940÷1274 = +52.3%、03-6 = +53.1%，同区间；⚠ 原表 1210 与周报初版 1270 系两处不同来源混用，现统一为 E6 1215）。**结论方向不变**（randrw 仍是最差项、仍卡 F46、仍需 3× 量级）。
>
> ⚑ 附带核实：**两向数值差 ≤0.03% 是负载构造决定的**（50/50 等量混合、同块大小、同 job 下发），本地 3 个批次 58 个 randrw 轮次读写差全部 ≤1 MiB/s ⇒ 非笔误、非复制粘贴。

1. **⚑ randrw 的“网卡不可达”已解除，但当前单挂载软件路径仍远未达标**：03-6 把 RX 放大 2.31→1.25 后，网卡侧达标只需 62.5%；候选基座实测每向约 1890，而双向达标需 12500，总缺口约 3.31×。F46 只证明当前单挂载存在共享约束，**尚未证明固定 4.1 GiB/s librados 硬顶，也尚未证明不可达**；归属先由 N1/N2，健康态读写再由 Phase X 划界。
2. **读侧三项都受当前单挂载读链路饱和影响**，需求 = 1.57×~3.31×。~~F42b 已证服务能力约 64 并发流、再加 job 主要增加等待。达 6250 的量化闸门：同样 128×256K 负载若只靠单请求延迟改善，FUSE 读延迟须从 7.7~8.4ms 降到 ≤5.12ms（约降 33%）~~ ⚑ **2026-08-20 由 §2.4e 取代**（F60/F61：并发可以加、延迟已触服务率地板、渐近上限 6280 MiB/s ⇒ 该闸门不再是"降 33% 延迟"，而是"提高 OSD 服务率"）；任何 3–6% 小收益都不能宣称解决达标问题。多挂载若扩展，只能先作为 per-mount 架构证据，不能替代单挂载测试项验收。
3. **写侧两项归到 F44**，randwrite 需 2.07× ⇒ **meta 提交率 12K → 25K/s**（= 6250 ÷ 0.25 MiB）。上传管道全程空闲（在飞 PUT ~5/150）⇒ 加 Ceph 数据面能力对 randwrite **零收益**。

**⛔ 达标判定的分水岭（尚未在当前基座受控签收）**：F42/F46 的平台究竟是**单挂载/单 JuiceFS 进程限制**还是**整机共享 client/librados/host 路径限制**。历史 01-3 N4=5013 MiB/s 已反证“无条件整机 4.1 硬顶”并偏向 per-mount，但版本、128K/ra0、档位与环境不同，不能替代当前基座 N1/N2：

- 若属单挂载 ⇒ **多挂载/分片可作为架构候选**；是否适配现有单挂载业务、尤其 randrw 语义，须另做 Phase X 和部署评估；
- 若属整机共享 ⇒ 需再加一次 direct-librados 多进程对照，划清 JuiceFS 与共享 librados/主机路径的边界，随后按架构限制交付。

⇒ **N1/N2 单/双挂载边界实验列为读侧 P0**（见 §11.3 决策树）。


---

### 2.4e ⚑ 读侧约束链闭环（F50-F68；2026-08-20/21/22 汇总，来源：03-16 / 03-17b / 03-17c / 03-17d / 03-17e / 03-17f / 03-17g，全部经 opencode 从原始文件复算）

> 这一批把"读侧卡在哪"从 F42 的模糊命名一路推到**可数的物理量**：单挂载 → msgr-worker → 有效 worker = OSD 数 → 在飞并发 × 请求大小 ÷ 延迟 → OSD 服务率。
> ⛔ **口径警告**：F56 的 1438 MiB/s/核（T48 shape，msgr=1/2/3）与 F58/F59/F61 的绝对值（T47C/T49D，msgr=8）**属不同批次，不得同表比较**；跨批次可复现性实测 ±3%（J64 4997.3 vs A8/P64 5167.2 = −3.3%）。

| # | 事实 | 出处 |
|---|---|---|
| **F50** | 单挂载对象 GET op 速率上限约 **16.9K/s（≈4.2 GiB/s @256K）**，瓶颈在客户端对象层，OSD 侧占比仅 19%–40%。⚑ 机制已由 F56 补齐：这就是**默认 3 个 msgr-worker 满载时的 op 率** | 03-16 §7 + 03-17b |
| **F51** | **机器/集群级第二道约束约 25–26K GET/s（≈6.1–6.4 GiB/s = 6246–6554 MiB/s）**，双挂载 j64→j128 仅 +4.0%。⚑ 与目标 6250 MiB/s **重叠**，是全阶段读侧最大不确定性；F61 的单挂载渐近上限 6280（5971–6588）落在同一区间，二者是否同一道墙**待 03-17e 多挂载聚合验**（归属未定，若触及 EC 池 PG 数属 B6） | 03-16 §7 |
| ~~**F52**~~ | ~~挂载序位效应：同 volume 第二个挂载稳定慢 8%–10%~~ ⇒ **已并入 F56/F54 订正，不再独立成条**（同一抽签现象，与"挂载位"无因果） | 03-16 → 03-17b |
| **F53** | 单挂载 librados 线程结构实测确认：`msgr-worker-0..N-1`（N = `ms_async_op_threads`，默认 3）、`io_context_pool` ×2（`librados_thread_count`，默认 2）、`ms_dispatch`、`ms_local`、`ceph_timer`、`safe_timer` ×2；两旋钮均可经**进程私有 `ceph.conf` + `CEPH_CONF`** 注入（⛔ 不动全机 `/etc/ceph/ceph.conf`） | 03-16 §7 |
| **F54** ⚑ | ~~读侧客户端双稳态，判别量 = 对象层 in-flight（好 ≈97.4 / 坏 ≈80.0）~~ ⇒ **订正为多值抽签**：8 轮 bw 连续分布 2994–4101、in-flight 连续 77.5–95.2，原二分是 2 个样本的错觉。判别器换成 **msgr-worker 逐线程 CPU 离散度（r=−0.9907）**，in-flight 只是弱代理（r=0.81） | 03-17b（附录 A A31） |
| **F55** | **会话间慢漂移**（1699→1495，msgr=1 下仍在，机制未明）**保留**。⚑ 03-17d 新增同族证据：**批内轮漂移显著**，35 min 内 r1→r3 单调 **−5.4%（≈−2.7%/轮）**，拉丁方分解占方差 23%（F=25.3 显著） | 03-17b + 03-17d §11.4 |
| **F56** ⚑ | per-mount 读带宽 ≈ **1438 MiB/s × msgr-worker 满载核数**（T48，n=8，r=0.9962）；默认 3 worker ⇒ ≈4184 MiB/s。⚑ **仅在 worker CPU 饱和区可作粗估**：T47C 批内 MiB/核 单调下降 1692→1473→1395→1362→1361，回归斜率随子集变化（1243~1408，截距显著非零）⇒ ~~"4 worker≈5579、5≈6975、6≈8369"外推作废~~（实测 6 worker 均值 4898/峰值 5412，低 35–41%）。⚑ 03-17d 补充：msgr=8、并发 64→128 的窄区间内 MiB/核 = **1356–1372（±0.6%）**，数值稳定但不可外推 | 03-17b + 03-17c §11.3 + 03-17d §11.6（附录 A A32） |
| **F57** | `io_context_pool` 线程在读负载下 CPU **恒为 0** ⇒ **`librados_thread_count` 对读侧无效**，不再扫 | 03-17b |
| **F58** | 🔴 **有效 worker 数上限 = OSD 数 6**：数据面连接只有 6 条，A8 恒 6/8 活跃（每轮固定 2 个 worker 0% CPU），A6 有 1/4 轮只 5/6 活跃即掉到 4178；A8 均值仅比 A6 高 5.5%、峰值相当（5374 vs 5412）⇒ **取 msgr=8 的唯一价值是抽签稳健性**（A8 4/4 无碰撞、CV 2.8%；A6 1/4 碰撞、CV 9.4%），12/16 worker 预测零收益，⛔ 不扫。03-17d 9 轮再次确认活跃数恒 6 | 03-17c §11.2 + 03-17d §11.6 |
| **F59** ⚑ | ≥6 worker 后限制项 = **在飞并发 × 请求大小 ÷ 单 GET 延迟**（Little 定律闭环，`bw ~ 1/GET延迟` r=0.9999；GET 恒 249 KB，读放大 objbw/fio-bw = 1.02）。延迟 9.27→3.82→3.83→3.11→2.91 ms，≥6 worker 触 ≈2.8 ms 地板，此时 worker 最忙仅 47–73%、客户端总 CPU 8.5 核 ⇒ **msgr 已不是瓶颈**（H1 只在 conn/worker≥2 的 CPU 饱和区成立）。⚑ **后半段推断撤销**：~~"全 20 轮在飞 GET 恒 63.94 ⇒ 窗口在客户端被夹死在 64"~~ —— 那 20 轮 numjobs 全是 64，见 F60 | 03-17c §11.3（附录 A A30） |
| **F60（新）** | 🔴 **对象层读并发 = 打开的文件句柄数 = numjobs，`iodepth` 惰性**：J64/J96/J128 三臂 9 轮，在飞 GET = 63.4/95.8/128.2 = **0.990/0.998/1.001 × numjobs**，且恒等于 `fuse_open_handlers − 1`（每 job 独占一个文件）⇒ **单句柄同一时刻只有 1 个在飞对象 GET**。fio 侧 clat 394/572/722 ms 与 `numjobs×iodepth×256KB/bw` 精确吻合（J128：728 ms 预测 vs 722 ms 实测）⇒ 16384 个 fio IO 全堆在 FUSE 之上。**这是 F39（iodepth 在 FUSE 上无效、有效并发 ≡ numjobs）在对象层的精确版**；串行化点在 JuiceFS/FUSE 单句柄读路径，具体锁未定位（留 03-17e） | 03-17d §11.2 |
| **F61（新）** | 🔴🔴 **单挂载 randread 256K 的渐近服务上限 ≈ 6280 MiB/s（三轮独立拟合 5971 / 6282 / 6588，9 点 OLS 6265 = 25835 GET/s）**：每轮内 3 臂同时段拟合 `延迟 = a + b×在飞并发` 得 R²=0.98923、最大残差 0.188 ms（闭环系统撞固定服务率的标准特征），达成率 `= N/(N+14.5)`。⇒ **目标 6250 = 该上限的 99.5%，靠加并发不可达**（外推 N=256→5928、N=512→6092、N=1024→6177，凑到 6250 需 N≈6200 且延迟到 240 ms 级）。J128 实测 **5516.7（峰值 5619）= 目标 88.3%**，是现配置读侧最好水平。归因：客户端三项全部出清（活跃 worker 恒 6、最忙 76.5%、合计 4.07/128 核、pub_rx 仅占 100GbE 的 48%），限制项在 OSD 侧 —— 并发 64→128 时 OSD `op_r` 率只 +10.8%（42068→46607/s）而每 op 延迟涨 **2.12×**（0.409→0.866 ms，增量主要在 `op_r_process` 而非入队），最热 OSD 更是 +10.4% 对 **2.40×** | 03-17d §11.5/§11.6 |
| **F62（新）** | ⚑ **主 PG 分布不均是读侧唯一可见的余量**：六台 OSD 的 client-facing `op_r/s` 恒为约 **6:6:5:6:5:4**（J128-r1 实测 8956/8883/7422/8916/7412/5873），批内 9 轮 **CV 恒 15.7%、max/mean 恒 1.132–1.136** ⇒ 结构性倾斜（33 PG 在 6 OSD 上不整），非随机波动。聚合上限由最热 OSD 决定（J128 时它已 8796 op/s、延迟 1.275 ms = 池均值 1.47 倍）⇒ 若主 PG 完全均衡，理论聚合 ≈ 5516.7×1.136 ≈ **6267 MiB/s**，与 F61 的 6280 **由两条完全独立的推算路径（客户端 Little 拟合 / OSD 侧倾斜度）撞在同一个数**，互为交叉验证。⛔ **EC 池 PG 调整属 B6 未授权**且 2.43M 对象重平衡有生产风险，本阶段只量化留档供上级决策 | 03-17d §11.7 |
| **F63（新，2026-08-21）** | 🔴 **档位判别器定型：唯一有效判别量 = `msgr-worker` 逐线程 CPU 的 active-only 离散度（CV），5–6 s 窗口即可定档；⛔「最忙 worker 占单核百分比」无判别力**。opencode 从 03-17b `threads-series.tsv` 按窗口复算：**CV** 在第一个采样点（t+5~6 s）就已稳定，好档 5.9–10.6% / 坏档 33.9–41.9%（相差 3.2×、零重叠），到 t+175 s 仅漂移 ≤1 pp（5.6–10.6 / 33.6–43.5）⇒ **180 s mseqread 判档探针可被一个 5 s CPU 采样替代**（下限受采样间隔 5 s 限制，不能声称更短）。反之**最忙 worker%**：5 s 窗口好 77.2–93.4% vs 坏 93.6–93.8%，60 s 窗口好 96.2–98.5% vs 坏 98.6–98.7%（仅差 0.1 pp）⇒ 8 轮里最忙 worker 恒 99–100%，**好档也一样**，坏档的真实特征是**其余 worker 欠载**（好档三者均 14000–18000 cs，坏档 worker-0 仅 6800–8800 cs）。⚑ **两条适用边界**：①上表阈值只在 **msgr=3 且 worker 饱和**下标定，msgr=8 时无 worker 饱和（03-17d 最忙仅 67.7–77.0%）且 8 个里恒 2 个零 CPU ⇒ **全量 CV 恒 59.0–59.7%、完全不判别**，必须改用 active-only（>2% 单核）CV，且阈值须在对应 worker 数下重新标定；②`ns/B` 是**按 numjobs 归一**的量（= `numjobs/bw`，03-17d 实测 11.479/16.297/21.222 vs 该式预测 11.86/16.73/21.72，差 <3%）⇒ 只有 numjobs 完全相同的负载才能与 `REF_NSB=3.287` 比较（缺陷 B4-17） | 03-17b 原始数据 + 周报-20260821「挂载好坏档根因定位」订正版 |
| **F64（新，2026-08-21）｜03-17d 无坏档的事后确认** | 用 F63 的臂内口径回溯 03-17d 9 次挂载：J64 臂内 ns/B 11.479/11.920/12.102（极差 5.3%）、J96 16.297/16.782/17.306（6.0%）、J128 21.222/21.229/22.458（5.7%），而 03-17b 坏档相对好档是 **+40.5%**（4.743 vs 3.377）；同时 9 轮最忙 worker 仅 67.7–77.0%（无饱和）、active-only CV 11.7–13.7%（无落位碰撞）⇒ **9 轮全部落在好档带内，F61/F62 未被坏档污染**。⚑ 这是"未设判档停机门"的补偿性验证，不能替代 03-17e 的前置判档协变量采集 | opencode 复算 03-17d `arm-covariates.tsv` |
| **F65（新，2026-08-21）｜`ms_async_op_threads=8` 的收益是真 CPU 饱和墙，+44.3%** | opencode 从 03-17e Seg A `run-*/fio-P.txt` + `threads-series.tsv` 复算（12/12 cell 与报告逐值一致）：**CAL3（msgr=3）3792/3853/4047 = 均值 3897.3** vs **S128（msgr=8）5612/5644/5613 = 均值 5623.0** ⇒ **+44.28%**，两臂各 3 次独立挂载抽签、区间**零重叠**。机理闭合：CAL3 的 3 个 worker 全部打满单核（17295/17492/17560 ÷ 18000 ticks = **96.1/97.2/97.6%**），合计 2.704 核；S128 最忙 worker 仅 76.8%、合计 4.112 核 ⇒ msgr=3 是**真饱和上限**而非坏档抽签。效率 3897.3/2.704 = **1441.5 MiB/s/核**，与 **F56 的 1438 仅差 +0.24%**（F56 在饱和区首次被独立复现）。⚑ **跨批次复现**：S128（5623，带 max-fuse-io 256K + msgr=8）对 **03-10（3831，同带 max-fuse-io 256K + msgr=3，8 天前、2 挂载）= +46.8%**，与同批的 +44.3% 差 2.5 pp ⇒ 两条独立路径一致，F65 稳。⛔ **适用边界**：①仅在 128 流 randread + `--max-fuse-io 256K` 证实；②低并发项（1 流 seqread / 16 流 mseqread）msgr=3 是否饱和无数据 ⇒ 无收益证据；③写侧无有效证据（Seg B 已否决，A36）；④成本 = 多耗 **1.41 个核**（4.112 vs 2.704），157 为共享机须计预算；⑤落地只能走**私有 conf + `CEPH_CONF`** 注入挂载封装，⛔ 不得改全机 `ceph.conf`（B6）。⚑ **2026-08-22 由 03-17f 升级（第三次独立估计，首次为同批同二进制且线程数逐 cell 验证）**：两臂唯一差异经私有 conf 逐字符核对（`86351c58` vs `e49fad2e`，仅 `ms_async_op_threads` 一行），哨兵按实测挂载 pid 采样 702~719（A，恒 8）/ 267~269（B，恒 3）样本 ⇒ **A36 硬伤①②已修复**。randread：A 5544/5495/5582（均值 5540.3，**CV 0.79%**）vs B 2731/4064/3264（均值 3353.0，**CV 20.01%**）。⚑ **点估计随估计量摆动**：均值 +65.2% / 中位 +69.9% / **好档对好档 +37.4%（建议引用此保守下界）** / 对 03-10 跨批 +44.7% ⇒ 效应方向与量级稳（**≥+37%**），但精确点值受对照臂抽签支配，⛔ 报数须给区间与下界，不宜单报 +65.2%。机理同批复现且更强：msgr=3 三 worker 全钉在 **97.4/98.9/98.9%**，msgr=8 最忙仅 **75.1~76.6%**；效率 1339.5~1433.5 MiB/s/核 ⇒ **F56 第四次复现且首次跨 msgr 设置成立**。⚑ **新增量化**：带宽 ≈ **1413 × 可交付 msgr 总核数**，落位偏斜决定能交付多少核（B1 偏斜 1.51× 只交付 1.972 核 ⇒ 2731；B2 偏斜 1.03× 交付 2.835 核 ⇒ 4064）⇒ **msgr=3 完美均衡天花板 = 3×1413 ≈ 4239**，B2 的 4064 已达 95.9%。⚑ **边界收紧（03-17f 实测补齐 F65 原④⑤之外的两项）**：**写侧确证中性**（A 2681 vs B 2661 = +0.7%，两臂写 worker 均未饱和：A 27~31%、B 48~67%）；**低并发确证零收益**（seqread 时 worker 仅 22~24%，A 1449 vs 03-10 1457.5 持平）。成本实测 +1.24~1.42 核 | 03-17e §2.3.1 / §7.2；03-17f §4.4 / §4.5 / §4.6 |
| **F66（新，2026-08-21）｜F51 定案：墙在机器/集群级，不在挂载实例级；03-16 的 per-mount 归因被否证** | Seg A 三臂拉丁方（每 cell 独立重挂）：**S128（1 挂载 128 流）5623.0** / **D64（2 挂载 × 64 = 128 流）5823.0（+3.56%）** / **D128（2 挂载 × 128 = 256 流）6125.7、max 6293（+8.94%）**。①**等流数**下加挂载只买到 **3.56%** ⇒ per-mount 开销不是约束主项；②挂载数与流数同时翻倍也只到 **6125.7** = F61 OLS 6265 的 **97.8%** / F61 渐近 6280 的 97.5% / F62 均衡理论 6267 的 **97.7%** / 目标 6250 的 **98.0%**（3 轮中仅 r2 的 6293 越线，不可复现）⇒ **四条不同推导路径收敛在 6126–6280 的 2.5% 带内**，约束在机器/集群级。⚑ 因此 **03-16 的 per-mount 瓶颈归因在 ≥128 流区间被否证**（per-mount 最多解释 3.6 pp）。③F62 交叉复核：`nb8.log` **21/21** 轮主 PG 分布恒为 `[0:6 1:6 2:5 3:6 4:5 5:4]`，与 F62 的 6:6:5:6:5:4 逐 OSD 完全一致（跨 8 天、跨批次不变）；Seg A 内 P/Q 不均衡 1.09–1.45× 同源。④F63 在 msgr=8 下**好档带标定为 active-only CV 11.3–14.5%（n=9）**，msgr=3 好档 3.6–11.2%（n=3）；⛔ 本批无坏档样本 ⇒ msgr=8 坏档阈值仍缺，仍按 R18 只做批内相对离群标注 | 03-17e §2.3.2 / §2.3.3 / §7.1 |
| **F67（2026-08-22 提出，同日由 03-17g 推翻）** | ⛔ ~~**写侧 −11% 定因 = 每写 meta 数上升**：meta 提交率 10,345~10,756/s 与 03-10 一致（排除 F44 夜间态），而 **meta/写op 由 0.91 升到 0.984~0.991（+8.7%）**，代入 F44 模型 `10620÷0.989×0.25 = 2684` 与实测均值 2684 逐 MiB 吻合 ⇒ B-catchup 每个 256K 写多花 8.7% meta 提交~~ | **整条撤回，两处硬伤**：①**跨计数器错配** —— "0.91" 实为 `meta_ops_total_Write ÷ fuse_ops_total_write`，"0.989" 实为 `transaction_..._total ÷ fuse_ops_total_write`；opencode 在 03-10 原始数据里把两者同时算出（03-10：0.9100~0.9110 **与** 0.9544~0.9807 并存）⇒ 同口径比每写 meta 数 **0.9106 → 0.9140，仅 +0.4%，等于没变**；②**"模型逐 MiB 吻合"是恒等式** —— `(Δtxn/T) ÷ (Δtxn/Δwr) × 256KiB ≡ (Δwr/T) × 256KiB ≡ fio 带宽`，Δtxn 被完全约掉，17 个 cell 误差全 ≤0.02%（BW 跨 2572~3274），零信息量。⇒ 保留的唯一有效残余：`transaction/写op` 确从 0.9684 升到 0.984~0.995（**+2.5%**，真实但小一个量级，且 O 臂 0.9946 ≥ N 臂 0.9936 ⇒ 与二进制无关）。**正确定因见 F68**；教训见 **A39** | 03-17f §4.7（原） / **03-17g §11.1、§11.2（推翻）** |
| **F68（新，2026-08-22）｜写侧 −11% 真因 = meta `Write` 单 op 延迟 +35.6%，退化在 TiKV 不在 Ceph** | 03-17g 同日 ABBA（O=`1f60618c` / N=`de93563f`，仅换二进制，系统默认 msgr=3 + 256K，各 2 个独立挂载）：**O 2572/2603（均 2587.5）、N 2743/2733（均 2738.0）⇒ 新二进制写侧 +5.82%**（臂内极差 ≤1.2%，ABBA 已抵单调漂移）⇒ **⛔ 二进制不是原因，且效应符号相反**。**同日重锚**：O 臂 = 03-10 完整口径，2587.5 vs 03-10 均值 **3018.25 = −14.27%** ⇒ 缺口全部来自 8 天环境漂移；正确分解 **0.857（漂移）× 1.058（二进制）= 0.907**，与 N 臂实测 −9.29% 一致，再叠加 msgr=8 写侧中性即得 03-17f 的 −11.17%。**机理三条同时成立**：①每写 meta 数不变（0.9106→0.9140）；②meta **在飞并发反升 22.8%**（1517→1863）⇒ 客户端未减压；③**meta `Write` 每 op 延迟 138.10 → 187.32 ms（+35.6%；O 臂对 03-10 为 +42.5%）** ⇒ 纯延迟受限。**同期 PUT 延迟下降 10.1%（7.54→6.79 ms）⇒ ⛔ 退化不在 Ceph 对象路径，在 TiKV meta 路径**。跨 17 轮（3 批次 × 2 二进制 × 2 msgr）**`r(BW, 1/meta_Write_延迟) = 0.9151`**，延迟跨度 122.4~199.9 ms（⚑ 非恒等式，在飞并发不恒定，但一阶解释权归 meta 延迟）。**⚑ 双态证据（最强）**：03-17f 的离群轮 NB8-r1（128K 口径，仅比每 meta op 的量）BW 3274 / 延迟 **136.62 ms** = 03-10 快态，而**同一挂载同一配置的 r2/r3 掉到 172.27 / 179.30 ms**（BW 2639/2659）⇒ 排除二进制、配置、落位抽签一切解释，**meta 引擎存在快态 ≈137 ms / 慢态 ≈180~200 ms 双态，切换粒度远小于 F44 的"日/夜"**。⇒ **B1 由"写侧四分支选择"升为唯一定因路径**，目标收窄为：meta `Write` 延迟为何 138→187 ms、双态触发条件是什么 | 03-17g §11.3~§11.6 |

**⚑ 读侧收口结论（2026-08-20，取代 §2.4d 第 2 条的"延迟须降 33%"闸门）**：

1. **读侧目标 6250 在现 6 OSD EC 池上不可达**（⚑ 2026-08-21 由单挂载扩大到整机）。四条独立证据同向：F61 渐近上限 6280、F62 均衡后理论 6267、**F66 双挂载实测聚合 6125.7（max 6293）**、~~F51 机器级 6246–6554~~ —— 全部落在 6126–6280 的 2.5% 带内，且挂载数翻倍只买到 8.94% ⇒ **读侧封板**。
2. **交付定档**：单挂载 randread 256K，`ms_async_op_threads=8` + numjobs=128 ⇒ **5517 MiB/s（峰值 5619）= 目标 88.3%**，为 03 阶段读侧推荐配置。⛔ 不再扫单挂载并发（J192/J256 预测 5824/5928，收益 <8% 而延迟翻倍）。
3. ~~**仅剩两条读侧动作**~~ ⚑ **2026-08-21：① 已完成并定案（F66）；Seg B 段被否决（A36），另立 03-17f 重测；剩余唯一旁支为下面的 ②**。① **03-17e 多挂载聚合验 F51**（任务书 `doc/perf-tasks/03-17e-multimount-aggregate-and-new-baseline.md`，md5 `7933b2e042d8c17af5b1e634f6246c22`；Seg B 的 `OBJ_MAX=8000000`/`OBJ_TARGET=2500000` 已于 2026-08-21 获授权；Seg A = S128/D64/D128 三臂拉丁方 + CAL3 判档标定 cell，判 6280 是单挂载墙还是机器级墙；Seg B = 新配置全量七项，供"是否固化新基线"决策）；② 低成本旁支 —— 墙在"OSD op 率"而非字节数，故**加大单 op 携带字节**理论可用同样 op 预算换更多带宽；03-6 曾测 1M≈256K 无增益，但那是 msgr=3、在飞夹在 64 的老口径，建议在 msgr=8 + J128 下复测 bs=1M（先确认对象层 GET 是否从 248 KB 增大，若不变则该路直接否掉，成本 1 次挂载）。
4. **主线转写侧 B1**（按 03-17d 任务书 §十 第三分支的指定动作）。
5. ⚑ **03-17e 起恢复判档，但按 R18 的"协变量模式"**：每次挂载在负载开始后采一个 ≥5 s 的 `msgr-worker` CPU 窗口，算 **active-only CV** 写进协变量表；**记录、不停机、不重挂剔除**。同批加 1 个 `msgr=3` 参照挂载专门给判别器标定阈值（不进臂间比较）。依据 F63 + R18。（⚑ 已执行，标定结果见 F66 ④）
6. ⚑ **2026-08-22 由 03-17f 定案：`ms_async_op_threads=8` 批准固化为交付基座的客户端配置项；"新七项基线"读侧四项可固化、写侧三项暂缓**。**（a）msgr=8 固化成立**，四条依据：①因果链完整（注入→线程数逐 cell 验证→饱和解除→带宽，**A36 硬伤①②已修**）；②读侧 randread **+37.4%（好档对好档下界）~ +65.2%（均值）**，与 F65 原估 +44.3%/+46.8% 同向同量级；③**方差塌缩 CV 20.01%→0.79%（25×）**，对生产 SLA 比均值增益更值钱；④写侧中性（+0.7%，两臂写 worker 均未饱和）。**落地表述必须是规则而非数字**：`ms_async_op_threads ≥ OSD数 × 1.33`（6 OSD ⇒ 8，因活跃 worker 恒 = OSD 连接数 = 6，8 个里恒有 2 个闲，余量留给落位抽签），OSD 数变更须重算；注入限**私有 conf + `CEPH_CONF`**（⛔ 不动全机 `ceph.conf`、⛔ 不执行 `ceph config set`，B6 —— 03-17f 已验证 `ceph config dump` md5 全 28 cell 恒 `fd455189`）。**三条限制**：⛔ 仅高并发读有效（低并发零收益，seqread 时 worker 仅 22~24%）；⛔ 不解决目标缺口（randread 5544 = 6250 的 **88.7%**，七项无一达标 ⇒ **必要不充分**）；⛔ 成本 +1.24~1.42 核，生产客户端须留余量。**（b）七项基线**：读侧四项可固化（seqread 1449 / mseqread 5366 / randread 5544 / randrw 读 1870，均在 2.43M 对象对齐态上跑），⚑ 但 `seqread`/`mseqread` 必须随附 hit_rate 标注（**87.0~87.6% / 92.8~93.3% 命中，主要在测 buffer**，故 mseqread 对 03-10 的 +34.7% ⛔ 不可归因为存储路径增益）；⚑ **写侧三项已于 2026-08-22 由 03-17g 解锁固化**（randwrite 2707 / seqwrite 1570 / mseqwrite 4676），附注必须是："系 meta 引擎**慢态**下的值（meta `Write` 延迟 187 ms）；`de93563f` 二进制自身在写侧为 **+5.8%**，不构成代价；同配置在 meta 快态（≈137 ms）下 randwrite 可达 3018 量级" —— 依据 **F68**，⛔ 原"−11% = B-catchup 确定代价"的表述（F67）已撤回，不得写入交付说明。

---

## 三、判据体系（唯一判定口径）

### 3.1 判定式

```
效应成立 ⟺ |median(B臂) − median(A臂)| ≥ 门槛
门槛 = max( 该项可检出效应量 , 2 × 该项同实例漂移底噪(lag1) )
```

- **lag1 口径是唯一可用口径**，因为 A/B 紧邻交错下相邻单元恒为异臂。若允许任意间隔，门槛按全对 max 计算，读侧三项全部失效（F13）。
- 落在门槛内 ⇒ 结论**"无收益"**，不得写成"略有提升"。
- 判据触发后须**再交错一对复核**。

### 3.2 判据表（⚑ 2026-08-11 补齐写侧四项）

| 项 | 可检出效应量 | 同实例底噪 lag1 | **门槛** | 38h 极差（参考） | 裁定 |
|---|---|---|---|---|---|
| `mseqread` | ≥1.5% | 0.50% | **1.50%** | 1.69%(lag8) | ✅ 判据（最灵敏） |
| `randread` | ≥3.0% | 1.40% | **3.00%** | 2.22% | ✅ 判据 + **档位协变量** |
| `randwrite` | ≥4.0% | 2.13% | **4.26%** | 5.00% | ✅ **写侧首选判据** |
| `randrw`（读侧） | ≥3.0% | 2.78% | **5.56%** | 2.78% | ✅ 判据 |
| `mseqwrite` | ≥7.0% | 3.78% | **7.56%** | 4.24% | ⚠ 勉强可用，只判大效应 |
| `seqwrite` | ≥5.0% | **7.25%** | 14.50% | **11.79%** | ⛔ **移出判据集**（F15/F16） |
| `seqread` | ≥4.0% | **3.89%** | 7.78% | — | ⛔ **移出判据集**（F13） |

> 数据来源：读侧 `03-2-…md` §四；写侧 `/tmp/opencode-03-4-bw-raw.tsv`（601 行）+ `rounds.tsv`，由 opencode 从 fio 汇总行独立重算。
> ⛔ 两个移出项的 `ITEMS` 位置可保留用于旁证与异常检测，**不得用于宣告收益或无收益**。

### 3.3 三条硬纪律

1. **紧邻交错强制**：一律 `A B A B A B`，**禁分臂串行**（会使判据全线失效并可能伪造"有效"结论），`A-B-A` 降为退化下限，**禁 A-B 两点比较**，两臂必须同时段。
2. **每臂 ≥3 run 取 median**：门槛余量最薄处只有 0.4-0.5 pp，单对比较的不确定性吃掉全部余量。
3. **报数纪律**：写侧剔首轮（F9）；randrw 标读侧/合计（F10）；`ITEMS` 各臂完全相同且不与全量签收值比（F11）；报告须给**两版底噪** —— 原始极差（**门槛取这版**）+ 回归掉 randread 的残差极差（参考）。

### 3.4 签收基线现状

| 项 | 签收值 (MiB/s) | 状态 | 03-4 同实例实测 median（5 项 ITEMS 口径） |
|---|---|---|---|
| mseqread | 4160 | ✅ 正式 | — |
| randread | 1880 | ✅ 正式 | 1846.0 |
| randrw | 1215 读侧 | ✅ 正式 | 1222.5 |
| randwrite | ≈2860 | ⚑ 暂签 | 2940.5 |
| seqwrite | 1400 | ⚠ 条件 | 1433.0 |
| mseqwrite | 4800 | ⚠ 条件 | 4854.0 |
| ~~seqread~~ | ~~1290~~ | ~~⚠ 条件~~ ⚑ **已转正，见下** | — |

> ⚠ 右列是 `ITEMS = randread randrw seqwrite mseqwrite randwrite`（5 项）口径，按 F11 **不可与左列直接比**，仅供同口径内部参照。
> `randwrite` 转正：需干净池数据点，由 T1 基线臂自然产出（**不能用 03-4 的数据**，其 r1-r3 受 mseqwrite 残留污染）。
> ~~`seqread` 转正：需 ≥3 实例含日间窗口。~~

#### ⚑ 3.4b 签收表订正（2026-08-15）—— 必须标注基座

> 🔴 **本节新增一条硬纪律**：上表全部是 **128K 出厂基座**的签收值。F47 已证 256K 基座下 seqread/seqwrite **各高 +13%/+20%**，randread 高 +115.6% ⇒ **签收值脱离基座即无意义，此后一切签收值必须带基座标注**。

**① `seqread` 转正 ✅（2026-08-15，条件达成）**

| 实例 | r1 | r2 | 判档 ns/B |
|---|---:|---:|---|
| T44B-1 | 1254 | 1247 | 3.442 PASS |
| T44B-2 | 1246 | 1261 | 3.339 PASS |
| T44B-3 | 1240 | 1276 | 3.538 PASS |

- 3 实例、6 轮全部落 week3 平台 [1234, 1324]，跨实例极差仅 **2.9%**，**3 实例均在日间窗口**（08:57→09:24, 157 时间）⇒ §3.4 转正条件（≥3 实例含日间）**达成**。
- 签收值 **1253.5 MiB/s @ 128K 基座**（opencode 已从 fio 原文复算 6 轮，与报数一致）。
- ⚑ **关键限定（DeepSeek 与 GPT 均未标注，opencode 从 `instances.txt` 查实）**：段B 用的是 **stock 二进制 + `max_read=131072`（128K）**，**不是** 256K 候选基座。⇒ ① 该转正只对 128K 基座有效；② 256K 基座下该项为 **1457**（F47），转正须重做；③ **GPT 审计 §三 表把 seqread 记作"1240~1465"是把两个基座的数混在一格**，不可引用。
- ⛔ 转正不改变架构限制判定（§2.4：达标需 41 µs < 网络往返物理下限 200 µs）⇒ **转正即结案，不进调参队列**。

**② `randwrite` 转正条件已实质满足，但须按基座重签**

- 干净池 + 补丁基座数据点已有 **4 挂载**（03-8 T38-A1/A2、03-10 T40-P1/P2），中位 **3003.5 @256K+补丁基座**，落 128K 历史平台 [2942, 3258] 内。
- ⛔ 暂不转正的唯一理由：**F45 环境三层态**——该值只在"池内对象接近基线 2.36M"的健康态成立，中间态 600~1200、塌态 ~550。⇒ 转正须附**对象数前置条件**（起点 ≤3.11M）才有意义。列为收口项。


---

## 四、目标与验收

**⚑ 2026-08-11 用户重新定调**：**目标 = 有效数据带宽达到网卡带宽的一半 = 6250 MiB/s（100GbE 全双工，各向 12500），重点项 randrw。** 并接受 **seqread 单流为架构限制**（§2.4 已证：需 41 µs 延迟 < 网络往返物理下限），重点转多并发项（生产环境即多并发）。

**主目标**：在不重启 OSD、不重铺 layout 的约束下，逐层量出容量余量并消除瓶颈，使多并发项趋近 6250；每项收益都通过 §三 判据。

**次目标**：`randwrite` 转正；把 T1/T2 协议固化进 `TUNING-SKILL.md` 使后续旋钮可直接套用。

| 级别 | 标准 |
|---|---|
| **单旋钮判定** | 满足 §3.1 判定式 |
| **阶段成功** | 至少一项正式签收项（mseqread / randread / randrw）取得可判收益；或给出"在线旋钮空间已穷尽"的清单式结论 |
| **阶段失败但可交付** | 全部旋钮无可判收益 ⇒ 结论"EC 4+2 + FUSE 在在线可调空间内已到位"，剩余空间归因到需重启 / 需重铺 / 需换架构三类，交 04 阶段 |

~~**不设 ≥6250 MiB/s 达标线**：01 阶段已证 FUSE 单进程封顶为架构固有，6250 属 04 阶段范畴。~~
⚑ **2026-08-11 作废**：① 用户明确 6250 就是本阶段目标；② 其立论依据（01-3 单进程 6 核封顶）已被 **F33** 推翻（实测 randwrite 13.73 核）；③ **F35** 证明七项中仅 randread 一项在当前配置下物理不可达，其余六项达标时线速占用 ≤67%。**方法论纪律：可调空间必须由逐层容量余量（roofline）界定，禁止用跨实验、跨口径的终值相减来给调优空间封顶**（此错误于 2026-08-11 发生过一次，见附录 A）。

---

## 五、旋钮盘点

> ⚑ **2026-08-11 优先级下调（P0 → P1）**：本节全部旋钮作用在 **OSD 层**，而该层已被三条实测证据界定为有充裕余量 —— **F37**（设备层 ≥3.7×，落盘 56.6× 吞吐 0）、**F37**（110 GiB BlueStore 缓存全废吞吐 0）、`01-5:161`（OSD 进程 CPU ~1 核未饱和）。
> ⇒ **读侧的 OSD 旋钮先验判低**（按层判，**不是**按读写判：读项本身仍是重点，客户端侧的读旋钮已升 P0）。
> ⇒ **写侧先验未被排除**（F35 只证明写侧网卡放大 ~1.0，未测写侧 OSD 层余量），K3/K4/K5 保留。
> ⇒ 执行方式改为**两级筛查 + 周末无人值守**（03-7），范围不砍项。

### 5.1 判定方法（可复现）

1. 源码判 runtime 可改：`src/common/options/{global,osd}.yaml.in` 是否带 `runtime` flag（`/home/lilingfeng/project/ceph`，⚠ 本地是 main，集群是 v17 quincy）；
2. 实查现值：`ceph config get osd.N <key>` / `ceph config dump`；
3. **落地验证（必做）**：`ceph config set` 后用 `ceph tell osd.N config get <key>` 确认运行态已变，而非只写进 mon config store。

### 5.2 T1 候选：OSD 侧在线可切（全部已核 `runtime=true`）

| # | 旋钮 | 现值 → 试值 | 机制假设 | 目标项 | 预期 | 门槛 | 可判 |
|---|---|---|---|---|---|---|---|
| K1 | `bluestore_default_buffered_read` | `true` → `false` | 旁路 OSD 侧 page-cache 双缓冲，省一次拷贝 | randread / mseqread | 2-5% | 3.0 / 1.5% | ✅ |
| ~~K2~~ | ~~`rocksdb_cache_size` 512 MiB → 2-4 GiB~~ | ❌ **已作废（2026-08-11 桌面核查）**，见 §5.5 与 F28/F29 | — | — | — | ⛔ parked → 04 |
| K3 | `bluestore_throttle_bytes` / `_deferred_bytes` | 64/128 MiB → 256/512 MiB | 写路径 in-flight 限流；02-1 已确认 buffer 压力 sleep 是写劣化根因 | randwrite / randrw | 3-10% | 4.26 / 5.56% | ✅ |
| K4 | `bluestore_prefer_deferred_size_ssd` + `_deferred_batch_ops_ssd` | 0/16 → 65536/64 | EC 4+2 下 256K 写切成 64K/shard，恰等于 `max_blob_size_ssd`；走 deferred(WAL) 批量合并可减少小 IO 落盘 | randwrite | 5-15% | 4.26% | ✅ |
| K5 | `bluestore_max_blob_size_ssd` | 64 KiB → 256 KiB | 加大 blob 降元数据条目数 ⇒ **可能同时缓解 F6 的对象数膨胀**（双重收益） | 写侧 + 拐点位置 | 3-10% | 4.26% | ✅ |
| K6 | `bluestore_csum_type` | `crc32c` → `none` | 削 OSD CPU；读路径也做校验，读写都可能受益 | 全部 | 2-6% | — | ⚠ 需拍板（数据完整性） |
| K7 | `osd_mclock_max_capacity_iops_ssd`（归一化 osd.3） | osd.3 缺失→默认 21500；同伴 63911-79983 | §5.4 异常一 | 全部 | 未知 | — | ✅ 先诊断 |
| K8 | `osd_mclock_profile` | `balanced` → `high_client_ops` | **阴性对照**：源码 `mclock_common.h` 两 profile 的 client `limit` 均为 0(=max)，空闲集群预期 ≈0 | 读侧 | ~0 | — | ✅ **已做，见 §十一.1** |

> K3/K4/K5 的目标项已按 §3.2 改写：原表写 `seqwrite / mseqwrite`，现**首选 randwrite（门槛 4.26%）+ randrw（5.56%）**，seqwrite 移出。

### 5.5 ⚑ 运行时生效性桌面核查（2026-08-11 新增，开工前置）

**为什么必须做**：`ceph config set` 成功 ≠ 旋钮生效。`ceph config get` 只证明写进了 mon config store；`ceph tell osd.N config get` 只证明 OSD 进程的配置视图变了；**都不证明数据路径真的换了行为**。真生效只有两条路径：

- **A 路**：出现在 `BlueStore::get_tracked_conf_keys` / `handle_conf_change`（`BlueStore.cc:5905`）⇒ 变更时有回调重算内部状态；
- **B 路**：数据路径内每 op 直读 `cct->_conf->xxx` ⇒ 天然实时。

**核查结果**

| 旋钮 | 真生效？ | 路径 | 证据 |
|---|---|---|---|
| **K1** `bluestore_default_buffered_read` | ✅ | **B 路** | 读路径内直读 `cct->_conf`（`BlueStore.cc:13129` / `:13547`），每 op 生效 |
| ~~**K2**~~ `rocksdb_cache_size` | ❌ **无效** | 两路都不在 | ① `BlueStore::_open_db` 调 `db->set_cache_size(cache_kv_ratio × cache_size)`（`BlueStore.cc:8262`）置 `set_cache_flag=true`；② `RocksDBStore.cc:554` 的 `if (!set_cache_flag) { cache_size = conf->rocksdb_cache_size; }` **分支永不进入** ⇒ 512 MiB 从未被读取；③ 不在 `handle_conf_change` 列表；④ **即使读了也没用** —— block cache 在 `do_open` 里 `create_block_cache()` 建一次，改配置不会 resize ⇒ **必须重启 OSD ⇒ 撞 F7** |
| **K3** `bluestore_throttle_bytes` / `_deferred_bytes` | ✅ | **A 路** | 两者均在 `handle_conf_change` |
| **K4** `bluestore_prefer_deferred_size_ssd` / `_deferred_batch_ops_ssd` | ✅ | **A 路** | 均在 `handle_conf_change` |
| **K5** `bluestore_max_blob_size_ssd` | ✅ | **A 路** | 在 `handle_conf_change` |
| **K6** `bluestore_csum_type` | ✅ | **A 路** | 在 `handle_conf_change` |
| **K7 / K8** mclock | ✅ | 独立子系统 | K8 已在 03-3 实证可生效（判为无收益是判据结论，不是失效） |

> ⚠ **版本风险**：本地 checkout 是 **main 分支且为浅克隆**（`git log -S` 无法回溯），集群是 **v17 quincy**。但 K2 的裁定**与版本无关** —— 第 ④ 条（block cache 在 open 时建一次、改配置不 resize ⇒ 需重启 OSD ⇒ 撞 F7）在任何版本都成立，因此**不需要 v17 源码复核即可结案**。
>
> 🔴 **纪律（写入 `TUNING-SKILL.md`）**：**任何旋钮进入排期前，必须先做这张表的桌面核查**。K2 的教训 —— 它带着 `runtime` flag、`ceph config set` 会成功、`ceph tell config get` 会显示新值，然后跑 4.5h A/B 得到"无收益"，而这个"无收益"是**假阴性**，会被写进报告当成真结论。桌面核查花 30 分钟，省 4.5h 机器时间 + 一个错结论。

### 5.6 ⚑ `rocksdb_cache_size` 的真实归属（K2 结案调查，2026-08-11）

> 起因：用户指出"这个选项被设计出来必然有用，只是我们的场景不让它生效，需要查清它在什么情况下生效、有没有必要调"。§5.5 只证明了"在我们这里无效"，没回答"它为谁而生"，结论不完整。本节补齐。

**全树只有一处调用 `db->set_cache_size()`**：`BlueStore::_prepare_db_environment`（`BlueStore.cc:8262`，由 `_open_db` 在 `:8278` 调用）。⇒ **凡是不经 BlueStore 的 RocksDBStore 使用者，`set_cache_flag` 都是 false，`rocksdb_cache_size` 就生效**（`RocksDBStore.cc:554`）。

**它的两个真实消费者**

| 消费者 | 怎么用 | 是否 runtime 真生效 |
|---|---|---|
| **① `ceph-mon`（我们集群正在用）** | **它在 mon 里是"比例阀"，不是绝对缓存大小**：`OSDMonitor::_set_cache_ratios()`（`OSDMonitor.cc:1061`）算 `cache_kv_ratio = rocksdb_cache_size / mon_memory_target`，余量再对半分给两个 OSDMap 缓存：`cache_inc_ratio = cache_full_ratio = (1 − cache_kv_ratio) / 2`。三者交给 PriorityCache 管理器 `pcm` 统一伸缩（`pcm->insert("kv"/"inc"/"full")`） | ✅ **是**。`OSDMonitor::handle_conf_change` 跟踪 `"rocksdb_cache_size"`（`:501/:514`）→ 调 `_update_pcm_cache_settings()` |
| **② 离线 / 非 BlueStore 的 KV 使用者** | `ceph-kvstore-tool` 等直接开 RocksDBStore 的路径；**历史上是 FileStore 的 omap DB** —— 这就是这个选项的由来，也解释了为什么它的默认值 512 MiB 是个"OSD 量级"的数 | ✅（离线场景无所谓 runtime） |

**为什么在 BlueStore 里必然失效**：BlueStore 引入统一缓存预算（`bluestore_cache_size × bluestore_cache_kv_ratio`）后接管了 KV 缓存的分配，`_prepare_db_environment` 无条件覆盖。**与 `bluestore_cache_autotune` 无关** —— autotune=true 时初值仍来自这里，之后由 PriorityCache 重新平衡；autotune=false 时就是静态比例。两条路都不读 `rocksdb_cache_size`。

**我们集群的实际数值**（已实查）

```
rocksdb_cache_size     = 536870912   (512 MiB)
mon_memory_target      = 2147483648  (2 GiB)
mon_memory_autotune    = true        ← 默认，比例阀确实在生效
mon_osd_cache_size_min = 134217728   (128 MiB)
⇒ cache_kv_ratio = 512 MiB / 2 GiB = 0.25
⇒ inc / full 两个 OSDMap 缓存各得 (1 − 0.25)/2 = 0.375 → 各 768 MiB
mon 实际堆用量 = 363 MiB（远低于 2 GiB target，比例阀不吃紧）
mon quorum age = 13d（无 map 抖动）
```

**评估：不值得调，且不是"效应低于门槛"，是结构性为零**

| 理由 | 证据 |
|---|---|
| mon **不在 JuiceFS 数据路径上** | 数据路径 = FUSE → JuiceFS → TiKV(元数据) + Ceph OSD(数据，走 librados)。mon 只在 OSDMap epoch 变化时被查询，不参与每个 IO |
| 我们的负载**不产生 map 变化** | `pg_num` 冻结 32、autoscale off、无扩缩容、quorum age 13d |
| 比例阀**不吃紧** | mon 堆用量 363 MiB ≪ `mon_memory_target` 2 GiB ⇒ 三个缓存都没到需要抢内存的地步 |
| OSDMap **本身极小** | 6 OSD / 32 PG 的 map，`inc`/`full` 各 768 MiB 是巨幅过配 |

⇒ **调它 = 改变一个不在数据路径上的守护进程里、三个都不紧张的缓存之间的分配比例。对七项基准的预期效应是结构性 0，不是"测不出"。** ⇒ **parked → 04，且不排测试**。

**复活条件（什么时候它真的重要）**

1. **大规模集群 + OSDMap 高频抖动**（数百至数千 OSD、频繁 up/down）：mon 的 RocksDB 与 OSDMap 缓存会实质影响 mon CPU、`ceph -s` 响应与 map 分发延迟；
2. **mon 内存吃紧**（`mon_memory_target` 成为约束）：这时才需要在 kv 与两个 OSDMap 缓存之间重新分配；
3. **离线大 store 操作**：`ceph-kvstore-tool` 处理超大 mon/osd store 时；
4. FileStore 后端（已废弃/移除）——历史场景。

> 🔴 **顺带记住正确的阀门**：若将来真要调 **OSD 侧** KV 缓存，正确旋钮是 `bluestore_cache_size`（现 30 GiB）与 `bluestore_cache_kv_ratio`（现 0.30），**不是** `rocksdb_cache_size`。但这两个 `level: dev`、**无 runtime flag**，且 block cache 只在 `_open_db` 建一次 ⇒ 必须重启 OSD ⇒ 撞 F7。这是 04 阶段（可接受重启窗口时）的事。

### 5.3 已确认不可在线切（本阶段 parked → 04）

| 旋钮 | 原因 |
|---|---|
| `cephx_sign_messages`（02 计划 A2.1，原 P1 最大项） | 无 runtime flag；只影响新建连接，均匀生效须重启全部 OSD ⇒ 撞 F7。**02 计划排 P1 是在不知 F7 的前提下做的** |
| `ms_crc_data` / `ms_crc_header` | 无 runtime flag |
| `osd_op_num_shards_ssd`(8) / `_threads_per_shard_ssd`(2)（02 计划 A2.3） | 无 runtime flag |
| `ms_async_op_threads` | 无 runtime flag |
| `objecter_inflight_op_bytes` / `_ops` | 无 runtime flag；且实测调大只 +3%（噪声内），已排除 |
| `bluestore_cache_size` / `_cache_kv_ratio` | 无 runtime flag（dev 级，`level: dev`）。已显式设为 30 GiB / 0.30 且 `autotune=false` ⇒ **这两个才是 KV 缓存的真正阀门（实际 ~9 GiB，F29），但都改不了** |
| `osd_memory_target` | runtime=true 但已无空间：§5.4 异常二 |
| ⚑ **`rocksdb_cache_size`（原 K2）** | **带 `runtime` flag 但改了不生效**：block cache 在 `_open_db` 建一次、不 resize ⇒ 需重启 OSD ⇒ 撞 F7。且实际 KV 缓存已是 ~9 GiB（F29），"512 MiB 太小"的前提本身不成立。详见 §5.5 |
| C 线全部（NVMe 队列 / IRQ 亲和 / MTU / TCP） | 需重启服务或撞 157 红线；MTU 9000 还需 IT 确认交换机，且等价全链路重连 ⇒ 撞 F1+F7 |

### 5.4 集群实查发现的两个异常

**异常一：osd.3 的 mClock IOPS 容量缺失，比同伴低 3-3.7×**

```
osd.0 = 78571   osd.1 = 75968   osd.2 = 79983
osd.3 = (config dump 中无此项) → 取默认 21500
osd.4 = 65524   osd.5 = 63911
```

`osd_op_queue = mclock_scheduler`（Quincy 默认），容量值由 OSD 首次启动自测并写回 mon config store，osd.3 显然没写回。诚实的期望：两 profile 的 client `limit` 均为 0（不设上限），空闲集群下 mClock 不硬性截断客户端 op，容量值主要影响 reservation 绝对刻度 ⇒ **预期效应可能很小**。但值得做：EC 4+2 写须集齐 6 shard、读需 4/6，任何 OSD 被结构性压低都会传导到每个 IO，而这是**免费、在线、可回滚**的不对称修正。与长期未量化的 **PG 主分布 9:1 倾斜 + straggler（osd.4 延迟 4-5×）** 同族。⛔ 换 `osd_op_queue=wpq` 需重启 ⇒ parked。

**异常二：`osd_memory_target` 已是 350 GiB/OSD，02 计划 A2.2 作废**

```
host:ceph-node1 = 375831164518 (350 GiB)   node2 = 375831178854   node3 = 377978635264
默认层 = 2147483648（被 host 层覆盖）      osd_memory_target_autotune = true
```

02 计划 A2.2 写"调到 16GB"，实际已是它的 **22 倍**（cephadm autotune 按主机内存分配）。同时 `bluestore_cache_size` 被钉在 30 GiB 且 `autotune=false` —— 两者**语义冲突**。⇒ A2.2 作废，改为 K2：不动总量，只调 KV/RocksDB 那一份的生效路径。

---

## 六、T1：OSD 侧在线旋钮 —— P0

> 本阶段**唯一能拿到 1.5-3% 分辨力**的战役类型，所有能塞进 T1 的都优先塞进来。

### 6.1 协议

```
[前置] 挂载一次 → 探针 randread 75s → 必须落在高档带 [1830,1930]，否则优雅卸载重挂
   ↓ 记录 pid + starttime_ticks（全程比对，禁 forced-mount）
[全程 SKIP_REMOUNT=1，绝不 remount、绝不重启 OSD]
   ↓
紧邻交错（强制）： A B A B A B      ← 不是 AAA BBB
   每个 A/B 单元 = 1 个 run，相邻单元必须异臂，两臂同时段
   ↓
[写侧战役] 每轮起点走 §八 对象数门禁
   ↓
判定：§3.1 判定式
```

**实例同一性与档位处置（F18）**

- 用 **pid + starttime_ticks** 硬判据，**不用带宽值**做看门狗。
- `randread` 每 run 记录，作**外部负载协变量**，不作中止依据。
- 中止线放到远离常态波动处：single-run median **<1700**（落在 F3 多档 1714 与 1334 之间的空隙），且须**连续 2 run** 触发。
- 报告给两版底噪（§3.3 第 3 条）。

**配置变更纪律**

- 一次只测**一个**旋钮；测组合前每个必须单独测过。
- 每次 `ceph config set` **必须配对给出恢复命令**，事先 `ceph config dump > snapshot` 存档。
- `ceph health` 连续 3 次（间隔 5min）非 OK ⇒ 立停。

### 6.2 排期与成本

单项实测耗时（5 轮/run，03-4 标定）：`randread` 28min｜`randrw` 39min｜`seqwrite` 40min｜`mseqwrite` 55min｜`randwrite` 65min。**5 项全量 = 227min，与 10 run 实测 median 230min 吻合。**

| 序 | 战役 | 旋钮 | `ITEMS` | 单 run | 6 run 交错 |
|---|---|---|---|---|---|
| ~~T1.0~~ | ~~协议自检~~ | ~~K8~~ | — | — | ✅ **已完成**（§十一.1） |
| ~~T1.1 原~~ | ~~读侧 OSD 缓存~~ | ~~K2~~ | — | — | ❌ **取消**（§5.5：旋钮不生效） |
| **T1.1** | 读侧缓冲旁路 | **K1** | `randread mseqread` | ~44min | **~4.5h** |
| **T1.2** | 容量归一化 | **K7** | `randread randrw` | ~67min | **~7h** |
| **T1.3** | 写侧限流 | K3 | `randread randrw randwrite` | ~132min | **~13h** |
| **T1.4** | 写侧 deferred | K4 | `randread randwrite` | ~93min | **~9.5h** |
| **T1.5** | blob 尺寸 | K5 | `randread randrw randwrite` | ~132min | **~13h** |
| **T1.6** | 校验和 | K6 | 全项 | ~227min | ~23h ⚠ 需拍板，放最后 |

> ⚑ **2026-08-11 重编号**：原 T1.1(K2) 取消，其后各项依次前移。**读侧只剩 T1.1(K1) + T1.2(K7) ≈ 11.5h**（原 16h），写侧 T1.3-T1.5 ≈ 35.5h。
> 🔴 **K2 的取消是 03 阶段第一个"桌面核查省下机器时间"的案例**：它原本是 P1 期望最高项（预期 3-8%），若照排会花 4.5h 得到一个假阴性。

> ⚑ **写侧 `ITEMS` 已剔除 seqwrite / mseqwrite**，依据：① 两项均已移出或勉强可用（§3.2）；② mseqwrite 是 F23 击穿 10M 红线的唯一成因、F27 起点不对齐的唯一来源、且最贵（55min/run）。剔除后单 run 从 227min 降到 132min（**−42%**），峰值对象数从 10.69M 降到 **~4.1M**（41% 余量），unaligned 轮归零。代价：触发 F11，写侧战役自带基线臂即可，不与全量签收值比。**此项待拍板（§十五 第 3 项）。**

---

## 七、T2：JuiceFS 挂载参数（多实例分布比较）—— ⚑ **P0（2026-08-11 由 P1 升级）**

> **升级理由**：F31（FUSE 拆包 128 KiB vs bs 256k）、F32（并发压缩 ~100×）、F33（6 核封顶作废）三条都指向**客户端路径**是唯一尚未被排除的层，而本节旋钮正作用于此。
> **T2.3（`--max-fuse-io`）已提前单独执行** ⇒ `doc/perf-tasks/03-6-max-fuse-io-and-instrumentation.md`。

> `--max-uploads` / `--buffer-size` / `--max-fuse-io` / `--max-readahead` / `--cache-size` **全部需 remount** ⇒ 每次都是重抽档位（F1）。JuiceFS 侧无在线可切等价旋钮（`juicefs config` 只改 format 级，属 T4）。

### 7.1 设计

```
每臂挂载 N=12 次，A/B 交错（禁分臂串行）
每次挂载：randread 探针 75s → 记录档位 → 跑 ITEMS
   ↓
取每臂**探针落在高档带 [1830,1930] 的子集**（命中率 ~61%，n≈7/臂）
   ↓
比较两臂高档子集的 median
```

**为什么成立**：F4 + 03-1 独立复现 —— 高档带内极差仅 **1.3%**（n=6）。"探针判为高档"这个门控把档位污染压到 1.3% 量级，配合 n≈7 取中位数，残余档位噪声 ≈0.5%，足以支撑 3% 级效应判定。

**⚠ 探针能力边界（03-1 已否证的三件事）**：留一法 r=**−0.1291**、传导增益 0.221、SNR 差 22 倍。⇒ ✅ 探针**仅**用于：确认 randread 自身在高档、确认实例同一性、作外部负载协变量。🔴 **探针归一化已作废**；🔴 不能用探针筛"其它项也在高档"；🔴 不要把探针当看门狗。

**⚠ 读侧挂载参数（`--max-readahead` 类）本阶段不做**：探针本身就是 randread，改读侧参数会同时改探针，门控失效；写侧探针分辨力不足（需 11-29%，而要判的中档只差 4.8-8.8%）⇒ 推迟到 04，或等出现"与读路径解耦又能分辨 5% 档差"的第三种探针（目前无候选）。`--max-readahead 0`（ra0）口径已由用户拍板锁定，本阶段不动。

### 7.2 排期

| 序 | 战役 | 参数 | `ITEMS` | 挂载次数 | 机器时间 |
|---|---|---|---|---|---|
| **T2.1** | 上传并发 | `--max-uploads 150 → 300` | `randread randwrite` | 24（交错） | ~8h |
| **T2.2** | 写缓冲 | `--buffer-size 300 → 1024` | `randread randrw randwrite` | 24 | ~10h |
| **T2.3** | FUSE IO 上限复核 | `--max-fuse-io 128K → 256K` | 写侧先做一半 | 24 | ~10h（读侧另 ~10h） |
| — | 读侧参数 | — | — | — | ⛔ 推迟 04 |

> **T2.3 是 02 计划 B1 的清算项**：02-1b 的"读 +16% 写 +70%"建立在**漂移基线**上（当时写 683↔1760 差 2.6×），必须在收敛基线上重验。这是 02 计划里唯一"已声称大收益但可能是假的"结论，优先级应高但成本最高 ⇒ 先做写侧一半看方向。

---

## 八、T3：写侧对象数预算（T1.3-T1.5 / T2.1-T2.3 的前置约束）

不是独立战线，是**所有写侧战役的物理前置**。

### 8.1 预算算式（⚑ 2026-08-11 依 F19-F27 全部实测值重写）

| 量 | 值 | 来源 |
|---|---|---|
| 对象大小 | **256 KiB 恒定精确** | F19（20,054/20,054 行） |
| 稳定起点（基线） | **2,359,530** 对象 = 576 GiB | prep 建 layout 3×128G + seqread 32G + seqwrite 32G + mseqread 64G + mseqwrite 64G，逐字节对上 |
| **性能**安全上限（轮前） | 3.11M | F6 |
| **性能**拐点区间 | (3.78M, 4.40M] | F6 |
| **单轮 gross 生成量** | `randrw` **0.88M**｜`seqwrite` **1.04M**｜`randwrite` **2.13M**｜`mseqwrite` **3.47M** | F21 实测 |
| 换算系数 | 顺序写 **f=1.000**（256 KiB/新对象）｜随机覆写 **f=0.876**（292 KiB/新对象） | F20 |
| 峰值实测 | **10,691,356**（含 mseqwrite） / **~4.1M**（剔除 mseqwrite+seqwrite 后推算） | F23 / F21 |
| 回收手段 | **只有显式 `gc --compact`**，一遍即可（第二遍逐字节无效） | F25 / F26 |
| 不可回收残留 | 顺序写 ~9.2%（结构性待删切片）；randrw ~0 | F27 |
| **集群容量**安全上界（已验证） | 17.03M / 6.1 TiB / 15.30% 全程 HEALTH_OK | 03-1 |

> 🔴 **两个"上限"不是一回事，不要混**：**3.11M / 拐点 3.78M 是性能拐点**（超过它 randrw 开始掉，影响数据可用性）；**17.03M 是集群容量安全上界**（超过它才有健康风险）。写侧战役同时受两者约束，但违反前者只是数据作废，违反后者才是安全事件。

**峰值口径算式**：`峰值 = 该项轮前起点 + 单轮 gross`。mseqwrite 最坏情形 = r4 起点 7.0M + 3.47M = **10.5M** ⇒ 这就是 F23 击穿的来源。

### 8.2 纪律

1. **门禁逐轮生效，不是逐战役**。轮前查 `OBJECTS`，达标即过；未达标则**立即显式 `gc --compact` 一遍**（不是等待），仍不达标则记 `UNALIGNED` 后**继续**（不中止战役），事后按起点值筛轮。
2. 🔴 **禁止依赖"等待自愈"**。F25 已证池可 8 小时零变化；F14 的 66min 自愈**只观测到一次、机制未定**（附录 A）。原 §8.2"首选办法是等待"**作废**。
3. 🔴 **必须有运行时看门狗**。轮边界闸门物理上看不见轮内峰值（F23）⇒ 写侧战役开工前必须补：由 `pool-tracer` 采样（15s 间隔，实测 20,054 行 0 ERR、最大间隔 16s）触发停机，或把闸门下沉到 fio 运行期间。**这是写侧战役的开工前置**（§十一.2）。
4. **`OBJ_GC_PASSES` 改 1**（F26），每战役省下大量 gc 时间。
5. **报数必须附**：该轮**轮前对象数** + 该 run **峰值对象数**（来自 `pool-trace.tsv`），使事后可做 F6 偏相关校正。
6. **优先用低对象数 `ITEMS`**：剔除 mseqwrite 后峰值从 10.69M 降到 ~4.1M，门禁问题基本消失（§6.2 注）。
7. **A/B 交错是硬要求**：即使有门禁，残余对象数漂移仍与时间序共线，且 F13/F16 已证同实例本身存在与时间共线的漂移。

---

## 九、T4：重铺类（layout / format 级）—— 暂缓

**属于这一类**：JuiceFS **BlockSize**（format 时定，只能建新卷）、format 级 **compress**、**EC profile / 新池**、**pg_num**。

**为什么不能直接测**：重铺 = 换 layout epoch，等价一次超级 compact（对象数归零）⇒ 跨 epoch 绝对值不可比 ⇒ 两臂都必须重铺且 A/B 交错重铺。σ_layout（同参数重铺的批间标准差）**未知**且有理由认为不小：删池重建曾致 32/32 数据 PG 的 acting set **100% 改变**、randread 漂移 16-37%（`00-baseline-20260723.md:223,250,306`）。⛔ 脚本 `phase0_layout`（`FULLBASELINE_V4.sh:722`）含 `rm -rf ${TEST_DIR}/*`（384 文件，10.5min）。

**定案打法**

1. **先做 σ_layout 试点**：同参数重铺 K=4，只跑 `randread randrw randwrite`，6-8h。σ_layout ≤2% → 可做随机区组设计；>5% → T4 全线关闭到 04。
2. **BlockSize 当"验证机制预测"而非"精测"**：256K→1M 是"对象数 ÷ 4"的 **300% 级**效应（F6 给了对象数→性能的定量关系），属验证性实验，K 可压到 2-3。⚑ F19 使这个预测更硬：对象数与字节量严格 256 KiB 挂钩，BlockSize 改 1M 后该恒等式的系数应变为 1 MiB，**可直接用 `stored/objects` 验证是否真生效**。
3. "同池两卷交错测"降级为**备选**：写侧共享池，对象数会被另一卷污染。

---

## 十、暂不做清单与复活条件

### ⚑ 2026-08-11 新增：`--max-readahead` 已从"测量任务"转为"取舍决策"

老集群 `production/doc/perf-analysis/10_A_4` §6.1 的**真冷态 sweep**（128G / 256K block / `--cache-size 0` / 客户端+3 OSD 全 drop_caches）已证明该旋钮**实际是二元的**：

| readahead(MiB) | 随机读 | 顺序读(4M) | 顺序写(4M) |
|---|---|---|---|
| **0（全关）** | **52.0** | 51.9 | 59.5 |
| 1 | 37.7 | 90.3 | 66.3 |
| 4 | 37.5 | 97.1 | 58.6 |
| 8 | 37.4 | 97.1 | 62.0 |
| default | 37.2 | 95.7 | 69.4 |

**随机读在 ra=1/4/8/default 间纹丝不动（37.2→37.7，噪声内），只有 ra=0 才跳变；顺序读完全相反。中间档不存在。** 本集群数据同向：ra0 使 randread +53~91%、mseqread **−50%**。

⇒ **`--max-readahead` 扫档（原计划 A 项）已取消**，不再花机器时间。剩下的是一个三选一决策，**待用户拍板**：

| 选项 | randread | mseqread | 代价 |
|---|---|---|---|
| 保持 ra=2M（现状） | 1768，**6250 物理不可达**（RX 103.5%） | 4161（67%） | randread 永久不达标 |
| 改 ra=0 | ~2700-3400，放大 1.02 ⇒ 6250 可达 | ~2080（33%） | 拿 mseqread 换 randread |
| 按负载分挂载点 | 两者皆优 | 两者皆优 | **业务已否决非单挂载方案** |

**纪律（新增）**：旋钮进排期前，除 §5.5 的源码生效性核查外，**还必须检索 `production/doc/perf-analysis/` 的老集群记录**。2026-08-11 因未做此检索，设计了一个会重跑既有结论的扫档实验（已取消，未耗机器时间）。这是同类失误的第二次（第一次是 K2）。



| 项 | 为什么不做 | 复活条件 |
|---|---|---|
| 需重启 OSD 的一切旋钮（cephx / Messenger / 线程模型 / `osd_op_queue=wpq`） | F7：重启本身 4.5-13% 噪声 > 效应 3-7% | 找到"重启后噪声可复位"的手段；或效应预期 >15%；或接受 24+24 次 sham restart |
| `--layout` / format 级 / EC profile / pg_num | σ_layout 未知，可能 16-37% | §九 试点显示 σ_layout ≤2% |
| 读侧挂载参数（`--max-readahead` 类） | 探针被污染，无可用替代探针 | 出现第三种探针，能分辨 5% 档差且与读路径解耦 |
| C 线（NVMe 队列 / IRQ / MTU / TCP） | 需重启服务或撞 157 红线；MTU 需 IT 确认 | 用户放宽 157 sysctl 红线；MTU 需网络组答复 |
| 档位机制归因 | F2 已主动放弃 | 有新假设且可在不重启前提下检验 |
| **38h 尺度单调退化机理（F16）** | ⚑ **本版新增**：超出作业尺度（战役 ≤14h），交错已压掉 87%，追机理需多实例×多昼夜对照 | 出现单战役必须 >14h 的需求；或退化幅度增大到吃掉判据余量 |
| **`seqwrite` / `seqread` 作判据** | 门槛 14.50% / 7.78% ≫ 效应量 5% / 4% | 找到降噪手段使 lag1 底噪降到效应量的一半以下 |
| **`rocksdb_cache_size`（原 K2）** | ⚑ 在 BlueStore 里不生效；真实消费者是 mon 的缓存比例阀，而 **mon 不在数据路径上** ⇒ 效应结构性为 0（§5.6） | ① 集群规模增至数百 OSD 且 OSDMap 高频抖动；② mon 内存吃紧（`mon_memory_target` 成为约束）；③ 离线大 store 操作 |
| **OSD 侧 KV 缓存（`bluestore_cache_size` / `_cache_kv_ratio`）** | `level: dev` 无 runtime flag，block cache 只在 `_open_db` 建一次 ⇒ 需重启 OSD ⇒ 撞 F7 | 04 阶段获得可接受的 OSD 重启窗口，或找到重启后噪声可复位的手段 |
| 04 阶段候选：io_uring FUSE / kernel mount | 内核 5.15 < 6.1 | 内核升级 |

---

## 十一、执行台账与排期

### 11.1 已完成任务台账

| 任务书 | 承载 | 产出结论 | 报告 |
|---|---|---|---|
| **03-1** | 探针交叉验证 | 🔴 **否证**：写侧探针不能作细粒度门控（留一法 r=−0.129、传导增益 0.221、SNR 差 22 倍）。副产品：传导增益首个定量值、高档带内极差 1.3%(n=6)、高档命中率 ~61%、**17.03M/6.1TiB 安全性验证** | `03-1-probe-cross-validation-20260806.md` |
| **03-2** | 读侧漂移底噪 | **F13**：randread 1.30% / mseqread 0.50%+单调漂移 / seqread 3.89% ⇒ 填上 T1 判据分母，seqread 移出 | `03-2-…md` |
| **03-5** | **T1.1** K1 `bluestore_default_buffered_read` | ✅ **判无效应**（mseqread +0.219% / randread +0.271%，门槛 1.50%/3.00%；升序臂序列 `ABABAB` 完全交织，置换检验 p=0.50/0.60）。**真正产出 = 正对照打靶命中**：命中率 68.94%→0.42%、驻留缓存 109.79 GiB→0、落盘读取 56.6×，吞吐 0 变化 ⇒ **F37 排除设备层与 OSD 缓存层**，并可白赚 **18.3 GiB/OSD** 内存 | 待写 `03-5-*.md` |
| **B 项** | 写侧 TX 放大测量 | ✅ **零机器时间完成**（172 轮归档重算）⇒ **F35**：写侧 TX 放大 1.00-1.34，网卡不是写侧障碍；七项中仅 randread 在 6250 目标下物理不可达 | 并入本计划 §2.4 |
| ~~A 项~~ | ~~`--max-readahead` 扫档~~ | ❌ **取消**（老集群 `10_A_4` §6.1 已做，旋钮是二元的，无中间档）⇒ 转为待拍板决策，见 §十 | — |
| **03-3** | **T1.0** K8 协议自检 | ✅ **协议可信**：K8 判为无收益（预期 0，实得 0）。副产品：**F17 交错抵消 87%**、**F18 randread 是协变量不是看门狗** | `03-3-…md` |
| **03-4** | 写侧漂移底噪（WD1-WD10，38h17min，10 run 全 rc=0） | **F15/F16/F19-F27** 共 12 条。核心：写侧四项底噪测定、seqwrite 移出、对象数模型实测、**OBJ_MAX 红线被击穿（F23）**、**后台自愈不可依赖（F25）** | 待写 |

> 03-4 首次尝试（周五）因 V4 设计缺陷中止（`item_seqwrite`/`item_mseqwrite` 只调 `compact_cooldown` 不含 `gc --compact`，且"意外→中止→等人"在无人值守下必然丢数据）。改造为"记录后继续 + 预授权决策表 + 时间盒"后一次跑通 10 run。归档 `157:/tmp/opencode-03-4-weekend-20260809.tar.gz`（50M）。

#### ⚑ 11.1b 台账续（03-6 ~ 03-15，2026-08-17 Gate 0 补录）

> 全部结论均经 **opencode 从 fio 原文 / 逐秒计数器独立复算**；复算资产 `/tmp/opencode/t36/`~`t314/`。

| 任务书 | 承载 | 产出结论 | 报告 |
|---|---|---|---|
| **03-6** | `--max-fuse-io` 三臂 9 挂载（128K/256K/1M） | 🔴🔴 **本阶段最大收益**：randread **+115.6%**、randrw 各向 **+53.1%**、RX 放大 2.08→1.05。**F39/F40** 确立；边界：仅 IO≥256K 且对齐的随机读有效，多流顺序读 +1.0%，1M ≡ 256K。副产品：**挂载档位判别器（ns/B，分离度 27×）** | `03-6-…-20260812.md` |
| **03-7L** | 写侧 256K + 客户端并发 + OSD K3/K4/K7 | 🔴 **发现 256K 使 randwrite 塌 −82%（5.5×）** ⇒ 参数**不可固化**；randrw 好档对好档 +52.3%（复现 03-6）。**F41/F42/F43** 确立。K3/K4/K7 判**条件性不可判**（randwrite 被 551 钉死 ⇒ 结构性零灵敏度） | `03-7-lite-…-20260813.md` |
| **03-8** | randwrite@256K 崩塌根因修复验证（ABBA 4 挂载） | ✅ **主判据 PASS**：补丁臂中位 **3003.5** vs stock 臂 **551×4**，效应 **+445%**，两臂完全分离（min P 2970 > max S 551），落回 128K 平台 [2942,3258]。randrw 同档位分层无回归（三口径全在 ±5.56% 内）。**F44 首次提出**（写侧 meta 提交率 ~12K/s 墙）。⛔ 偏差：ns/B 判档门未实现 ⇒ **两个坏档跑满全程**（ABBA 恰好对称，主判据幸未受损；此后判档门脚本化强制） | `03-8-…-20260813.md` |
| **03-9** | `-o max_read` 读写分离 + F42 sweep v3 + 负控 | ❌ **段A 全失败但结论有价值**：`-o` 透传在 v1.3.1 **不可用**（FORM1 被静默吞、FORM2 挂载超时），**源码定案** `--max-fuse-io` 硬写 `opt.MaxWrite`、CLI 无 `--max-read` ⇒ **读写分离路线不存在，补丁是唯一路线**（R12 四处查证完毕）。✅ 段B **F42b** 曲线（拐点 j32-64）；段C 负控 bs=128k 仍塌 556 ⇒ 崩塌与 max_write 相关而非 bs | `03-9-…-20260813.md` |
| **03-10** | 补丁版 256K **全 7 项基线**（2 挂载 ×7×2） | ✅ **生产候选基座画像成立**：4 个可判项（mseqread/randread/randrw/randwrite）全 PASS ⇒ 补丁路线累计 **4 挂载**一致。**F47** 首测发现单流顺序项 +13~20%。⚑ 方法论产出：名义偏低 2~6% 系**平台带（同实例底噪 ±1.5/3%）比好档跨实例散布（±3.5%）还窄**所致 ⇒ 改用比值型内部标准（randread÷mseqread 0.956/0.968 vs 历史 0.959）判非劣 | `03-10-…-20260814.md` |
| **03-11** | F44 敏感性 + F42 第二实例 + 坏档率 p 精确化 | ✅ **F44 升格“已点名（客户端侧）”**：三档并发 meta 延迟 84.6→184.1→312.5ms、在飞 511→1010→2007、提交率仅 +6%；PUT 管线空闲。✅ F42 框定到**客户端 file/data reader→rados 同步调用链排队**，但 03-15 复核确认不能继续点名 librados 内部具体队列。✅ p̂=6/20=0.30，合并 12/36=0.333；档位是连续谱 | `03-11-…-20260814.md` + `03-15-gate0-…md` |
| **03-12** | TiKV/PD 服务端指标抓取（F44 归属闭环） | ❌ **归属未闭环**（对象残留 / clock skew / metrics 截断）。F49：关键服务端序列缺失，thrashing 只保留候选。对象数与性能是**强状态协变量**，因果路径未闭环；写类判读须加起点/轮内对象闸门。03-15 已离线实现精确无 `head` 采集器，待后续写任务实采 | `03-12-…-20260815.md` + `03-15-gate0-…md` |
| **03-13** | F42 下载并发扫描（`--max-downloads`） | ❌ 分支结论作废：stdout/awk 字符串比较伪造“破墙”，T2 被错误跳过。真实数据只支持 **F48：200–1024 无可信收益证据**，不支持正式否证；是否补 T2 由 03-16 层级证据决定。已撤销对执行方私改脚本的错误怀疑 | `03-13-…-20260815.md` + `03-15-gate0-…md` |
| **03-14** | 读写共享性 + seqread 转正 | 🔴 **F46**：确认**单挂载内读写共享容量约束**，但撤回固定 4.1 GiB/s librados 硬顶命名。写/读窗口 219s/180s 失配；真并发 0–180s 合计 4152。buffer 峰订正 8→590.4MiB；对象 7.05→7.66M，绝对写容量无效；高写占比秒达 4573–4715。✅ seqread 转正（限 128K 基座） | `03-14-…-20260815.md` + `03-15-gate0-…md` |
| **03-15** | Gate 0（零机器）：证据对账、脚本修复、N1/N2 入口冻结 | ✅ 归档 03-12/13/14；j64/j128 goroutine 六 dump 复算；修 §13.3 10–16；新增 N1/N2 专用 wrapper 与 03-16 GLM 任务书。F42/F46 收敛到可证命名；未执行生产测试 | `03-15-gate0-offline-reconciliation-20260817.md` |

| **03-16** | Phase R：N1/N2 单/双挂载读边界（B2） | **F50/F51/F52 确立**，读侧瓶颈定位到 per-mount；⚑ **该 per-mount 归因已被 03-17e 的 F66 在 ≥128 流区间否证**（等流数下 per-mount 仅占 3.56%） | `03-16-n1n2-read-boundary-20260820.md` |
| **03-17** | librados/messenger 线程（原设计） | ❌ **原设计作废**：判档门与自变量同源 ⇒ 选择性剔除会系统性拔高 worker 少的臂（**R18**） | `03-17-interim-abort-20260820.md` |
| **03-17b/T48** | 挂载好坏档根因诊断 | **F53/F57/F58** 确立：根因 = `msgr-worker` 连接落位抽签 + 单核天花板；判别器定型见 **F63**（active-only CV，5 s 窗口即可定档） | `03-17b-msgr-worker-bottleneck-20260820.md` |
| **03-17c/T47C** | msgr-worker 数量扫描 | **F56**（1438 MiB/s/核，仅饱和区可粗估）、**F59/F60**（对象层读并发 ≡ numjobs，iodepth 惰性） | `03-17c-msgr-worker-scaling-20260820.md` |
| **03-17d/T49D** | 读并发扫描（拉丁方 3×3） | **F61**（单挂载渐近上限 6280，达成率 N/(N+14.5)）、**F62**（主 PG 倾斜 6:6:5:6:5:4，均衡后理论 6267）、**F64**（本批无坏档）、F42b 订正 | `03-17d-read-concurrency-sweep-20260820.md` |
| **03-17e** | Seg A 多挂载聚合 + Seg B 新配置七项 | **Seg A 成立**：**F66**（机器级墙，双挂载聚合 6125.7；否证 03-16 的 per-mount 归因）、**F65**（`ms_async_op_threads=8` +44.3%，机理为 msgr=3 三 worker 96–98% 打满，独立复现 F56 与 F39）；🔴 **Seg B 整段撤回**（**A36** 四条硬伤 + 任务书口径错 **A37/B9**）⇒ 重做 03-17f | `03-17e-multimount-and-new-baseline-20260821.md` |

| **03-17f** | 交付配置七项新基线（03-17e Seg B 重做）+ msgr ABBA | ✅ **msgr=8 批准固化**（**F65** 升级：3v3 同批同二进制、线程数逐 cell 验证、+37.4%~+65.2%、**CV 20.01%→0.79%**、写侧中性、低并发零收益）；~~**F67**（写侧 −11% 定因 = meta/写op 0.91→0.989）~~ **已由 03-17g 推翻，正确定因见 F68**；F56 第四次复现、F58/F62 再证；读侧四项基线可固化，写侧基线已由 03-17g 解锁；B4-18~21 全部修复，新增 B4-23/24 | `03-17f-deliver-config-baseline-20260821.md` |
| **03-17g** | 写侧 −11% 的二进制归因（同日 ABBA，O·N·N·O，仅 randwrite） | ⛔ **F67 整条推翻**（跨计数器错配 + 恒等式当验证，见 A39）；✅ **F68** 立：真因 = meta `Write` 延迟 138→187 ms（+35.6%），二进制反向 **+5.8%**，同日重锚 O 臂 −14.27% ⇒ 缺口全归 8 天环境漂移；**meta 引擎双态**（同挂载内 137→179 ms 跳变）；写侧三项基线解锁固化；B4-24 未复现（本批每轮 cleanup 全回收）；⚑ 新缺口 **B4-25**（obj-gate 只采 post-cleanup，无轮内峰值） | `03-17g-binary-meta-attribution-20260822.md`（md5 `04074719c3f89720916af204ab533b88`） |

**⚑ 本批（03-6~03-14）的两条元结论：**

1. **阶段 03 已从“扫参数”转为“约束作用域闭环”**：读侧 F42/F46（单挂载读饱和 + 单挂载读写共享，具体层级待 N1/N2）＋写侧 F44/F49（客户端 meta 提交率约 12K/s，服务端归属待精确指标）。OSD 侧扫描在两者划界前停止（F37 已证后端余量）。
2. 本批约 4.5h 返工源于脚本/采集缺陷。03-15 Gate 0 已修 §13.3 第 10–16 项并新增专用 N1/N2 wrapper；后续每份任务仍须独立 preflight，不能把“仓库已修”当作线上有效证明。


### 11.2 开工前置（~~当前唯一阻塞~~ ⚑ 已全部解除，见 11.2b）

| # | 项 | 阻塞谁 | 状态 |
|---|---|---|---|
| 1 | ~~**V4 磁盘门禁 20G → 5G**~~ | **一切战役** | ✅ **已完成（08-11）**，新 md5 `4198ea2676ba56744a3cd5eba17a5eab`，已上传 157 两端核对一致。原因：157 根盘 98% 满且由外部租户占据（docker 95G / `/home/server` 70G / weka 48G / turboai 31G / `/var/log` 29G / `/tmp/ray` 24G），**我们全部数据仅 306M**。20G 门禁使任何外部租户写 1G 就能掐死长跑（WD11/WD12 即如此）。清盘无意义也无权限 |
| 2 | 03-5 任务书（**T1.1 / K1** `bluestore_default_buffered_read`） | T1.1 | ✅ **已完成（08-11）** `doc/perf-tasks/03-5-t1.1-buffered-read-k1.md`，318→342 行，6 个 bash 块全过 `bash -n` |
| 3 | **运行时对象数看门狗**（§8.2 纪律 3） | **仅写侧 T1.3-T1.5 / T2** | ⏳ 待做，不阻塞读侧 |
| 4 | `OBJ_GC_PASSES` 2→1（F26） | 写侧（省时） | ⏳ 待做 |
| 5 | 旋钮授权 | K1/K7 ✅ **已批（08-11）**｜K3-K5 待读侧结果 | ✅ / ⏳ |

#### ⚑ 11.2b 当前真实阻塞（2026-08-15 重写）

> 原 §11.2 的阻塞项（门禁/任务书/旋钮授权）已全部过期。**当前阻塞不再是"能不能开工"，而是"两面墙的归属"和"脚本可信度"。**

| # | 阻塞项 | 阻塞谁 | 性质 | 状态 |
|---|---|---|---|---|
| **B1** | 🔴🔴 **F49：TiKV 服务端逐 op 延迟缺失** | **写侧全部分支决策**（F44 归属三选一）；⚑ **2026-08-22 升级为写侧唯一定因路径**：F68 已把写侧 −11% 收敛到"meta `Write` 客户端观测延迟 138→187 ms + 双态跳变"，客户端侧已无可再分解的量（每写 meta 数不变、在飞并发反升、PUT 延迟反降），**下一刀必须落在 TiKV 服务端** | 精确采集设计已在线验证；仓库脚本已实现去 `head`、pair/label 校验、全量 gzip 与 manifest，**尚待生产 preflight/实采** | **P0（原 ⏳ Phase R 后，已提前）** |
| **B2** | ~~🔴 **F42/F46 未区分"单挂载限制 vs 整机共享限制"**~~ | randread/mseqread/randrw 架构判定 | — | ✅ **2026-08-20 解除**：03-16 判定读侧约束在 **per-mount**（F46/F51）；剩余"6280 是单挂载墙还是机器级墙"由 03-17e 收尾 |
| **B3** | ~~🔴 **librados T2 被 03-13 脚本缺陷错误跳过**~~ | 读侧旋钮空间是否穷尽 | — | ✅ **2026-08-20 实质回答**：03-17b/c/d 已把 librados 两旋钮扫穷（F57 `librados_thread_count` 读侧无效；F58 有效 worker 上限 = OSD 数 6，msgr=8 只买抽签稳健性，12/16 ⛔ 不扫） |
| **B4** | ⚠ **脚本缺陷族重开**（原 03-15 "Gate 0 离线关闭"只覆盖到 B4-5；现共 24 项，其中 03-17f 一次修掉 4 项） | 一切后续战役的数据可信度 | 已修：B4-6/7（t46）、B4-8/9/10/13（t47/t49）、B4-11/12（取消 ns/B 停机门）、B4-15（相对路径，t48 未改）、B4-16（`RUNTIME`/`GAP` 硬赋值吞环境变量，t47/t48 仍有、t49 已修并经冒烟验证）。**未修**：B4-1 `t43-f42.sh` stdout 纪律、B4-2 `t42-segD.sh` grep+head、B4-3 `t39-segB.sh` 缺 `SKIP_REMOUNT=1`、B4-4 `t44-night-morning.sh` sample 缺 meta 键、B4-5 I1 key 集合不统一、**B4-14** `t39-nsbgate.sh` 默认 `NSB_TOL=10` 而调用方未导出 ⇒ 历史判档实际按 ±10% 卡（文档写 ±18%）、**B4-17（新，2026-08-21）** `ns/B` 是按 numjobs 归一的量（= `numjobs/bw`），t47/t49 用各臂自身 numjobs 取值却与 `REF_NSB=3.287` 同表 ⇒ 跨 numjobs 判档全为假失败；~~**B4-18**~~ ✅ **2026-08-22 已修（03-17f）** 哨兵 PID 解析错误 + 跨段同路径覆盖 ⇒ 03-17f 改为按实测挂载 pid 采样、逐 cell 独立文件名，7/7 cell 读数与期望一致（702~719 / 267~269 样本）；~~**B4-19**~~ ✅ **已修** 哨兵零值不停机 ⇒ 03-17f 设为读数 0 或 ≠ 期望即 STOP，全程未触发；~~**B4-20**~~ ✅ **已修** V4 单次 remount 致伪重复 ⇒ 03-17f 改为调用 V4 六次、每次 `REPEAT=1 --remount`，得 6 次真独立抽签（B 臂 randread CV 20.01% 正是真抽签方差的证据）；~~**B4-21**~~ ✅ **已修** 回传物不完整 ⇒ 03-17f 交付 V4 全目录 + 哨兵 + 私有 conf，4.99 MB；**B4-22** V4 dry-run 在未挂载状态下做文件存在性检查，产出 4 条假 ⚠️，冒烟检查失去判别力（**未修**）；**B4-23（新，2026-08-22）** 哨兵在 cell 起始会短暂锁到**上一 cell 仍存活**的挂载（03-17f 中 B1 首样本读到 `msgr=8` 而 expected=3，未告警）⇒ 需加"pid 必须晚于本 cell 挂载时刻"约束，低危（每 cell 仅 1 个过渡样本，已剔除）；**B4-24（新，2026-08-22）** `gc --compact` 在 `mseqwrite` 后**两遍零回收**（4,212,534 → 4,212,534 → 4,212,534），池内对象一度冲到 **6,417,590**（对齐基线的 2.64 倍），仅靠 `SOFT-PASS (ABOVE_START_MAX)` 软门放行 ⇒ 03-17f 中 A1/A2 的 randwrite 实际在 4.13M/4.21M 对象上跑（效应 ≤3%，未改变结论，但**是混淆项**），中危，须查 gc 调用与 `TrashDays=0`/延迟删除交互；`instrument.sh` I2b 线程数据坏；`latency-budget.py` write-only 假 40× 读放大 | ▶ 逐战役随手修，⛔ 不回改已归档脚本 |
| **B8** | ~~⚠ 坏档根因未知，卡住全部读侧臂间比较~~ | 读侧臂间比较 | — | ✅ **2026-08-20 解除**：根因 = `msgr-worker` 连接落位抽签 + 单核天花板（F53/F58）；判别器定型见 **F63**，实验设计纪律见 **R18** |
| **B5** | ⚠ **补丁未走上游流程、未做长稳** | 生产候选基座**签收** | — | ⏳ 未开始 |
| **B6** | ⚠ 元数据组件容量类变更授权（TiKV block-cache / 重启） | F44 分支 1 | 生产组件，**须单独取得授权** | ⏳ 未申请 |
| B7 | 运行时对象数**轮内**看门狗（F45 要求，非仅轮边界） | 一切写侧战役的可比性 | 原 §11.2 第 3 项升级 | ⏳ 待做 |

⛔ **K5 暂缓（效应不可逆）、K6 未批。建议：客户端与 meta 两面墙解除前，停止一切 OSD 侧旋钮扫描**（F37 正对照已证余量 ≥3.7×，先验收益极低）。

### 11.3 决策树

> ⚑ **2026-08-15 全面重写**。原决策树（T1.1→T1.2→决策点→T1.3-T1.5→T2）建立在"OSD 侧扫描为主线"的假设上，该假设已被 F37/F41/F42/F44 推翻。原树保留于 §11.3b 以备溯源。

```
[约束已框定、层级待划界]  读侧 F42/F46（单挂载读饱和 + 读写共享）  写侧 F44（客户端 meta 提交率 ~12K/s）
        ↓
┌─────────────────── 03-15：零机器时间（P0，立即）───────────────────┐
│ ✅ ① 归档 03-12/13/14 → results/，记录 bytes/MD5/SHA-256              │
│ ✅ ② j64/j128 dump 对比：确认等待翻倍；不能越权点名内部具体队列       │
│ ✅ ③ 修 t42/t43/t39/t44 + I2b/analyzer/env/I1，新增专用 wrapper       │
│ ✅ ④ Gate 0 报告 + 03-16 GLM 任务书；未使用机器时间                    │
└──────────────────────────────────────────────────────────────────┘
        ↓
┌──────────── 03-16：读侧闭环（只读、低风险，P0）────────────┐
│ ★ N1/N2 单/双挂载边界实验  ← 决定能否达标的分水岭（B2）      │
│   两个独立 pair；各 pair 跑 P/Q solo + concurrent@64/128，3 轮交错 │
│   两挂载读互不重叠范围，各做 2 次 ns/B 门；gate 后冻结 PID/start │
│      ├─ 合计明显扩展 ⇒ 约束偏 per-mount；多挂载仅为架构候选    │
│      └─ 合计仍 ~4.1 ⇒ 墙属整机共享 ⇒ 加 direct-librados 对照 │
│            ├─ direct 能过 6250 ⇒ 墙在 JuiceFS               │
│            └─ 也不能    ⇒ 墙在共享 librados/主机路径         │
│ ⛔ 本任务不混跑写/TiKV/T2/max-downloads；原始包回传后由 GPT 分支 │
└────────────────────────────────────────────────────────────┘
        ↓
┌──────── 03-17：由 Phase R 结果选择唯一边界补测 ────────┐
│ per-mount 倾向 ⇒ 健康池 Phase X：Xref/Xsep（架构控制，非验收）│
│ host/shared 倾向 ⇒ direct-rados 多进程对照；再决定是否跑 T2   │
│ 中间/不一致 ⇒ 按 CPU/NIC/OSD/I1/pprof 补最小复现              │
└────────────────────────────────────────────────────────┘
        ↓
┌──────────── 03-18：写侧 F44（指标先行，P0）────────────┐
│ 数学闸门：6250 ÷ 0.25 MiB = 25,000 提交/s（现 ~12K，差 2×）  │
│ 前置：用已验证采集方案重抓 TiKV 服务端指标（B1，~15min，      │
│       第一轮健康 randwrite 本身即状态测量，禁写前 preprobe）   │
│ ⛔ 拿到服务端逐 op 延迟前不得声称 block cache 是根因          │
│      ├─ 分支1 RocksDB/block cache（4.3GB/节点 vs 工作集 42GB）│
│      │    须 sham restart 测噪声 + A-B-A 块设计 + 单独授权    │
│      ├─ 分支2 单 region/单 store 热点（PD hot-region 确认）   │
│      ├─ 分支3 服务端快而客户端 meta 延迟高 ⇒ 客户端事务流水线 │
│      │    （配置空间基本结束，转批量提交/异步/代码改造评估）  │
│      └─ 分支4 已持续饱和且调优达不到 25K/s ⇒ 出架构结论       │
│ 写侧状态对齐：起点对象 ≤3.11M、轮内看门狗、gc --compact 一遍  │
└──────────────────────────────────────────────────────────┘
        ↓
[后续：按 03-17 选择是否做健康池读写共享复核；不预先捆绑]
        ↓
[汇总]  有过门候选 → 跑一次完整七项基线 + 最终稳定性 → 生产配置建议
        无候选     → ⛔ 不再重复全基线，直接出架构分析交付物
        ↓
[可选] T1.6 K6（需拍板） / T4 σ_layout 试点（需拍板） / 多挂载架构方案（若 B2 判单挂载）
```

**⛔ 停止条件（2026-08-15 确立，防止无限扫参）**

| 项 | 停止条件 |
|---|---|
| seqread | ✅ **已停**：架构限制证据完备（需 41µs < 网络往返下限 200µs），基线已转正 |
| OSD / 设备层 | ✅ **已停**：F37 正对照证余量 ≥3.7×，在两面墙解决前不再扫描 |
| randread / mseqread | 客户端参数均无足够收益 **且** 双挂载能过 6250 ⇒ 判**单挂载架构限制**；双挂载也不扩展 ⇒ 用 direct-librados 划清 JuiceFS 与共享主机/librados 边界 |
| randwrite | TiKV/cache/region 可逆手段不能把持续提交率推近 **25K/s** ⇒ 判**同步元数据事务架构限制** |
| randrw | 受控并发确认读写共享 ~4.1 GiB/s 墙 ⇒ **单挂载下读写各 6250 不可达**（F46 已给出 3.31× 的量化缺口） |

> 每项最终必须落到三态之一：**① 达标 ② 未达标但仍可调（须指出具体队列/参数与理论余量）③ 架构限制（须有 roofline + 延迟/提交率预算 + 边界对照）**。

<details>
<summary>§11.3b 原决策树（2026-08-11 版，已过期，仅备溯源）</summary>

```
[判据分母]  ✅ 齐了：读侧 F13(03-2) + 写侧 F15(03-4)   ← 调优不再缺任何前置测量
[T1.0 协议自检]  ✅ 通过（03-3）
        ↓
[前置 1+2：门禁 5G + 03-5 任务书]
        ↓
[T1.1 K1] → [T1.2 K7]                  读侧 ~11.5h，门槛最好（1.5-3.0%）
        ↑ 原 T1.1(K2) 已于 2026-08-11 桌面核查取消（旋钮不生效，§5.5）
        ↓
   【决策点】读侧是否已榨干？是否值得投写侧 ~35h？
        ↓ 是（且先清前置 3+4）
[T1.3 K3] → [T1.4 K4] → [T1.5 K5]      写侧 ~35.5h
        ↓
[T2.3-写侧] --max-fuse-io 清算 → [T2.1] → [T2.2]
        ↓
[汇总]  有可判收益 → 组合验证（4 臂：基线 / A / B / A+B）→ 生产配置建议
        无            → 结论"在线空间已穷尽"，归因三类，转 04
        ↓
[可选] T1.6 K6（需拍板） / T4 σ_layout 试点（需拍板）
```

</details>

### 11.4 优先级表

> ⚑ **2026-08-15 重排**。原表保留于末尾折叠块。

| 优先级 | 项 | 预期收益 / 产出 | 机器时间 | 风险 |
|:---:|---|---|---|---|
| ✅ | 03-1~03-14（判据分母 / 协议自检 / max-fuse-io / 补丁 / 全基线 / 两面墙定位） | 已完成 | 已花 ~105h | — |
| ✅ | **03-15 Gate 0 零机器时间**（归档 + goroutine 对比 + 脚本/分析器/wrapper + 文档） | B4 离线关闭；F42 收敛到可证边界 | **0** | 0 |
| ✅ | ~~**03-16 Phase R：N1/N2 单/双挂载读边界**（B2）~~ | 已完成（F50/F51/F52）；其 per-mount 归因已由 F66 在 ≥128 流区间否证 | 实花 ~4h | — |
| ❌ **取消** | ~~**Phase X 或 direct-rados（二选一）**~~ | ⚑ **2026-08-21 取消**：F66 证明墙在机器/集群级而非挂载实例级（等流数下 per-mount 仅 3.56%，双挂载聚合封顶 6125.7 = F61/F62 的 97.7~97.8%）⇒ per-mount 旁路（Phase X）与 direct-rados 均无收益空间，读侧封板 | — | — |
| ✅ | ~~**03-17e：多挂载聚合验 F51 + 新配置全量七项基线**~~ | **Seg A 成立**：F51 定案（**F66** 机器级墙）+ msgr=8 增益定量（**F65** +44.3%）+ F63 在 msgr=8 下好档带标定；**Seg B 整段撤回**（A36 四条硬伤 + 任务书口径错 A37/B9） | 实花 Seg A 1.2h + Seg B 4h（后者作废） | — |
| ✅ | ~~**03-17f：交付配置七项新基线**~~（任务书 md5 `7fffb3caf87372450b9e2601fd29b76c`，报告 `03-17f-deliver-config-baseline-20260821.md`） | 四问全部有答：①msgr=8 **可固化**（规则化表述，见 §2.4e 收口结论 6a）；②msgr 效应第三次独立估计 **+37.4%~+65.2%**，写侧首次有证据 = **中性**；③四格仍不可判（128K 行 n=1 且线程未验证，A36 未愈）但 F39 再复现（+114.2% vs +115.6%）；④Gate 0 判 PASS 且其触发被 **F67** 定因 | 实花 ~5h | — |
| ✅ | ~~**03-17g：写侧 −11% 的二进制归因**~~（任务书 `doc/perf-tasks/03-17g-binary-meta-attribution.md`，md5 `52c7e8dd7e08fcc5c844cd6c958fc3ac`） | 定 **F67** 归因：同日 ABBA 两臂（O=`1f60618c` / N=`de93563f`），仅换二进制，`ITEMS=randwrite`、系统默认 conf（msgr=3，= 03-10 原始条件）、`--max-fuse-io 256K`，各 2 个独立挂载，用 03-17f 公式（`transaction_total` 增量 ÷ `fuse_ops_total_write` 增量）复算 meta/写op。**实测 O 臂 0.9139/0.9144（`meta_ops_total_Write` 口径）、0.9948/0.9943（`transaction` 口径）⇒ 命中"归因推翻"分支，且查明 0.91 与 0.989 本就是两个计数器**（F67 撤回，F68 立）。⚑ 关键设计：**按 `/proc/<pid>/exe` 的 md5 验实跑二进制**（R20 等价物，不只验 shim 意图）。⚑ 副产物零成本：O 臂 = 03-10 完整口径 ⇒ **同日重锚 3018.5**，可把"二进制"与"8 天集群漂移"两因子完全拆开 | 实花 ~2 h（4/4 VALID） | — |
| **P0（下一步）** | **B1：TiKV 服务端逐 op 延迟采集（F49），目标收窄为 F68 的两个具体问题** | ①meta `Write` 延迟为何从 138.10 ms 升至 187.32 ms（+35.6%），拆到 TiKV 服务端排队/提交/复制哪一段；②**双态触发条件** —— 同一挂载连续三轮内出现 136.62 → 172.27/179.30 ms 跳变（03-17f NB8），须查是否与 region 分裂/compaction/leader 迁移同步。⚑ 判据现成：`r(BW, 1/延迟) = 0.9151`（n=17），拿到服务端分解后可直接归因。⚑ 936 行方案已在线验证，仅待 preflight + 实采 | ~15 min 实采（+ preflight） | 低 |
| **P1** | 修 **B4-24**（`gc --compact` 在 mseqwrite 后两遍零回收，池内对象冲到 6.42M）+ 新增 **B4-25**（obj-gate 只落 post-cleanup 单点，无轮内峰值） | 否则后续任何含 mseqwrite 的批次，写项都带对象数混淆（03-17f 中 A1/A2 的 randwrite 是在 4.13M/4.21M 对象上跑的）。⚑ **B4-24 在 03-17g 未复现**（4/4 轮 `objects=2434613`、`stored` 逐字节回到原点）⇒ 触发条件疑与 mseqwrite 专有（03-17g 只跑 randwrite），降级但不关闭；⚑ 附带旁证：N 臂（2.43M 对象）2738 vs 03-17f B 组（4.1M+）2661 = **+2.9%** ⇒ 对象数效应 ≤3% 成立 | 待估 | 低 |
| **P0** | **TiKV 服务端指标重抓**（B1，仓库采集器已实现） | 解锁 F44 四分支唯一选择 | ~15min | 低（含一轮写负载） |
| **P1（条件）** | **补跑 librados T2**（B3） | 仅在 Phase R/direct 证据支持该层时验证读侧旋钮 | ~2h | 低（ceph.conf 须强制还原 + md5 校验） |
| **P1** | 受控复核 `--max-downloads` 512（≥3 实例/臂 + semaphore 等待证据） | 确认或否决 F48 的唯一残留线索 | ~4h | 低 |
| **P1（条件）** | 健康池读写共享墙复核（R/W/C 或 Xref/Xsep） | F46 状态依赖与架构控制；双挂载合计不作验收 | ~4–6h | 含写，须对象看门狗 |
| **P2** | F44 选定分支后的写侧实验 | 写侧唯一出路 | 视分支 8~20h | ⚠ 分支 1 需重启 TiKV + **单独授权** |
| **P2** | 补丁上游流程 + 长稳签收（≥8h soak @256K+补丁） | 生产候选基座签收（B5） | ~10h | 低 |
| **P3** | `randwrite` 转正（附对象数前置条件，§3.4b） | 收口 | 0（搭车） | 0 |
| **P3** | `seqread`/`seqwrite` @256K 基座重签（F47） | 收口 | 0（搭车） | 0 |
| **P4** | T1.6 K6 `csum=none` | 2-6% | ~23h | ⚠ 数据完整性，需拍板 |
| **P4** | T4 σ_layout 试点 | 决定 T4 开不开 | 6-8h | 中（`rm -rf` 测试目录） |
| **P4** | 多挂载/分片架构方案设计 | 仅当 B2 判"单挂载限制"时启动 | — | — |
| ⛔ | **OSD 侧 K3/K4/K5 继续扫描** | ❌ **停止**（F37 余量 ≥3.7×，两面墙未解前先验收益极低） | — | — |
| ⛔ | seqread 调参 / 需重启 OSD / C 线 / F16 机理 | — | — | §十 |

> **稳定性专项工作已结束**，此后搭车产出：每个战役的基线臂天然是稳定性样本。⚑ 补充纪律（2026-08-15）：**单战役不超过 14h**（避免跨昼夜漂移）；**可同实例切换的旋钮强制 `A B A B A B`**；**需 remount 的参数每臂 ≥3 个过 ns/B 闸门的实例且两臂同时间窗交错**。

<details>
<summary>§11.4b 原优先级表（2026-08-11 版，已过期）</summary>

| 优先级 | 项 | 预期收益 | 机器时间 | 风险 |
|:---:|---|---|---|---|
| ✅ | 03-2 / 03-3 / 03-4（判据分母 + 协议自检） | 已完成 | 已花 ~60h | — |
| **P0** | 前置 1+2（门禁 + 03-5 任务书） | 解除阻塞 | 开发 ~1h | 低 |
| ~~P1~~ | ~~T1.1 K2~~ | ❌ **取消**（§5.5 旋钮不生效） | — | — |
| **P1** | **T1.1 K1** `buffered_read` | 2-5% | ~4.5h | 低 |
| **P2** | **T1.2 K7** osd.3 归一化（兼诊断 straggler） | 未知，免费 | ~7h | 低 |
| **P2** | 前置 3+4（看门狗 + GC_PASSES） | 解除写侧阻塞 | 开发 ~2h | 低 |
| **P2** | T1.3 K3 / T1.4 K4 / T1.5 K5 | 3-15% | ~35.5h | 低 |
| **P2** | T2.3-写侧（清算 02 计划最大存疑结论） | 高信息量 | ~10h | 低 |
| **P3** | T2.1 / T2.2 | 中 | ~18h | 低 |
| **P3** | `randwrite` 转正 | 收口 | 0（T1 基线臂自然产出） | 0 |
| **P3** | `seqread` 转正（需 ≥3 实例含日间） | 收口 | ~6h（可搭 T2 便车） | 低 |
| **P4** | T1.6 K6 `csum=none` | 2-6% | ~23h | ⚠ 数据完整性，需拍板 |
| **P4** | T4 σ_layout 试点 | 决定 T4 开不开 | 6-8h | 中（`rm -rf` 测试目录） |
| ⛔ | 需重启 OSD / 读侧挂载参数 / C 线 / F16 机理 | — | — | §十 |

</details>


---

## 十二、02 计划条目映射（不丢项）

| 02 计划 | 03 归属 | 变化 |
|---|---|---|
| A1 cluster_network 修复 | 已完成（Z1 生效，`10.3.2.0/24` 已核） | 关闭 |
| A2.1 cephx 分级 | §5.3 parked → 04 | **从 P1 降级**（无 runtime flag + F7） |
| A2.2 osd_memory_target 16GB | **作废** → K2 | 实查已 350 GiB（§5.4 异常二） |
| A2.3 Messenger/线程模型 | §5.3 parked → 04 | 无 runtime flag |
| A3 OSD 扩容 / A4 EC stripe | T4 / 04 | A4 属重铺类 |
| B1 FUSE dispatch（max-fuse-io） | **T2.3**（P2） | 从"已确认收益"改为"待清算的存疑结论" |
| B2 JuiceFS 内部参数 | **T1（K1/K3-K7）+ T2.1/T2.2** | 按切换代价重分，逐项配门槛；⚑ K2 已取消（§5.5） |
| B3 多实例可扩展性 | 04（依赖 A 线） | 不变 |
| B4 io_uring / kernel mount | 04 | 内核 5.15 阻塞 |
| C1-C4 | §十 暂不做 | 撞红线 / 需重启 |
| D1 randwrite-ow 波动分析 | **已完成**（02-2 稳定性攻坚是它的超集） | 关闭 |
| D2 大 block-size 基线 | 04 | 新口径，与本阶段基线不可比 |
| D3 细粒度并发扫描 | 04 | 并入 B3 |

---

## 十三、脚本状态与待改

### 13.1 当前版本

| 项 | 值 |
|---|---|
| `FULLBASELINE_V4.sh` | **md5 `4198ea2676ba56744a3cd5eba17a5eab` / 1368 行**（2026-08-11 磁盘门禁 20G→5G）｜✅ 已上传 157 并核对一致 |
| 历史 | ~~`dec5ee132fd6be25bbe744c6024466f1`~~（03-4 改造后）｜~~`4551ef3c0d405734ea0a4a281427a989` / 1373 行~~（有 PLATEAU 等待逻辑的旧版）｜~~`3fd1281fea1c08342051d64fc8eb1348` / 1272 行~~（无 `obj_gate`） |
| 🔴 纪律 | 上传到 157 后**必须在 157 上再算一次 md5 核对**（03-4 起跑前已因此发现 157 仍是旧版） |
| ⚑ `probe/instrument.sh` | **md5 `d41d2a77eb116a121c8f4a10fc6702b4 ⚑（08-12 修订：I2 改为每次迭代重解析 PID + 新增 pid 列；旧 3cb7b53d… 作废）`**（2026-08-11 新建）｜I1-I4 瓶颈定位采集器，**只读**，与 V4 解耦（不动 md5 锚点）｜✅ 已上传 157 并核对一致，已冒烟通过（5 采样器 / 18 类产物 / 12 个 I4 JSON 全可解析） |
| ⚑ `analyze/latency-budget.py` | **md5 `ff793241c23afc622fd79d60190cd4f9 ⚑（08-12 两次修订：randrw 读写分列 + i2_cores 表头驱动；旧 3106208502… / f1b46376… 均作废）`**（2026-08-11 新建）｜跨层延迟预算提取器｜✅ 已在 172 轮归档数据上验证（PUT 次数 1,011,328 = 1405 MiB/s×180s÷256KiB 精确吻合） |
| 🔴 纪律 | **不留多份脚本副本**（wrapper 是编排器不是副本，允许） |
| 🔴 纪律 | **任何战役必须搭车 I1-I4**（用户 2026-08-11 要求：调参 run 必须同时产出瓶颈定位数据） |
| 🔴 纪律 | 长跑期间冻结本体，改动后 md5 须同步到 `FULLBASELINE-SKILL.md`、本计划、后续每份任务书的校验行 |

### 13.2 03-4 已落地的改造

1. `item_seqwrite` / `item_mseqwrite` 循环内 `compact_cooldown` → `aggressive_cleanup`（含 `gc --compact`），与 randrw / randwrite 一致；
2. `obj_gate()` 整段替换：**删除全部 PLATEAU / OBJ_TIMEOUT / 30min-sleep 等待逻辑**（F25 证明等待无效）。新逻辑 = 取数 → 查 `OBJ_MAX`（唯一硬停）→ 达标则过 → 否则显式 `gc --compact` → 仍不达标则 **SOFT-PASS**（记 `UNALIGNED` 后继续）；
3. 新增 `OBJ_GC_PASSES` / `OBJ_GC_SETTLE`；新增顶层 `pool_sample()`（取数失败重试 5×30s）；
4. wrapper 时间盒化：连续 2 次 rc≠0 才停、tracer 自愈重启、`drain_ladder` 阶梯排空。

> **设计原则（已验证有效，写入 `TUNING-SKILL.md`）**：无人值守长跑必须"记录后继续 + 预授权决策表 + 时间盒终止"，不能"遇意外就中止等人"。

### 13.3 待改清单

| # | 改动 | 理由 | 优先级 |
|---|---|---|---|
| 1 | **磁盘门禁 20G → 5G** | §11.2 第 1 项 | **P0** |
| 2 | **运行时对象数看门狗**（轮内峰值可见） | F23 红线被击穿 | **P2**（写侧前置） |
| 3 | `OBJ_GC_PASSES` 2 → 1 | F26 第二遍逐字节无效 | P2 |
| 4 | `PROGRESS.txt` 的 `randread_median` 口径统一 | 与 `rounds.tsv` **10/10 不一致**（系统性偏低 0.16-0.59%，如 WD3 报 1850 实为 1861），疑取了 `_bw.1.log` 逐秒均值 | P2 |
| 5 | `rounds.tsv` 表头修正（现 5 列，数据实为 8 列 `LABEL round BW_MiBs hit status pg_gate pg gear`） | 易误读 | P3 |
| 6 | `summary()`(:874) / `steady_state_eval`(:1015) 硬编码 7 项 → 读 `ITEMS` | 缺项走 `NA`，不崩不误判但输出难读 | P3 |
| 7 | 清理 4 处重复函数定义（`drop_caches` / `compact_cooldown` / `aggressive_cleanup` / `mkdir -p`） | 当前无害（后定义生效且函数体一致） | P3 |
| 8 | 交错编排 wrapper（`A B A B A B` 逐 run 交替 `ceph config set` + 记录恢复命令） | §6.1 | ✅ 已走通（03-1/03-3/03-4 模式） |
| 9 | 每 run 前后存 `dump_mempools` + `perf dump` | H1 观测 | ✅ 已采集，结论未定（附录 A） |

#### ⚑ 13.3b 新增待改（2026-08-15，全部由 opencode 独立复现根因）

> 🔴 **本批共造成约 4.5h 机器时间返工 + 1 条被伪造的结论 + 1 次对执行方的错误怀疑。脚本纪律已是本阶段最大的效率损失来源。**

| # | 脚本 | 缺陷与**已复现的**根因 | 后果 | 修法 | 优先级 |
|---|---|---|---|---|---|
| **10** | `t43-f42.sh` | 🔴 **stdout 污染 + awk 字符串比较** ⇒ **伪造结论**。三层叠加：① `mount_and_run()` 把实例行/判档行/progress 行/bw 全写 stdout，`B3=$(...)` 得到多行串；② `DELTA` 的 awk 除零产出**空串**；③ 分支 `awk -v b="$B3" 'BEGIN{exit !(b>=4200||d>=3)}'` —— b 是字符串 `"T43A-md1024 opts=…"`，awk 做**字符串比较**：`"T43A…" >= "4200"` 因 `'T'(0x54) > '4'(0x34)` **返回真** ⇒ 走"破墙"分支。**opencode 已用仓库版脚本原样复现该输出** | F42 被误判为"下载并发墙"（附录 A A24）；**librados T2 被跳过**（B3）；⚑ **并据此错误怀疑执行方私改脚本 —— 该怀疑已撤销**（原推断误以为 b=0，实为污染串） | ✅ 03-15 已修：函数改写纯数字结果文件，正则校验，awk `+0`；补 EXIT 还原、统一 I1 和全部 bw logs | **关闭** |
| **11** | `t42-segD.sh` | 🔴 **`head -80/-200` 按 exposition 顺序截断**；宽前缀匹配 11,340 行/端点/次，调大 head 仍会失败 | F49 归属不可判；写侧分支无法选择；已耗约 5h | ✅ 03-15 已实现：TiKV/PD 端点分离，精确指标名，**完全移除 head**；fio 前按 endpoint 校验 histogram sum/count label 集合及 plain metric；保留全量 pre/post gzip、1Hz 追加文件和实际行数 manifest。设计已有在线取样依据，仓库实现仍待生产 preflight | **离线关闭** |
| **12** | `t39-segB.sh` | 缺 `export SKIP_REMOUNT=1` ⇒ 判档门的 V4 探针之后 V4 **自行 remount** ⇒ sweep 跑在未判档第二实例 | 判档闸门形式失效两次 | ✅ 03-15 已修：`SKIP_REMOUNT=1`，保存并在 anchor/每点前强校验 PID+starttime，不一致 STOP | **关闭** |
| **13** | `t44-night-morning.sh` | 效应 I1 缺 meta/read-size 等键；pre 快照在候选重挂前；并发 fio 无 bw logs | F46 meta 率非实测、效应期不可复核 ns/B、快照易误读、缺权威逐秒口径 | ✅ 03-15 已修：统一 I1 键、快照移到过门候选生效后、全量 bw logs；t43/t46 同步统一 | **关闭** |
| **14** | `instrument.sh` I2b | i2-threads 数据坏（utime 全 0，comm 含空格时 `$14/$15` 错位；采样间隔证据不足） | 线程级 CPU 证据无效两次 | ✅ 03-15 已修：以最后一个 `) ` 切分 proc stat，正确读 field 14/15，meta 记录实际间隔 | **关闭** |
| **15** | `latency-budget.py` | `GET/IO` 对 write-only 项取伴生读 op 作分母，制造约 40× 假读放大 | 曾误读写侧存在巨大读放大 | ✅ 03-15 已修：`randwrite/seqwrite/mseqwrite` 的该列为 NaN/NA | **关闭** |
| **16** | `t42-segD.sh` 静置探针 | 60s “静置探针”本身是 randwrite，会污染随后对象/元数据状态 | 状态检查与效应轮不独立 | ✅ 03-15 已取消 mutating preprobe；只记录 health/compact 后对象状态，第一轮正式健康 randwrite 才是状态测量 | **关闭** |
| **17** | V4 `FULLBASELINE_V4.sh`（⛔ 不得修改，仅记账） | ① `PROGRESS.txt` median 口径与 `rounds.tsv` **10/10 不一致**（同 §13.3 第 4 项）；② `rounds.tsv` 表头 5 列 vs 实际 8 列；③ `summary()`/`steady_state_eval` 硬编码 7 项；④ 4 处重复函数定义；⑤ **item 之间会重挂** ⇒ 03-7L 段2 实际是 8 个实例、每项各抽一次档 | ⑤ 曾致"4 挂载"被误记 | 待 V5；⑤ 须写入任务书编写指南（每项一次档位抽签） | P2 |


---

## 十四、口径与红线

### 14.1 测试口径（已固化在 `skills/FULLBASELINE-SKILL.md`）

- **验收口径**：逐秒均值，窗口 `15 ≤ sec − t0 ≤ 175`（**全局 t0**，非每文件 t0 —— 后者有 1.3% 系统性偏差）；取该项**逐轮中位数**。不认 fio 汇总 BW（`rounds.tsv` 的 `BW_MiBs` 列是 fio 汇总值，**不是验收口径**）。
- **方向位**：randrw 必须过滤方向（V4 `:979` 取读侧），报数标口径（F10）。
- **首轮规则**：`randwrite` / `randrw` 首轮单独记录、不进 L2 统计（F9）。
- 固定口径：block size 256K（对齐 EC stripe_width）、openfiles=128、`--max-readahead 0`、`--cache-size 0`、pool 3 EC 4+2 pg_num=32（冻结、autoscale off）。
- **门控前置**：任何 A/B 对比前必须探针门控并固定实例（F1）。
- **ITEMS 纪律**：子集绝对值 ≠ 全量绝对值，每战役自带同 `ITEMS` 基线臂（F11）。

### 14.2 取数纪律（踩过的坑）

> ⚑ **2026-08-11 新增 4 条**：
> 1. **`%util` 对 NVMe 无意义**（仅表示 ≥1 请求在飞），禁止用它判断设备饱和；判饱和用"加倍负载看吞吐是否掉"（F37 的方法）。
> 2. **`c_amp` 只算 RX÷读字节**（`compute_c_amp`），写侧放大必须自行从 `nic.txt` 第 9 字段（`/proc/net/dev` TX bytes）取；`nic.txt` 采的接口是 **`enp139s0f0np0` = 100GbE public**（数据面确实走 100GbE，`02-2h-e6:1169` 的"走 eno12399"说法不成立）。
> 3. **放大率是档位免疫的（CV 0.04-0.94%），吞吐不是（跨实例极差 29.9%）** ⇒ 放大率类效应可用少量挂载判定，吞吐类必须走完整 T2 协议。
> 4. **旋钮进排期前必须检索 `production/doc/perf-analysis/`（老集群记录）**，否则会重跑既有结论（2026-08-11 已发生一次）。


- `ceph df --format=json` 的 `stored` / `max_avail` **全是字节**：`/2**30`=GiB、`/2**40`=TiB。同类量必须同口径 —— 曾把 `906,484,580,352 B`（906 **GB** = 844 GiB）当成 906 GiB。
- 取对象数**必须** `ceph df --format=json` + `python3`（157 **无 `jq`**）；不许 parse `2.36M` 这种缩写；取数失败**不许回落成 0**。
- `pgrep -c fio` 会匹配内核线程 `vfio-irqfd-clean`（恒 ≥1）⇒ 必须 `pgrep -af fio`。
- `perf dump` 全是**累计计数器**，必须取增量再算相关（直接用会得到伪相关）。
- 🔴 **一切除法/百分比/极差/CV/底噪/判据判定由 opencode 做，执行方只出原始表 + TSV**。依据：GLM 在 03-4 期间出过四个数值错（排空耗时 300s→实为 106s、峰值 906GiB→844GiB、排空速率 1.02→2.30 GiB/s、回收率 3324→9408 obj/s），另有 `randread_median` 口径不一致（§13.3 第 4 项）与磁盘占用误报（称 fio 日志占 20G，实为 306M）。
- `gc` 日志要**完整统计行，禁 `tail -3`**（旧 V4 的 `tail -3` 正是长期看不清 gc 行为的原因）。

### 14.3 红线（`skills/SYSTEM-SAFETY-SKILL.md`）

1. ⛔ 禁 `pkill -f fio` / `pkill -f juicefs` / `killall`（157 是 **3075 人共享机**，跑 WekaIO/K8s）
2. ⛔ 禁 `--layout`（含 `rm -rf ${TEST_DIR}/*`）、禁 `--allow-restart`
3. ⛔ 不重启 157、不重启任何 OSD；不动 157 内核 / 100GbE NIC / md0 / WekaIO
4. ⛔ 不改 IRQ / RPS / pg_num（autoscale 保持 off）
5. ⛔ **不动 `/tmp/ray`**（24G，owner `server`，外部租户）及任何非我们创建的目录
6. ⚠ `ceph config set` 属改集群配置：**每个旋钮单独授权**，须附配对恢复命令，事先 `ceph config dump` 存档
7. ⚠ `ceph health` 连续 3 次（间隔 5min）非 OK ⇒ 立停上报
8. ✅ 已授权 sudo：`ceph tell osd.N compact`、3 节点 `drop_caches`、`juicefs gc --compact`、`umount`/`mount`
9. 157 的 `/tmp` 易失 ⇒ 每个战役结束**当天**归档到 `results/`

### 14.4 长跑红线（03-4 验证有效，5 条）

① `objects` > 上限｜② health 连续 3 次非 OK｜③ 实例变（pid / starttime_ticks 变化或 `forced-mount`）｜④ `/tmp` < 5G 或 `max_avail` < 10 TiB｜⑤ 连续 2 run rc≠0 或连续 2 run randread median < 1700。**其余一切"记录 + 继续 + 事后报告"。**

---

## 十五、待拍板

| # | 问题 | 建议 |
|---|---|---|
| ~~1~~ | ~~授权哪些 `ceph config set` 旋钮？~~ | ✅ **2026-08-11 用户已批 K1 + K7**。K1 授权块见 `doc/perf-tasks/03-5-t1.1-buffered-read-k1.md` §旋钮授权（含配对恢复命令）；K7 待 03-6 任务书。写侧 K3-K5 待读侧结果出来后再批 |
| **2** | V4 磁盘门禁 20G → 5G（动脚本本体，md5 会变） | 建议批。不改则一切战役无法起跑，且清盘既无意义（我们占 306M）也无权限（其余是外部租户） |
| **3** | 写侧 `ITEMS` 是否剔除 `seqwrite` + `mseqwrite`？ | **建议剔除**。收益：单 run −42%、峰值对象数 10.69M→~4.1M、unaligned 归零、三个写侧战役共省 ~28h。代价：触发 F11（需自带基线臂，不与全量签收值比）；失去唯一的大块写项 |
| 4 | K7 给 osd.3 补 mclock 容量 | 建议授权。这是**修一个明显的配置缺失**（21500 vs 同伴 63911-79983），即使无性能收益也应修 |
| 5 | K6 `bluestore_csum_type = none` 是否可测？ | 涉及**数据完整性**。建议先不做，仅在 K1-K5 全无收益时评估，且测完立即改回 |
| 6 | 接受"一次只测一个旋钮 + 组合前各自单测"的时间成本？ | 建议接受，否则出了差异归因不了 |
| 7 | T4 σ_layout 试点（6-8h，含 `rm -rf` 测试目录）是否排？ | 建议暂不排，等 T1 结果。BlockSize 是 300% 级效应，值得做但要等在线空间榨干 |
| 8 | T1 是否强制排夜间？ | ⚑ **建议放宽**。原理由是"档位稳定只在夜间测过"，但 03-3（日间 4.5h）+ 03-4（38h 跨昼夜）已覆盖日间，且 F17 证明交错抵消 87% 的时段漂移。**改为：不限时段，但两臂必须同时段、且不得跨周末边界比较绝对值（F16 周六/周日 seqwrite 差 5.88%）** |

---

## 附录 A：已作废 / 已降级的结论（防止复引用）

### ⚑ 2026-08-11 新增撤回（6 条，含 1 条本方法论错误）

| # | 被撤回的结论 | 原出处 | 撤回依据 |
|---|---|---|---|
| A18 | ~~"JuiceFS 单进程 CPU 封顶 ~6 核（595%），多实例才能倍增，属架构固有"~~ | `01-3:13,63-66`；被 03 计划 §四引用为"6250 属 04 阶段"的依据 | **F33**：实测 randwrite **13.73 核**、randrw **10.09 核**。595% 是该负载的反馈平衡点，不是进程上限。01-4 的 `CPU=5.43+0.044ms×IOPS` 模型仅对当时的 randread 成立 |
| A19 | ~~"EC 4+2 下磁盘 IOPS 饱和：per-OSD 290 MB/s，%util=100.4%，磁盘 BW 非瓶颈但 IOPS 是"~~ | `01-5:131-148,688-689` | **F37**：实测持续 **1073 MB/s/OSD**（该值 3.70×）且吞吐纹丝不动。`%util` 对 NVMe 仅表示"≥1 请求在飞"，不是饱和度 |
| A20 | ~~"C_amp 2.08 = EC 4+2 fast_read 的 1.5× 读放大 + JuiceFS 客户端预读 0.5×"~~ | `02-2h-e6-v4-fullbaseline-20260803.md:677` | **F38**：同一 EC 池上 seqread/mseqread 放大率 = **1.02**。EC 若放大客户端字节，顺序读也应 2.5×。2.07 全部是 readahead 浪费 |
| A21 | ~~"2.5× 放大的真主因 = EC 取片 + librados 协议帧（strace 每 256K 读 = 139 次 socket read ≈ 4 OSD × 35）"~~ | `production/doc/perf-analysis/10_A_4-readahead-prefetch-split-test.md:186-187` §6.2 第 4 条 | 同 **F38**。strace 数的是 **syscall 次数**，不是字节；字节级测量（NIC 计数器）证明 EC 贡献为 0。⚠ 该文 §6.1 的 **sweep 数据仍然有效且已被采纳**（见 §十） |
| A22 | ~~"`rocksdb_cache_size` 无用"~~ | 本文 §5.5 初版 | 降级为"**并非无用**，它是 ceph-mon 的缓存比例阀且 mon 里 runtime 真生效；只是 mon 不在 JuiceFS 数据路径上 ⇒ 对七项效应结构性为 0"。见 §5.6 |
| A23 | 🔴 ~~"JuiceFS 单挂载 2404（01-2d）− 当前 1848 = −23%，是 03 阶段唯一有意义的靶子"~~ | opencode 于 2026-08-11 会话中提出 | **本方法论错误由用户当场否决。** 两数之间隔着池重建、cluster_network 修复、数据量增长、并发档位差异，跨实验相减既不能证明回退、更**不能给调优空间封顶**。正确做法 = 逐层容量余量（roofline），见 §四纪律 |

### ⚑ 2026-08-15 新增撤回（4 条，含 2 条本方错误）

| # | 被撤回的结论 | 原出处 | 撤回依据 |
|---|---|---|---|
| **A24** | 🔴 ~~"`--max-downloads` 已破 F42 墙（1024 档达标）⇒ F42 = 下载并发墙，librados T2 可跳过"~~ | 03-13 脚本分支输出 + 报告 §2 | **结论由 stdout/awk 字符串比较缺陷伪造**。真实数字 3867/4079/3833 中 1024 未达预登记触发线；但单实例单轮和 ns/B 档位混入也不允许反向正式否证。最终改判 **F48：200–1024 无可信收益证据**。T2 脚本已修，是否补跑由 03-16 层级/在飞证据决定，不再无条件排期 |
| **A25** | ~~"03-13 的 157 侧脚本与仓库版不一致，执行方（GLM）可能私自改过分支逻辑，须其声明"~~ | 03-13 报告 §6.1 复核裁定（DeepSeek 侧） | 🔴 **本方错误，对执行方的怀疑撤销。** 原推断假设 `b=0、DELTA="" ⇒ 条件为假`；实际 b 是**被污染的多行字符串**，awk 对其做**字符串比较**（`"T43A…" >= "4200"` 为真）⇒ **仓库版脚本原样即可复现"破墙"输出**，无需任何私改。执行方按脚本输出执行无误 |
| **A26** | ~~"03-12 TiKV 指标缺失的根因 = 脚本 grep 前缀按旧版 TiKV 命名假设（`tikv_grpc_msg_duration` 等），核心指标名写错"~~ | 03-12 报告 §12.1（DeepSeek 侧） | 🔴 **本方错误。** 仓库版 grep 前缀**本来就正确**（已含所需全部指标族）。真根因是 **`head -80` 按 exposition 顺序截断**：opencode 用**正确前缀 + `head -80`** 实拉端点**精确复现出同样 3 个指标**（4/1/75，75 行被 `tikv_engine_bloom_efficiency` 的 label 展开吃光）。⚑ **连带作废原修法**："`head -80`→`-200`"会第 4 次失败（该前缀匹配 11,340 行/端点/次）——正确修法见 §13.3 第 11 项 |
| **A27** | ~~"03-14 段A 读写并发合计 = 3920 MiB/s（读 2931 + 写 989）"~~ | 03-14 报告 §2/§7.1 | ⚑ **数值订正（结论方向不变且更强）。** 两侧 fio **窗口失配**：写跑 219s、读仅 180s（DeepSeek 与 GPT 均未察）。逐秒重算真并发窗口（0-180s）为 **读 2949 / 写 1203 / 合计 4152**；尾部 39s **近乎空转**（读 32/写 12 MiB/s，系 fio 收尾 flush），**不是"独占写"**。⇒ 合计更贴近 F42 墙（4.1 GiB/s）。新增两条单实验内证据：逐秒 **r = −0.899（n=178）**、**合计 CV 7.9% vs 读 18.9%/写 59.9%** ⇒ F46 成立强度提高，且不再依赖跨实验对照 |

> ⚑ **A25/A26 的共同教训（已并入 `RULES.md`）**：**"脚本行为与预期不符"的第一假设必须是"我对脚本语义的理解错了"，而不是"执行方改了脚本"或"环境命名不同"。** 两次都是在**未实际复现**的情况下就给出了根因判断，其中一次还据此怀疑了执行方。**新增规则 R15：任何指向他方的根因结论，必须先在本地/线上复现该行为，复现不出则只能记为"待查"。**

| **A28** | 🔴 ~~"randrw 两向 = 1923 MiB/s（占目标 30.8%，需 3.25×；128K 基线 1210）"~~ | 本文 §2.4d 初版 + 周报 20260816 初版 §零/§四 | 🔴 **本方口径错误（用户当面质疑"两向为何完全一致"后自查发现）。** 1923 **不是任何原始数据的统计量**，而是由跨会话保守幅度 +51.4% 反推（1270×1.514=1922.8）。按 03-10 原文重算：读 = 1961/1888/1854/1859（均值 **1890.5**）、写 = 1961/1889/1855/1859（均值 **1891.0**）⇒ 占目标 **30.2%/30.3%**、需 **3.31×**；128K 基线改用同期同条件好档实例 03-7L WF2（1273/1275 ⇒ **1274**），效应 **+48.4%**（好档受控对照 03-7L WS1÷WF2 = 1940÷1274 = **+52.3%** 作为上限保留）。**结论方向不变。** ⚑ 附带核实：两向差 ≤0.03% **是负载构造的必然结果**（50/50 等量混合、同块大小、同 job 下发），58 个 randrw 轮次全部满足读写差 ≤1 MiB/s ⇒ 非笔误 |

> ⚑ **2026-08-15 订正（A29，与 A28 同批自查，性质为"口径统一"而非"数值造假"）**：
> ① **randread**：原登记 4058 = **跨会话 11 个好档观测的中位**（3818, 3823, 3833, 3844, 3867, **4058**, 4064, 4073, 4079, 4087, 4094；中位已复算 = 4058，推导正确）。该集合混入了 03-11 并发扫描档与 03-13 `--max-downloads` 扫描档（参数与候选基座不同）⇒ 表值统一改用 03-10 四点均值 **3831（61.3%，1.63×）**，跨批次实测改列为印证区间。
> ✅ **03-6 归档已于 2026-08-15 从 157 取回并逐点复核**（`157:/tmp/opencode-fullbaseline-v4/T36-*` → 本地 `/tmp/opencode/t36/`，tar md5 `7f217978bd0ce093e68ea820598584ec`，9 实例 ×3 项 ×2 轮 = 54 点）：**128K 臂 randread = 1882.2**（1882/1878, 1895/1886, 1871/1881）、**256K 好档臂 = 4059.5**（4032/4064, 4059/4083）⇒ **+115.7%，与原报的 +115.6% 一致，4058 属实**；坏档实例 B1（2901/2957）不剔除则为 +95.7%。**randrw：128K 臂 1269.8（即周报初版的 1270 有据）、256K 好档臂 1945.2 ⇒ +53.2%**。**mseqread：4195.0 → 4231.2 = +0.9%（无收益，与 F39 一致）**。⚑ **1M 臂 randread 4059.8 vs 256K 4059.5（差 0.01%）、randrw 1953.3 vs 1945.2** ⇒ "1M 与 256K 等价、天花板 = 应用 IO 大小"获得精确数值支撑。⇒ **A29 中"03-6 无法复核"一句作废；幅度类结论一律以 03-6 同批次受控对照为准，绝对水平以 03-10 为准（批次间漂移约 ±4%，如 randrw 128K：E6 1215 vs 03-6 1270）。**
> ② **randwrite**：3003（03-8 补丁臂中位）→ 03-10 四点均值 **3018**，03-8 的 2970~3583 保留为独立印证。
> ③ **mseqwrite** 4698→**4699**、**seqwrite** 1680→**1681**（四点均值精确值）；**128K 基线列** randread 1882→E6 签收值 **1880**。
> **七项结论方向、卡墙归属、停止条件全部不变**；变化最大的 randread 由 64.9% 降为 61.3%（更保守）。
>
> ⚑ **A28/A29 新增规则 R16（表格口径三条）**：①**每个数值必须注明统计口径**（均值 / 中位 / 单点）与**数据批次**；②**同一张表禁止混用口径**（如一行取跨会话中位、另一行取单批次均值）；③**跨会话汇总集合只能包含与被评价配置完全一致的实例**——本次 randrw 的中位里混进了 stock 版实例、randread 的中位里混进了参数扫描档，这是抬高数值的真实原因。
>
> ⚑ **R15 再次被违反的记录**：A28 中 opencode 在**未查证推导过程**的情况下断言"1923 是反推值"，实际它有可复现的中位数推导。**教训：怀疑一个数字之前，先去找它的推导；找不到推导才能说"无法回溯"。**





| 结论 | 处置 | 依据 |
|---|---|---|
| **F14「JuiceFS 后台任务自动回收，66min 内自愈排空，对象数无永久泄漏」** | 🔴 **降级为"曾观测到一次，机制未定，不得作为设计依据"** | F25：池在 3,453,658 上静置 8 小时零变化（2568 行采样全同）。原 §8.2 纪律"首选办法是等待"**作废**，改为显式 `gc --compact` |
| 「脚本无 `gc --delete` ⇒ 对象泄漏 1.83M/实例」 | ❌ 作废（我在 03-1 报告 §七 写错，已用 ⚑⚑ 撤回） | F25/F27：不是泄漏，是回收需显式触发 + 约 9.2% 结构性残留 |
| 「探针归一化」/「用探针筛其它项也在高档」/「探针当看门狗」 | ❌ 全部作废 | 03-1 留一法 r=−0.129；03-3 F18 |
| 「r=0.88 ⇒ 写侧指标可作读侧调优的门控探针」（03-1 执行方结论） | ❌ 作废，改判为"组间同向传导成立（增益 0.221），组内无相关" | 03-1 复核，详见 `03-1-…md` §二 |
| 「T2 读侧调优难度降到与写侧同级」 | ❌ 作废，仍是最难 | 同上 |
| 档位在线看门狗（`±2%` / 区间式） | ❌ 作废 | F18：日间 4.5h 跌 1.72%，距下界仅 0.04% 余量 ⇒ 必然误报 |
| `obj_gate` 的 PLATEAU 三前置设计（`drain_started` + `elapsed≥2400s` + `plateau_count≥3`） | ❌ 作废，整套等待逻辑已删 | F25 |
| 「字节口径优先于对象口径」 | ❌ 撤回，两者是**同一个测量** | F19：`objects × 256 KiB ≡ stored` 逐行成立 |
| 「每 292 KiB 生成 1 对象」（作为全局常数） | ❌ 作废，换算系数**分项**：顺序写 256 KiB / 随机覆写 292 KiB | F20：把 292 套到顺序写低估 25% |
| 旧对象数基准 0.86 / 0.98 / 2.01 / 3.02 / 3.32M、6.47M、32.3M、35.85M、6.02M 门限论证 | ❌ 全部作废 | F21 实测值取代 |
| `OBJ_TARGET = 2.50M`（仅高于基线 140K） | ❌ 作废，比不可避免的欠账还小，是设计缺陷 | F27；已改 2.90M |
| 02 计划 A2.2「osd_memory_target 调到 16GB」 | ❌ 作废 | §5.4 异常二：已是 350 GiB |
| 02 计划 §11.4「分臂串行」 | ❌ 作废，改 A/B 紧邻交错 | F13/F17 |
| 「03-1 的 mseqwrite 绝对值」 | ❌ 报废，仅可作组内/组间相对比较 | X3 起已越过 F6 拐点下界 3.78M，X3-X8 全程在拐点之上，污染与实例序/档位序共线 |
| **K2 `rocksdb_cache_size` 512 MiB → 2-4 GiB（原 T1.1，P1 期望最高项）** | ❌ **取消**（2026-08-11 桌面核查 + 归属调查） | §5.5 / F28：带 `runtime` flag 但 `set_cache_flag` 使 512 MiB 在 BlueStore 里永不被读取，且 block cache 在 `_open_db` 建一次不 resize ⇒ 需重启 OSD ⇒ 撞 F7（**版本无关**）。⚑ §5.6 / F30 补充：该选项**并非无用** —— 它是 **mon 的缓存比例阀**且在 mon 里真生效，只是 mon 不在数据路径上 ⇒ 对我们效应结构性为 0。复活条件见 §十 |
| **01-5 §十二「512 MiB KV cache 太小，EC 每 op 4× RocksDB 查会吃延迟」** | ❌ **前提作废** | F29：实际 KV 缓存 = 30 GiB × 0.30 = **~9 GiB**（18×），运行态 meta 用量 1.22 GiB 旁证。缓存不小，假设失去依据 |
| H1「服务端 BlueStore 缓存渐进升温」 | ⏸ **未定论**，挪 T1.1/T1.2 顺带观测 | 取增量后 `d_buffer_hit%` 69.085→69.265、Spearman(run)=+0.886（n=6 临界）但与 mseqread 仅 0.486 不显著、幅度仅 +0.18pp；`d_onode_hit%` 无趋势(−0.371) |

### ⚑ 2026-08-20 新增撤回（5 条，全部为本方分析层错误 / 口径缺陷；出处 03-17b/03-17c/03-17d）

| # | 被撤回的结论 | 原出处 | 撤回依据 |
|---|---|---|---|
| **A30** | 🔴 ~~"全 20 轮在飞 GET 恒 63.94 ± 0.69（CV 1.1%）⇒ 读侧窗口可能在客户端被夹死在 64"~~（F59 后半段）；连带 ~~"F42b：读在 64 并发流饱和"的强形式~~ | 03-17c 报告 §11.3 + 本文 §2.4b F42b | **03-17d 直接否掉**：那 20 轮的 `numjobs` **全部是 64**，"恒 63.94"是自变量的镜像而非上限。改 numjobs 后在飞 = 63.4/95.8/128.2 精确跟随（**F60**），带宽 J64→J128 仍 +10.4% ⇒ 64 不是饱和点而是收益递减区起点。⚑ **本方教训**：F39 早在 03-6 就已立"有效并发 ≡ numjobs"，03-17c 却把"在飞 = 64"读成客户端硬窗口 —— **提出"新墙"之前必须先检索 F 表里是否已有该量的定义（新增 R17）** |
| **A31** | ~~"读侧客户端双稳态，判别量 = 对象层 in-flight（好 ≈97.4 / 坏 ≈80.0）"~~（F54 原文） | 03-17（原）报告 | **样本数错觉**：n=2 的两点被读成两个稳态。8 轮连续分布 bw 2994–4101、in-flight 77.5–95.2 ⇒ 改判**多值抽签**，判别器换 msgr-worker CPU 离散度（r=−0.9907） |
| **A32** | 🔴 ~~"F56 外推：4 worker ≈5579、5 ≈6975、6 ≈8369 MiB/s"~~ | 03-17b 报告 §7 | **T47C 实测推翻**：6 worker 均值 **4898**、峰值 5412，比外推低 **35–41%**。原式的"常数 1438 MiB/核"非常数（批内 MiB/核 1692→1361 单调下降，回归斜率随子集 1243~1408 且截距显著非零）⇒ F56 降级为"**仅 worker CPU 饱和区可作粗估**"，⛔ 禁止用于跨区外推 |
| **A33** | ⚑ ~~脚本 `arm-covariates.tsv` 的 `worker_cv_pct` 列可作 worker 失衡指示器~~ | t47/t48/t49 脚本口径 | **该列把 0% 空闲 worker 计入分母** ⇒ msgr=8 时恒 ≈59%、只反映"存在空闲 worker"。跨臂比较必须用 **active-only CV**（>2% 单核才计入）：A8 各轮 10.9–12.2% vs A6 碰撞轮 31.6%；03-17d 三臂 11.7–14.5%。本批 `bw ~ active-only CV` r=0.41（弱）而 `bw ~ 活跃 worker 数` r=0.951（强）⇒ 指示器应是**活跃数**。⚑ 同类计数陷阱：OSD `op_before_dequeue_op_lat` 的 `sum` 不可除以 `op_r` 计数（两者 avgcount 不同，会把入队等待放大 5–7 倍） |
| **A34** | 🔴 ~~"157 是 96 核"~~（全文多处） | 本文及 03-x 系列报告沿用 `nproc` 输出 | **157 实为 128 逻辑 CPU**（0-127，2 socket × 32 核 × 2 HT，Xeon 8462Y+，NUMA 奇偶交错）。`nproc=96` 是 **SSH 会话亲和掩码 `0,17-64,81-127`**（缺 1-16、65-80）造成的假象，挂载进程继承该掩码。⇒ 一切"占 x/96 核"的表述按 128 重算；`/proc/<tid>/stat` 的 `processor` 列出现 >95 属正常 |

| **A35（2026-08-21）** | ⚑ ~~"03-17 三次 ns/B 判档失败都是真坏档，归因门槛设计不成立"~~；⚑ ~~"坏档 = 某个 worker 被打满，判别量可用最忙 worker 占单核百分比"~~；⚑ ~~"逐 worker 连接分布实测为 2/2/2（好）与 4/2/1（坏）"~~ | 03-16 §六 + 周报-20260821「挂载好坏档根因定位」初版 | ①03-16 Pair B 三次是 4.925/4.744/**3.636**，第三次仅偏 **+10.6%**，在文档写明的 ±18% 下本应 PASS，是 `NSB_TOL=10` 默认值（B4-14）判死的 ⇒ 门槛问题对第三次**成立**；②**最忙 worker% 无判别力**（8 轮恒 99–100%，好档也一样；60 s 窗口好 98.5% vs 坏 98.6%），坏档特征是**其余 worker 欠载**，判别量只能是 active-only CV（F63）；③逐 worker **连接条数从未实测**，是按"未饱和 worker 单连接开销 66.7 s"反推，且只对 iter7 反推出 1/2/4，好档三 worker 全饱和⇒条数不可辨识（"2/2/2"还与"7 条连接"自相矛盾） |
| **A36（2026-08-21）｜03-17e Seg B 全段撤回** | 🔴 ~~"新方案补丁 + `ms_async_op_threads=8` 的七项全量结果可固化为新基线"~~；🔴 ~~"msgr=8 相对 msgr=3 带来 randread +119.8%"~~ | 03-17e Seg B（`nb8.log` / `nb3.log` / `sentinel/`） | opencode 审计四条硬伤，任一条即足以否决：①**线程数从未验证生效** —— 哨兵 86/86 采样 `msgr_workers=0`、`stop.log` 86 条 `msgr-worker=0 != 3` 被当噪声忽略，且哨兵取的 pid 2319972/2496730 ≠ NB3 实测挂载 pid 2498793，NB8 段哨兵数据还被同路径覆盖丢失 ⇒ `NB8=8`/`NB3=3` 均无证据；②**每臂只有 1 次挂载抽签** —— NB8 全 21 run 共用 pid=2319972、NB3 全 6 run 共用 pid=2498793，3 轮是伪重复（CV 0.2–0.4%），而 V4 自身横幅写明跨实例极差 **29.9%** ⇒ 有效 n=1 vs n=1；③**对照臂与历史锚点冲突且两项自相矛盾** —— NB3 randread 1179 比 03-10 同口径 3831 低 **69.2%**、比 E6 1880 低 37.3%；由 Seg B 反推的增益 randread **+119.8%** vs randrw **+79.1%**，而 Seg A 同一对比（3 独立抽签/臂）只有 **+44.3%**，差 1.8–2.7 倍；④**同日同机同 fio 规格只有 Seg A 的 46.1%**（2591 vs 5623）—— V4 `item_randread` 与 t50 的 P 侧命令逐参数相同，未控因子（`--max-fuse-io 256K` / 抽签）效应量大于待测因子。附带：报告原文"全部 21 轮 VALID"掩盖了 **L2 两项 FAIL**（seqwrite 极差 9.6%、randwrite 26.6%）。⇒ msgr=8 的收益只由 Seg A 的 **F65** 承载，写侧与新基线一律重测（03-17f） |
| **A37（2026-08-21）｜任务书口径记错，非执行方责任** | 🔴 ~~"历史 E6 与 03-10 是同一口径，两者都不带 `--max-fuse-io 256K`，所以 03-17e Seg B 也不加即可与之可比"~~（写进 03-17e 任务书 §五 并成为 GLM 的执行依据） | 03-17e 任务书 §五 | **03-10 实际带 `--max-fuse-io 256K`**（`doc/perf-tasks/03-10-…md:42`：`--max-uploads 150 --cache-size 0 --max-fuse-io 256K`，且要求验 `max_read=262144`）；本文 §2.4d 亦把"生产候选基座"定义为 `--max-fuse-io 256K --max-uploads 150 --cache-size 0`。只有 **E6** 是 128K 出厂基座（卷 BlockSize 也是 128K）⇒ "E6/03-10 同口径"是把两个口径并成一个，去掉该参数后**既不等于 03-10、也不等于 E6**，Seg B 测出了第三种口径。实测代价：Seg B randread 2591 = Seg A 同 fio 规格的 **46.1%**，差值 **+117.0%** 与 03-6 的 **+115.6%** 只差 1.4 pp（反倒又复现了一次 F39），4 h 机器时间 + 27 个 run 作废。⚑ 叠加设计错误：新基线要固化的是**交付配置**（补丁 + msgr=8 + max-fuse-io 256K），与历史可比性应靠**增设历史口径锚点臂**解决，⛔ 不应把主臂降级为历史口径 ⇒ 单臂设计同时承担两个互斥目标必失其一。新规则 **R19**，重做 **03-17f** |
| **A38（2026-08-22）｜Gate 0 门限的"作废"表述说过头 + 二进制 ABBA 的必要性误判** | 🔴 ~~"原 2942 阈值因口径不可比已作废"~~（写进 03-17f 报告 §3）；🔴 ~~"`de93563f` vs `1f60618c` 的写侧差异要单开一次二进制 ABBA 才能分辨"~~（写进给执行方的放行报文） | 03-17f 报告 §3 + 2026-08-22 放行报文 | **第一条**须一分为二：2942 作为**单点停机门的绝对阈值**确实不成立（n=1 撞 n=3 区间下界，跨实例散布 ~30%，放行正确）；但作为**信号**它指向了真实缺口 —— 6 次独立挂载 randwrite 全落 [2623,2713]，与 03-8 平台带 [2942,3258]、03-10 的 3018.5 均不重叠，是 **−11% 的真实回退**。正确表述："该门限的**绝对值**不可比，但其触发指向了真实的写侧 meta 效率回退。" ⇒ ⚑ **教训：把一个门限判为"口径不可比"时，只能否定其阈值，不能顺带否定其信号。****第二条**：写侧归因根本不需要 ABBA —— **F67** 用同一批已有数据的 meta 计数器就把因子分解出来了（meta 率不变、meta/写op 0.91→0.989、模型预测 2684 对实测 2684），剩余不确定性仅是"0.91 的计数器口径未核对"，一个同日两臂对照即可闭合（⚑ 我最初判为"15 min 单 cell"仍是错的 —— 只跑旧二进制 = 今天白天 vs 昨天傍晚的跨天比较，而 F44/F45 已证 meta 管线有日/夜态 ⇒ 同日对照臂是强制项，成本 ~1.5~2 h，见 03-17g §一.1；这已是同一类错误的第三次，故 R21 §② 特别写明"先榨干数据"之后仍须**检查新设计本身是否又引入跨批比较**）。⇒ ⚑ **教训：先把已有原始数据里的机理计数器榨干，再决定要不要新开对照实验**；本条若不自查，代价是 ~4.5h 无谓机器时间。新规则 **R21**。⛔ **2026-08-22 当日订正（见 A39 ③）：本条"根本不需要 ABBA"的判断是错的** —— F67 的因子分解本身建立在跨计数器错配上，而 ~1.5~2 h 的 ABBA 恰好拆出了二进制 +5.8% 与环境漂移 −14.3% 两个互相抵消的反向因子；若真按本条只跑 15 min 单 cell，会得到"0.914≈0.91 ⇒ 无变化"而彻底错过 F68。**该机器时间不是无谓的** |
| **A39（2026-08-22）｜F67 的两处方法论硬伤（当日提出、当日推翻）** | 🔴 ~~"写侧 −11% = B-catchup 每个 256K 写多花 8.7% meta 提交"~~（写进 03-17f 报告 §4.7、主计划 F67、周报 §2.1 收益拆分表） | 03-17f §4.7 + F67 + 周报-20260821 §2.1 | **①跨计数器错配**：把 03-10 的 `meta_ops_total_Write ÷ fuse写op`（0.91）与 03-17f 的 `transaction_total ÷ fuse写op`（0.989）当同一个量相减。03-17g 在 03-10 原始数据里把两者同时算出，证明 03-10 本身就同时存在 0.9106 与 0.9684 两个值 ⇒ 真实变化只有 **+0.4%**。⚑ **R21 §① 写完的次日即被自己触发**，说明该规则不能只写进文档 —— **必须落进复算脚本：任何跨批比值都强制打印分子分母的计数器全名**。**②把恒等式当交叉验证**："模型预测 2684、实测 2684、逐 MiB 吻合"—— 代数约简后 `(Δtxn/T)÷(Δtxn/Δwr)×256KiB ≡ (Δwr/T)×256KiB ≡ fio 带宽`，Δtxn 完全约掉，17 个 cell 误差 ≤0.02%（BW 跨 2572~3274 全域）。⚑ **教训：宣称"模型与实测吻合"之前，先对模型做代数约简，检查它是否已退化为恒等式**；吻合度越完美（逐 MiB）越应先怀疑恒等，而非当作强证据。**③连带订正 A38 与 R21**：A38 第二条曾以 F67 为据断言"写侧归因根本不需要 ABBA"，现证该判断错误 —— 恰恰是 ABBA 才拆开了二进制（+5.8%）与环境漂移（−14.3%）两个反向因子，若按 A38 只跑单 cell 复核，会得到"0.914 ≈ 0.91 ⇒ 无变化"却完全看不到真因。⇒ **"先榨干已有数据"不等于"可以不做对照臂"；当已有数据全部来自同一批次时，因子分解出的结论本身就是跨批推断，仍须同日对照验证** | ⇒ R21 §② 已按本条重写 |

> ⚑ **本批新增规则（已同步 `RULES.md`）**：
> - **R16 ⑤（表格口径第 4 条）**：**多臂实验必须用拉丁方或同轮配对**，⛔ 禁止把不同轮次的臂均值直接相减。03-17d 实测批内轮漂移 −2.7%/轮（占方差 23%，F=25.3 显著）：若拿 J128-r1(5619) 比 J64-r3(4887) 会把真实的 +10.4% 夸大到 +15.0%。
> - **R17**：**宣称"发现新墙 / 零证据 / 首次观测"之前，必须先检索已立的 F 表与 01-x/02-x 历史报告**。A30 是反例：F39 已定义"有效并发 ≡ numjobs"，仍把"在飞 = 64"当成新窗口墙写进 F59。
> - **R21（2026-08-22）｜比值型/派生型历史指标，引用前必须核对计数器口径；且新开对照实验前必须先榨干已有原始数据**：①凡引用历史报告里的**比值或派生量**（如 `meta/写op`、`ns/B`、`PUT/写字节`、命中率、效率 MiB/s/核），必须回查其**分子分母各用哪个计数器**的原文，⛔ 禁只比数值 —— 这是 R19 从"挂载参数口径"到"指标定义口径"的延伸（B4-17 的 `ns/B` 归一化事故、F67 的 0.91 均属此族）。②发现跨批差异时，**先在已有原始数据里找机理计数器做因子分解**，只有当分解后仍剩不可辨的因子时才立新对照臂；⛔ **2026-08-22 当日订正**：原文举 F67 为"正例"（把 ~4.5h ABBA 降级为 15 min 单 cell）**已作废** —— F67 的因子分解正是踩了 §① 的坑，且其结论被同日 ABBA 全盘推翻（A39）。**改写后的 §②**：先榨干已有数据是为了**想清楚要测什么**，不是为了**省掉对照臂**；判据是"分解后的结论是否仍依赖跨批次数据"——若依赖（F67 的 0.91 来自 8 天前），则对照臂是强制项。③**新增**：凡跨批比值，复算脚本必须同时打印分子分母的计数器全名（A39 ①）；④**新增**：宣称"模型与实测吻合"前必须对模型做代数约简，排除恒等式（A39 ②）。
> - **R19（2026-08-21）｜引用历史批次前必须回查其挂载/口径原文**：任务书里凡出现"与 X 同口径""差异只有 N 项"，必须**逐项列出 X 的挂载参数、二进制 md5、卷设置、fio 形状的原文出处（文件:行号）**，⛔ 禁凭记忆或凭报告标题断定口径。A37 是反例：把"E6/03-10"并列成同一口径，而 03-10 带 `--max-fuse-io 256K`、E6 是 128K 基座，代价是 4 h 机器时间 + 27 个 run 全废。**推论**：当"与历史可比"和"固化交付配置"两个目标冲突时，⛔ 不得压进单臂设计 —— 主臂必须是**交付配置**，可比性靠**增设历史口径锚点臂**解决（成本通常只是 2~3 个 cell）。
> - **R18（2026-08-21）｜判档门的适用边界**：**判档探针 + 重挂重试（"取到好档才测"）只在"单配置验收 / 稳定性复现"场景合法**（02 阶段用法正确，剔坏档只是去噪）；**一旦进入臂间比较，且档位与自变量同源，该门必须从"停机门"降级为"协变量"**——档位的成因是 `msgr-worker` 连接落位抽签（F53/F58），worker 越少越易偏斜，此时"重挂到判档通过"= 按与结果相关的变量做选择性剔除，会**系统性拔高 worker 数少的臂**（这正是 03-17 原设计判失效的原因，A30 同族）。正确做法：①从源头消方差（msgr=8 后无落位碰撞，F58）；②判档值**只记不停机**（03-17c/d 起 `NSB_TOL=100000`）；③需要判档能力时按 F63 采 active-only CV，并做**批内相对离群标注**而非绝对阈值剔除。

---

> **附：文档索引**
> - 03 阶段：本计划 / `doc/perf-tasks/03-*.md` / `doc/perf-report/03-*.md`
> - 02 阶段：`doc/perf-analysis/02-backend-raw-cap-and-juicefs-tuning-plan.md`（§十一 交接章）
> - 01 阶段：`doc/perf-analysis/01-baseline-review-and-nolimit-plan.md`
> - 交付物：`report/基线测试报告-JuiceFS-E6-20260806.md`、`report/周报-JuiceFS调优工作汇总-20260802.md`
> - 方法：`skills/{FULLBASELINE,TUNING,SYSTEM-SAFETY}-SKILL.md`、`skills/TASK-BOOK-AUTHORING-GUIDE.md`
> - 数据：`results/stability-raw/`（+ `MANIFEST.md`）、`157:/tmp/opencode-03-4-weekend-20260809.tar.gz`
> - 源码：Ceph `/home/lilingfeng/project/ceph`（⚠ 本地是 main，集群 v17 quincy，查 flag 须注意版本差异）、JuiceFS `/home/lilingfeng/project/juicefs`
</content>
</invoke>
