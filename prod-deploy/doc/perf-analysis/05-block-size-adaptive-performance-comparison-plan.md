# 05阶段计划：不同I/O Block Size下的JuiceFS性能与竞品对比

> 日期：2026-09-14（2026-09-15 补入独立复算审计与05-2立项，任务编号顺延）
>
> 状态：`STAGE05_CLOSED_WITH_LIMITS / 05-6_COMPLETED_OFFLINE`
>
> 前置结论：04阶段已锁定exact patched JuiceFS v1.4.1与256 KiB通用交付基线；05阶段不推翻原存储规格，而是补齐不同应用I/O尺寸下的性能曲线和配置选择。
>
> 首项任务：`doc/perf-tasks/05-1-randrw-block-size-sweep-and-adaptive-tuning.md`
>
> 补充任务：`doc/perf-tasks/05-1b-randrw-blocksize-and-bs-coupled-parameter-closure.md`
>
> 独立复算审计：`doc/perf-report/audit-05-1-05-1b-bs-data-usability-and-comparison-20260915.md`
>
> 最近执行：2026-09-20已完成05-3b随机读12格、05-4b单流读10格、05-5b多流读10格，正确私有msgr8配置、raw复算和恢复签收通过。旧05-3/05-4/05-5遗漏该配置的原始记录保留，不拼入新曲线。
>
> 最终收口：05-4b/05-5b读写及05-4b条件候选已完成；05-3b randwrite最终RUN在4K两重复点已触发10%稳定性停止门，按`RANGE_ONLY`收口，不再重复执行20格；最终维护和证据持久化均已完成。05-6离线汇总完成，05阶段关闭。不自动扩容、停启OSD或扩大维护范围。
>
> **05-3b执行进展（2026-09-20）：** 私有msgr8实际worker核验通过，4M采集canary与读12格全部完成，128份逐IO日志逐格精确对账。六档正式窗两位置均值依次为4K `260.10`、16K `939.70`、64K `2492.11`、256K `4571.78`、1M `3034.96`、4M `4215.30 MiB/s`，最大位置差2.17%；1M下凹和4M轮内高CV仍存在，不能从旧RUN差值单独归因线程收益。scrub/私有挂载恢复，HEALTH_OK；DB余量仍约5 GiB，小于首写批11.54 GiB预算，写侧`NOT_RUN/CAPACITY_BLOCKED`。3261个文件本地SHA校验与GPT/Luna复核通过，读侧`READ_VALID_L1`。[报告](../perf-report/05-3b-random-bs-baseline-repair-and-retest-20260920.md)。
>
> 并行阶段：`doc/perf-analysis/06-randrw-cache-and-write-path-tuning-plan.md`（06阶段=randrw进一步调优，
> 与05并行、互不阻塞；randrw调优已移出05范围，05-3--05-6编号不动）

**2026-09-20执行收口（最终现场检查15:31北京时间）：** 获批的16个mseqwrite文件定向维护已完成，slice完整引用由约4.823 TiB降至64 GiB，pool对象减少约1997万，六OSD DB可用恢复至17.99—18.10 GiB。05-4b读10格（RUN `20260920-133000`）及05-5b读10格（RUN `20260920-145000`）均完成并通过独立复核；05-5b首次RUN因分析器文件名错误失败，已独立留档、修正后全矩阵重跑，不拼接。最终HEALTH_OK、6/6 OSD、97 PG clean，无测试fio/私有挂载，主挂载不变。未开始正式写。

维护观测暂态DB峰值3.5293 GiB，按两倍工程预留更新预算后，05-3b各写批需18.594—32.862 GiB/OSD，05-4b完整写phase需35.782 GiB，05-5b需88.899 GiB；这些是保守测试准入预算而非已测净增长。128 GiB/OSD扩容示例仅覆盖单phase；按当前模型累计全部剩余写流程及条件式四格，需要204.809 GiB起始空闲/OSD，可另行审批DB256 GiB/OSD、tmpfs576 GiB/节点方案，并先复核实时内存安全余量。**未执行扩容或OSD停启，不将此规划当成既有授权。** 写侧保持容量阻断，不再反复等待或新增回收试验。详情见05-3b报告§10.3—10.5与持久化容量证据。

**2026-09-20晚间更新（supersedes上述“写侧容量阻断/未开始正式写”状态）：** 用户批准对每类写资产采用首次及每格后精确文件、单线程compact的统一维护起点，不递归、不GC、不删建资产、不扩容或停启服务。05-5b标准写11格、05-4b标准写11格均已在该合同下完成，逐格维护后对象/stored回到固定起点，最小DB余量分别高于17 GiB和25.5 GiB；结果标记`CONDITIONAL_MAINTENANCE_START`，不冒充无需人工维护的长期稳态。05-4b的16M/FUSE1M四格候选两组配对收益为`+12.95%/+9.21%`，超过按位置噪声计算的8.17%材料阈值，登记`FUSE1M_L1_SCREEN_CONTINUE`。当前仅剩05-3b randwrite写曲线，先以单格canary量化128文件维护耗时，再决定20格正式矩阵；早先整phase累计容量预算不再直接作为逐格维护起点下的阻断结论。

**2026-09-21 randwrite范围收口：** canary证明单个256K控制格的历史对象可由精确128文件维护恢复；维护实测约35—58分钟（canary 2113秒、最终维护3466秒）。最终RUN `20260921-025300`在正确msgr8、维护后起点下得到4K两重复点15.367/11.853 MiB/s，`D_X=25.82%`，已超过预注册10%停止门。采样器的异步写尾差规则已按绝对带宽误差订正，两个点可作工程范围证据；因缺`C_after`且稳定性门已必然失败，不继续后续写批、不跨RUN拼接，05-3b写侧标记`WRITE_RANGE_ONLY_STATE_DRIFTED`。最终精确维护释放2,637,684个对象和约324.893 GiB stored，scrub恢复、HEALTH_OK，本地raw归档及独立复算通过；05-6汇总现已完成。

## 05收口执行附录（2026-09-20，supersedes conflicting execution clauses below）

本附录保留已完成的05-3b读侧与既有randrw数据，不回跑、不拼接替换。先在正式比较批次外对已知16M顺序写资产做一次定向compact（16个4 GiB `mseqwrite`文件，64 GiB逻辑内容、报告§8约4.83 TiB slice引用）；范围仅限这些文件，读资产不动，不递归删除pool/volume内容，逻辑内容和inode保持不变。维护后最多观察30分钟自然DB回收，并要求健康、读容量和被动静默门通过。这是05阶段容量恢复动作，不是生产autoGC问题的最终解法。

读阶段与写阶段分开设门：05-4b READ 10格和05-5b READ 10格先执行，不能因05-3b randwrite尚未完成而阻断；之后执行05-3b randwrite20格、05-4b write11格、05-5b write11格；05-4b的16M/FUSE1M候选仅条件式执行4格，最后进入05-6汇总。任何健康、读容量或被动静默失败仍阻断读格；写格另须通过写容量门。

本轮容量决策预算为3小时；自然DB回收观察另设最多30分钟上限。写前容量计划必须重新校准并纳入报告§9已见暂态DB分配峰值的transient reserve，不得降低既有容量门或把恢复很快误作安全余量；不足即停止探测并提交DB扩容方案，扩容/迁移DB/停启OSD或其他服务变更需另行明确批准。compact不得进入正式比较批次；若批准inter-batch维护，须统一预声明并建立新的state epoch，报告为conditional-maintenance start，不跨epoch宣称精确BS效应，也不逐cell自动重置。

§8—9的决策依据为：64 MiB微型验证289遍累计18.0625 GiB，自动回收周期96→9、95→8，末态94 slices/5.875 GiB在5分钟内未变；osd.0曾出现暂态余量3.516 GiB并约2分钟后回到5.325 GiB。因此本动作只服务05阶段容量恢复，不能当作生产autoGC问题已解决。

## 一、为什么开展05阶段

既有七项基线中，`seqwrite/mseqwrite`使用4 MiB，其余测试使用256 KiB。现有结论因此只能回答两个离散负载点，不能回答：

1. 应用I/O从小块增长到大块时，JuiceFS与有方的带宽、IOPS和延迟如何变化；
2. 两套系统在相同fio命令下的相对关系是否随BS改变；
3. JuiceFS是否需要按BS调整FUSE请求上限、预读或卷BlockSize，调整后能获得多少增益；
4. 哪些配置可以作为通用基线，哪些只能用于特定负载或专用挂载。

05阶段以`randrw`为第一优先级，因为它是存储规格最关心、也最接近生产混合读写的I/O模型。

## 二、四个概念必须分开

| 变量 | 所在层 | 能否在线切换 | 作用 |
|---|---|---|---|
| fio `bs` | 应用负载 | 是 | 每个应用I/O的大小，是本阶段横轴 |
| JuiceFS `BlockSize` | 卷格式 | 否；需独立卷 | 对象切分粒度，影响对象数、放大和单对象时延 |
| `--max-fuse-io` | 挂载 | 需重挂 | 单次FUSE请求上限；当前通用值256 KiB，可选上限1 MiB |
| `--max-readahead` | 挂载 | 需重挂 | 调整读侧在途窗口；随机负载不得沿用顺序读RA32结论 |

因此每项测试固定分为两层：

- **标准曲线**：固定当前交付卷和挂载，只改变fio `bs`；该层可与有方同命令直接比较。
- **适配曲线**：固定fio负载后再调整JuiceFS参数；每次只改一个层次。若改变卷BlockSize，必须使用fresh匹配对照卷，不能和历史业务卷直接相减。

## 三、统一测试口径

### 3.1 当前JuiceFS对照基线

```text
binary=/tmp/juicefs-1.4.1-patched
expected_md5=24fae0852051c80ca571cb2f20275d46
Volume BlockSize=256 KiB
mount=--max-fuse-io 256K --max-uploads 150 --cache-size 0
Ceph client private config=ms_async_op_threads=8
writeback=off
```

主对比不启用本地读缓存或writeback，避免把本地介质能力计入分布式数据路径。缓存能力如需展示，单列为条件性附加曲线。

### 3.2 BS档位

| 负载类别 | 核心档位 | 说明 |
|---|---|---|
| `randread/randwrite/randrw` | `4K, 16K, 64K, 256K, 1M, 4M` | 覆盖小IO、规格点和大块随机IO |
| `seqread/seqwrite/mseqread/mseqwrite` | `64K, 256K, 1M, 4M, 16M` | 20 MiB只作为已披露竞品命令的附加读点 |

若L1曲线已经出现清晰平台，不自动增加相邻档。所有系统在同一BS点必须保持相同`rw/ioengine/iodepth/numjobs/direct/runtime/filesize`，不得用改变QD或job数后的值冒充BS效应。

### 3.3 对比输出

每个BS至少报告：

- READ、WRITE各自的MiB/s、IOPS、平均及P95/P99完成延迟；
- JuiceFS标准配置值、JuiceFS该BS最优有效配置值；
- 有方同命令三轮值及均值；
- `JuiceFS标准/有方`、`JuiceFS最优/有方`百分比；
- 实际二进制、卷BlockSize、挂载参数和fio完整命令。

`randrw`两向分别比较，禁止相加后判断领先；如需对配置排序，使用两方向相对对照增益的较小值，防止以牺牲一向换取另一向。

#### 3.3.1 双栏统计与三态裁决（2026-09-15 新增预注册合同）

依据 `doc/perf-report/audit-05-1-05-1b-bs-data-usability-and-comparison-20260915.md` §三.1 与
`doc/deploy-log/review-05-1-05-1b-audit-and-05-2-20260915.md` §三：正式窗与 fio summary 在小 BS 上
系统性相差 `4%--9%`，因此**两侧都必须输出两栏**，且不得交叉比较。

```text
两栏 = ① fio 180 s summary
      ② [15,175) 正式窗 + W1--W4 + W4/W1 + 秒级 CV（按实际 timed-I/O 起点、重叠加权自然秒）

允许写方向性结论 = 两栏相对方向同号  AND  效应超过预注册的双方漂移/噪声范围
两栏异号             → STATISTIC_SENSITIVE，只报区间
两栏同号但未超过噪声 → INCONCLUSIVE，只报区间
两栏同号且超过噪声   → 才允许写领先/落后方向

μ_side(BS) = (位置1 + 位置2) / 2
ε_side(BS) = |位置1 − 位置2| / (2 × μ_side)
           = |位置1 − 位置2| / (位置1 + 位置2)  （相对半幅，各侧分别计算）
ε_pair(BS) = ε_ours(BS) + ε_theirs(BS)           （无量纲，保守取和）
判定        = |JuiceFS/有方 − 1| > ε_pair(BS)
```

内部调优决策继续以正式窗为主口径（指导书 §二.13.4 不变）；summary 只作与公开 fio 命令对齐的
并列旁证。有方脚本已保留 128 份 per-job 逐秒日志，因此两栏对两栏均可复算，⛔ 不需要二选一。

**口径归属**：对外表的"JuiceFS 标准配置"列只能填**通用交付**口径（B256 + FUSE256K + buffer300）；
`FUSE1M`、`B1M`、`B4M` 等值只能填入"JuiceFS 该 BS 最优有效配置"列——04-8 已确认 FUSE1M 全局化
会使 256 KiB randwrite 回归，混填会重演该错误。

#### 3.3.2 双方状态协议（2026-09-15 新增，竞品 sweep 的前置条件）

我方 4K--64K 的 `W4/W1` 为 `0.41--0.61`（180 s 内未达稳态），有方 randrw 三轮亦自降 `18.33%`。
若不先冻结共同外部合同，跑完只会得到两个互相重叠、无法分辨的区间。因此 sweep 前必须固定：

1. 相同 runtime、BS 矩阵与 fio 负载（`randrw 50/50`、`libaio`、`128 job × iodepth 128`、
   `direct=1`、`128 × 1 GiB`）；
2. 轮间**不 relayout**、不增删文件；
3. 相同名义 cooldown；
4. 两侧都报告**正反位置原始值**、`W1/W4` 与位置漂移，⛔ 不得只报平均值；
5. 测试前申报资产状态（新建/已承载历史写入）与环境信息（fio 版本、内核、挂载类型/参数、
   容量、客户端 CPU/内存/网络、测试时间）。

⚠️ 该协议只冻结**共同外部合同**，⛔ 不宣称两系统内部状态（元数据、GC、后端健康、恢复机制）等价。
最终结论固定表述为：

> 各自已部署环境下、相同 fio 命令与共同外部状态合同的端到端性能比较；
> 不得归因为文件系统软件单因素。

有方测试由用户在对应环境独立执行，不写入JuiceFS任务书。当前通用七项脚本已从
`/home/lilingfeng/fio-7item-test.sh`归档至
`scripts/benchmark/fio-7item-test.sh`，归档SHA256为
`8986bbfe981b28be3379c4496cdbb441e774a78cfbb5bcfc091e39b4fdbbc7c0`。归档版本默认仍是原七项BS口径，后续有方BS sweep须以参数化的受审版本执行并冻结实际脚本SHA256，且不得执行归档脚本的`clean`入口代替精确人工清理。有方结果还须附fio版本、内核、挂载类型/参数、文件系统容量、客户端CPU/内存/网络和测试时间；缺这些信息时只作观测值。

randrw 专用 BS sweep 脚本为 `scripts/benchmark/fio-randrw-bs-sweep.sh`，已于 2026-09-15 按复核
结论补齐：双栏统计、精确时间锚、漂移裁决（`POINT_USABLE / RANGE_ONLY / MATRIX_DRIFTED`）、
资产清单、冻结负载守卫（改动即 `NOT_COMPARABLE`）、`layout` 与正式矩阵分离（`CREATE_DATASET`
默认 0）以及汇总阶段容错。执行前须先跑 `--self-test` 并冻结实际脚本 SHA256。历史实测版本 SHA256 为
`1120a83c021fe45e1d35f87ed5beaa87be58736f635983de757ca29ee413e2d3`，已完成 `--self-test` 与
本机真实 fio 两段式 12 格冒烟（含守卫行为验证）。最终收尾修复后的短冒烟明确标记为
`NOT_COMPARABLE`，相对路径 `SHA256SUMS` 复核 `180/180 OK`；证据在
`/mnt/c/SunRise/test/audit-05-1-05-1b/20260915-recompute/script-verification-final/`。

2026-09-16 T-D 补正后，未来采集版新增 CPU、`MemTotal`、NIC、完整挂载选项与
`CLIENT_CACHE_CONFIG` 强制声明；其 SHA256 为
`85693e785aa4f222b8a3e86a111630ee7cccdcad8bdd4e1f640b4cab727e8bfe`，`--self-test` 通过。
修订版不反向改写历史 RUN；`1120a83c…` 已原样保存在证据根 `scripts/` 中。

## 四、任务分解

```text
05-1  randrw：标准BS曲线 → 大BS FUSE挂载适配（已完成）
05-1b randrw：已完成卷BlockSize联动收尾
  ├─ B1M：+28.34%～+40.70%强工程信号，受首轮漂移限制
  ├─ B4M：原始同RUN结果为+17.94%～+18.80%，但buffer1024压低了B256对照
  │        ⚠️ 已被审计降级为`NET_GAIN_UNPROVEN`并撤出候选；05-2不再补三臂
  └─ B64：4K/16K/64K均无材料性正收益，保持B256/FUSE256K
审计   05-1/05-1b数据可用性与对外可比性独立复算（不占编号，已完成）
  ├─ 数值全部可复现；但只有256K（与4M的FUSE1M口径）具备做精确百分比对比的分辨力
  ├─ 订正1：B4M的+18%是相对被buffer1024压低的对照臂，净收益未证
  ├─ 订正2：05-1 Phase A无JuiceFS机制指标，标准曲线不得用于机制归因
  └─ 机制发现：12/12个B256大BS格上传槽打满150 ⇒ 写向天花板是参数闸门
05-2  randrw：已完成；1M提高上传并发无稳定净收益，256K未触发接近闸门，条件buffer取消
05-3  randread/randwrite：旧矩阵已执行；配置/日志/写状态缺口由05-3b纠偏重测
05-4  seqread/seqwrite：旧矩阵已执行；正确基线曲线及16M写候选由05-4b复核
05-5  mseqread/mseqwrite：旧矩阵配置有误且写侧受DB溢出阻断；05-5b完整重测
05-6  汇总JuiceFS标准/最优曲线并导入有方**已归档**数据，给出配置建议和阶段结论（原05-5）
T-C   （条件诊断，不占正式编号）几何自证：固定BS只改filesize/iodepth使`iodepth×bs/filesize`
      扫过3%→12.5%→50%，检验有方≥1M跳变是否为在途窗口自命中所致
T-D   （已完成，不占正式编号）05-2报告D1--D3订正 + sweep脚本补记客户端资源
      + 七项/sweep归档交叉检查 + 重建证据根SHA256SUMS
```

⚑ **randrw 的进一步调优已移出05阶段**，另起并行的06阶段承接：
`doc/perf-analysis/06-randrw-cache-and-write-path-tuning-plan.md`（06-1 缓存与writeback正确配置筛选、
06-2 range-scoped flush调查构建）。05阶段保持"BS曲线与竞品对比"定位，⛔ 不再新增randrw调优任务；
05-3--05-6 编号不动，⛔ 不因06阶段立项而二次顺延。

05-1优先复用现有B256卷及固定`rw_test`资产，不重新layout。后续任务优先复用各自现有固定资产。只有卷BlockSize候选验证必须新建临时卷和一次性layout；该动作不得与标准曲线混在一个效应量中。

### 4.1 编号调整说明（2026-09-15）

05-2 原为"randread/randwrite BS曲线"。审计发现randrw大BS写向被`--max-uploads 150`打满
（12/12格PUT在途量`147.6--149.8`），且该解闸实验在03-19、04-6b Phase C、05-1b §2.6三次预注册
后从未执行；它直接决定"randrw是否还有提升空间"这一被重点关注的问题，也决定后续曲线该用哪个
配置作为"JuiceFS最优"列。因此把该实验插入为05-2，原05-2--05-5顺延为05-3--05-6。
⛔ 引用旧编号的文档须以本表为准。

### 4.2 05-2 经复核后的精简（2026-09-15）

经 Opus↔GPT 两轮复核（`doc/deploy-log/review-05-1-05-1b-audit-and-05-2-20260915.md`），
05-2 初稿的三处设计缺陷已修订：

| 原稿 | 问题 | 修订后 |
|---|---|---|
| 256K 设为唯一通过项 | 上传槽饱和只在 1M/4M 有实测；256K 推算在途量仅 `105--140/150`（70%--93%） | **1M 为唯一通过项**；256K 先 1 格筛选，过 `256K_UPLOAD_PROXIMITY_GATE`（`uploading` p95 >= 120）才做四格 C/T |
| 1M 用单次 2×2 | 只有 uploads 主效应对一阶漂移免疫；buffer 主效应与交互项与漂移共线 | 条件式两步：先 uploads 单变量 `C1→T1→T2→C2`，命中后再做 buffer 配对 |
| Phase V（B4M 三臂）为必做 | 与上传闸门主问题独立，且需建卷/layout/destroy | **删除**；B4M 维持 `NET_GAIN_UNPROVEN` 并撤出交付候选 |
| 机制门含"OSD 写延迟恶化 <10%" | Little 定律下并发↑必然延迟↑；违反指导书 §二.14 | 机制门改为**对象 PUT 字节吞吐与应用 WRITE 带宽同向提升**；延迟/CV/`W4/W1` 降为记录项 |
| scrub"发现污染再申请暂停" | 违反指导书 §二.19.1（禁止运行中改口径） | G0 前二选一勾定，默认按 Phase 暂停 + 精确恢复 |
| 每格主动 OSD compact | 新增环境控制动作却未列授权 | 默认与 05-1/05-1b 一致的被动恢复门；主动 compact 降为条件动作，须预列命令/目标/次数并单独授权 |

规模：性能格 `15 → 5--13`，临时卷 `2 → 0`，layout `1 → 0`，环境时长 `3.5--4.5 h → 1.5--3 h`。
⛔ 本任务不预注册 `U450/U600`。

## 五、JuiceFS适配原则

1. **小于或等于256 KiB的随机I/O**：先保持FUSE256K；若64K出现明显对象粒度问题，才比较fresh B64与fresh B256。JuiceFS卷BlockSize最小为64 KiB，小于64 KiB的fio BS不能机械一一对齐。
2. **1 MiB及以上I/O**：先在同一B256卷比较FUSE256K与FUSE1M；只有FUSE层有材料信号后，才考虑卷BlockSize匹配。
3. **随机混合读写**：不直接套用顺序读的B4/RA32。RA32提升的是顺序读对象在途量，对randrw可能制造无效预读；默认readahead与RA0只允许做一次材料性筛选。
4. **卷BlockSize**：只在临时fresh对照卷上验证，候选范围为64 KiB至16 MiB的2次幂；同一候选必须有fresh B256对照、相同数据合同和相同挂载参数。
5. **通用配置门**：某BS上的峰值不等于通用配置。若要改变通用基线，必须对原七项及256 KiB规格点完成非劣回归；否则只形成“特定BS专用配置”。

## 六、判定规则

- L1材料信号：READ和WRITE方向相对两侧对照均同向，较小方向增益至少5%，且超过同RUN锚点漂移；机制指标方向一致。
- 一向提升、一向下降：记为Pareto权衡，不签“randrw整体提升”。
- 标准曲线首尾256K锚点漂移超过10%，或任一BS正反向两点差异超过10%：只保留工程观察，先归因运行状态，禁止与有方做精确百分比结论。
- 只有存在可交付且材料性候选才升级L2；L1无信号即停止该参数，不扫更多档位。
- 有方与JuiceFS硬件、网络、缓存或保护策略不一致时，结论限定为“各自已部署环境、相同fio命令的端到端对比”，不得归因为文件系统软件单因素。

## 七、稳定性、安全与证据边界

- 157上的WekaIO、K8s、网络、内核、md0和非本任务路径是红线；禁止全局`drop_caches`。
- 标准曲线只复用固定测试资产，禁止轮间layout；采用正向+反向BS次序及首尾256K锚点量化状态漂移。
- 短L1默认保持scrub开启并记录正式窗重叠；已发生scrub污染的长矩阵补测，可在用户单独授权后
  用既有状态驱动脚本暂停并恢复`noscrub/nodeep-scrub`，必须保留恢复证据。
- 不改生产卷BlockSize，不新建或删除Ceph pool，不停止/重启OSD、TiKV或业务服务。
- 临时卷必须使用本RUN专属META、Name、UUID和挂载路径；format、layout、destroy分开计划，destroy前再次核对身份并单独授权。
- 原始证据唯一持久化根为`/mnt/c/SunRise/test/05-*/<RUN_ID>/`；遵循`TEST-DATA-LIFECYCLE-POLICY.md`，先持久化和校验，再清远端临时副本。

## 八、阶段交付物

1. 每项任务的正式报告及可复算raw；
2. 七项“BS—带宽/IOPS/延迟”曲线；
3. JuiceFS标准配置、有方、JuiceFS按BS最优配置三方对比表；
4. 面向生产的配置表：通用基线、特定BS专用候选、不得采用项；
5. `doc/deploy-log/results-table.md`更新及05阶段最终报告。

05阶段原结束条件是完成双方同命令的全部规定BS数据；执行中有方环境永久不可获取，05-3b randwrite又按
预注册稳定性门停止，因此最终改为：完整交付已签收曲线，缺失项明确标记`NOT_COMPARABLE`或
`RANGE_ONLY`，不得跨RUN拼接或重复运行追求表格完整。05-6完成这些边界说明后即关闭阶段。

## 九、执行进展

| 日期 | 任务 | 结果 |
|---|---|---|
| 2026-09-14 | 05-1 Phase A | 有效RUN `20260914-124132`完成4K--4M randrw曲线；256K首尾锚漂移约`-2.1%`；1M位置对只报范围，其余档位通过 |
| 2026-09-14 | 05-1 Phase B | 有效RUN `20260914-141125`：1M fio下FUSE1M最小双向效应`+16.40%`，登记专用挂载L1候选；4M最小效应`+2.92%`，停止 |
| 2026-09-14 | 05-1生命周期 | 当时经复核跳过fresh卷BlockSize筛选；最终GC两轮完成，对象数回到任务前值、三节点pending为0、Ceph健康，证据持久化且环境闭环 |
| 2026-09-14 | 05-1b计划 | 重新审视后确认原目标尚缺卷BlockSize与BS联动参数；新任务只补B64/B1M/B4M及一个条件触发旋钮，不重跑05-1有效曲线 |
| 2026-09-14 | 05-1b阶段A | FUSE64筛选受约15.6%同臂漂移影响，记`RESOLUTION_INSUFFICIENT`；不登记候选，S组回退FUSE256K |
| 2026-09-14 | 05-1b M组 | fresh B1M相对fresh B256两位置READ/WRITE提升`+28.34%～+40.70%`，对象请求粒度256KiB→1MiB；但对照臂漂移约8%，只签强工程信号，待L/S继续 |
| 2026-09-14 | 05-1b M组闭环 | 两临时卷精确销毁；pool objects/stored回到创建前且60秒后复核一致，Ceph/TiKV和生产测试挂载正常 |
| 2026-09-14 | 05-1b L组首轮 | B4M首位置fio summary为`28.34/27.61 MiB/s`，对照约`2215/2217 MiB/s`；128份per-job日志为空，记`EVIDENCE_INVALID_L`并转补测 |
| 2026-09-14 | 05-1b L组归因 | FUSE平均操作时间`26.98→2136.21 ms`，GET/PUT时延未恶化，请求生成率降至`29.9/13.6 req/s`；定位为对象层之前的客户端/FUSE缓冲排队 |
| 2026-09-14 | 05-1b S组首轮 | 第4格例行scrub与正式窗重叠约12秒；B256 4K为`31.36/31.37 MiB/s`，B64为`32.65/32.67 MiB/s`，单位置约`+4.12%/+4.16%`，转全曲线补测 |
| 2026-09-14 | 05-1b首轮环境闭合 | M/L/S六只临时卷精确销毁，三组pool创建前/清理后/60秒后完全一致；这不代表任务关闭，L/S需新RUN补齐 |
| 2026-09-14 | 05-1b B4M诊断 | RUN `20260914-221613`确认buffer300导致4 MiB完成集中到正式窗后；buffer1024恢复约`1996/2006 MiB/s`，确定正式两臂共同使用1024 |
| 2026-09-14—15 | 05-1b L正式补测 | RUN `20260914-225012`：B4M相对B256两组READ/WRITE效应`+17.94%～+18.80%`，同臂漂移≤0.69%，登记4 MiB randrw专用卷L1候选 |
| 2026-09-14—15 | 05-1b S正式补测 | B64在16 KiB仅约`+1.2%～+1.7%`、64 KiB无收益；4 KiB独立收尾两组均为负、均值约`-4.3%`，保持B256/FUSE256K |
| 2026-09-15 | 05-1b最终闭环 | 两次scrub lease均恢复原flag；L/S临时卷精确销毁，无挂载/进程残留，Ceph `HEALTH_OK`、6/6 OSD、97/97 PG clean；权威包SHA256=`8ef69d78...132c4` |
| 2026-09-15 | 05-1/05-1b独立复算审计 | 54个性能格正式窗均值全部复现、非性能门通过；但按BS分级只有256K（±2.1%）与4M的FUSE1M口径（±4.3%）可做精确百分比对比，4K/16K/64K/1M只能给区间（同配置离散度`5.5%--61.5%`） |
| 2026-09-15 | 审计订正1 | B4M的`+17.94%～+18.80%`改判`NET_GAIN_UNPROVEN`：候选`B4M/b1024=2226`与现成`B256/FUSE1M/b300=2202--2311`同水平，`+18%`来自对照臂被buffer1024压低`2201.56→1873.85`（读放大`1.06→1.37`）；已撤出候选，05-2不补三臂，未来只有明确部署意图时才同RUN重验 |
| 2026-09-15 | 审计订正2 | 05-1 Phase A的`metrics-pre/post.prom`为空（`METRICS_MISSING_ALLOWED_PHASE_A`），标准曲线只有fio端点，不得用于机制归因；机制结论一律引用05-1b逐秒采样 |
| 2026-09-15 | 审计机制发现 | 12/12个B256大BS格PUT在途量`147.6--149.8`/上限150，逐秒gauge全窗`=150.0`；写向天花板=`150槽×256KiB÷PUT延迟`（`15.03--19.95 ms`→`1880--2496 MiB/s`）。B1M/B4M解闸后在途量回落到`60/27`，对象层总流量由`4.12--5.15`升到`6.07--6.15 GiB/s`；客户端仅`7.75--10.20`核/64核。100GbE本批未采到可签收NIC计数，历史`3220 MiB/s`为fio randwrite观测而非网卡能力上限，不能据此量化NIC余量 |
| 2026-09-15 | 复核收敛 | Opus↔GPT两轮往返达成一致（`doc/deploy-log/review-05-1-05-1b-audit-and-05-2-20260915.md`）：审计结论受纳；05-2按§4.2修订三处设计缺陷（256K主判→1M主判、2×2→条件式两步、删Phase V），机制门改对象PUT字节吞吐，scrub二选一前置，compact降条件动作，⛔ 不预注册U450/U600 |
| 2026-09-15 | 对比合同 | 新增§3.3.1双栏统计与三态裁决（同号且超过`ε_pair=ε_ours+ε_theirs`才写方向性结论）与§3.3.2双方状态协议；`fio-randrw-bs-sweep.sh`已补齐双栏/时间锚/漂移裁决/资产清单/负载守卫/layout分离/汇总容错，`--self-test`通过 |
| 2026-09-15 | 执行顺序 | 冻结状态协议 → 有方12格sweep → 精简后05-2（1M单变量→256K条件→buffer条件）→ 仅对区间仍重叠的BS建05-1c定点补测；⛔ 不做全量重测 |
| 2026-09-15 | 05-2立项 | `--max-uploads 150→300`已在03-19、04-6b Phase C、05-1b §2.6三次预注册但从未在写侧执行（01-3测的是randread，该参数控写不控读）。复核后05-2以1M单变量为唯一主判，256K先过接近闸门筛选，buffer按机制条件触发；不做B4M三臂；原05-2--05-5顺延为05-3--05-6 |
| 2026-09-15 | 05-2完成 | RUN `20260915-141750`：1M U300相对U150两组READ为`+4.32%/-4.64%`、WRITE为`+4.41%/-4.66%`，同臂噪声约`5.3%`，裁决`RESOLUTION_INSUFFICIENT / SCREEN_CONTINUE=FAIL`；上传在途升至`286--300`但PUT吞吐为`+1.62%/-5.88%`、PUT延迟升至约`21 ms`，停止更高上传并发。256K B1的uploading p95=`98.15<120`，B2未触发；buffer分支取消；通用配置不变 |
| 2026-09-19 | 竞品环境不可用 | 有方环境**不再可获取**，原计划的≥1M补测（纯randwrite、>180 s长窗、后端字节计数）全部取消。**≥1M对比行永久标记`NOT_COMPARABLE`**，不是"证据待补"：⛔ 不得写"我们慢7倍"，也⛔ 不得写"对方数不实"，只保留区间+三条观测（29 GB/s顶与其256K纯读顶同值；randrw@4M写侧14629 MiB/s超过其自身mseqwrite@4M的13210 MiB/s；拐点对应`iodepth×bs/filesize`穿过~10%）+两种未排除解释（H-cache客户端缓存 / H-array本地NVMe阵列）。05-6只能导入已归档件（12格sweep `MATRIX_DRIFTED/VALID_RANGE_ONLY`，SHA256=`7b805c24...1d70d94`；七项数据主机名未记录，归属`c2-gpu-node4`仍为`推断`） |
| 2026-09-19 | 对外免责订正 | 原拟"`direct=1`绕过L1页缓存"作为公平性免责**作废**：`pkg/fuse/fuse.go:250`只对内部特殊节点设`FOPEN_DIRECT_IO`，该标志在两侧都**不禁用守护进程自身用户态缓存**，且双方都用了`direct=1`，故对称、不构成公平性差异。**正确且唯一的公平性免责是我方配置**："我方用户态缓存是被`cache-size=0`显式关闭的（`SelfCheck`连带强制关writeback与prefetch），对方用户态缓存状态未记录（D4）"。`direct=1`另有一条只针对我方数的成立用途：因绕过L1，每次读都下到FUSE触发全inode flush，真实应用走缓冲I/O时该惩罚小得多 ⇒ 该randrw数不可外推到缓冲I/O应用 |
| 2026-09-19 | randrw调优移出05 | 新起并行的06阶段（`06-randrw-cache-and-write-path-tuning-plan.md`）承接randrw进一步调优：06-1缓存与writeback组合包筛选（四格，零源码改动）、06-2 M1 flush路径调查（构建溯源门→动态profile/Amdahl门→语义回归门）。依据：①05-2裁决`RESOLUTION_INSUFFICIENT / SCREEN_CONTINUE=FAIL`，U300无一致收益，无继续扫上传并发的依据；②04-tmp3j直接RADOS 256K写QD32/64/128=`3271/3660/3949 MiB/s`且**未饱和**，高于JuiceFS在randrw达成的对象PUT`2177--2275 MiB/s` ⇒ **纯写口径上无`2.2 GiB/s`硬上限**（⚠️ 纯写!=混合，剩余约束可能在客户端路径也可能来自GET/PUT混合服务关系，待06阶段区分）；③05全系列都跑在`cache-size=0`（代码连带强制关writeback+prefetch），缓存模式从未被正确探测过。⚠️ 经GPT评审，此前"持久化模式已达上限`95.0%--100.1%`无空间"属**循环计算已撤销**（上限取自被评价样本自身），"缓存模式只利用本地能力4%"属**双重错配已撤销**（单方向vs聚合、混合vs纯读）；两模式的上限现均记为**未知**。05-3--05-6编号不动 |
| 2026-09-16 | 06阶段审计回写 | 06-1的`+16.3%～+19.6%`为窗长依赖瞬时量，不构成收益；宿主脏页吸收/回压是轮内衰减的强支持解释。06-2的Amdahl Gate2B因无关键路径语义且观测开销约23%而回溯`GATE_INVALID`；M1仍为合理嫌疑，未证主因，06阶段无生产候选。05阶段竞品曲线与既有正式数据不因此改变。 |
| 2026-09-16 | T-D离线补正 | 05-2已按mean/p95/max订正并补稳健性；确认`150`是并发上限而非吞吐上限。已完成七项与BS sweep交叉检查，历史环境缺口标记不可追溯；未来脚本补强制客户端资源/缓存申报，历史与修订脚本均入证据根并重建递归清单。未访问有方环境，未执行性能负载。 |
| 2026-09-20 | 05-5执行 | RUN `20260920-072228`完成mseqread 10/10格：64K--16M正式窗均值约`2821--2903 MiB/s`、位置漂移≤`1.99%`，BS增大无材料性收益。mseqwrite完成9/11格，已有档位约`3854--4168 MiB/s`且配对漂移≤`7.37%`；随后`osd.4`的40GiB BlueFS DB满并触发约70MiB spillover，按健康门停止，缺`64K-B/4M-C`。scrub及私有挂载已恢复，但Ceph仍为`HEALTH_WARN/BLUEFS_SPILLOVER`；在DB容量独立修复前不继续正式矩阵，状态`WRITE_PARTIAL_INFRASTRUCTURE_BLOCKED`。 |
| 2026-09-20 | 05-3/4/5签收订正与补测立项 | 三RUN均使用系统配置SHA `8dd48e57…`，遗漏计划要求的私有 `ms_async_op_threads=8`；同哈希文件只读解析为3。旧数据只能作实际配置观察，不能代表通用交付曲线；05-5第9写格post-health失败，256K配对均值/漂移不得作为健康结果。新增05-3b/05-4b/05-5b，按顺序纠偏重测，共用修正只做一次；05-3b改用带256K前后锚的随机写小批次。当前仅任务书就绪，脚本修正/离线Gate及BlueFS恢复未完成；不直接启动负载、不预设DB必须重建。 |
| 2026-09-20 | 05-3b读侧执行 | 共用配置/日志修正与离线Gate完成；BlueFS告警已自然恢复，但DB余量约5GiB仍不满足写预算。获批创建专属NVMe日志目录后完成4M采集canary与12格读矩阵；全程实际8线程、完成日志字节与IO次数精确对账，六档位置差≤2.17%，256K均值4571.78MiB/s，1M下凹仍可复现。scrub/私有挂载恢复，HEALTH_OK；写侧未运行，不因告警消失放宽容量门。读raw归档和独立复核另见05-3b报告。 |
| 2026-09-20 | 05-4b/05-5b写侧完成 | 在每格精确文件compact形成统一维护起点的合同下，单流写五档均值`1008～1957 MiB/s`，多流写五档均值`3778～4160 MiB/s`；16M单流写FUSE1M配对收益`+12.95%/+9.21%`，登记专用挂载候选。结果不等价于无人工维护的长期稳态。 |
| 2026-09-21 | 05-3b写侧范围收口 | 最终RUN的4K两重复点为`15.367/11.853 MiB/s`，差异`25.82%`超过10%停止门；不继续后续BS、不跨RUN拼点。精确维护释放2,637,684个对象和约324.893GiB stored，最终HEALTH_OK、证据归档和独立复算通过。 |
| 2026-09-21 | 05-6阶段汇总 | 仅使用既有权威证据完成标准曲线、专用候选、有方范围和不可比边界汇总；通用B256/FUSE256K/U150配置不变，05阶段状态`STAGE05_CLOSED_WITH_LIMITS`。 |
