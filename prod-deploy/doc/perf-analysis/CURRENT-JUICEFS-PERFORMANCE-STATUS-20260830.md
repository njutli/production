# JuiceFS 性能调优现状总览

> 更新日期：2026-09-04（已同步U141、04-1b、04-2、04-6及缓存专项）
> 目标：每个读写方向有效带宽达到 `6250 MiB/s`（100GbE 单向带宽的一半）
> 当前结论：**03阶段已完成，exact patched v1.4.1已批准替代v1.3.1；在线/低风险参数空间已经收口。04-6未发现新的可交付旋钮，但因mseqread机制证据不闭合及randrw约8.2%状态漂移，严格裁决为`STAGE04_CONTINUE_DIAGNOSIS`。04-tmp3竞品大块口径L1已完成，不改写上述七项基线或架构裁决。**

## 一、当前最优交付配置

| 项 | 当前值 | 说明 |
|---|---|---|
| JuiceFS | patched v1.4.1 + B-catchup，`/tmp/juicefs-1.4.1-patched` | md5 `24fae0852051c80ca571cb2f20275d46`；U1 `REPLACE_APPROVED` |
| 关键代码修复 | B-catchup / FlushTo 补派发 | 避免 256K FUSE 下 randwrite 塌至约 `551 MiB/s` |
| 挂载参数 | `--max-fuse-io 256K --max-uploads 150 --cache-size 0`，保持默认readahead | 04-tmp确认显式ra0对randrw仅约`+1.6%`，不进入交付 |
| 条件性写增强 | 客户端空间充足时，在独立持久本地盘配置`--cache-dir`并启用`--writeback` | 仅用于独占文件的突发写；容量按突发净积压加余量估算，远端持久化以staging归零为准；W16不是推荐容量 |
| Ceph 客户端 | 私有 `ceph.conf`：`ms_async_op_threads = 8` | 不修改集群配置；当前 6 个 OSD，规则为线程数 `>= OSD 数据连接数 × 1.33` |
| TiKV 存储 | 维持当前共享 NVMe 生产形态 | WAL/Raft 分离尚未进入交付配置 |

**版本边界：**U1/U141d 已批准上述exact patched v1.4.1替代patched v1.3.1；原版 v1.4.1
仍排除，randwrite仅约`551 MiB/s`。重新构建若校验和改变，须先闭合可重现制品身份与P0 smoke。

**测试边界：**长时间因果 A/B 可在单个 phase 内临时设置 `noscrub + nodeep-scrub` 以排除例行巡检干扰，但必须单独授权、审计并在 phase 后恢复；这不是性能配置，也不交付生产。

**缓存边界：**当前全部正式基线均使用`--cache-size 0`。04-tmp2d已用交付配置完成有效读缓存曲线：
75%容量时mseqread/randread约`+173%/+233%`，热集全驻留后约`35--37 GiB/s`，可作为有额外本地盘
时的独立生产canary方向。写侧04-tmp2e确认16 GiB在持续满压下不足：前90秒约`2770.80 MiB/s`，
但全窗降到`2436.50 MiB/s`且900秒后仍残留`2 blocks/524288 B`。该结果否决W16容量档，不否决
低占空比业务的突发吸收价值；项目决定在客户端有充足独立持久空间、文件由单客户端独占且接受异步
持久化边界时，将writeback作为条件性生产增强配置。下文各测试项带宽和瓶颈结论仍指无缓存数据路径。

## 二、各测试项当前值

为避免混淆，本节同时保留两种不同用途的数值：

- **历史交付锚点**：03-17f 在 patched v1.3.1 上、同一生产候选配置、3 次独立挂载所得中位数，
  用于描述 03 阶段曾达到的绝对带宽；
- **当前版本代表实测**：U141b/U141d 在相同数据资产、挂载参数和 Ceph 数据面下测得的 exact patched
  v1.4.1 臂均值，用于描述当前批准版本的实测量级。它们来自两个版本替代矩阵，**不是一次新的七项
  FULLBASELINE，也不应解释为历史最高值**。

目标按方向分别验收；`randrw` 读写不得相加后验收。

| 测试项 | 方向 | 1.3.1 历史交付锚点 MiB/s | 当前 1.4.1 代表实测 MiB/s | 当前达成率 | 当前判断 |
|---|---|---:|---:|---:|---|
| seqread | 读 | `1449` | `1432.9` | `22.9%` | 单流串行延迟墙 |
| mseqread | 读 | `5366` | `5259.6` | `84.2%` | 约 93% 命中客户端 buffer，不是纯存储路径值 |
| randread | 读 | **`5544`** | **`5326.1`** | **`85.2%`** | 当前最接近目标；受 6 OSD 服务率及读负载不均限制 |
| randrw | 读 | `1870` | `1749.3` | `28.0%` | 与写向共享总服务能力 |
| randrw | 写 | `1870` | `1749.2` | `28.0%` | 与读向共享总服务能力 |
| seqwrite | 写 | `1570` | `1555.0` | `24.9%` | 单流写提交链限制 |
| mseqwrite | 写 | `4676` | `4933.1` | `78.9%` | 在线参数空间已结束 |
| randwrite | 写 | `2707` | `2441.1` | `39.1%` | 同步元数据事务服务率限制 |

### 2.1 v1.4.1 替代证据

当前版本获批的依据是“未检测到材料性退化”，而不是“v1.4.1 更快”。其中 U141b 四项沿用
预注册的配对中位效应，U141d 四项采用预注册的二阶轮序趋势模型；因此不能仅用上表两臂均值重新
计算版本效应。

| 测试项 | v1.4.1 版本效应 | 双侧 95% CI | 判定 |
|---|---:|---:|---|
| seqread | `−0.05%` | — | 基本一致 |
| mseqread | `+0.57%` | — | 基本一致；无材料性退化方向 |
| randread | `+0.58%` | — | 基本一致；无材料性退化方向 |
| randrw 读 / 写 | `−0.52% / −0.51%` | `[−5.52%, +4.48%] / [−5.49%, +4.47%]` | 无可测方向；排除超过 5% 的退化 |
| seqwrite | `−0.28%` | — | 基本一致 |
| mseqwrite | `+2.96%` | `[−3.47%, +9.38%]` | 有正向倾向但精度不足；不得宣称确认提升 |
| randwrite | `−2.08%` | `[−5.10%, +0.94%]` | 有负向倾向但与无差异不可区分；排除超过 5% 的退化 |

最终裁决为 `REPLACE_APPROVED`：后续实验与交付统一使用 exact patched v1.4.1；社区 stock
v1.4.1 仍因 randwrite 约 `551 MiB/s` 而排除。批准现有精确二进制不要求重跑七项全量基线；若
二进制重新构建或关键配置改变，则必须重新闭合制品身份及相应验证。

### 2.2 randrw readahead最终闭合

04-tmp在当前最终栈上只改变是否追加`--max-readahead 0`。正式八轮显示READ/WRITE效应分别为
`+1.62%/+1.64%`，双侧95% CI为`[+0.04%, +3.21%] / [+0.01%, +3.28%]`；两向区间上界
均低于`+5%`材料阈值。保持默认readahead，不再补测该方向。早期ra0的大幅收益主要出现在128K
FUSE/旧基座；当前256K FUSE已经把randrw GET/IO约`1.62→1.15`、RX放大约`2.30×→1.22×`，
两种优化作用明显重叠。本结论不得外推到randread。

改变测试语义或集群状态后取得的上界仅作架构参考，**不能替代上表交付值**：

| 项 | 实验上界 | 为什么不能交付 |
|---|---:|---|
| randread | 双挂载 D128 合计中位 `6125.7 MiB/s`，单轮最大 `6293 MiB/s` | 改变挂载与并发语义，且未稳定越过目标 |
| randwrite | B256 + fresh 临时 TiKV + RAM logs 中位 `3756.51 MiB/s` | 改变 inode 数、元数据起点、集群拓扑和日志介质；仅达目标 `60.10%` |

## 三、瓶颈一览

| 测试项 | 已定位瓶颈 | 当前处置 |
|---|---|---|
| seqread | 单流 `psync/iodepth=1`，但默认 readahead 会形成约 `7--8` 个内部预读请求流水线。当前 V14 代表值 `1432.9 MiB/s` 对应每产出 `256 KiB` 有效数据约 `174.5 µs`，目标 `6250 MiB/s` 要求约 `40 µs`，仍需提高 `4.36×`；历史 ra0 约 `171 MiB/s` 反推无预读串行周期约 `1.46 ms`。 | **当前实现和已测在线参数空间内已收口。** 未发现能补齐 `4.36×` 缺口的参数；进一步提升需要增加单流内部流水线并发、显著降低对象读取端到端延迟，或改变客户端/请求并行语义，属于架构级方向 |
| seqwrite | 单流 `4 MiB`、`psync/iodepth=1`；当前 V14 代表值 `1555.0 MiB/s` 对应每产出 `4 MiB` 有效数据约 `2.57 ms`，目标要求约 `0.64 ms`，仍需提高 `4.02×`。16 流 mseqwrite 可达约 `4933 MiB/s`，说明聚合后端能力显著高于单流，约束主要位于单流 FUSE、写缓冲、异步上传及提交路径；现有证据没有同口径逐层延时，不能进一步点名为“同步元数据提交链” | **单流路径约束已确认，具体限制层尚未完全归因。** 当前没有已证实可补齐 `4.02×` 缺口的在线参数，但“必须改变架构才能提升”尚未严格证明 |
| randrw | 同一挂载内读写共享服务容量；04-6的`64→128→64`从约`1555/1556`升至`1757/1757`后回落到`1427/1428 MiB/s`，低并发回环漂移约`8.2%`；M02后TiKV compaction debt与NVMe await未随对象数归零 | **在线参数已收口，但严格架构闭环尚缺。** 保持默认readahead；若继续只做固定并发状态债务定点归因，不重跑容量矩阵 |
| mseqread | 04-6的`8→16→8`为`4384→4687→4393 MiB/s`，加倍并发只增`6.8%`，但P50未上升；frozen raw离线机制复算显示R02读量由BlueStore缓存服务、六块数据盘P50 util均为`0%`，因此不是数据盘服务率墙，具体限制仍在客户端/FUSE、messenger或OSD内存读路径之间 | **参数调优已收口、具体组件未闭合。** 不追加盲目参数轮；若必须定位，只做主动剖析/插桩，不重跑同一容量曲线 |
| mseqwrite | 04-6的`8→16→8`为`4064→3854→4090 MiB/s`；加倍并发反降约`5.5%`，P50升约`4.2×`；W02六块数据盘正式窗P50 util均`100%`，OSD完成率反降至两侧低档均值的`94.56%` | **当前硬件、EC4+2拓扑、无缓存且8→16流范围内已确认Ceph数据盘写服务平台。** 未找到可生产在线旋钮；不得外推成跨硬件/拓扑数学上限 |
| randread | 客户端和网卡有余量，瓶颈在6 OSD服务率及实际读负载不均；04-1b将测试Pool的`I_primary=1.40625→1.03125`后，带宽`3452.5→3924.5 MiB/s（+13.67%）` | **布局方向已验证但不进入当前交付。** 收益来自特定测试Pool且缺正式`op_r`，不能外推到历史基线；仅保留为新Pool架构候选 |
| randwrite | 128 inode先受“按inode串行+同步TiKV事务”限制；增至256 inode后进入TiKV KV/compaction与同步提交共享服务率墙 | **参数路径已封板，规模/region架构归因已挂起。** 当前约`12K` meta提交/s，目标需约`25K/s`，小参数无法补齐近2倍缺口；04-6只判断是否还有新的可交付旋钮 |

### randwrite 波动的已确认原因

高并发同步元数据写持续形成 compaction debt；WAL/Raft 与 KV/compaction 共用物理设备时，设备排队进一步放大同步日志尾延迟，最终降低 meta 事务完成率并造成带宽尾段下滑。它是**软排队和服务率不足**，不是 RocksDB hard stall；清理只能统一起点，不能阻止本轮重新积累。

已验证的物理分离探针：WAL/Raft 移至 RAM 后，带宽配对中位提高 `5.05%`，CV 配对中位改善 `2.36 pp`，`W4/W1` 提高 `0.0694`。`5.05%` 是真实第二块 NVMe 的带宽**上界**；生产价值主要是稳定性，当前三节点没有空闲 NVMe，尚不能固化。

## 四、当前正在做什么

| 优先级 | 工作 | 状态 | 要回答的问题 |
|---|---|---|---|
| P0 | U1 / U141d 版本锁定 | ✅ **已完成：`REPLACE_APPROVED`** | randrw读/写`−0.52%/−0.51%`、randwrite`−2.08%`、mseqwrite`+2.96%`，均排除>5%退化 |
| P1 | R1 / 04-1与04-1b PG Primary布局 | ✅ **已完成并关闭** | 均衡Primary有`+13.67%`工程信号但仅达目标`62.79%`；不纳入当前交付 |
| P1 | A1 / 04-2 H-C-L归因 | ✅ **已完成并关闭：分辨力不足** | C/L名义差`-4.54%`、同臂噪声`8.45%`；H漂移`96.70%`，不能固化fresh收益 |
| — | A2a / 04-3a 元数据规模联合效应 | ⏸ **已挂起：`PARKED_ARCHITECTURE_RESEARCH`** | 成功也主要支持扩容/namespace拆分，不能产生当前生产配置，且成本较高 |
| — | A2b / 04-3b region因果 | ⏸ **已挂起：前置条件不成立** | 仅在04-3a有材料信号且已决定评估TiKV扩容/分片时复活 |
| P1 | Z0 / 04-6 最终收尾 | ✅ **RUN `20260903-214003`完成并关闭** | 9/9 cell与恢复通过；mseqwrite平台闭合，无新可交付旋钮；因mseqread仍部分扩展且randrw漂移，签`STAGE04_CONTINUE_DIAGNOSIS`；后续收口由04-6b承担 |
| 独立专项 | 04-tmp3 竞品大块单流 | ✅ **RUN `20260904-095827`完成并关闭** | 20M direct读R臂相对A `+65.27%`；16M写F臂相对A `+10.60%`且具L2确认资格；4/4披露目标未达，不覆盖256K七项基线 |
| 独立专项 | 04-tmp3b 竞品大块I/O路径对齐 | ⏳ **任务书READY；脚本未准备；未授权** | 04-tmp3读侧信号进入RA镜像确认；写侧因卸载异常需补async验证；条件升级L2 |
| P1 | Z0b / 04-6b 端到端容量账与残余调优收口 | ⏳ **任务书READY；脚本未准备；未授权** | 承接04-6/U141d/04-tmp3/04-1b持久raw；七项容量账+R8/F1/U300各一个ABBA+randrw状态回环 |
| — | 04-tmp2f writeback排空归因与容量曲线 | ⏳ **任务书PLANNED；脚本未准备；未授权** | 闭合W16排空失败文件级根因，测32/64/96/128GiB找到首个观测安全点 |
| P3 | M1 / 04-4 元数据事务方案 | ✅ **已完成：`M1_SINGLE_OPTION_ONLY`** | 唯一可进入门控原型的是同 inode 跨 chunk metadata batch；模型为 T2 conditional，不是实测承诺 |
| — | M2 / 04-5 batch 原型 | ❌ **已废弃；不执行** | 即使源码原型有效，也无法在当前交付周期直接生产化；仅保留历史设计参考 |

## 五、后续方向

1. **04-6之后只保留最小机制缺口**：不重跑9-cell容量矩阵。mseqwrite已闭合到当前范围的数据盘写服务平台；若管理上仍要求定位mseqread具体组件，只做主动剖析/插桩；randrw则另立固定并发状态债务归因。不得以“未发现参数”代替架构证明。
2. **04-3a/04-3b挂起**：不再为了拆完不稳定的`25%--30%`历史组合现象投入生成器、快照和长矩阵；仅在生产出现规模相关退化，或已经决定评估TiKV扩容/namespace拆分时复活。
3. **R1与04-2不再补测**：Primary均衡只保留为新Pool架构候选；nested-loop未被确认是主要瓶颈，04-2没有生产配置收益。
4. **M1结论保留，M2废弃**：同inode跨chunk metadata batch仍是纸面上唯一具有材料上界的源码方向，但完整研发和生产化周期过长；当前不做插桩、原型或环境验证。
5. **有硬件时评估真实WAL/Raft分盘**：每个TiKV节点需要一块对称持久盘；采购理由是稳定性，带宽承诺不得超过已测上界`5.05%`。
6. **架构扩容按业务立项**：Ceph OSD/pool/EC重构和TiKV扩容/分片不再作为当前调优尾项；只有容量、稳定性或生产退化需求明确时单独立项。
7. **本地缓存作为条件性生产增强**：04-tmp2d已确认容量/命中率与读收益的稳定关系；若客户端有
   额外本地盘，可按热工作集加缓存开销和空闲余量规划读缓存。04-tmp2e确认writeback能够吸收写入
   前段突发，但W16在持续满压下排空失败；因此仅在独占文件、低占空比、空间充足且受监控的客户端
   启用writeback。04-tmp2f计划闭合排空失败文件级根因并测容量矩阵，尚未授权。读写缓存收益均不
   进入无缓存七项基线，也不能解决同步TiKV元数据事务主导的长期写侧服务率瓶颈。
8. **大块竞品口径按业务需求决定是否确认**：04-tmp3的R读臂是强L1信号，F写臂也具最小确认资格，
   但cp读未观察到同向收益，且四项披露目标均未达。只有当20 MiB direct读或16 MiB写是真实业务模型时，
   才另立精简A/R或A/F L2，并对256K七项做回归；不自动继续。

## 六、一句话状态

> **exact patched v1.4.1已锁定；读缓存和writeback均形成有本地盘时的条件性生产增强，其中writeback只承接短突发且W16明确排除；Primary均衡有工程信号但未形成当前交付配置；04-6已闭合mseqwrite当前范围的数据盘写服务平台且未发现新生产旋钮，仍留mseqread具体限制组件与randrw状态债务两个最小缺口；04-tmp3已完成，只保留按业务需求启动的大块读/写L2候选。**

主要事实源：

- [03 阶段整体报告](../perf-report/03-JUICEFS-PERFORMANCE-TUNING-FINAL-REPORT-20260828.md)
- [U141d v1.4.1 替代最终报告](../perf-report/u141d-juicefs-v141-replace-v131-final-20260831.md)
- [04-2 H/C/L归因报告](../perf-report/04-2-hcl-native-vs-nested-attribution-20260902.md)
- [04-tmp2d 读缓存曲线报告](../perf-report/04-tmp2d-production-aligned-read-cache-curve-20260903.md)
- [04-tmp2e writeback容量报告](../perf-report/04-tmp2e-writeback-capacity-curve-20260903.md)
- [04-6 阶段收尾报告](../perf-report/04-6-stage04-final-capacity-and-tuning-exit-decision-20260903.md)
- [04-tmp3 竞品大块单流报告](../perf-report/04-tmp3-competitor-large-block-sequential-benchmark-20260904.md)
- [2026-09-05 周报](../../report/周报-JuiceFS调优工作汇总-20260905.md)
- [04 阶段计划](04-metadata-architecture-and-layout-plan.md)
- [测试结果总表](../deploy-log/results-table.md)
- [集群变更型实验 TODO](../perf-tasks/TODO-cluster-changing-experiments-after-stage03.md)
