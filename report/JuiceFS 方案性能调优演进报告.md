# JuiceFS 方案性能调优演进报告

> 环境：3 节点 Ceph（6 OSD，EC 4+2，SSD）/ JuiceFS v1.3.1 / TiKV 元数据 / 1Gbps 网络
> 达标标准：单项带宽 ≥ 59 MB/s（千兆网卡半速）
> 数据来源：全量标准化 fio 测试，每组配置双轮取最高值，原始日志可追溯

---

## 一、调优工作总览

| 阶段 | 关键动作 | 核心成果 |
|------|---------|---------|
| **阶段 0：初始状态** | RGW + 4M block，千兆网络 | 完成集群部署，基本功能实现；顺序写仅 18.6 MB/s，随机读写仅 4.6 MB/s |
| **阶段 1：基础设施修复** | 排查发现 node1/node2 网卡仅 100Mb/s；三节点网络升级千兆 + node1 重装纳管 | 后端裸能力提升约 10 倍 |
| **阶段 2：存储架构优化** | 部署双 RGW + HAProxy 负载均衡；去 RGW 直连 RADOS；block-size 4M→256K | 顺序写 +189%，随机读写 +235%~276% |
| **阶段 3：客户端参数调优** | 暖态缓存（cache=100G）；--max-readahead 0；--max-uploads 150 | 随机读 +254%，顺序写 +23% |
| **阶段 4：深度诊断与排除** | 白盒实验排查瓶颈方向；穷举排除无效方向；定位 v1.3.1 loadRange bug 并 patch 修复；mu=150 冷态全量验证；ra sweep 验证 | 随机读 2.5× 放大根因确认（非 EC/messenger，为 v1.3.1 bug）；冷态写类全面提升；暖态 randrw +30% |

---

## 二、关键调优突破

### 突破 1：网络基础设施修复（阶段 1）

调优起步时，通过自底向上隔离发现：裸盘 SSD 随机写可达 420 MB/s，但 `rados bench` 后端仅 9.87 MB/s、延迟 17.6 秒——典型的网络拥塞特征。定位到 ceph-node1/node2 网卡仅协商到 100Mb/s（约 12.5 MB/s），是整套系统的头号物理瓶颈。

修复后后端裸能力从 ~10 MB/s 提升到 ~108 MB/s，端到端顺序吞吐从 ~8 MB/s 提升到 ~80 MB/s。

### 突破 2：去 RGW 直连 RADOS（阶段 2）

通过 128G 同口径对照实验，去掉 RGW（S3 网关）HTTP 协议层、改用 JuiceFS librados 直连 Ceph：

- 顺序写 18.6 → 53.8 MB/s（**+189%**）
- 随机读写 4.6 → 15.6 MB/s（**+239%**）

### 突破 3：block-size 4M → 256K（阶段 2，核心突破）

通过白盒实验 + `juicefs stats` 采集 + fio bs-sweep + block-size-sweep 精确定位根因：**JuiceFS 默认 4MB block 对 256K 随机读造成 ~16× 读放大**——客户端拉了一堆用不上的数据，object 层 ~100 MB/s 全浪费，FUSE 有效读仅 ~5 MB/s。

将 block-size 改为 256K 后，经多次重测互证：

- 纯随机读提升约 3.7 倍
- 顺序读写不降反略升
- 存储空间零代价（EC 开销不变）、内存反而更低

### 突破 4：暖态缓存 + 客户端参数调优（阶段 3）

在 256K block 基础上叠加客户端调优：

- **暖态缓存（cache=100G）**：随机读 33.6 → 105 MB/s（+213%），随机读写 +34%
- **--max-uploads 150**：顺序写 +23%、顺序读 +14%、布局写 +39%
- **--max-readahead 0**：写类全面受益（顺序写 +33%、随机写 +8%），但顺序读和随机读写受损，需按负载选配

### 突破 5：loadRange bug 定位与修复（阶段 4，核心突破）

通过 JuiceFS 源码分析，定位到 v1.3.1 版本独有 bug：`cached_store.go:153` 的 `loadRange` 函数中条件 `len(p) <= blockSize/4`（即 64K）错误地阻止了 128K FUSE 半块读取，导致每次随机读都触发完整 256K block 读取，造成 **2× 读放大**。

该 bug 的修复 commit 为 `eaf3d21f`（已在 main 分支，未 backport 到 release-1.3）。应用 patch 后：
- 随机读放大从 2.17× 降至 1.11×（专项验证）

**关键发现：ra 对 randread 无影响**。之前认为"ra=0 显著提升随机读"的结论是带 bug 版本的假象——bug 造成的 2× 放大与预读叠加，ra=0 恰好消除了预读部分。修复 bug 后，ra=0/1/2/4/8 的 randread r1 均为 51-53 MiB/s，无显著差异。因此**无需设置 ra=0**，default（2 MiB）即可。

**mu=150 冷态全量验证**：将 max-uploads 从默认 20 提升至 150，冷态全量测试结果显示写类指标全面提升：

| 指标 | mu=20（上周） | mu=150（本周） | 涨幅 |
|------|-------------|---------------|------|
| 顺序写 | 50.8 | 57.0 | +12.2% |
| 多线程写 | 41.5 | 43.7 | +5.3% |
| 布局写 | 33.3 | 39.1 | +17.4% |
| 随机写 | 53.5 | 55.7 | +4.1% |
| 随机读写 R | 17.3 | 19.4 | +12.1% |
| 随机读写 W | 17.0 | 19.1 | +12.4% |

mu=150 的作用机制：max-uploads 控制并发上传 goroutine 数。128 个 fio 并发 job 争抢 20 个 upload slot 时写请求排队严重；提升至 150 后排队延迟大幅降低。读操作不走 upload pipeline，故 randread 不受影响（符合预期）。

---

## 三、深度诊断工作（阶段 4）

为定位调优后残余瓶颈，执行了大量诊断实验，形成完整证据链：

### 3.1 瓶颈分层定位

| 诊断层 | 路径 | 放大倍数 | 初步判断 |
|--------|------|---------|------|
| L1 裸 librados（rados bench 256K rand） | 绕过 JuiceFS/FUSE | **1.04×** | EC 取片传输开销仅 4%，后端可能非放大主要来源 |
| L2 JuiceFS FUSE（端到端） | 全链路 | **~2.5×** | ~~放大可能主要在 JuiceFS 内部~~ **已定位：loadRange bug 造成 2× 放大，patch 后降至 1.11×** |

### 3.2 strace 系统调用级追踪

通过 `strace` 追踪 JuiceFS 进程的 Ceph socket 读取行为：

- 每个 fio 256K 读取触发 **139 次 socket read() 调用 ≈ 4 OSD × 35**
- 数量上接近 EC 4+2 四分片，初步排除 slice 碎片化假设
- ~~怀疑残余放大与 Ceph messenger 协议帧开销有关，仍在进一步确认~~
- **已定位**：139 次 socket read 的根因是 loadRange bug 导致全块读取，patch 后每次随机读仅触发 67K 后端读取（1.11× 放大），socket read 次数大幅下降

### 3.3 穷举排除的无效方向

| 方向 | 排除依据 |
|------|---------|
| 磁盘介质（HDD→SSD） | 裸盘实测 4K 随机读 92,500 IOPS，确认为 SSD |
| WAL/DB 换更快介质 | 内存盘测试仅 +4.8%，无效¹ |
| 全内存盘集群 | 纯 RAM 替换 SSD 后吞吐不变（106.6 vs 106.4） |
| BlueStore 引擎参数 | 三组参数全部无效 |
| EC→副本（size=3） | 实测反而慢 16%（写放大 3× > EC 1.5×） |
| 磁盘调度器/IO 合并 | 所有指标纹丝不动 |
| RGW HTTP 层 | 去掉后随机读仅 +22%，非根因 |
| JuiceFS 并发/缓冲/预读参数 | buffer-size 反降，prefetch 无变化 |
| 元数据（TiKV） | 延迟仅 1ms，非瓶颈 |
| FUSE 内核节流 | waiting≈0 全程 50 采样点 |
| FUSE splice/max_read/max_pages | 系统 kernel 5.15 / fusermount 3.10.5 不支持 |
| EC 2+1 / MTU jumbo / overwrites | L1=1.04× 已证 EC/网络非放大源 |
| JuiceFS v1.4 升级 | 单客户端反而 -3.3%（但 v1.4 含 loadRange 修复，需重评随机读净收益） |
| CephFS 内核态对照 | 读 3.6× 但写触发 EC RMW 反降 |

> ¹ **BlueFS stall / 独立 NVMe 的定位订正（2026-07-06，详见 `doc/perf-analysis/11_1-step2-stall-compaction-branch.md`）**：
> 高强度写期间出现的 `DB_DEVICE_STALLED_READ_ALERT` 经 GLM 干净基线复核确认为**可管理现象、非硬瓶颈**——干净态（`compact_queue_len=0`）下 layout 128G + 64G 多 job 写全程零 stall；触发需"单流持续写 + OSD 已有 compaction 积压"两条件同时满足，`compact` 命令可秒级清除积压（restart OSD 不行）。因此**独立 NVMe 做 DB/WAL 从"生产必须"降为"可选优化"**（仅在无法控制写入模式且 compaction 长期跟不上的场景才必要）。此结论与本行"WAL/DB 换介质收益有限"一致。
> 另注：Ceph 因 RAID 卡（PERC H730）不透传介质而将 SSD 误判为 HDD，导致 `bluestore_prefer_deferred_size=65536`（256K 写的 64K 分片走 deferred 双写）。rados 直打池改回 SSD 行为（=0）后端 **+23%**（72.3 vs 58.6），但经 JuiceFS 端到端未显化——**JuiceFS 写路径吞吐才是写侧真实瓶颈，待查（见 11_1 待办 A）**。
| 多客户端聚合 | 有效但验收线同步抬高，治标不治本 |

### 3.4 本周新发现（2026-07-03）

| 发现 | 依据 |
|------|------|
| 随机读 2.5× 放大根因 = loadRange bug（非 EC/messenger） | 源码分析 + patch 后放大 2.17×→1.11× + randread r1 +10.7% |
| ra 对 randread 无影响（patch 后） | ra sweep: ra=0/1/2/4/8 的 randread r1 均为 51-53 MiB/s |
| 之前"ra=0 救随机读"是带 bug 版本的假象 | bug 造成 2× 放大 + ra 预读叠加；ra=0 消除预读部分但 bug 放大仍在 |
| randrw 瓶颈部分来自 upload 并发度不足 | mu=150 冷态 randrw +86%（14.3→26.6），暖态 +30%（18.9→24.6） |
| 全量测试 randread r1 低于稳态值 | 全量测试在 layout 写 128G 后仅 cooldown 120s 即跑随机读，OSD RocksDB compaction 未完成导致 r1=37.2；同配置在 OSD 稳定后（drop OSD cache）r1=52.0 |

---

## 四、调优演进对比

### 4.1 冷态（不使用 JuiceFS 缓存）

以原始状态为基准，列出各指标增减百分比

| 测试 | 配置 | 顺序读 | 顺序写 | 多线程读 | 多线程写 | 随机读 | 随机写 | 随机读写 R | 随机读写 W |
|------|------|--------|--------|----------|----------|--------|--------|------------|------------|
| RGW（原始状态） | bs=4M / bs=256K / cache=0 / mu=默认 / S3 via haproxy | 78.8 | 18.6 | 109 | 20.7 | 30.6 | 32.7 | 4.6 | 4.6 |
| 直连（一轮调优） | bs=4M / bs=256K / cache=0 / mu=默认 / Ceph RADOS | 79.9 (+1.4%) | 53.8 (+189%) | 107 (-1.8%) | 39.7 (+92%) | 32.4 (+5.9%) | 33.3 (+1.8%) | 15.6 (+239%) | 15.4 (+235%) |
| 冷态全量（二轮调优） | bs=256K / bs=256K / cache=0 / mu=默认 | 80.4 (+2.0%) 已达标 | 50.8 (+173%) 达标86%差14% | 110 (+0.9%) 已达标 | 41.5 (+100%) 达标70%差30% | 33.6 (+9.8%) 达标57%差43% | 53.5 (+64%) 达标91%差9% | 17.3 (+276%) 达标29%差71% | 17.0 (+270%) 达标29%差71% |
| 冷态 patched mu=150 **（本周）** | bs=256K / cache=0 / mu=150 / patched v1.3.1 | 78.5 (-0.4%) 已达标 | 57.0 (+206%) 达标97%差3% | 108 (-0.9%) 已达标 | 43.7 (+111%) 达标74%差26% | 47.8 (+56%) 达标81%差19% | 55.7 (+70%) 达标94%差6% | 19.4 (+322%) 达标33%差67% | 19.1 (+315%) 达标32%差68% |

**本周调优记录（2026-07-03）：**

| 调优手段 | 说明 |
|---------|------|
| loadRange bug 修复 | 定位 v1.3.1 `cached_store.go:153` 条件 `len(p)<=blockSize/4=64K` 阻止 128K FUSE 半块读取 → 全量读 → 2× 读放大。应用 patch（commit eaf3d21f）修复 |
| max-uploads 20→150 | 提升并发上传 goroutine 数，减少写请求排队延迟 |
| RA Sweep 验证 | 测 ra=0/1/2/4/8，patch 后 ra 对 randread 无影响（均为 51-53），无需设 ra=0 |

相比上周"冷态全量（二轮调优）"的进展（同口径 MAX）：

| 指标 | 上周 mu=20 | 本周 mu=150 | 涨幅 | 说明 |
|------|----------|-----------|------|------|
| 顺序写 | 50.8 | 57.0 | +12.2% | mu=150 提升并发上传 |
| 多线程写 | 41.5 | 43.7 | +5.3% | 同上 |
| 布局写 | 33.3 | 39.1 | +17.4% | 同上，128 jobs 受益最大 |
| 随机写 | 53.5 | 55.7 | +4.1% | 同上 |
| 随机读写 R | 17.3 | 19.4 | +12.1% | 写侧并发提升减轻读写竞争 |
| 随机读写 W | 17.0 | 19.1 | +12.4% | 同上 |
| 随机读 | 33.6 | 47.8 | +42.3% | patch 修复读放大；MAX 含 OSD cache 预热效应，r1=38.6 |
| 顺序读 | 80.4 | 78.5 | -2.4% | 波动范围，不受 mu 影响 |

¹ 全量测试 r1=38.6（post-layout OSD 压力）；ra sweep（drop OSD cache，OSD 稳态）同配置 r1=52.0。MAX=47.8 受 r2/r3 OSD cache 预热拉高。


---

### 4.2 暖态（使用 JuiceFS 缓存）

以冷态全量（二轮调优）为基准，列出各指标增减百分比

| 测试 | 配置 | 顺序读 | 顺序写 | 多线程读 | 多线程写 | 随机读 | 随机写 | 随机读写 R | 随机读写 W |
|------|------|--------|--------|----------|----------|--------|--------|------------|------------|
| 冷态全量（二轮调优） | bs=256K / bs=256K / cache=0 / mu=默认 | 80.4 | 50.8 | 110 | 41.5 | 33.6 | 53.5 | 17.3 | 17.0 |
| 暖态全量（二轮调优带缓存） | bs=256K / bs=256K / cache=100G / mu=默认 | 80.6 (+0.2%) | 48.7 (-4.1%) | 109 (-0.9%) | 40.6 (-2.2%) | 105 (+213%) | 51.1 (-4.5%) | 23.2 (+34%) | 22.6 (+33%) |
| 暖态 ra=0 | bs=256K / bs=256K / cache=100G / mu=默认 / ra=0 | 48.8 (-39.3%) | 67.3 (+32.5%) | 99.6 (-9.5%) | 49.1 (+18.3%) | 155 (+361%) | 57.9 (+8.2%) | 8.8 (-49.1%) | 8.6 (-49.4%) |
| 暖态 mu=150 **（当前最优）** | bs=256K / bs=256K / cache=100G / mu=150 | 91.5 (+13.8%) 已达标 | 62.4 (+22.8%) 已达标 | 109 (-0.9%) 已达标 | 42.6 (+2.7%) 达标72%差28% | 119 (+254%) 已达标 | 56.0 (+4.7%) 达标95%差5% | 18.9 (+9.2%) 达标32%差68% | 18.7 (+10%) 达标32%差68% |
| 暖态 patched mu=150 **（本周）** | bs=256K / cache=100G / mu=150 / patched v1.3.1 / 预热 | 71.5 (-11.1%) 已达标 | 44.0 (-13.4%) 达标75%差25% | 109 (-0.9%) 已达标 | 42.0 (+1.2%) 达标71%差29% | 146 (+335%) 已达标² | 57.5 (+7.5%) 达标97%差3% | 24.6 (+42.2%) 达标42%差58% | 24.2 (+42.4%) 达标41%差59% |

**本周调优记录（2026-07-03）：**

使用 patched v1.3.1（loadRange 修复）+ mu=150 + 预热（读 128G test_dir 填充 cache）+ 5 轮随机测试。

相比上周"暖态 mu=150"的进展（同口径 MAX）：

| 指标 | 上周 mu=150 | 本周 patched mu=150 | 涨幅 | 说明 |
|------|-----------|-------------------|------|------|
| 随机读写 R | 18.9 | 24.6 | +30.2% | patch 修复读放大 + mu=150 减轻写排队 |
| 随机读写 W | 18.7 | 24.2 | +29.4% | 同上 |
| 随机写 | 56.0 | 57.5 | +2.7% | 小幅提升 |
| 随机读 | 119 | 146² | +22.7% | >124 为缓存命中，非真实后端能力 |
| 顺序读 | 91.5 | 71.5 | -21.9% | 预热填充 test_dir 缓存挤占 seq_dir 空间 |
| 顺序写 | 62.4 | 44.0 | -29.5% | 缓存状态差异，非 patch 影响 |

² 暖态 randread r1=36.6→r5=146 单调爬升，r4/r5 >124 为缓存命中。暖态 randread 仅定性参考，不作达标依据。

³ **writeback 开启**：数据为本地缓存/内存性能，不代表后端 Ceph 真实吞吐。\-\-writeback 将写请求先落本地缓存异步刷后端，读可命中本地缓存，因此带宽远超千兆物理上限（~118 MB/s）。此行为生产环境有益（降低写延迟、提升读命中），但不适用于后端能力评估。数据来源：2026-06-29 测试，cache=100G + mu=150 + writeback，取 3 轮 MAX。

**writeback 开启数据（2026-06-29）：**

| 指标 | 数值 | 说明 |
|------|------|------|
| 顺序读 | 1193 MB/s | 本地缓存命中（NIC_RX 仅 16.5 MB），非后端读取 |
| 顺序写 | 346 MB/s | 写落本地缓存异步刷盘（NIC_RX 仅 10.9 MB） |
| 多线程读 | 12800 MB/s | 内存级速度，fio 输出为 12.5 GiB/s |
| 多线程写 | 431 MB/s | 合并写入本地缓存 |
| 布局写 | 417 MB/s | 128G 大规模写入本地缓存，后续异步上传 |
| 随机读 | 864 MB/s | r1=197→r3=864，缓存逐轮填充 |
| 随机写 | 688 MB/s | 随机写本地缓存极高 |
| 随机读写 R/W | 268/270 MB/s | 本地缓存命中的混合读写 |

writeback 通过将 FUSE 层的写操作缓存到本地磁盘、异步上传至 Ceph，消除了写操作的后端延迟。读操作受益于本地缓存命中。NIC RX 监控证实网络流量极低（顺序读/写均 <20 MB），所有数据走本地路径。此配置适用于对写入延迟敏感、允许异步持久化的生产场景，但冷态首次读仍需走后端。

---

## 五、当前状态总结

### 5.1 冷态达标指标（≥59 MB/s）

> 配置：patched v1.3.1 + cache=0 + mu=150 + default ra

| 指标 | 冷态最优值 | 较原始状态提升 |
|------|----------|--------------|
| 顺序读 | 80.1 MB/s | +2%（78.8→80.1） |
| 多线程读 | 109 MB/s | — |

### 5.2 冷态未达标指标

| 指标 | 冷态最优值 | 距 59 | 说明 |
|------|----------|-------|------|
| 顺序写 | 57.0 MB/s | 差 3% | mu=150 提升 12%（50.8→57.0），最接近达标 |
| 随机写 | 55.7 MB/s | 差 6% | mu=150 提升 4%（53.5→55.7），接近达标 |
| 随机读 | 52.0 MB/s | 差 12% | patch 修复 loadRange bug 后提升 55%（33.6→52.0），仍有差距但不远 |
| 多线程写 | 43.7 MB/s | 差 26% | mu=150 提升 5%（41.5→43.7），EC 写放大 + 网络瓶颈 |
| 随机读写 R | 26.6 MB/s | 差 55% | mu=150 提升 54%（17.3→26.6），读写竞争为残余瓶颈 |
| 随机读写 W | 26.3 MB/s | 差 55% | 同上 |

> 数据来源：冷态全量 mu=150（2026-07-03，顺序类 + 随机写 MAX）+ ra sweep default ra（2026-07-03，随机读 r1，取稳态 OSD 值）+ mu=150 独立随机测试（2026-07-03，随机读写 r1）。post-layout OSD RocksDB compaction 未完成时随机读 r1 偏低（~38），稳态下为 52.0。

### 5.3 残余瓶颈分析

> 冷态下仅顺序读和多线程读达标。以下为未达标指标的瓶颈定位进展：

- **随机读写（randrw）是最大缺口**：冷态最优 26.6/26.3，距 59 差 55%。读写混合场景下读被写阻塞，即使 mu=150 提升 upload 并发度（14.3→26.6，+86%），仍有 2.2 倍差距。多客户端聚合测试中 randrw 总带宽可达 76.6（3 客户端），但单客户端仍受限。
- **随机读已大幅改善但仍差 12%**：patch 修复 loadRange bug 后随机读从 33.6 提升至 52.0（+55%）。ra sweep 证实 ra 对 randread 无影响，无需设 ra=0。剩余 12% 差距可能与 EC 对象读取粒度及客户端并发调度有关。
- **顺序写差 3%、随机写差 6%**：接近达标的两个指标。mu=150 对顺序写提升 12%，对随机写提升 4%。主要瓶颈为 1Gbps 网络带宽限制（单网卡线速 ~118 MB/s，EC 写放大 1.5× 后 net 有效 ~79 MB/s）。

**本周进展（2026-07-03~04）：**

- **随机读 2.5× 放大根因已确认**：v1.3.1 loadRange 条件 bug（非 EC/messenger），patch 后降至 1.11×。之前 strace 观测到的 139 次 socket read 是 bug 导致的全块读取，非 EC 协议开销。
- **ra 对 randread 无影响（patch 后）**：ra sweep 验证 ra=0/1/2/4/8 的 randread r1 均为 51-53 MiB/s。"ra=0 救随机读"是带 bug 版本的假象。
- **randrw 瓶颈部分来自 upload 并发度不足**：mu=150 冷态 randrw +86%（14.3→26.6）。但最优 26.6 仍距 59 差 2.2 倍，读写竞争机制需进一步诊断。
- **多客户端聚合（2026-07-04）**：patched + mu=150 + default ra 下，3 客户端冷态聚合 randread 74.2 MiB/s，randrw 总带宽 76.6 MiB/s。后端带宽是共享瓶颈，P2→P3 基本持平。
- **修复落地路径**：patch 是验证手段，生产落地需升级到含 eaf3d21f 的 v1.4.x 或向 release-1.3 backport。

**本周进展（2026-07-06，写侧根因订正，详见 `doc/perf-analysis/11_1-step2-stall-compaction-branch.md`）：**

- **写类未达标核心 = JuiceFS 写路径吞吐，不是 mu/buffer 参数**：rados 直打 EC 池 256K 裸能力均值 52.7，而 JuiceFS multi-seqwrite 仅 40.8（约 -23% JuiceFS 层损耗）；顺序写/随机写在 mu=150 是贴 59 上下浮动（运行间方差），扫 mu/buffer 未坐实达标。写侧下一步转 JuiceFS 写路径根因追查（11_1 待办 A）。
- **BlueFS stall 是可管理现象、非硬瓶颈**：经 GLM 干净基线复核（4998 采样点），干净态 layout 128G + 64G 多 job 写全程零 stall；触发需"单流持续写 + OSD 已有 compaction 积压（`compact_queue_len>0`）"，`compact` 命令可秒级清除（restart OSD 不清磁盘 LSM 积压）。**过去冷态基线数据可信，不必重测**；独立 NVMe 降为可选优化。
- **SSD 被误判为 HDD（RAID 卡不透传）→ deferred 双写**：改回 SSD 行为后端 +23%，但经 JuiceFS 端到端未显化（并入待办 A 追查）；是否纳入基线待端到端证据后再定。
